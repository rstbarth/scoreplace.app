// Retiramos o DOMContentLoaded event para evitar condição de corrida com ES Modules (type="module")
// já que o script está no final do <body> e o DOM já estará parseado.

// === Modal Ajuda / Manual ===
(function setupHelpModal() {
  if (document.getElementById('modal-help')) return;

  var _t = window._t || function(k) { return k; };
  var helpSections = [
    {
      id: 'about',
      title: _t('help.about'),
      icon: '🏆',
      content: '<div style="text-align:center; margin-bottom:1rem;">' +
        '<div style="font-size:2.2rem; margin-bottom:0.3rem;">🏆</div>' +
        '<div style="font-size:1.3rem; font-weight:800; color:var(--text-bright);">scoreplace.app</div>' +
        '<div style="font-size:0.8rem; color:var(--text-muted); margin-top:2px;">Versão ' + (window.SCOREPLACE_VERSION || '0.2.3-alpha') + '</div>' +
        '<div style="font-size:0.8rem; color:var(--text-main); margin-top:8px; line-height:1.6;">Jogue em outro nível — torneios, partidas casuais, presença e locais esportivos.</div>' +
        '<div style="font-size:0.7rem; color:var(--text-muted); opacity:0.6; margin-top:4px;">Fase Alpha — funcionalidades em desenvolvimento.</div>' +
        '</div>' +
        '<div style="background:rgba(251,191,36,0.1); border:1px solid rgba(251,191,36,0.3); border-radius:10px; padding:12px 14px; margin-bottom:1rem;">' +
        '<div style="font-weight:700; font-size:0.85rem; color:#f59e0b; margin-bottom:6px;">⚠️ App em Desenvolvimento</div>' +
        '<p style="font-size:0.78rem; color:var(--text-main); line-height:1.6; margin:0;">Este aplicativo está em fase <b>Alpha</b> e pode apresentar bugs, erros e falhas inesperadas. Estamos trabalhando continuamente para melhorar a experiência. Sua paciência e feedback são fundamentais!</p>' +
        '</div>' +
        '<div style="text-align:center; margin-bottom:1rem;">' +
        '<button class="btn btn-indigo hover-lift" onclick="(function(){' +
        "var v=window.SCOREPLACE_VERSION||'?';" +
        "var ua=navigator.userAgent;" +
        "var user=(window.AppStore&&window.AppStore.currentUser)?window.AppStore.currentUser.displayName:'(não logado)';" +
        "var email=(window.AppStore&&window.AppStore.currentUser)?window.AppStore.currentUser.email:'';" +
        "var subject=encodeURIComponent('scoreplace.app v'+v+' — Feedback');" +
        "var body=encodeURIComponent(" +
        "'Olá equipe scoreplace!\\n\\n'" +
        "+'Descreva aqui o problema ou sugestão:\\n\\n\\n'" +
        "+'---\\n'" +
        "+'Versão: '+v+'\\n'" +
        "+'Usuário: '+user+'\\n'" +
        "+'Email: '+email+'\\n'" +
        "+'Dispositivo: '+ua+'\\n'" +
        "+'Data: '+new Date().toLocaleString('pt-BR')+'\\n'" +
        ");" +
        "window.open('mailto:scoreplace.app@gmail.com?subject='+subject+'&body='+body,'_blank');" +
        '})()">' +
        '<span style="font-size:1.1rem;">📩</span> Reportar Problema ou Sugestão</button>' +
        '</div>' +
        '<p style="font-size:0.7rem; color:var(--text-muted); text-align:center; margin-bottom:0.5rem;">Ao clicar, será aberto seu app de e-mail com informações técnicas preenchidas automaticamente para nos ajudar a resolver mais rápido.</p>' +
        '<div style="font-size:0.7rem; color:var(--text-muted); margin-top:8px; text-align:center;">© 2026 scoreplace.app. Todos os direitos reservados.</div>'
    },
    {
      id: 'primeiros-passos',
      title: _t('help.firstSteps'),
      icon: '🚀',
      content: '<p><b>Bem-vindo ao scoreplace.app!</b> O app cobre 4 áreas complementares:</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Os 4 pilares</div>' +
          '<p>🏆 <b>Torneios</b> — Crie, organize, participe. Eliminatórias, Dupla Elim., Grupos + Elim., Suíço, Liga, Rei/Rainha. Check-in, sorteio, resultados ao vivo.</p>' +
          '<p>📡 <b>Partida Casual</b> — Partidas avulsas sem criar torneio. Placar ao vivo em tela cheia, sincronizado entre jogadores via código de sala/QR. Estatísticas detalhadas no perfil.</p>' +
          '<p>📍 <b>Presença</b> — "Status online" pra quadras e clubes. Check-in imediato ou ida planejada; amigos veem no dashboard e podem se juntar.</p>' +
          '<p>🏢 <b>Locais</b> — Descubra clubes e arenas na sua cidade ou em viagens. Mapa com pins, filtros, reviews, integração com torneios e presença.</p>' +
          '<p style="font-size:0.78rem;color:var(--text-muted);margin-top:6px;">As 4 áreas se conectam: criar torneio num local visto em Locais prefilla o venue; ver presenças lista torneios agendados no mesmo lugar; etc.</p>' +
        '</div>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Passo 1: Fazer Login</div>' +
          '<p>Clique no botão <b>"Entrar"</b> no canto superior direito da tela.</p>' +
          '<p style="font-weight:600;color:#34d399;margin-top:8px;">Forma mais rápida: Google</p>' +
          '<ol style="padding-left:20px;margin:6px 0;">' +
            '<li>Clique no botão <b>"Entrar com Google"</b> (botão branco grande no topo)</li>' +
            '<li>Uma janela do Google vai abrir. <b>Escolha sua conta</b> Google (ou digite seu e-mail Google)</li>' +
            '<li>O Google pode perguntar: <b>"Continuar para scoreplace.app?"</b> — clique em <b>"Continuar"</b></li>' +
            '<li>Se pedir permissões, clique em <b>"Permitir"</b> — usamos apenas para identificar você</li>' +
            '<li>Pronto! Você já está logado. Seu nome e foto aparecem no canto superior direito</li>' +
          '</ol>' +
          '<p style="font-size:0.75rem;color:var(--text-muted);margin-top:6px;">Outras opções: Link Mágico (recebe um link por e-mail, sem senha), SMS (código no celular), ou E-mail e Senha tradicional.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Passo 2: Completar o Perfil</div>' +
          '<ol style="padding-left:20px;margin:6px 0;">' +
            '<li>Clique no seu <b>avatar</b> (foto ou iniciais) no canto superior direito</li>' +
            '<li>Preencha: <b>nome</b>, <b>gênero</b>, <b>cidade</b>, <b>esportes preferidos</b></li>' +
            '<li>Adicione seu <b>telefone</b> para receber notificações por WhatsApp</li>' +
            '<li>Marque seus <b>locais de preferência</b> no mapa para ser notificado de torneios próximos</li>' +
            '<li>Clique em <b>"Salvar"</b></li>' +
          '</ol>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Passo 3: Criar ou Participar de um Torneio</div>' +
          '<ol style="padding-left:20px;margin:6px 0;">' +
            '<li><b>Criar:</b> Na dashboard, clique em <b>"+ Novo Torneio"</b>, escolha o esporte e clique em "Criar Torneio"</li>' +
            '<li><b>Participar:</b> Navegue pelos torneios públicos em <b>"Explorar"</b> e clique em <b>"Inscrever-se"</b></li>' +
            '<li><b>Convite:</b> Se recebeu um link de convite, clique nele, faça login e você será inscrito automaticamente</li>' +
          '</ol>' +
        '</div>' +
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Passo 4: Convidar Amigos</div>' +
          '<ol style="padding-left:20px;margin:6px 0;">' +
            '<li>Dentro de um torneio, clique em <b>"Convidar"</b></li>' +
            '<li>Compartilhe via <b>WhatsApp</b>, <b>e-mail</b>, <b>QR Code</b> ou <b>link direto</b></li>' +
            '<li>Quem clicar no link e fizer login será inscrito automaticamente</li>' +
          '</ol>' +
        '</div>'
    },
    {
      id: 'dashboard',
      title: _t('help.dashboard'),
      icon: '📊',
      content: '<p>A <b>Dashboard</b> é sua página principal — concentra torneios, próximas partidas, amigos no local e atalhos para as 4 áreas principais do app.</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Hero e Ações Rápidas</div>' +
          '<p><b>Welcome para usuário novo</b> — Sem nenhum torneio criado ou disponível, você vê um card de boas-vindas com 4 CTAs coloridos: 🏆 Criar torneio, 🏢 Descobrir locais, 📍 Registrar presença, 👥 Encontrar amigos. Some assim que existir qualquer torneio visível pra você.</p>' +
          '<p><b>Barra de ações</b> — +Novo Torneio, Convidar amigos (QR Code do app), Pro, Apoie, Locais, Presença.</p>' +
          '<p><b>Contadores de resumo</b> — Todos, Organizados, Participando, <b>Inscrições Abertas</b>, Favoritos, Encerrados. Clique para filtrar.</p>' +
          '<p><b>"Inscrições Abertas"</b> — União dos <i>seus</i> torneios com inscrição aberta + torneios públicos do feed de descoberta que você ainda não entrou. Clicar "Inscrever-se" num card de descoberta aqui mesmo inscreve em 1 clique (sem precisar abrir a página do torneio).</p>' +
          '<p><b>Filtros secundários</b> — Pills coloridas por modalidade (roxo), local (verde), formato (amarelo). "✕ Limpar filtros" reseta.</p>' +
          '<p><b>Visualização</b> — Toggle Cards/Lista. Cards = visual completo; Lista = uma linha por torneio (compacto, bom pra muitos torneios).</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Widgets inline</div>' +
          '<p><b>Próximas partidas</b> — Widget no topo lista suas partidas pendentes (sem resultado ainda) em torneios ativos. Clique pra ir direto pra página de resultado.</p>' +
          '<p><b>Amigos no local</b> — Até 6 amigos com check-in ativo ou presença planejada hoje. Avatar com borda dourada, horário relativo ("🟢 agora · há X min" ou "🗓️ HH:mm"). Clicar prefilla a view de Presença com o mesmo venue/modalidade. Tem empty states: zero amigos mostra CTA pra #explore; zero presenças mostra linha discreta com link pra Minha presença.</p>' +
          '<p><b>Sua presença ativa</b> — Pill no topo da dashboard quando você tem check-in, plan ativo OU partida casual em andamento. <b>Check-in</b> mostra "📍 Você está em [Local] · expira em Xh" com dot verde. <b>Plan</b> mostra "🗓️ Planejado: [Local] hoje às HH:mm". <b>Partida casual</b> mostra "⚡ Partida casual em andamento · Sala ABC123" com botão "⚡ Voltar" que leva direto pro live scoring. Botões "Ver local" (abre modal do venue) e "Cancelar" (remove a presença) nas entradas de presença. Silent quando não há nenhum dos 3.</p>' +
          '<p><b>Complete seu perfil (banner âmbar)</b> — Aparece quando você já tem torneios mas faltam <i>cidade, modalidades preferidas ou locais preferidos</i> no perfil. Explica o que falta e os benefícios (torneios perto de você, parceiros da sua modalidade, presença rápida). "✕" descarta na sessão atual; reaparece no próximo login se os campos continuarem vazios.</p>' +
          '<p><b>🏟️ Meus locais</b> — Widget com até 5 locais onde você joga (preferências do perfil + venues dos seus torneios). Clique no nome abre a modal do venue. Botão "📍" verde à direita faz check-in direto: prefilla <code>#presence</code> e te leva pra lá com o local já selecionado. Link "Descobrir outros →" leva pra <code>#venues</code>. Só aparece quando existe pelo menos 1 venue relacionado.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Cards de Torneio</div>' +
          '<p>Cada card mostra: nome, esporte, formato, inscritos, barra de progresso, countdown e status. Cor de fundo indica seu papel (organizador = índigo, participante = teal).</p>' +
          '<p><b>Estrela ⭐</b> — Favoritar/desfavoritar. Aparecem no filtro "Favoritos".</p>' +
          '<p><b>Foto do local</b> — Fundo do card quando o venue tem <code>venuePhotoUrl</code>.</p>' +
          '<p><b>Logo do torneio</b> — Miniatura 56×56. Gerada automaticamente ou upload manual em Editar Torneio.</p>' +
          '<p><b>Botão de inscrição inline</b> — ✅ Inscrever-se / 🛑 Desinscrever-se / ✓ INSCRITO (badge), conforme seu estado atual. Funciona inclusive com torneios do feed de descoberta (hidratação defensiva injeta o doc em <code>AppStore.tournaments</code> antes do enroll).</p>' +
        '</div>' +
        '<div style="background:rgba(139,92,246,0.06);border:1px solid rgba(139,92,246,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Paginação e Agrupamento</div>' +
          '<p>Torneios carregam em lotes de 12. Botão "Carregar mais" aparece quando há mais pra mostrar.</p>' +
          '<p>No filtro "Todos", torneios encerrados ficam em <code>&lt;details&gt;</code> colapsável no final, separados em "seus encerrados" (se participou/organizou) e "outros encerrados".</p>' +
          '<p>No filtro "Abertos" com discovery esgotado, botão "🔍 Descobrir mais" pede a próxima página do feed público via cursor.</p>' +
        '</div>'
    },
    {
      id: 'criar-torneio',
      title: _t('help.createTournament'),
      icon: '➕',
      content: '<p>Duas formas de criar um torneio:</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Criação Rápida</div>' +
          '<p>Clique em "+ Novo Torneio" → escolha o esporte → "Criar Torneio". Pronto! Nome automático, formato Eliminatórias, inscrição individual.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Formulário Completo</div>' +
          '<p>Na criação rápida, clique em "Detalhes Avançados" ou use "Editar" em um torneio existente.</p>' +
          '<p><b>Esporte</b> — Beach Tennis, Tênis, Padel, Pickleball, Tênis de Mesa. Cada um tem padrões de pontuação próprios.</p>' +
          '<p><b>Formato</b> — Eliminatórias, Dupla Eliminatória, Grupos + Elim., Suíço, Liga. Cada formato tem configurações específicas.</p>' +
          '<p><b>Modo de Sorteio</b> — Sorteio (aleatório) ou Rei/Rainha da Praia (grupos de 4 com parceiros rotativos).</p>' +
          '<p><b>Modo de Inscrição</b> — Individual, Apenas Times ou Misto (aceita ambos).</p>' +
          '<p><b>Tipo de Jogo</b> — Simples (1v1) ou Duplas (2v2). Ambos ligados = chaves paralelas.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Campos do Formulário</div>' +
          '<p><b>Local</b> — Busca automática no Google Maps com foto e previsão do tempo.</p>' +
          '<p><b>Quadras</b> — Número de quadras (1-50) e nomes personalizados. Afeta a duração estimada.</p>' +
          '<p><b>Datas</b> — Início, fim e prazo de inscrição. Contagem regressiva automática nos cards.</p>' +
          '<p><b>Máximo de participantes</b> — Limite opcional. Pode auto-encerrar inscrições ao atingir.</p>' +
          '<p><b>Público/Privado</b> — Público aparece na aba Explorar. Endereço pode ser público ou restrito.</p>' +
          '<p><b>Categorias</b> — Gênero (Fem/Masc/Misto) × Habilidade (A/B/C). Gera chaveamentos separados.</p>' +
          '<p><b>Pontuação (GSM)</b> — Configure sets, games, tiebreaks e super tiebreak por esporte.</p>' +
          '<p><b>Logo</b> — Geração automática por Canvas ou upload. Cadeado trava contra auto-regeneração.</p>' +
          '<p><b>W.O.</b> — Individual (só o ausente perde) ou Time Inteiro (time eliminado).</p>' +
          '<p><b>Inscrições tardias</b> — Fechado, Lista de Espera ou Expansão (aceita e ajusta chave).</p>' +
          '<p><b>Desempate</b> — Arraste para reordenar critérios: confronto direto, saldo, Buchholz, etc.</p>' +
          '<p><b>Templates</b> — Salve configurações como template. Carregue em torneios futuros.</p>' +
        '</div>'
    },
    {
      id: 'formatos',
      title: _t('help.formats'),
      icon: '🎯',
      content: '<p>O scoreplace.app suporta <b>5 formatos</b> de torneio + modo Rei/Rainha:</p>' +
        '<div style="background:rgba(59,130,246,0.06);border:1px solid rgba(59,130,246,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<p style="margin:0;"><b>🏆 Eliminatórias Simples</b> — Perdeu, está fora. Opcional: disputa de 3º lugar, repescagem (melhores perdedores da R1 disputam vagas extra). Quando inscritos não são potência de 2, painel inteligente sugere: BYE, Play-in, Suíço, reabrir ou enquete.</p>' +
        '</div>' +
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<p style="margin:0;"><b>🔄 Dupla Eliminatória</b> — Precisa perder 2x para sair. Chave de vencedores (Winners) e perdedores (Losers). Grande final entre o campeão de cada chave.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<p style="margin:0;"><b>⚽ Fase de Grupos + Eliminatórias</b> — Estilo Copa do Mundo. Participantes divididos em grupos, todos jogam entre si. Os N melhores de cada grupo avançam para mata-mata. Configurável: número de grupos e classificados por grupo.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<p style="margin:0;"><b>♟️ Suíço</b> — Todos jogam todas as rodadas. Emparelhamentos por pontuação (adversários de nível similar). Ótimo para muitos participantes em poucas rodadas. Rodadas configuráveis. Classificação com Buchholz e Sonneborn-Berger.</p>' +
        '</div>' +
        '<div style="background:rgba(236,72,153,0.06);border:1px solid rgba(236,72,153,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<p style="margin:0;"><b>📊 Liga</b> — Temporada contínua com classificação por pontos. Duração configurável. Inscrições sempre abertas (opcional). Sorteios automáticos ou manuais. Cada jogador tem um toggle "Ativo" no seu card: desativado = folga com 0 pontos; quem fica de fora por número ímpar recebe a média dos seus pontos. Rodadas podem usar formato Rei/Rainha. Ideal para comunidades regulares.</p>' +
        '</div>' +
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.15);border-radius:10px;padding:12px;">' +
          '<p style="margin:0;"><b>👑 Rei/Rainha da Praia</b> — Modo de sorteio especial: grupos de 4 jogadores com parceiros rotativos (AB vs CD, AC vs BD, AD vs BC). Pontuação individual. Top 1 ou 2 de cada grupo avançam para eliminatória até coroar o Rei/Rainha. Pode ser usado na Liga também.</p>' +
        '</div>'
    },
    {
      id: 'inscricao',
      title: _t('help.enrollment'),
      icon: '✍️',
      content: '<p><b>Inscrever-se</b> — Abra o torneio e clique em "Inscrever-se". Em torneios com categorias, escolha sua categoria. Em torneios por times, preencha os nomes da equipe.</p>' +
        '<p><b>Por convite</b> — Clique no link recebido, faça login e será inscrito automaticamente (+ amizade com quem convidou).</p>' +
        '<p><b>Cancelar inscrição</b> — Botão "Cancelar Inscrição" na página do torneio. Confirmação obrigatória.</p>' +
        '<p><b>Encerramento automático</b> — Inscrições encerram ao atingir o limite ou na data/hora de prazo.</p>' +
        '<p><b>Inscrições tardias</b> — Dependendo da configuração: Fechado (bloqueado), Lista de Espera (suplente) ou Expansão (aceita e ajusta chave).</p>' +
        '<p><b>Lista de espera</b> — Suplentes podem substituir ausentes no check-in. Contagem separada no card.</p>' +
        '<p><b>Modos</b> — Individual (cada um por si), Times (duplas/equipes inscritas juntas) ou Misto (aceita ambos).</p>'
    },
    {
      id: 'sorteio',
      title: _t('help.draw'),
      icon: '🎲',
      content: '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Antes do Sorteio</div>' +
          '<p>O organizador encerra inscrições (manual ou automático) e clica em "Sortear".</p>' +
          '<p><b>Painel de resolução</b> — Se os inscritos não são potência de 2 (eliminatórias), painel inteligente com Nash sugere: BYE, Play-in, Repescagem, Suíço, Reabrir, Lista de Espera ou Enquete.</p>' +
          '<p><b>Times incompletos</b> — Se há sobra de jogadores que não formam time completo: Reabrir, Bots, Lista de Espera ou Ajuste Manual.</p>' +
          '<p><b>Cabeças de chave (VIP)</b> — Marque jogadores como VIP na lista de inscritos para prioridade no seeding.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">O Sorteio</div>' +
          '<p><b>Eliminatórias</b> — Confrontos aleatórios respeitando VIPs. BYEs distribuídos intercalados.</p>' +
          '<p><b>Grupos</b> — Painel mostra todas as distribuições possíveis (grupos iguais e mistos). Classificados por grupo configurável.</p>' +
          '<p><b>Liga/Suíço</b> — Gera rodada por pontuação. Sorteios podem ser automáticos (agendados) ou manuais.</p>' +
          '<p><b>Rei/Rainha</b> — Grupos de 4 com rotação de parceiros (3 partidas por grupo).</p>' +
          '<p><b>Divulgação imediata</b> — Todos recebem notificação quando o sorteio é realizado.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Enquete entre Participantes</div>' +
          '<p>Quando há impasse (potência de 2, times incompletos), crie uma enquete para os participantes votarem.</p>' +
          '<p>Prazo padrão: 48h. Contagem regressiva ao vivo. Recomendação por Nash (justiça 45%, inclusão 35%, praticidade 20%).</p>' +
          '<p>Antes de votar: apenas descrições. Após votar: resultado parcial e seu voto visíveis. Pode mudar o voto até encerrar.</p>' +
        '</div>'
    },
    {
      id: 'checkin',
      title: _t('help.checkin'),
      icon: '✅',
      content: '<p><b>Check-in</b> — Após iniciar o torneio, faça a chamada na aba "Inscritos". Toggle Presente/Ausente por participante.</p>' +
        '<p><b>Filtros</b> — Todos, Presentes, Ausentes, Pendentes. Facilita o controle no dia do evento.</p>' +
        '<p><b>Indicadores nas chaves</b> — Ponto verde (presente), vermelho (ausente), cinza (pendente). Partida verde quando ambos presentes.</p>' +
        '<p><b>W.O. (Walkover)</b> — Marque ausente → botão W.O. dá vitória automática ao adversário. Escopo configurável: Individual (só o ausente) ou Time Inteiro.</p>' +
        '<p><b>Substituição automática</b> — Marque W.O. e o próximo presente da lista de espera entra no lugar.</p>' +
        '<p><b>Limpar check-in</b> — Botão "Limpar" reseta todos os check-ins para recomeçar.</p>' +
        '<p><b>Ordenação</b> — Botões A-Z (alfabética) e 🕐 (cronológica) no cabeçalho da lista.</p>'
    },
    {
      id: 'resultados',
      title: _t('help.results'),
      icon: '📝',
      content: '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Lançando Resultados</div>' +
          '<p><b>Placar simples</b> — Digite o placar diretamente nos campos numéricos do card da partida e clique ✓ para confirmar.</p>' +
          '<p><b>Game-Set-Match (GSM)</b> — Para torneios com sets, clique em "Lançar Sets" para abrir o overlay dedicado. Insira o placar set a set; tiebreaks aparecem automaticamente quando necessário.</p>' +
          '<p><b>Editar resultado</b> — Clique no ✏️ para reabrir os campos e corrigir o placar.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Progressão Automática</div>' +
          '<p><b>Eliminatórias</b> — Vencedor avança automaticamente. Ao completar todas as partidas, o torneio encerra e exibe o pódio.</p>' +
          '<p><b>Grupos</b> — Ao completar todos os jogos do grupo, os classificados avançam para eliminatória automaticamente.</p>' +
          '<p><b>Liga/Suíço</b> — Classificação atualizada em tempo real. "Fechar Rodada" gera a próxima. Na Liga, jogadores inativos (toggle desativado) ficam de folga com 0 pontos; quem fica de fora por número insuficiente de jogadores recebe a média dos seus pontos.</p>' +
          '<p><b>Rei/Rainha</b> — Standings individuais por grupo. Top jogadores avançam para eliminatória.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Compartilhar e Exportar</div>' +
          '<p><b>Compartilhar resultado</b> — Botão de compartilhar em cada partida concluída (WhatsApp, clipboard).</p>' +
          '<p><b>Exportar CSV</b> — Baixe todos os resultados em planilha para Excel/Google Sheets.</p>' +
          '<p><b>Imprimir</b> — Imprima o chaveamento completo (orientação paisagem automática).</p>' +
          '<p><b>📅 Adicionar à agenda</b> — Na página do torneio, botão abre picker com 3 opções: <b>Google Calendar</b> (abre em nova aba com o evento pré-preenchido), <b>Outlook.com</b> (equivalente pra Microsoft), <b>Apple/Outlook (.ics)</b> (download de arquivo .ics que o Apple Calendar, Outlook desktop e Thunderbird importam). Preenche título, data, duração (assume 4h se endDate ausente), local (venue + endereço) e link do torneio. Só aparece se <code>startDate</code> está definido.</p>' +
          '<p><b>🏢 Local (botão)</b> — Na linha do endereço (ao lado do 🗺️ Google Maps), botão azul ciano leva pra modal do venue no scoreplace: movimento ao vivo (check-ins + torneios hoje), contatos (📞 💬 📷 ✉️), avaliações. Atalho "torneio → local" num tap — útil quando usuário quer ver quem mais está jogando ali ou entrar em contato com o clube.</p>' +
        '</div>'
    },
    {
      id: 'convidar',
      title: _t('help.invite'),
      icon: '👥',
      content: '<p>Clique em "Convidar" dentro do torneio para abrir o painel com 5 canais:</p>' +
        '<p><b>👥 Amigos na plataforma</b> — Envie convites para todos os seus amigos com um clique. Eles recebem notificação direta.</p>' +
        '<p><b>📱 QR Code</b> — Código exclusivo para projetar no evento. Escaneie com a câmera e se inscreva na hora.</p>' +
        '<p><b>💬 WhatsApp</b> — Link formatado aberto direto no WhatsApp. Ideal para grupos.</p>' +
        '<p><b>📧 E-mail</b> — Envie convite por e-mail digitando o endereço do destinatário.</p>' +
        '<p><b>🔗 Copiar link</b> — Copie o link para colar em Instagram, Telegram, SMS ou qualquer rede.</p>' +
        '<p><b>Auto-inscrição</b> — Quem clicar no link e fizer login é inscrito automaticamente + amizade com quem convidou.</p>' +
        '<p><b>Sem login</b> — Visitantes podem ver o torneio sem login. Ao clicar em "Inscrever-se", o login é solicitado.</p>'
    },
    {
      id: 'perfil',
      title: _t('help.profile'),
      icon: '👤',
      content: '<p><b>Acessar</b> — Clique no seu avatar (foto ou iniciais) no canto superior direito.</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Informações Pessoais</div>' +
          '<p><b>Nome</b> — Exibido em torneios, chaveamentos e classificações.</p>' +
          '<p><b>Gênero</b> — Usado para atribuição automática de categorias (Fem/Masc).</p>' +
          '<p><b>Data de nascimento</b> — Exibida na aba Explorar para outros jogadores.</p>' +
          '<p><b>Cidade e localização</b> — Busca automática no mapa. Botão 📍 usa GPS.</p>' +
          '<p><b>Esportes preferidos</b> — <b>Botões toggleáveis</b> (Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel). Clique pra marcar/desmarcar. Antes era campo de texto livre — agora são pills consistentes com os filtros de discovery e o picker de presença. Selecionados ficam em âmbar. Usado pra sugerir torneios e parceiros.</p>' +
          '<p><b>Locais de preferência</b> — Busca <b>dinâmica</b> conforme você digita (2+ caracteres, debounce de 150ms). Dropdown de sugestões do Google Places aparece automaticamente. Botão 📍 ao lado usa GPS. Adicione múltiplos locais no mapa — eles aparecem no widget "🏟️ Meus locais" da dashboard pra check-in rápido.</p>' +
          '<p><b>Categoria/nível</b> — Seu nível de jogo (A, B, C, etc.).</p>' +
          '<p><b>Telefone</b> — Com código do país para notificações por WhatsApp.</p>' +
          '<p><b>Avatar</b> — Escolha entre avatares pré-definidos ou use sua foto do Google.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Configurações</div>' +
          '<p><b>Idioma</b> — 🇧🇷 Português ou 🇺🇸 English. Bandeiras clicáveis com destaque visual.</p>' +
          '<p><b>Tema</b> — Noturno (escuro), Claro, Pôr do Sol, Oceano. 4 opções visuais.</p>' +
          '<p><b>Notificações</b> — Todas (padrão), Só Importantes ou Só Fundamentais. Canais: Plataforma, E-mail, WhatsApp.</p>' +
          '<p><b>Dicas visuais</b> — Ative/desative as dicas que aparecem quando você fica parado.</p>' +
        '</div>' +
        '<div style="background:rgba(239,68,68,0.06);border:1px solid rgba(239,68,68,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Estatísticas e Conta</div>' +
          '<p><b>Meu Desempenho</b> — Torneios, partidas, vitórias, derrotas, aproveitamento e títulos.</p>' +
          '<p><b>Histórico</b> — Últimos 8 torneios com posição final e link direto.</p>' +
          '<p><b>Excluir conta</b> — Exclusão permanente com dupla confirmação (digitar "EXCLUIR"). Remove todos os dados.</p>' +
        '</div>'
    },
    {
      id: 'notificacoes',
      title: _t('help.notifications'),
      icon: '🔔',
      content: '<p>O scoreplace.app notifica sobre tudo que acontece nos seus torneios:</p>' +
        '<p><b>Inscrição</b> — Confirmação ao se inscrever. Organizador também é notificado.</p>' +
        '<p><b>Alterações</b> — Data, local, formato ou configurações mudaram? Você sabe.</p>' +
        '<p><b>Sorteio e resultados</b> — Notificação quando rodada é sorteada e quando resultado é lançado.</p>' +
        '<p><b>Comunicados</b> — Mensagens do organizador com nível: Fundamental, Importante ou Geral.</p>' +
        '<p><b>Lembretes</b> — Automáticos: 7 dias, 2 dias e no dia do torneio.</p>' +
        '<p><b>Torneios próximos</b> — Baseado na sua localização (perfil). Avisado quando abrem inscrições perto.</p>' +
        '<p><b>Amizade e co-organização</b> — Pedidos de amizade e convites para co-organizar aparecem nas notificações. Aceite ou recuse diretamente.</p>' +
        '<p><b>Presença de amigos (🗓️ plan / 📡 check-in)</b> — Dois tipos de aviso quando um amigo se mexe num local. <b>Planejando (🗓️):</b> "Fulano vai jogar X em Y às HH:mm. Quer ir junto?" — botão "🏢 Ver local". <b>Check-in imediato (📡, vermelho):</b> "Fulano chegou em Y pra jogar X agora. Vem junto!" — botão "📡 Vou também". Check-in tem throttle: 1 notificação por venue+modalidade+dia pra evitar spam quando o usuário faz/refaz check-in várias vezes.</p>' +
        '<p><b>Níveis</b> — <span style="color:#ef4444;">Fundamentais</span> (inscrição, dia do torneio), <span style="color:#f59e0b;">Importantes</span> (alterações, 2 dias antes), <span style="color:#60a5fa;">Gerais</span> (7 dias antes, comunicados, proximidade, presença de amigos).</p>' +
        '<p><b>Push</b> — Notificações push no navegador (requer permissão). Funcionam mesmo com o app fechado.</p>' +
        '<p><b>Ações inline</b> — Convites (amizade, co-host, transferência de host) permitem aceitar/recusar direto da notificação. Avisos com <code>tournamentId</code> têm botão "Ver torneio" / "Ver chaveamento". <code>presence_plan</code> e <code>presence_checkin</code> levam pro venue. <code>casual_invite</code> (⚡) leva direto pra sala da partida casual.</p>'
    },
    {
      id: 'explorar',
      title: _t('help.explore'),
      icon: '🌐',
      content: '<p><b>Aba Explorar</b> — Encontre jogadores e expanda sua rede na comunidade.</p>' +
        '<p><b>Pedidos pendentes</b> — No topo, aceite ou recuse pedidos de amizade recebidos.</p>' +
        '<p><b>Seus amigos</b> — Lista com avatar, cidade, esportes e idade. Remova amigos se quiser.</p>' +
        '<p><b>Conhecidos</b> — Jogadores de torneios que você participou mas que ainda não são amigos. Envie convite de amizade.</p>' +
        '<p><b>Busca</b> — Encontre jogadores por nome, cidade ou esporte. Envie convites diretamente.</p>' +
        '<p><b>Cancelar</b> — Cancele um pedido de amizade enviado que ainda está pendente.</p>'
    },
    {
      id: 'organizador',
      title: _t('help.organizers'),
      icon: '🛠️',
      content: '<p><b>Visão de Organizador</b> — Botão "Organizador/Participante" no cabeçalho alterna perspectivas.</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Ferramentas do Organizador</div>' +
          '<p><b>Editar</b> — Reabra o formulário completo para alterar qualquer configuração.</p>' +
          '<p><b>Comunicar</b> — Envie mensagens para todos os inscritos com nível de importância (Fundamental/Importante/Geral).</p>' +
          '<p><b>+ Participante / + Time</b> — Adicione inscritos manualmente.</p>' +
          '<p><b>Add Bot</b> — Preencha vagas com jogadores fictícios para testes.</p>' +
          '<p><b>Categorias</b> — Gerencie categorias: mova, mescle, atribua participantes.</p>' +
          '<p><b>Exportar CSV</b> — Baixe resultados em planilha.</p>' +
          '<p><b>Salvar Template</b> — Salve configurações para reusar em futuros torneios.</p>' +
          '<p><b>QR Code</b> — Gere QR para projetar no evento.</p>' +
          '<p><b>Modo TV</b> — Projete placar ao vivo em telão (fullscreen, auto-refresh 30s).</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Fluxo do Organizador</div>' +
          '<ol style="padding-left:20px;margin:6px 0;">' +
            '<li>Crie o torneio (rápido ou detalhado)</li>' +
            '<li>Convide participantes</li>' +
            '<li>Gerencie categorias (se aplicável)</li>' +
            '<li>Encerre inscrições (manual ou automático)</li>' +
            '<li>Sorteie o chaveamento</li>' +
            '<li>Inicie o torneio + check-in</li>' +
            '<li>Lance resultados ou aguarde os jogadores</li>' +
            '<li>Feche rodadas (Liga/Suíço) ou aguarde encerramento automático</li>' +
            '<li>Encerre e veja o pódio!</li>' +
          '</ol>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Co-organização</div>' +
          '<p><b>Compartilhar organização</b> — Convide um participante para ser co-organizador (botão 👑 Organização).</p>' +
          '<p><b>Transferir</b> — Transfira a organização completa para outro participante.</p>' +
          '<p>Co-organizadores têm acesso a todas as ferramentas exceto excluir o torneio.</p>' +
        '</div>'
    },
    {
      id: 'categorias',
      title: _t('help.categories'),
      icon: '🏷️',
      content: '<p>O organizador pode definir categorias para qualquer formato de torneio, permitindo que participantes de diferentes perfis disputem separadamente:</p>' +
        '<p><b>Categorias de gênero</b> — Botões de alternância: Feminino (♀), Masculino (♂), Misto Aleatório (⚥) e Misto Obrigatório (⚤). Não são excludentes — o organizador pode ativar quantos quiser.</p>' +
        '<p><b>Categorias de habilidade</b> — Campo de texto livre onde o organizador digita as categorias separadas por vírgula (ex: A, B, C, D).</p>' +
        '<p><b>Categorias combinadas</b> — O sistema combina automaticamente gênero × habilidade. Exemplo: ativando Fem + Masc e digitando A, B gera: Fem A, Fem B, Masc A, Masc B.</p>' +
        '<p><b>Inscrição com categoria</b> — Ao se inscrever em um torneio com categorias, o participante escolhe a categoria desejada. Se o perfil já tiver gênero preenchido, as opções são filtradas automaticamente.</p>' +
        '<p><b>Sorteio e chaves por categoria</b> — O sorteio gera confrontos separados por categoria. Participantes só enfrentam adversários da mesma categoria.</p>' +
        '<p><b>Classificação por categoria</b> — A tabela de classificação mostra resultados separados por categoria em formatos Liga e Suíço.</p>' +
        '<p><b>Misto Obrigatório</b> — Times devem ter 50% homens e 50% mulheres.</p>' +
        '<p><b>Misto Aleatório</b> — Aceita homens e mulheres sem restrição de proporção. A composição é definida pelo sorteio.</p>' +
        '<p><b>Gerenciar categorias</b> — O botão "Categorias" permite ao organizador: ver quantos inscritos há em cada categoria (agrupados por gênero), mesclar categorias por arrastar e soltar (ex: Fem A + Fem B → Fem A/B), e atribuir categoria a participantes sem categoria. Clique em uma categoria para ver os inscritos. Categorias mescladas exibem um ícone de desmesclagem no canto superior direito, permitindo desfazer a mesclagem e reatribuir participantes às categorias originais.</p>' +
        '<p><b>Remover participante da categoria</b> — Na visualização detalhada de uma categoria, cada card de participante tem um botão para removê-lo daquela categoria. O participante fica "sem categoria" e pode ser reatribuído a qualquer outra.</p>' +
        '<p><b>Atribuição automática</b> — Quando o organizador cria categorias em um torneio que já tem inscritos, os participantes com gênero no perfil são atribuídos automaticamente. O badge "(perfil)" indica que a categoria veio do perfil do participante.</p>' +
        '<p><b>Notificação de categoria</b> — Participantes atribuídos a uma categoria recebem uma notificação ao acessar o torneio, com opção de questionar o organizador por e-mail.</p>' +
        '<p><b>Histórico</b> — Participantes que se inscreveram sem categoria e depois foram alocados mostram o indicador "(sem cat.)" ao lado da categoria, mantendo o histórico de que a inscrição ocorreu antes das categorias.</p>'
    },
    {
      id: 'local',
      title: _t('help.venue'),
      icon: '📍',
      content: '<p><b>Busca automática</b> — Digite o nome do local e o Google Maps sugere endereços reais com foto e coordenadas.</p>' +
        '<p><b>Localização GPS</b> — Botão 📍 usa sua localização atual para preencher o campo.</p>' +
        '<p><b>Acesso</b> — Público (visível para todos), Sócios ou Convite.</p>' +
        '<p><b>Endereço público/privado</b> — Toggle controla se o endereço aparece na aba Explorar.</p>' +
        '<p><b>Quadras</b> — 1 a 50 quadras com nomes personalizados (ex: Quadra Central, Quadra 2).</p>' +
        '<p><b>Duração estimada</b> — Cálculo automático: duração da partida × número de partidas ÷ quadras + aquecimento + chamada.</p>' +
        '<p><b>Previsão do tempo</b> — Quando o local tem coordenadas e a data é nos próximos 5 dias, exibe previsão automática.</p>' +
        '<p><b>Foto do local</b> — Aparece como fundo nos cards do dashboard e na página de detalhes.</p>'
    },
    {
      id: 'chaveamento',
      title: _t('help.bracket'),
      icon: '🏅',
      content: '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Navegação no Chaveamento</div>' +
          '<p><b>Zoom</b> — Slider e botões +/- para ajustar o tamanho. Arraste para navegar.</p>' +
          '<p><b>Ocultar rodadas</b> — Clique para esconder rodadas concluídas e economizar espaço.</p>' +
          '<p><b>Navegação por grupos</b> — Botões coloridos (A, B, C...) para pular direto ao grupo desejado.</p>' +
          '<p><b>Só meus jogos</b> — Toggle filtra para mostrar apenas suas partidas.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Classificação (Liga/Suíço)</div>' +
          '<p><b>Tabela de standings</b> — Colunas: Posição, Nome, Pts, V, E, D, PF, PC, Saldo. GSM adiciona ±S e ±G.</p>' +
          '<p><b>Ordenação</b> — Clique nos cabeçalhos (▲/▼) para ordenar por qualquer coluna.</p>' +
          '<p><b>Clique no nome</b> — Abre estatísticas globais do jogador ou histórico de partidas no torneio.</p>' +
          '<p><b>Confrontos diretos</b> — Matriz NxN expansível abaixo da classificação (verde=vantagem, vermelho=desvantagem).</p>' +
          '<p><b>Rodadas anteriores</b> — Seção colapsável mostrando resultados de rodadas passadas.</p>' +
          '<p><b>Estatísticas do torneio</b> — Destaques automáticos: mais vitórias, maior sequência, total de partidas.</p>' +
        '</div>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Ferramentas</div>' +
          '<p><b>Modo TV</b> — Fullscreen para projetar em telão. Auto-refresh 30s. Sair com ESC.</p>' +
          '<p><b>Imprimir</b> — CSS otimizado para impressão (paisagem, bordas, fundo branco).</p>' +
          '<p><b>Exportar CSV</b> — Classificação ou resultados em planilha.</p>' +
          '<p><b>Compartilhar resultado</b> — Cada partida tem botão de compartilhar (WhatsApp/clipboard).</p>' +
          '<p><b>Pódio</b> — Torneios encerrados exibem podio visual com medalhas (🥇🥈🥉).</p>' +
        '</div>'
    },
    {
      id: 'partida-casual',
      title: 'Partida Casual',
      icon: '📡',
      content: '<p>Jogue partidas avulsas com placar ao vivo em tela cheia — sem precisar criar um torneio.</p>' +
        '<div style="background:rgba(56,189,248,0.06);border:1px solid rgba(56,189,248,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Como funciona</div>' +
          '<p><b>1. Abrir</b> — Botão "📡 Partida Casual" na dashboard ou menu.</p>' +
          '<p><b>2. Configurar</b> — Escolha esporte, single/dupla, pontuação (sets, games, tie-break). Configurações salvas por esporte.</p>' +
          '<p><b>3. Convidar</b> — QR Code ou código da sala (6 caracteres). Jogadores logados entram automaticamente no lobby.</p>' +
          '<p><b>4. Montar times</b> — "Sortear" ON = duplas aleatórias ao iniciar. OFF = arraste jogadores para Time 1 / Time 2.</p>' +
          '<p><b>5. Jogar</b> — Placar ao vivo em tela cheia. Todos os participantes veem e podem marcar pontos em tempo real.</p>' +
        '</div>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Placar ao Vivo</div>' +
          '<p><b>Pontuação</b> — Toque ▲ para marcar ponto, ▼ para corrigir. Placares grandes para fácil visualização.</p>' +
          '<p><b>AD (vantagem no game)</b> — Toggle logo abaixo da contagem (15-30-40 / 1-2-3). Ligado: em 40-40 (deuce) exige 2 pontos de vantagem para fechar o game. Desligado: ponto de ouro (quem fizer o próximo ponto vence). Padrão por modalidade — Tênis e Padel ligado, Beach Tennis / Pickleball / Tênis de Mesa desligado.</p>' +
          '<p><b>Vantagem de 2 pontos (set)</b> — <b>Ativada por padrão em todas as modalidades.</b> Ligada: o set não termina em 5-6 — ao empatar em (g-1)-(g-1) ou a cada novo empate que impossibilite a vantagem de 2 games, o app pergunta "Prorrogar ou Tie-break". Desligada: o set acaba no primeiro a chegar em g games, sem extensão e sem tie-break.</p>' +
          '<p><b>Sets/Games</b> — Automático: ao completar games, avança set. Empate (ex: 5×5) oferece Prorrogar ou Tie-break (quando "Vantagem de 2 pontos" ligada).</p>' +
          '<p><b>Prorrogação</b> — Continua até vantagem de 2 games. Botão "⚡ Tie-break" ganha destaque com <b>glow pulsante</b> durante a prorrogação para facilitar a mudança a qualquer momento.</p>' +
          '<p><b>Tie-break</b> — Vence quem fizer 7 pontos primeiro com 2 de vantagem; se empatar em 7 continua até alguém abrir 2.</p>' +
          '<p><b>Ordem de saque</b> — Arraste a bolinha 🎾 para trocar o sacador. Após o 1º game o time que começou trava; após o 2º game a ordem inteira trava com um cadeado 🔒.</p>' +
          '<p><b>Decisões restritas aos jogadores</b> — Escolha de prorrogar vs tie-break e botão de tie-break só aparecem para usuários cadastrados envolvidos na partida. Não-jogadores veem "Aguardando decisão".</p>' +
          '<p><b>Nomes editáveis</b> — Toque no nome para alterar. Fotos de perfil exibidas para jogadores logados.</p>' +
        '</div>' +
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.20);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Tela de Resultado</div>' +
          '<p><b>Layout</b> — Vencedor no topo com troféu, placar e nomes clicáveis. Logo abaixo vem o gráfico Momentum (animação visível de imediato). Depois a seção Comparação dos Times e os stats do perdedor.</p>' +
          '<p><b>Gráfico Momentum</b> — Duas linhas cumulativas animadas: azul para o Time 1, vermelha para o Time 2. Linhas desenham da esquerda para a direita em ~3s com marcadores de ponto final e divisores de set (S1, S2...). Botão ↻ Replay reexecuta a animação.</p>' +
          '<p><b>Comparação dos Times</b> — Barras lado a lado: Sets, Games, Pontos, % Pontos no Saque, % Pontos na Recepção, Games Mantidos, Quebras de Saque, Killer Points (40-40), Maior Sequência e Maior Vantagem.</p>' +
          '<p><b>Cards de jogador clicáveis</b> — Toque em qualquer jogador para ver suas estatísticas individuais: games servidos/mantidos, aproveitamento, maior sequência, e também pts servidos/ganhos/% no saque.</p>' +
          '<p><b>Jogar Novamente + 📤 Compartilhar + Re-sortear duplas</b> — Botões fixos no rodapé da tela de resultado (não somem ao rolar). <b>Jogar Novamente</b> recomeça a partida com placar zerado. <b>📤 Compartilhar</b> dispara Web Share API (WhatsApp, Instagram DM, SMS) com placar set-a-set, vencedor e link do app; clipboard como fallback em desktop. Toggle <b>Re-sortear</b> (só em duplas) redistribui os jogadores aleatoriamente entre os times antes de reiniciar.</p>' +
          '<p><b>Botões não cortam</b> — O rodapé usa 100dvh + safe-area-inset para não ficar escondido pelo URL bar do navegador ou pelo home-indicator.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Lobby e Compartilhamento</div>' +
          '<p><b>Código da sala</b> — 6 caracteres. Digite na dashboard ("📷 Escanear QR") ou acesse #casual/CODIGO.</p>' +
          '<p><b>👥 Avisar amigos do scoreplace</b> — Botão no modal de convite (abaixo de Copiar link e WhatsApp) dispara notificação direta pra todos os amigos da scoreplace com link pra entrar na partida. Eles recebem "Fulano começou uma partida casual de X" com botão "⚡ Entrar na partida". Mais direto que WhatsApp pra quem já tá no app.</p>' +
          '<p><b>Lobby em tempo real</b> — Organizador e participantes veem quem já entrou. Atualização automática a cada 3s.</p>' +
          '<p><b>Times visíveis no lobby</b> — Quando o organizador monta os times (arraste jogadores, edite nomes), as mudanças aparecem em tempo real no lobby dos outros usuários sob "⚔ Times formados".</p>' +
          '<p><b>Sair libera a vaga</b> — Clicar em Sair (ou Voltar ao Dashboard) remove seu uid do slot e te redireciona para o dashboard — outro jogador pode ocupar a vaga imediatamente.</p>' +
          '<p><b>Fechar partida</b> — O botão ✕ Fechar durante a partida pede confirmação; ao confirmar o abandono, sua vaga é liberada e você volta para o dashboard.</p>' +
          '<p><b>Resultado</b> — Ao finalizar, confirme o resultado. Estatísticas detalhadas salvas no seu perfil (persistentes).</p>' +
        '</div>'
    },
    {
      id: 'presenca',
      title: 'Presença no Local',
      icon: '📍',
      content: '<p>Registre quando você está num local esportivo pra que seus amigos saibam e possam se juntar. Funciona como "status online" para quadras e clubes.</p>' +
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Como acessar</div>' +
          '<p><b>Hero do dashboard</b> — Botão "📍 Presença" abre a view completa. Na dashboard também há o widget "Amigos no local" mostrando até 6 amigos com avatar + horário.</p>' +
          '<p><b>Atalho</b> — Hash direto: <code>#presence</code>.</p>' +
          '<p><b>A partir de um local</b> — Abra o card de um venue em Descobrir Locais e use "📍 Estou aqui agora" ou "🗓️ Planejar ida" direto na modal — evita navegar até a view de Presença.</p>' +
        '</div>' +
        '<div style="background:rgba(56,189,248,0.06);border:1px solid rgba(56,189,248,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Dois tipos de presença</div>' +
          '<p><b>📍 Estou aqui agora</b> — Check-in imediato. Válido por 3h (janela padrão) a partir do momento da marcação. Aparece como "🟢 agora" pros amigos. <b>Amigos recebem notificação "📡 Fulano chegou em X pra jogar Y. Vem junto!"</b> (throttled: 1 por venue+modalidade+dia pra não spammar).</p>' +
          '<p><b>🗓️ Planejar ida</b> — Agendamento futuro. Escolha data/hora de início e (opcionalmente) de saída — deixe em branco se não quiser fixar horário de fim. Amigos recebem notificação "🗓️ Fulano vai jogar X em Y às HH:mm. Quer ir junto?". <b>Botão 📅 na linha</b> abre o picker de agenda (Google Calendar / Outlook / .ics) pra você não esquecer.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Multi-modalidade</div>' +
          '<p>Um local pode oferecer várias modalidades (Beach Tennis + Tênis + Pickleball, por ex). Selecione todas com pills toggleáveis e o doc de presença fica com todas gravadas. Filtros respeitam a seleção e o "Movimento" consolida tudo num único lugar.</p>' +
          '<p><b>Check-in multi-sport</b> — Quando você está disponível pra jogar mais de uma modalidade, basta deixar todas selecionadas antes de clicar "Estou aqui". O doc guarda <code>sports[]</code> e fica queryable por qualquer uma delas.</p>' +
        '</div>' +
        '<div style="background:rgba(139,92,246,0.06);border:1px solid rgba(139,92,246,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Visibilidade e privacidade</div>' +
          '<p><b>Amigos (padrão)</b> — Só seus amigos veem sua presença. Aparece com avatar no mapa/ widget da dashboard.</p>' +
          '<p><b>Público</b> — Qualquer usuário logado vê (sem expor sua identidade — conta como "+1" anônimo no "👥 Agora no local").</p>' +
          '<p><b>Desligado</b> — Nada é escrito. Use quando quiser jogar sem sinalizar presença. Configurável em Perfil → Presença.</p>' +
          '<p><b>Silenciar temporariamente</b> — No perfil, escolha silenciar 1h/4h/dia/semana. Seu check-in fica desativado até o timer acabar; widgets ignoram presenças de amigos durante esse período também (simétrico).</p>' +
        '</div>' +
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.18);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Integração com torneios e partidas casuais</div>' +
          '<p><b>Torneios no local</b> — O gráfico de barras por hora inclui torneios agendados no mesmo venue/modalidade/dia como presenças virtuais ("🏆 Torneio X às 18h").</p>' +
          '<p><b>Modal do venue</b> — Em Descobrir Locais, a seção "Movimento no local" mostra avatares de amigos com check-in ativo + "+N" de outros usuários e torneios acontecendo hoje.</p>' +
        '</div>'
    },
    {
      id: 'locais',
      title: 'Descobrir Locais',
      icon: '🏢',
      content: '<p>Encontre clubes, arenas e quadras abertas ao público — útil pra viagens, descobrir sua cidade ou compartilhar o local onde você joga.</p>' +
        '<div style="background:rgba(14,165,233,0.06);border:1px solid rgba(14,165,233,0.20);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Como acessar</div>' +
          '<p>Botão "🏢 Locais" no hero do dashboard ou hash <code>#venues</code>. Público — funciona sem login (descoberta de onboarding).</p>' +
        '</div>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Filtros e busca</div>' +
          '<p><b>Local</b> — Digite endereço, bairro ou cidade. O geocoder resolve em lat/lng e centraliza o mapa.</p>' +
          '<p><b>📍 GPS</b> — Botão ao lado do campo usa sua localização precisa (tem prioridade sobre o texto digitado).</p>' +
          '<p><b>Modalidade</b> — Filtra por Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel. "Qualquer" traz todos.</p>' +
          '<p><b>Mín. quadras</b> — Default 1. Útil pra filtrar clubes com mais infraestrutura.</p>' +
          '<p><b>Distância (km)</b> — Raio a partir do centro. Default 10km. O raio vira círculo visual no mapa.</p>' +
          '<p><b>Faixa de preço</b> — $/$$/$$$ ou "Qualquer". Filtra cadastrados; sugestões do Google ignoram.</p>' +
          '<p>Filtros persistem entre sessões em <code>localStorage</code> — você volta e o filtro ainda tá do jeito que deixou.</p>' +
        '</div>' +
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Mapa vs Lista</div>' +
          '<p>Toggle no topo. <b>Mapa</b> é o default — descoberta é intrinsecamente espacial.</p>' +
          '<p><b>Pins do mapa</b>: 🏢 <b>âmbar</b> = local cadastrado (free). 🏢 <b>índigo</b> = local cadastrado (Pro). 🔎 <b>cinza translúcido</b> = sugestão do Google (não cadastrado). 📍 <b>verde</b> = sua localização.</p>' +
          '<p><b>Clique num pin âmbar/índigo</b> abre a modal de detalhe do venue (com Movimento, Reviews, CTAs). <b>Clique num pin cinza</b> abre o local direto no Google Maps em nova aba (ainda não existe na base do scoreplace — o dono pode reivindicar depois).</p>' +
          '<p><b>Gesture cooperative</b> — No mobile, o mapa exige dois dedos pra mexer (scroll com um dedo passa pra página). No desktop, Ctrl+scroll pra zoom. Isso evita o bug antigo de "ficar preso no mapa" ao tentar scrollar a tela.</p>' +
          '<p><b>Barra de resumo acima do mapa</b> — Mostra "🏢 N cadastrados · 🔎 M sugestões Google" em tempo real. Se zero resultados no raio atual, oferece botão "🔍 Expandir para Xkm" (triplica o raio) em 1 clique pra sair do beco sem saída.</p>' +
        '</div>' +
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.20);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Modal de detalhe do venue</div>' +
          '<p><b>Tags de proveniência</b>: "✅ Informações oficiais" quando reivindicado pelo proprietário; "📝 Cadastrado por [nome]" quando é cadastro colaborativo (qualquer usuário pode cadastrar um local que frequenta, estilo Wikipedia).</p>' +
          '<p><b>Contatos</b> — Botões 📞 Ligar, 💬 WhatsApp (auto-prefixo 55), 📷 Instagram, ✉️ E-mail quando disponíveis.</p>' +
          '<p><b>Movimento no local</b> — Amigos com check-in ativo (avatares com borda dourada), "+N outros" (presenças públicas/torneios em andamento), e eventos agendados "Mais tarde hoje" ordenados por horário.</p>' +
          '<p><b>Torneios neste local</b> — Próximos e recentes, linkados pra <code>#tournaments/&lt;id&gt;</code>. Usa o cache <code>AppStore.tournaments</code> (sem fetch extra).</p>' +
          '<p><b>Avaliações</b> — Estrelas 1–5 + texto opcional. Cada usuário logado deixa uma review (substitui se re-envia). Opção de anônimo quando só dá estrela sem texto. Média visível no header.</p>' +
          '<p><b>Ações</b>: <b>📍 Estou aqui agora</b> (check-in inline com toast — não navega), <b>🗓️ Planejar ida</b> (vai pra #presence com o dialog de agendamento auto-aberto), <b>🗺️ Ver no mapa</b> (Google Maps externo), <b>📍 Ver presenças</b> (view completa com gráfico por hora), <b>🏆 Criar torneio aqui</b> (abre criação rápida com o venue pré-preenchido), <b>📤 Compartilhar</b> (Web Share API nativa no mobile ou clipboard como fallback — link canônico <code>/#venues/&lt;placeId&gt;</code> que abre a mesma modal pra quem receber).</p>' +
        '</div>' +
        '<div style="background:rgba(139,92,246,0.06);border:1px solid rgba(139,92,246,0.18);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Cadastro em duas camadas (v0.15.36)</div>' +
          '<p><b>Camada 1 — Estabelecimento</b>: nome, endereço, horário, contatos, descrição, política de acesso (🌐 público, 👥 só sócios, 👥🏆 sócios + convidados de torneios, 🔒 privado). Qualquer usuário pode cadastrar (Wikipedia-style).</p>' +
          '<p><b>Camada 2 — Quadras (colaborativo)</b>: seção "🎾 Quadras do local" no form de edição. Qualquer jogador contribui com entradas tipo "4 quadras de Beach Tennis compartilhadas com Futevôlei" ou "2 quadras de saibro exclusivas de Tênis". Cada entrada tem: modalidade, quantidade, piso/tipo (opcional), flag compartilhada/exclusiva, observação. Cada doc rastreia o contribuidor.</p>' +
          '<p><b>Duas fases de edição:</b></p>' +
          '<ul style="margin:4px 0 8px 18px;font-size:0.78rem;">' +
            '<li><b>Colaborativa (sem dono)</b>: qualquer autenticado adiciona. Cada um edita/apaga apenas as entradas que criou.</li>' +
            '<li><b>Assumida pelo dono</b>: quando alguém reivindica o local como proprietário, passa a ser o <b>único editor de TUDO</b> — inclusive as quadras cadastradas antes pela comunidade. Contributors perdem o acesso de edição (a entrada fica, mas gerenciada pelo dono). Outros usuários não podem mais adicionar novas.</li>' +
          '</ul>' +
          '<p><b>Agregação</b>: na modal de detalhe do venue, a seção "🎾 Quadras disponíveis" soma todas as entradas por modalidade e mostra "6 quadras de Beach Tennis (saibro, areia) · 2 de Tênis (piso duro)" com tag 🔁 compartilhada quando aplicável. Rodapé lista quantos jogadores contribuíram.</p>' +
          '<p><b>Autocomplete do Google</b> — Digite o nome do local; o Google Places sugere. Ao selecionar, nome, endereço, lat/lng, telefone, site, horário e faixa de preço são pré-preenchidos quando disponíveis.</p>' +
          '<p><b>Reivindicar como dono</b> — Dentro do modal do venue cadastrado (sem dono ainda), botão "🏢 Reivindicar como dono" leva pra #my-venues em modo de edição com checkbox "Sou proprietário". Ao aceitar, o dono assume todas as informações (incluindo quadras) e vira o único editor. Se outro usuário já reivindicou, o sistema bloqueia e orienta contato com o suporte.</p>' +
        '</div>' +
        '<div style="background:rgba(59,130,246,0.06);border:1px solid rgba(59,130,246,0.18);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Plano Pro para proprietários</div>' +
          '<p><b>Destaque visual</b> — Cards e pins em índigo com glow (em vez do âmbar padrão). Ordenação server-side prioriza Pro no topo.</p>' +
          '<p><b>Painel analytics</b> — Visualizações (viewCount), check-ins nos últimos 7 dias, total de torneios cadastrados no local.</p>' +
          '<p><b>Como ativar</b> — Lista "Meus locais" no perfil mostra botão "🚀 Pro" pra cada venue free. Clique envia manifestação de interesse pra scoreplace.app@gmail.com com os dados do proprietário. Ativação manual no alpha; automação via Stripe quando a demanda justificar.</p>' +
        '</div>'
    },
    {
      id: 'estatisticas-detalhadas',
      title: 'Estatísticas Detalhadas',
      icon: '📊',
      content: '<p>O scoreplace guarda estatísticas detalhadas de cada partida na sua conta. Essas estatísticas sobrevivem mesmo se o torneio for apagado ou se a partida casual for encerrada.</p>' +
        '<p><b>Como acessar</b> — Toque no nome de qualquer jogador em qualquer tela (dashboard, chaveamento, classificação, resultado de partida casual) para abrir o modal de estatísticas. O botão <b>Voltar</b> fica fixo no topo, logo abaixo do menu, sempre visível durante o scroll.</p>' +
        '<div style="background:rgba(56,189,248,0.06);border:1px solid rgba(56,189,248,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">O que é registrado</div>' +
          '<p><b>Por partida</b> — Sets, games, pontos, % pontos no saque, % pontos na recepção, games mantidos no saque, quebras, killer points (40-40), maior sequência, maior vantagem, duração, esporte.</p>' +
          '<p><b>Por jogador</b> — Games servidos, games mantidos, aproveitamento (%), maior sequência de games mantidos, pontos servidos, pontos ganhos no saque e % no saque por ponto.</p>' +
          '<p><b>Casuais vs Torneios</b> — Estatísticas mantidas separadas entre partidas casuais (⚡) e torneios (🏆). Barras divergentes e ícones lado a lado em cada linha.</p>' +
        '</div>' +
        '<div style="background:rgba(34,197,94,0.06);border:1px solid rgba(34,197,94,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Barras Divergentes (Desempenho)</div>' +
          '<p>Derrotas à esquerda (vermelho), vitórias à direita (verde). O tamanho é proporcional ao total. Ordem das linhas:</p>' +
          '<p><b>1. Vitórias/Derrotas</b> — V-D totais.</p>' +
          '<p><b>2. Sets</b> — Sets vencidos e perdidos.</p>' +
          '<p><b>3. Games</b> — Games vencidos e perdidos.</p>' +
          '<p><b>4. Pontos</b> — Pontos vencidos e perdidos.</p>' +
          '<p><b>5. Tiebreaks</b> — TBs vencidos e perdidos.</p>' +
          '<p><b>6. Pontos TB Médios</b> — Média do placar vencedor em cada TB. À direita (verde): média dos seus pontos nos TBs que você venceu. À esquerda (vermelho): média dos pontos do adversário nos TBs em que ele venceu contra você.</p>' +
          '<p><b>7. TB Vencidos (mín/máx pts)</b> — Pontos que você marcou nos TBs que venceu. <b>Máx</b> = recorde positivo (o maior placar que você fez ganhando um TB). <b>Mín</b> = recorde negativo (seu menor placar em um TB vencido, geralmente o 7 limpo). Ambos os lados em verde — é o seu mesmo placar.</p>' +
          '<p><b>8. TB Perdidos (mín/máx pts)</b> — Pontos que o adversário marcou nos TBs em que ele venceu contra você. <b>Máx</b> = a maior surra (placar mais alto do adversário quando ganhou). <b>Mín</b> = a derrota mais apertada (menor placar do adversário ao vencer um TB, geralmente 7). Ambos os lados em vermelho.</p>' +
          '<p style="color:var(--text-muted);font-size:0.78rem;">As 3 linhas de TB (6-8) aparecem apenas quando há tiebreaks registrados.</p>' +
        '</div>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Top 3 Adversários / Parceiros</div>' +
          '<p><b>Adversários</b> — Contra quem você mais joga: casuais e torneios listados separadamente.</p>' +
          '<p><b>Parceiros</b> — Com quem você mais forma dupla: descubra sua dupla ideal pelas vitórias compartilhadas.</p>' +
        '</div>' +
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.20);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">Persistência</div>' +
          '<p>Os dados ficam salvos em <code>users/{seu_uid}/matchHistory</code> — cada jogador cadastrado envolvido recebe sua própria cópia. Mesmo que o torneio seja deletado ou a sala casual expire, seu histórico permanece.</p>' +
          '<p>Seção <b>📋 Torneios Disputados</b> no rodapé do modal lista cada torneio com link direto.</p>' +
        '</div>'
    },
    {
      id: 'desempate',
      title: _t('help.tiebreak'),
      icon: '⚖️',
      content: '<p>Critérios de desempate configuráveis pelo organizador (arraste para reordenar prioridade):</p>' +
        '<p><b>Confronto Direto</b> — Quem ganhou no confronto entre os empatados.</p>' +
        '<p><b>Saldo de Pontos</b> — Diferença entre pontos feitos e sofridos.</p>' +
        '<p><b>Número de Vitórias</b> — Total de vitórias no torneio.</p>' +
        '<p><b>Buchholz</b> — Força dos adversários enfrentados (soma dos pontos dos adversários).</p>' +
        '<p><b>Sonneborn-Berger</b> — Qualidade das vitórias (pontos dos adversários que você derrotou).</p>' +
        '<p><b>Sorteio</b> — Desempate aleatório como último recurso.</p>' +
        '<p><b>GSM extras</b> — Torneios com sets: saldo de sets (±S), saldo de games (±G), sets vencidos, games vencidos, tiebreaks vencidos.</p>'
    },
    {
      id: 'atalhos-teclado',
      title: _t('help.shortcuts'),
      icon: '⌨️',
      content: '<div style="display:grid;grid-template-columns:80px 1fr;gap:8px 16px;font-size:0.85rem;">' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">Ctrl+K</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Abrir busca rápida global</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">/</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Abrir busca rápida</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">D</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Ir para Dashboard</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">E</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Explorar torneios</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">N</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Criar novo torneio</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">?</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Abrir/fechar esta ajuda</span>' +
        '<kbd style="background:rgba(255,255,255,0.1);padding:4px 8px;border-radius:6px;text-align:center;font-family:monospace;color:var(--text-bright);font-size:0.8rem;">ESC</kbd><span style="color:var(--text-main);display:flex;align-items:center;">Fechar modal atual</span>' +
        '</div>' +
        '<p style="font-size:0.78rem;color:var(--text-muted);margin-top:12px;">Os atalhos funcionam quando nenhum campo de texto está focado.</p>' +
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-top:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">🔍 Busca rápida — o que ela encontra</div>' +
          '<p><b>Torneios</b> — Seus (scoped) + torneios públicos do feed de descoberta. Match por nome, esporte, local, formato. Clique navega direto pra página do torneio.</p>' +
          '<p><b>Locais</b> — Venues extraídos dos torneios no cache (via <code>venuePlaceId</code>). Match por nome e endereço. Clique abre a modal de detalhe do venue em <code>#venues/&lt;placeId&gt;</code>.</p>' +
          '<p><b>Jogadores</b> — Nomes de participantes em qualquer torneio (scoped + discovery), com contexto dos torneios em que aparecem.</p>' +
          '<p><b>Ações rápidas</b> — Quando o campo tá vazio, lista atalhos: Novo Torneio, Dashboard, Explorar, Notificações, Ajuda.</p>' +
        '</div>'
    },
    {
      id: 'dicas-app',
      title: _t('help.hints'),
      icon: '💡',
      content: '<p>O scoreplace.app exibe <b>dicas visuais contextuais</b> quando você fica parado por alguns segundos. Elas aparecem como balões com seta apontando para o elemento. Clique "Entendi" para dispensar ou "Desativar dicas" para parar.</p>' +
        '<p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:12px;">Você pode reativar as dicas no seu Perfil. São mais de 200 dicas ao todo, organizadas por área:</p>' +

        // ── Global / Topbar ──
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#818cf8;margin-bottom:8px;">🔝 Barra Superior (8)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Login</b> — Faça login para criar e gerenciar torneios<br>' +
            '• <b>Menu ☰</b> — Abra o menu para navegar pelo app<br>' +
            '• <b>Perfil</b> — Acesse estatísticas e configurações<br>' +
            '• <b>Tema</b> — Noturno, Claro, Pôr do Sol e Oceano<br>' +
            '• <b>Ajuda (?)</b> — Manual completo com todas as funcionalidades<br>' +
            '• <b>Busca rápida</b> — Ctrl+K para buscar torneios e jogadores<br>' +
            '• <b>Notificações</b> — Avisos de torneios e convites<br>' +
            '• <b>Explorar</b> — Torneios públicos da comunidade' +
          '</p>' +
        '</div>' +

        // ── Strategic ──
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#fbbf24;margin-bottom:8px;">💡 Apoie / Pro (4)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Apoie (Dashboard)</b> — PIX mantém a plataforma gratuita<br>' +
            '• <b>Pro (Dashboard)</b> — Torneios ilimitados, logo, Modo TV sem marca<br>' +
            '• <b>Apoie (Torneio)</b> — Cada contribuição faz diferença<br>' +
            '• <b>Pro (Torneio)</b> — Criar torneios ilimitados e personalizar' +
          '</p>' +
        '</div>' +

        // ── Dashboard ──
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#34d399;margin-bottom:8px;">📊 Dashboard (15)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>+ Novo Torneio</b> — Crie em menos de 1 minuto<br>' +
            '• <b>Filtro Todos</b> — Veja todos os torneios de uma vez<br>' +
            '• <b>Filtro Organizados</b> — Só os que você organiza<br>' +
            '• <b>Filtro Participando</b> — Só onde está inscrito<br>' +
            '• <b>Filtro Abertos</b> — Inscrições disponíveis<br>' +
            '• <b>Filtro Favoritos</b> — Seus favoritados<br>' +
            '• <b>Filtro Encerrados</b> — Classificação final e pódio<br>' +
            '• <b>Filtros gerais</b> — Por tipo, esporte, local, formato<br>' +
            '• <b>Cards/Lista</b> — Alterne visualização compacta<br>' +
            '• <b>Estrela ⭐</b> — Favoritar torneio<br>' +
            '• <b>Filtro por esporte</b> — Beach Tennis, Tênis, Padel...<br>' +
            '• <b>Filtro por local</b> — Torneios perto de você<br>' +
            '• <b>Filtro por formato</b> — Eliminatórias, Liga, Suíço...<br>' +
            '• <b>Carregar mais</b> — Próximos torneios (paginação)' +
          '</p>' +
        '</div>' +

        // ── Tournament Detail: Participant ──
        '<div style="background:rgba(59,130,246,0.06);border:1px solid rgba(59,130,246,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#60a5fa;margin-bottom:8px;">🏆 Detalhe do Torneio — Participante (6)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Convidar</b> — WhatsApp, QR Code, link<br>' +
            '• <b>Inscrever-se</b> — Organizador notificado automaticamente<br>' +
            '• <b>Cancelar inscrição</b> — Se inscrições abertas, pode voltar<br>' +
            '• <b>QR Code</b> — Projetar no evento<br>' +
            '• <b>Inscritos</b> — Lista, check-in, gerenciar<br>' +
            '• <b>Regras</b> — Configurações, desempate, histórico' +
          '</p>' +
        '</div>' +

        // ── Tournament Detail: Organizer ──
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#a855f7;margin-bottom:8px;">🛠️ Ferramentas do Organizador (17)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Editar</b> — Local, datas, formato, categorias<br>' +
            '• <b>Comunicar</b> — Mensagens para todos os inscritos<br>' +
            '• <b>Sortear</b> — Gerar chaveamento automaticamente<br>' +
            '• <b>+ Participante</b> — Adicionar manualmente<br>' +
            '• <b>+ Time</b> — Adicionar dupla/equipe<br>' +
            '• <b>Add Bot</b> — Jogadores fictícios para completar<br>' +
            '• <b>Categorias</b> — Mover, mesclar, atribuir<br>' +
            '• <b>Abrir/Fechar inscrições</b> — Controlar período<br>' +
            '• <b>Salvar Template</b> — Reusar configurações<br>' +
            '• <b>Clonar</b> — Copiar torneio sem participantes<br>' +
            '• <b>Encerrar</b> — Finalizar e exibir pódio<br>' +
            '• <b>Excluir</b> — Remover permanentemente<br>' +
            '• <b>Co-organização</b> — Convidar ou transferir<br>' +
            '• <b>Iniciar Torneio</b> — Liberar partidas<br>' +
            '• <b>Ver Chaves</b> — Chaveamento completo<br>' +
            '• <b>Exportar CSV</b> — Planilha de resultados<br>' +
            '• <b>Liga ativo</b> — Toggle de participação: ativo = entra nos sorteios; inativo = folga com 0 pts' +
          '</p>' +
        '</div>' +

        // ── Create Tournament ──
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#f59e0b;margin-bottom:8px;">➕ Criar/Editar Torneio (32)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Esporte</b> — Padrões de pontuação por modalidade<br>' +
            '• <b>Formato</b> — Eliminatória, Dupla, Grupos, Suíço, Liga<br>' +
            '• <b>Modo de sorteio</b> — Sorteio ou Rei/Rainha<br>' +
            '• <b>Modo de inscrição</b> — Individual, Times, Misto<br>' +
            '• <b>Tipo de jogo</b> — Simples (1v1) ou Duplas (2v2)<br>' +
            '• <b>Local</b> — Busca Google Maps + previsão do tempo<br>' +
            '• <b>Endereço público</b> — Visível ou privado<br>' +
            '• <b>Quadras</b> — Quantidade e nomes<br>' +
            '• <b>Categorias gênero</b> — Fem, Masc, Misto<br>' +
            '• <b>Categorias habilidade</b> — A, B, C separados por vírgula<br>' +
            '• <b>Pontuação (GSM)</b> — Sets, games, tiebreaks<br>' +
            '• <b>Logo</b> — Gerar automático ou upload<br>' +
            '• <b>Público/Privado</b> — Visível no Explorar<br>' +
            '• <b>Data de início</b> — Contagem regressiva nos cards<br>' +
            '• <b>Prazo de inscrição</b> — Encerramento automático<br>' +
            '• <b>Máximo participantes</b> — Limite opcional<br>' +
            '• <b>Auto-encerrar</b> — Ao atingir o limite<br>' +
            '• <b>Duração da partida</b> — Para estimativa total<br>' +
            '• <b>Classificados Rei/Rainha</b> — 1 ou 2 por grupo<br>' +
            '• <b>Grupos</b> — Número e classificados por grupo<br>' +
            '• <b>Rodadas Suíço</b> — Recomendado: log₂ participantes<br>' +
            '• <b>W.O.</b> — Individual ou Time Inteiro<br>' +
            '• <b>Inscrições tardias</b> — Fechado, Standby, Expansão<br>' +
            '• <b>Desempate</b> — Arrastar para reordenar critérios<br>' +
            '• <b>Template</b> — Carregar configuração salva<br>' +
            '• <b>Liga: temporada</b> — Duração em meses<br>' +
            '• <b>Liga: inscrições abertas</b> — Durante toda temporada<br>' +
            '• <b>Liga: sorteio manual</b> — Ou agendar automáticos<br>' +
            '• <b>Liga: agendamento</b> — Data, hora e intervalo<br>' +
            '• <b>Liga: novos jogadores</b> — Zero, Mínimo, Média<br>' +
            '• <b>Liga: inatividade</b> — Manter, Decair, Remover<br>' +
            '• <b>Liga: formato rodada</b> — Padrão ou Rei/Rainha' +
          '</p>' +
        '</div>' +

        // ── Invite Modal ──
        '<div style="background:rgba(34,211,238,0.06);border:1px solid rgba(34,211,238,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#22d3ee;margin-bottom:8px;">👥 Modal de Convite (5)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Amigos</b> — Convidar da plataforma com um clique<br>' +
            '• <b>WhatsApp</b> — Link direto para grupos<br>' +
            '• <b>Copiar link</b> — Instagram, Telegram, SMS<br>' +
            '• <b>QR Code</b> — Projetar no evento<br>' +
            '• <b>E-mail</b> — Enviar por e-mail' +
          '</p>' +
        '</div>' +

        // ── Power of 2 + Incomplete Teams ──
        '<div style="background:rgba(239,68,68,0.06);border:1px solid rgba(239,68,68,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#f87171;margin-bottom:8px;">🔢 Painéis de Resolução (10)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Nash (P2)</b> — Cores indicam equilíbrio: verde=melhor<br>' +
            '• <b>Reabrir (P2)</b> — Completar potência de 2<br>' +
            '• <b>BYE</b> — Avançar sem jogar na 1ª rodada<br>' +
            '• <b>Play-in</b> — Rodada extra para excedentes<br>' +
            '• <b>Suíço</b> — Várias rodadas antes de afunilar<br>' +
            '• <b>Enquete</b> — Participantes votam na solução<br>' +
            '• <b>Nash (Times)</b> — Score de equilíbrio por opção<br>' +
            '• <b>Reabrir (Times)</b> — Novos jogadores completam times<br>' +
            '• <b>Bots</b> — Preencher vagas com nomes fictícios<br>' +
            '• <b>Ajuste Manual</b> — Arrastar e soltar entre times' +
          '</p>' +
        '</div>' +

        // ── Poll ──
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#c084fc;margin-bottom:8px;">📊 Enquete (3)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Prazo</b> — Encerramento automático<br>' +
            '• <b>Opções</b> — Marcar e ver Nash<br>' +
            '• <b>Votar</b> — Mudar voto até encerrar' +
          '</p>' +
        '</div>' +

        // ── GSM ──
        '<div style="background:rgba(236,72,153,0.06);border:1px solid rgba(236,72,153,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#ec4899;margin-bottom:8px;">🎾 Pontuação GSM (8)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Sets para vencer</b> — 1 a 5 (amador vs profissional)<br>' +
            '• <b>Games por set</b> — 6 (tênis), 11 (mesa), 25 (vôlei)<br>' +
            '• <b>Tiebreak</b> — Para sets empatados<br>' +
            '• <b>Super tiebreak</b> — Set decisivo curto (10 pts)<br>' +
            '• <b>Contagem</b> — Numérica ou Tênis (15-30-40)<br>' +
            '• <b>Vantagem</b> — Deuce/advantage em 40-40<br>' +
            '• <b>Placar dos sets</b> — Entrada set a set automática<br>' +
            '• <b>Salvar sets</b> — Vencedor determinado automaticamente' +
          '</p>' +
        '</div>' +

        // ── Partida Casual ──
        '<div style="background:rgba(56,189,248,0.06);border:1px solid rgba(56,189,248,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#38bdf8;margin-bottom:8px;">📡 Partida Casual (8)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Partida Casual</b> — Jogue sem criar torneio<br>' +
            '• <b>Esporte</b> — Pontuação ajustada por modalidade<br>' +
            '• <b>Single/Dupla</b> — Toggle na configuração<br>' +
            '• <b>Sortear Duplas</b> — ON = aleatório, OFF = arrastar<br>' +
            '• <b>QR Code / Código</b> — Convide jogadores presentes<br>' +
            '• <b>Lobby</b> — Veja quem entrou em tempo real<br>' +
            '• <b>Ordem de saque</b> — ⇅ troca sacador no time<br>' +
            '• <b>Tie-break</b> — Ative a qualquer momento na prorrogação' +
          '</p>' +
        '</div>' +

        // ── Bracket ──
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#818cf8;margin-bottom:8px;">🏅 Chaveamento / Classificação (15)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Zoom</b> — Slider + arraste para navegar<br>' +
            '• <b>Imprimir</b> — Para colar na parede do evento<br>' +
            '• <b>Exportar CSV</b> — Excel / Google Sheets<br>' +
            '• <b>Ordenar colunas</b> — Clique nos cabeçalhos<br>' +
            '• <b>Compartilhar resultado</b> — WhatsApp / clipboard<br>' +
            '• <b>Modo TV</b> — Telão fullscreen, auto-refresh 30s<br>' +
            '• <b>Placar inline</b> — Digitar direto no card<br>' +
            '• <b>Confirmar placar</b> — Registrar resultado<br>' +
            '• <b>Editar resultado</b> — Reabrir campos<br>' +
            '• <b>Ocultar rodada</b> — Economizar espaço<br>' +
            '• <b>Fechar rodada</b> — Gerar próxima automaticamente<br>' +
            '• <b>Avançar eliminatória</b> — Classificados dos grupos<br>' +
            '• <b>Estatísticas do jogador</b> — Clique no nome<br>' +
            '• <b>Histórico de partidas</b> — Adversários e placares<br>' +
            '• <b>Só meus jogos</b> — Filtrar suas partidas' +
          '</p>' +
        '</div>' +

        // ── Check-in ──
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#34d399;margin-bottom:8px;">✅ Participantes / Check-in (8)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>VIP</b> — Prioridade no cabeçamento<br>' +
            '• <b>Remover</b> — Excluir participante<br>' +
            '• <b>Toggle check-in</b> — Confirmar presença<br>' +
            '• <b>Filtros</b> — Todos, Presentes, Ausentes<br>' +
            '• <b>Limpar</b> — Resetar todos os check-ins<br>' +
            '• <b>Ordenar</b> — A-Z ou cronológico<br>' +
            '• <b>W.O.</b> — Marcar ausente/walkover<br>' +
            '• <b>Substituir W.O.</b> — Próximo da lista de espera' +
          '</p>' +
        '</div>' +

        // ── Pre-draw ──
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#f59e0b;margin-bottom:8px;">🎲 Pré-Sorteio (3)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Confirmar sorteio</b> — Gerar chaveamento<br>' +
            '• <b>Renomear categoria</b> — Antes do sorteio<br>' +
            '• <b>Mesclar categorias</b> — Unir poucas inscrições' +
          '</p>' +
        '</div>' +

        // ── Explore ──
        '<div style="background:rgba(34,211,238,0.06);border:1px solid rgba(34,211,238,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#22d3ee;margin-bottom:8px;">🌐 Explorar (2)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Busca</b> — Nome, esporte, formato, cidade<br>' +
            '• <b>Pedido de amizade</b> — Conectar para convites futuros' +
          '</p>' +
        '</div>' +

        // ── Notifications ──
        '<div style="background:rgba(251,191,36,0.06);border:1px solid rgba(251,191,36,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#fbbf24;margin-bottom:8px;">🔔 Notificações (2)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Aceitar co-organização</b> — Convite para organizar junto<br>' +
            '• <b>Aceitar amizade</b> — Trocar convites de torneio' +
          '</p>' +
        '</div>' +

        // ── Profile ──
        '<div style="background:rgba(168,85,247,0.06);border:1px solid rgba(168,85,247,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#c084fc;margin-bottom:8px;">👤 Perfil (6)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Gênero</b> — Atribuição automática de categorias<br>' +
            '• <b>Idioma</b> — 🇧🇷 Português / 🇺🇸 English<br>' +
            '• <b>Esportes</b> — Para sugestões de torneios<br>' +
            '• <b>Localização</b> — Torneios perto de você<br>' +
            '• <b>Notificações</b> — Todas, Importantes, Fundamentais<br>' +
            '• <b>Dicas visuais</b> — Ativar/desativar este sistema' +
          '</p>' +
        '</div>' +

        // ── Venues (descoberta) ──
        '<div style="background:rgba(14,165,233,0.06);border:1px solid rgba(14,165,233,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#38bdf8;margin-bottom:8px;">🏢 Descobrir Locais (8)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Local</b> — Cidade, bairro ou endereço (geocoder)<br>' +
            '• <b>GPS</b> — Usa localização precisa do dispositivo<br>' +
            '• <b>Modalidade</b> — Filtra por esporte<br>' +
            '• <b>Distância (km)</b> — Raio virando círculo no mapa<br>' +
            '• <b>Mapa</b> — Pins coloridos por tipo<br>' +
            '• <b>Lista</b> — Cards com detalhes<br>' +
            '• <b>Expandir raio</b> — Triplica com 1 clique se zero resultados<br>' +
            '• <b>Cadastrar local</b> — Qualquer jogador (Wikipedia-style)' +
          '</p>' +
        '</div>' +

        // ── Venue Detail Modal ──
        '<div style="background:rgba(99,102,241,0.06);border:1px solid rgba(99,102,241,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#a5b4fc;margin-bottom:8px;">📍 Modal do Local (6)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Estou aqui agora</b> — Check-in inline sem navegar<br>' +
            '• <b>Planejar ida</b> — Dialog de agendamento auto-aberto<br>' +
            '• <b>Compartilhar</b> — Web Share API + link canônico<br>' +
            '• <b>Criar torneio aqui</b> — Venue pré-preenchido<br>' +
            '• <b>Avaliar</b> — Estrelas 1–5 + texto opcional<br>' +
            '• <b>Reivindicar</b> — Tag "✅ Oficial" pra proprietário' +
          '</p>' +
        '</div>' +

        // ── Presence ──
        '<div style="background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#fbbf24;margin-bottom:8px;">🗓️ Presença (7)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Local</b> — Dropdown combina perfil + torneios<br>' +
            '• <b>Pills de modalidade</b> — Seleção múltipla<br>' +
            '• <b>Estou aqui agora</b> — Check-in 3h (notifica amigos 📡)<br>' +
            '• <b>Planejar ida</b> — Agendamento (notifica amigos 🗓️)<br>' +
            '• <b>Adicionar à agenda</b> — Google/Outlook/.ics<br>' +
            '• <b>Cancelar</b> — Remove sua presença<br>' +
            '• <b>Gráfico por hora</b> — Movimento do dia' +
          '</p>' +
        '</div>' +

        // ── Dashboard Widgets ──
        '<div style="background:rgba(16,185,129,0.06);border:1px solid rgba(16,185,129,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#34d399;margin-bottom:8px;">📊 Widgets da Dashboard (5)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Meus locais</b> — Check-in em 1 tap nos venues<br>' +
            '• <b>Amigos no local</b> — Até 6 amigos com presença<br>' +
            '• <b>Complete seu perfil</b> — Nudge quando faltam campos<br>' +
            '• <b>Presença (hero)</b> — Botão 📍 pra #presence<br>' +
            '• <b>Locais (hero)</b> — Botão 🏢 pra descobrir' +
          '</p>' +
        '</div>' +

        // ── My Venues / Tournament Calendar / Notif CTAs ──
        '<div style="background:rgba(139,92,246,0.06);border:1px solid rgba(139,92,246,0.15);border-radius:10px;padding:12px;margin-bottom:10px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#a78bfa;margin-bottom:8px;">🔗 Integrações (5)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Cadastrar local</b> — Autocomplete do Google Places<br>' +
            '• <b>Reivindicar propriedade</b> — Tag oficial<br>' +
            '• <b>Calendário do torneio</b> — Google/Outlook/.ics<br>' +
            '• <b>Convite casual (notif)</b> — Entrar direto na sala<br>' +
            '• <b>Check-in de amigo (notif)</b> — Vai ao venue' +
          '</p>' +
        '</div>' +

        // ── Meta ──
        '<div style="background:rgba(107,114,128,0.06);border:1px solid rgba(107,114,128,0.15);border-radius:10px;padding:12px;">' +
          '<div style="font-weight:700;font-size:0.85rem;color:#9ca3af;margin-bottom:8px;">ℹ️ Meta (1)</div>' +
          '<p style="margin:0;font-size:0.8rem;line-height:1.7;">' +
            '• <b>Sobre as dicas</b> — Como desativar e reativar no Perfil' +
          '</p>' +
        '</div>'
    },
    {
      id: 'notas-versoes',
      title: _t('help.changelog'),
      icon: '📋',
      content: '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Place: após marcar presença/plano num preferido, seção colapsa pro local focado + gráfico de movimento por hora.</b> Até v0.16.19, clicar em <b>📍 Estou aqui agora</b> ou <b>🗓️ Planejar ida</b> num card de preferido só hidratava os botões e mostrava uma confirmação modal — toda a lista de preferidos continuava visível igual antes. O usuário pediu duas coisas conectadas: (1) <b>esconder os outros preferidos</b> enquanto a presença estiver ativa (foco único no local onde o usuário está), (2) mostrar logo abaixo um <b>gráfico de barras de movimento por hora</b> com a hora atual centralizada, número de pessoas por hora (amigos+você em amarelo, outros em cinza) e rótulo com o total em cima de cada barra. Implementado portando o componente de gráfico já existente em <code>#presence</code> (<code>presence.js</code>) pra <code>venues.js</code> — mesmo comportamento, mesmas cores, mesma janela de 13 horas deslizante centrada em <code>now</code>. Novo estado <code>state.focusedPreferred = {placeId, docId, type, startsAt, endsAt, sports, venueName, prefObj}</code> ativado nos success paths de <code>_doQuickCheckIn</code> / <code>_venuesConfirmInlinePlan</code> quando o venue é preferido (detectado por <code>_findPreferredByPid</code>), e limpo em <code>_venuesCancelMyPresenceHere</code> / <code>_venuesCancelPresenceFromConfirm</code> quando o docId bate com o foco atual. <code>renderResults</code> detecta <code>state.focusedPreferred</code> e renderiza somente o card do local focado (com título <i>⭐ Você está aqui</i> ou <i>⭐ Plano ativo</i>) + slot do gráfico (<code>#presence-chart-&lt;safePid&gt;</code>) + botão <b>ver todos ↗</b> (chama <code>window._venuesClearFocusedPreferred</code>) pra voltar à visão completa. Foco auto-expira quando <code>endsAt</code> já passou. Auto-refresh do gráfico a cada 60s pra deslizar a janela (via <code>_startChartAutoTick</code>/<code>_stopChartAutoTick</code>). Presenças são carregadas via <code>PresenceDB.loadForVenueDay(placeId, dayKey)</code> e filtradas por visibilidade (<code>public</code> = todos / <code>friends</code> = só eu + amigos), consistente com <code>#presence</code>. Extras: torneios no dia no mesmo local contam como "ocupação virtual" (1 presença por participante) no bucket da hora de início do jogo, mesma regra do <code>#presence</code>. Polimento: <code>refresh()</code> também detecta presença já ativa em preferido (via <code>PresenceDB.loadMyActive</code>) e auto-ativa o foco na entrada da página — assim reabrir <code>#place</code> enquanto ainda tem check-in ativo traz direto o gráfico de volta, sem precisar clicar de novo. <b>Regra cristalizada pra memória:</b> qualquer novo overlay em <code>body</code> fora da classe <code>.modal-overlay.active</code> precisa de detecção no <code>_softRefreshView</code> — mas gráfico-slot inline dentro do <code>#venues-results</code> usa o próprio container renderizado, não é overlay, então não precisa.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Consistência mobile↔desktop: botões de presença no card de preferido mesmo sem ficha cadastrada.</b> Usuário reportou via screenshot que no Chrome (desktop) os locais preferidos "⭐ LOCAIS PREFERIDOS" apareciam sem os botões <b>📍 Estou aqui agora</b> / <b>🗓️ Planejar ida</b>, mostrando só o rótulo "do seu perfil · sem ficha cadastrada" e, ao clicar, abrindo o Google Maps numa aba externa — enquanto no celular os mesmos cards tinham os botões inline. A inconsistência vinha de dois lugares: (1) <code>_preferredCardNoMatch</code> (caminho para preferred sem venue cadastrado no Firestore) era renderizada como <code>&lt;a href="maps..."&gt;</code> sem os botões de presença — diferente de <code>_preferredCardMatched</code> que tinha o slot inline hidratado. No celular, Firestore offline cache às vezes tinha um doc antigo que fazia o match cair no <i>matched</i>; no Chrome desktop (cache frio) caía no <i>no-match</i>. (2) <code>_venuesQuickCheckInPreferred</code> / <code>_venuesQuickPlanPreferred</code> bailavam cedo se <code>VenueDB.loadVenue</code> retornasse null — mas presença não precisa de ficha, só de <code>placeId</code> + <code>name</code> + <code>lat/lng</code>, dados que o profile já tem em <code>preferredLocations[]</code>. Fix em três camadas: (a) <code>_preferredCardNoMatch</code> agora renderiza o mesmo <code>[data-pref-presence-slot]</code> que o matched, rotulado com <i>pid sintético</i> derivado de lat/lng quando o preferred não tem placeId (profile antigo salvava só <code>{lat,lng,label}</code>); <code>_hydratePreferredPresenceSlots</code> preenche automaticamente sem precisar de mudança. (b) Novo helper <code>_resolvePreferredVenue(placeId)</code> que tenta Firestore primeiro e cai numa construção venue-shape (<code>{placeId, name, lat, lon, sports:[]}</code>) montada a partir de <code>cu.preferredLocations</code> quando a ficha não existe. <code>_venuesQuickCheckInPreferred</code> / <code>_venuesQuickPlanPreferred</code> usam esse helper em vez do <code>loadVenue</code> direto — sem ficha, <code>picks</code> cai em <code>prefSports</code> do usuário (ele marcou como preferido, assume os esportes dele); se <code>prefSports</code> também vazio, abre picker local. (c) Link para Google Maps vira ação secundária (ícone 🗺️ discreto no canto), não o comportamento primário do card — click no corpo do card não navega pra fora. <b>Regra cristalizada:</b> UX em features que dependem de dados opcionais (Firestore doc, GPS, login) precisa ter <i>paridade mínima</i> entre caminhos — se o <i>matched</i> oferece ação X, o <i>no-match</i> tem que oferecer equivalente funcional ou explicar por que não pode; nunca silenciar a ação só porque um dado secundário não resolveu.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Apagar local cadastrado — botão para dono ou criador em <code>#my-venues</code>.</b> Até v0.16.17 só havia <b>🔓 Liberar</b> (removia ownership mas preservava o doc, tornando o venue comunitário) — não existia jeito de apagar de vez pela UI. Usuário que cadastrou algum local errado (ex: endereço duplicado, teste, placeId incorreto) tinha que pedir pra equipe limpar via Firestore direto. Agora a UI cobre o caso: botão <b>🗑️ Apagar local</b> aparece em dois lugares — (1) <b>ícone de lixeira inline em cada card da lista "Locais cadastrados"</b> em <code>#my-venues</code>, à direita do nome (sem precisar abrir a edição); (2) <b>botão vermelho no rodapé do formulário de edição</b>, à esquerda, separado dos botões positivos (Cancelar/Salvar) pra reduzir clique acidental. Ambos exigem <b>dupla confirmação</b> (aviso da permanência + "Tem certeza? Não há como desfazer.") e só aparecem para o <b>dono formal</b> (<code>ownerUid === meu uid</code>) ou o <b>criador original</b> (<code>createdByUid === meu uid</code>). Terceiros só podem editar colaborativamente — nunca apagar. Nova função <code>VenueDB.deleteVenue(key)</code> valida a permissão client-side antes do <code>ref.delete()</code> (regra Firestore deve espelhar). Presenças e torneios que referenciavam o placeId ficam com referência pendurada mas a UI degrada graciosamente — cards caem no fallback <i>"sem ficha cadastrada"</i> em <code>#place</code>, mesma lógica já usada pra preferidos que nunca foram cadastrados. Reviews na subcoleção ficam orfãs mas inalcançáveis via UI. <b>Trade-off aceito em alpha:</b> operação é <i>hard delete</i>, sem soft-delete nem recovery window — fase alpha tem dados descartáveis por regra, recadastro é trivial se precisar. <b>Regra cristalizada:</b> operações destrutivas sem undo sempre precisam de dupla confirmação visual + permissão verificada em duas camadas (client + regras Firestore).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cadastro comunitário nunca mais vira "oficial" silenciosamente + preferidos fora do raio aparecem como matched.</b> Dois bugs conectados reportados pelo usuário após cadastrar o Residencial Spazio Verde 1. <b>(1) Cadastro comunitário ganhava tag "✅ oficial" indevidamente:</b> o fluxo de "Cadastrar quadras" chama <code>_venueSaveAndOpenCourts</code> (em <code>venue-owner.js</code>) quando o venue ainda não foi persistido — função que salva silenciosamente antes de abrir a tela de quadras. O payload desse save hardcodava <code>claimAsOwner: true</code>, o que fazia <code>VenueDB.saveVenue</code> setar <code>ownerUid = myUid</code> no doc (linha 82 de venue-db.js). Resultado: qualquer usuário que clicasse <b>🎾 Cadastrar quadras</b> sem ter tocado no toggle "🏢 Reivindicar como proprietário" era promovido a dono formal automaticamente — venue aparecia com badge <b>✅ oficial</b> (verde) em vez de 🤝 <i>cadastrado-pela-comunidade</i> (neutro). A outra função de save, <code>_venueOwnerSubmit</code>, lia corretamente o estado do botão (<code>data-claimed === "1"</code>); só o caminho "Cadastrar quadras" tinha o override hardcoded — provavelmente copiado errado quando o atalho foi criado. Fix: <code>claimAsOwner: claimAsOwner</code> (mesmo valor lido do botão como no submit regular). Venues cadastrados a partir daqui sem toggle de reivindicação ficam comunitários como esperado. <b>(2) Preferidos fora do raio ou fora do filtro de modalidade apareciam como "sem ficha cadastrada":</b> ao abrir <code>#place</code> no celular, o Spazio Verde 1 (em Sorocaba, ~130km da localização atual do usuário em SP) aparecia com o card de preferido <i>sem match</i> — "do seu perfil · sem ficha cadastrada" — mesmo o venue estando salvo no Firestore. Causa: o matching de preferreds em <code>renderResults</code> procurava só dentro de <code>state.results</code>, que é a lista filtrada por distância (<code>state.distanceKm</code>) e modalidade. Se o preferido estava fora desse raio (ou não oferecia o esporte filtrado), não achava o match e renderizava o <i>_preferredCardNoMatch</i> no lugar do <i>_preferredCardMatched</i> (com botões inline de presença). Um preferido explicitamente marcado pelo usuário nunca deveria ser escondido por filtro transitório. Fix: <code>refresh()</code> agora fetcha diretamente por <code>placeId</code> (via <code>VenueDB.loadVenue</code>) todos os preferreds que não saíram em <code>state.results</code>, guardando num <code>state.prefVenueCache</code>. O <code>resolvedPreferred</code> usa esse cache como fallback quando o match em <code>state.results</code> falha. Promise.all paralelizado, gotcha de null handled. <b>Regra cristalizada:</b> filtros geográficos/categoriais em queries de descoberta nunca devem esconder dados que o usuário explicitamente marcou como importantes (preferidos, favoritos, salvos). Esses sempre precisam de um caminho direto por ID que bypassa os filtros.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sem sugestão de horário de saída + confirmação com botão Cancelar após check-in/plano + fim do falso-positivo "divergência" no perfil.</b> Três ajustes após a v0.16.15. <b>(1) Sugestão de "Até" removida do modal de Planejar ida:</b> depois da v0.16.15 adicionar default de <i>Das + 2h</i>, usuário decidiu que propor horário de saída gerava mais fricção do que clareza — melhor deixar o campo em branco e só usar quando o usuário realmente quiser fixar. Voltou ao comportamento original: <i>Até</i> abre vazio por padrão, não recalcula quando <i>Das</i> muda. As pills toggleáveis de modalidade (introduzidas na v0.16.15) permanecem — essa parte funcionou bem. <b>(2) Confirmação rica em lugar de toast ao registrar presença ou plano:</b> antes, clicar em "Estou aqui agora" ou confirmar um plano mostrava só um toast discreto ("Check-in registrado!") que sumia em segundos. Para cancelar, o usuário tinha que ir em outra tela ou abrir a ficha do venue e usar o botão vermelho lá. Agora, após salvar, aparece um <b>modal de confirmação centralizado</b> com: título (<i>✅ Você está aqui agora</i> ou <i>🗓️ Ida planejada</i>), nome do local, modalidades, horário (quando é plano), mensagem curta sobre visibilidade pra amigos, e <b>dois botões</b> — <b>❌ Cancelar presença/plano</b> (gradient vermelho, à esquerda) e <b>OK</b> (primário, à direita). Clicar em Cancelar chama <code>PresenceDB.cancelPresence(docId)</code>, re-hidrata movimento do venue + botões de presença + slot de preferidos + widget da dashboard, e fecha o overlay. Clicar em OK (ou fora do modal) só fecha. Torna arrependimento imediato possível sem fricção — o usuário que registrou um check-in no local errado ou escolheu modalidade errada tem escape-hatch visível de imediato. Implementado via novo helper <code>_showPresenceConfirmation(opts)</code> em <code>venues.js</code>, chamado em <code>_doQuickCheckIn</code> (matched preferido e não-preferido) e <code>_venuesConfirmInlinePlan</code>. <b>(3) Fim do falso-positivo "Perfil — divergência" ao adicionar locais preferidos:</b> usuário reportou com screenshot que ao salvar um segundo local preferido, aparecia toast de <i>divergência</i> listando exatamente os mesmos dados sent→got, só com <b>ordem de chaves do objeto invertida</b> (<code>{lat,lng,label}</code> → <code>{lng,lat,label}</code>). Causa: o check de round-trip em <code>saveUserProfile</code> (v0.16.8) usava <code>JSON.stringify</code> pra comparar sent vs got — e <code>JSON.stringify</code> é <b>sensível à ordem de chaves</b>. Firestore preserva valores mas não ordem de inserção ao ler de volta, então qualquer payload com objetos aninhados (<code>preferredLocations[]</code>, <code>preferredCeps[]</code> se tivesse objetos) disparava falso positivo apesar do save estar <b>semanticamente correto</b>. Fix: substituído <code>JSON.stringify</code> por helper local <code>_stableStringify</code> que sorts keys recursivamente antes de stringificar — dois objetos com mesmos valores em ordens diferentes agora produzem a mesma string e não geram mismatch. O save sempre funcionou (confirmado pelo usuário: <i>"mas salvou"</i>); o bug era puramente no diagnóstico. <b>Regra cristalizada pra memória:</b> comparação de round-trip contra Firestore sempre deve normalizar ordem de chaves — Firestore não é obrigado a preservar ordem, e <code>JSON.stringify</code> é.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Planejar ida: "Até" sugerido faz sentido + modalidades como pills toggleáveis.</b> Dois ajustes no modal de <i>Planejar ida</i> do <code>#place</code> (acessível pelo botão 🗓️ no card do local ou na ficha do venue). <b>(1) "Até" sugerido com regra mínima de 2h:</b> antes, o campo <i>Até</i> (opcional) abria em branco e o usuário tinha que ou deixar aberto ou digitar. O campo <i>Das</i> sempre vinha com sugestão de "agora + 2h" (ex: 14:55 se o usuário abre o modal às 12:55). Isso dava cenas confusas quando o usuário mudava o <i>Das</i> pra um horário mais tarde e o campo <i>Até</i> continuava vazio — ou pior, se ele chutasse um <i>Até</i> e depois ajustasse <i>Das</i> pra depois, o sistema aceitava o erro de formulário até validar no confirm. Agora o <i>Até</i> inicializa com <b>Das + 2h</b> (ou vazio se overflow do dia, ex: Das=22:55 → Até=00:55 seria amanhã, que não faz sentido em "ida hoje"). Quando o usuário edita <i>Das</i>, o <i>Até</i> recalcula automaticamente <b>só se</b> o valor atual não estiver ao menos 2h depois — se o usuário já escolheu manualmente um <i>Até</i> válido mais longe (ex: 4h de jogo), a escolha dele é preservada. Texto explicativo do rodapé atualizado: <i>"Sugestão de Até é 2h após a chegada. Deixe em branco se não quiser fixar."</i> <b>(2) Modalidades como pills toggleáveis, todas ativas por padrão:</b> antes, as modalidades do plano vinham como texto estático na linha de contexto (<code>"Nome · Beach Tennis · Tênis · hoje"</code>) e o usuário não tinha como deselecionar uma modalidade sem cancelar o plano e abrir de novo com outra seleção. Agora, quando o plano cobre 2+ modalidades (típico em locais multi-esporte onde o atalho sem-picker da v0.16.14 propaga todos os esportes da interseção), elas aparecem como <b>pills clicáveis gradiente índigo</b> acima dos campos de horário, cada uma ativa por padrão. Clique numa pill desativa (fundo cinza + text-decoration line-through); clique de novo reativa. Ao confirmar, só as pills ativas entram no documento de presença. Se o usuário desativar todas (edge case), toast <i>"Selecione ao menos uma modalidade"</i> bloqueia o confirm. Quando há só 1 modalidade (caso simples), as pills ficam ocultas e o layout volta ao formato anterior com modalidade na linha de contexto — sem UI desnecessária. <b>Regra cristalizada:</b> defaults devem ser sempre coerentes entre si (Das < Até, Até >= Das + Xmin), e UI de default-propagation deve preservar escolhas manuais válidas do usuário — recalcular silenciosamente só quando o estado atual é inválido.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botões de presença direto no card do local preferido + atalho sem picker com esportes preferidos.</b> Duas mudanças no fluxo de presença do <code>#place</code>. <b>(1) "Estou aqui agora" / "Planejar ida" no card do local preferido:</b> antes, pra marcar presença ou planejar uma ida a um local favorito, o usuário tinha que abrir a ficha do local (toque no card → overlay do detalhe) pra só então ver os botões verdes. Isso transformava a seção "⭐ Locais preferidos" num atalho pra navegação, não pra ação — o favorito estava ali pra ser acessado mais rápido, mas a ação principal (check-in) continuava a 1 clique de distância. Agora cada card de preferido <i>matched</i> (que bate com um venue cadastrado na plataforma) renderiza os botões <b>📍 Estou aqui agora</b> (verde) e <b>🗓️ Planejar ida</b> (índigo) diretamente abaixo dos dados do local. Os botões trocam para <b>❌ Cancelar presença</b> / <b>❌ Cancelar plano</b> (vermelhos) quando o usuário já tem uma presença ativa naquele venue — com re-hidratação automática após cada mutação (check-in, plano, cancelamento), sem precisar re-renderizar a página. O card em si continua clicável na área central para abrir o detalhe (os botões usam <code>event.stopPropagation()</code> pra não dispararem navegação). <b>(2) Atalho sem picker quando há esportes preferidos:</b> o clique no "Estou aqui agora" de um card preferido agora usa um fluxo alternativo (<code>_venuesQuickCheckInPreferred</code>) que computa a interseção <i>esportes do venue ∩ esportes preferidos do usuário</i>. Se houver pelo menos um esporte em comum, registra presença com <b>todos</b> da interseção de uma vez — <b>sem abrir picker de modalidade</b>. Isso alinha o comportamento manual com o auto check-in via GPS (<code>presence-geo.js</code>), que já assume que um local preferido + esportes preferidos é contexto suficiente pra não precisar de perguntas. O fluxo padrão (com picker) continua como fallback quando não há interseção — ou seja, um venue cadastrado só pra Tênis num perfil que prefere Padel ainda pede pra escolher. Mesma lógica aplicada ao "Planejar ida" via <code>_venuesQuickPlanPreferred</code>. <b>Regra cristalizada:</b> onde o contexto já é forte (preferido + preferido), UI deve assumir e agir — friction adicional (picker, confirm) desnecessária em caminho favorito vira fricção lembrada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas Detalhadas: hierarquia invertida + espaçamento corrigido.</b> Três ajustes pedidos pelo usuário no modal de <i>Estatísticas Detalhadas</i> (clique no nome do jogador). <b>(1) Espaço excessivo acima da foto do usuário:</b> o modal tinha <code>&lt;div style="height:50px"&gt;</code> como spacer para o back-header sticky, mas o CSS <code>.sticky-back-header + *</code> já adicionava <code>margin-top:50px</code> automaticamente no próximo irmão — resultado: <b>100px</b> de espaço morto acima do avatar, não 50px. Spacer removido; o CSS continua dando o clearance necessário. Avatar agora sobe <b>~50px</b> no viewport. <b>(2) Casuais muito colados nos torneios nas linhas V/D:</b> nas linhas <i>Derrotas/Vitórias</i>, <i>Sets</i>, <i>Games</i>, <i>Pontos</i>, <i>Tiebreaks</i>, os números casuais (⚡) ficavam <code>gap:10px</code> ao lado dos torneios (🏆), ambos clusterizados no extremo de cada metade. Reestruturado: torneios continuam no extremo absoluto (0% e 100%), e casuais agora sentam <b>no meio do caminho entre o centro (50%) e o torneio</b> — ou seja, ~25% e ~75% da linha. Conseguido via <code>flex:1</code> spacers em ambos os lados do casual dentro de cada metade. <b>(3) Hierarquia visual invertida — percentagem agora é o número grande, absoluto vira parentético pequeno:</b> antes, o valor absoluto aparecia grande e bold (<code>0.9rem, font-weight:900</code>) com a porcentagem minúscula embaixo (<code>0.58rem, opacity:0.65</code>). Agora a <b>porcentagem é a número principal</b> (mesmo tamanho/peso), e o absoluto fica à direita entre parênteses no tamanho pequeno. Ex: <code>100% (3)</code> com 100% prominente e <code>(3)</code> sutil. Em métricas sem razão válida (médias/mínimos/máximos onde o par não é somável), só o número absoluto aparece grande como antes. Raciocínio: a porcentagem comunica <i>proficiência</i> (contexto comparativo), o valor absoluto comunica <i>volume</i>. Em estatísticas de desempenho, proficiência geralmente é a leitura primária — quem quer ver <i>volume</i> ainda consegue, só em formato menor.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil mais limpo: seção de "Sou dono de um local" removida + toast de sucesso simplificado.</b> Depois da v0.16.11 fechar definitivamente o bug de perfil-não-salvava, duas sobras visuais pediam ajuste: (1) a seção <b>🏢 Sou dono de um local</b> com o botão "Abrir Meus locais" ainda aparecia no modal do perfil, mesmo o gerenciamento de locais já tendo sido movido pra rota dedicada <code>#my-venues</code> (acessível via CTA no <code>#place</code>). Ficava redundante no perfil e quebrava a separação conceitual "perfil de jogador ≠ conta de dono de local". Removida do modal; acesso continua via <code>#place</code> → <b>Cadastrar meu local</b>, ou navegação direta pra <code>#my-venues</code>. (2) O toast ao salvar perfil mostrava <code>✅ N campos · v0.16.11</code> — útil durante o período de debug da v0.16.5-v0.16.11 (o número da versão no toast era o contrato com o usuário pra provar que estava na build nova), mas agora que o bug está fechado, ruído desnecessário. Trocado por <b>Perfil atualizado</b> / <i>"Suas alterações foram salvas."</i> — curto e sem diagnóstico visível. O diagnóstico detalhado (<code>⚠️ Perfil — erro: &lt;causa&gt;</code> / <code>⚠️ Perfil — divergência: gender: X→Y</code>) permanece nos caminhos de erro, onde ainda importa ver o que quebrou.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Três tags &lt;script&gt; não-fechadas no index.html quebravam o app silenciosamente.</b> O usuário reportou que a partir da v0.16.10 o botão Salvar do perfil disparava o toast <code>⚠️ Firestore não inicializado · v0.16.10-alpha</code>, e que o modal de login não fechava depois de logar com Google (aparecia o toast <i>"Login Realizado"</i>, o nome do usuário no topbar, mas o modal-login permanecia por cima da página). Ao investigar a cadeia, achei a causa-raiz em três linhas do <code>index.html</code>: <code>&lt;script src="js/store.js"&gt;</code> (linha 129), <code>&lt;script src="js/views/venues.js"&gt;</code> (linha 140) e <code>&lt;script src="js/main.js"&gt;</code> (linha 167) <b>não tinham a tag de fechamento <code>&lt;/script&gt;</code></b>. O parser de HTML, quando encontra uma tag script sem fechamento, consome tudo até o próximo <code>&lt;/script&gt;</code> que encontrar — então: (a) o <code>&lt;script src="js/firebase-db.js"&gt;</code> da linha 130 virava "conteúdo inline" do <code>store.js</code> → <code>firebase-db.js</code> <b>nunca carregava</b> → <code>window.FirestoreDB</code> ficava undefined → a chamada <code>FirestoreDB.init()</code> no <code>auth.js</code> lançava <code>ReferenceError</code> silencioso → <code>db</code> permanecia null → toast "Firestore não inicializado" ao salvar perfil; (b) o <code>&lt;script src="js/views/tournaments-utils.js"&gt;</code> da linha 141 virava conteúdo do <code>venues.js</code> → várias funções utilitárias de torneio sumiam; (c) o <code>&lt;script&gt;</code> de registro do Service Worker (linha 168+) virava conteúdo do <code>main.js</code> → <b>o SW nunca re-registrava</b>, o que explica por que o auto-update da v0.16.10 também não funcionava pro usuário e ele continuou vendo v0.16.9 depois do deploy. Todos os sintomas aparentemente desconexos (perfil não salva, modal de login não fecha, auto-update não dispara) tinham a mesma origem. <b>Fixes nesta versão:</b> (1) as três tags fechadas corretamente com <code>&lt;/script&gt;</code> no <code>index.html</code>; (2) <code>FirestoreDB.init()</code> em <code>firebase-db.js</code> ficou mais defensivo — não engole mais erro silenciosamente: verifica se <code>firebase</code> e <code>firebase.firestore</code> existem antes de chamar, grava mensagem real em <code>window.FirestoreDB.lastInitError</code>, e novo método <code>ensureDb()</code> tenta re-inicializar on-demand; (3) <code>saveUserProfile</code> em <code>auth.js</code> chama <code>ensureDb()</code> se <code>db</code> estiver null, e quando falha mostra no toast a <b>mensagem real do Firestore</b> em vez do texto genérico — se algum dia isso voltar a acontecer, o toast vai dizer exatamente o quê falhou ao inicializar. Regra cristalizada pra memória: <b>nunca deixar script tag sem fechamento no HTML — um erro aparentemente trivial quebra CASCATA de scripts subsequentes sem qualquer erro no console do navegador</b>. Validação sintática de JS não pega esse bug porque o erro está no HTML, não no JS. A partir daqui, qualquer edição do <code>index.html</code> deve ser grep-checada por <code>grep -c &quot;&lt;script&quot; index.html</code> vs <code>grep -c &quot;&lt;/script&quot; index.html</code> — os dois números têm que bater.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Diagnóstico específico sobre qual guard do save de perfil falha.</b> Na v0.16.9, o <code>saveUserProfile</code> ganhou guards que abortavam o save com toast genérico se alguma pré-condição não fosse atendida — mas o texto do toast era ambíguo (<i>"Sem UID ou Firestore"</i>) e não dava pra saber qual das três condições quebrou. Aqui separei em três guards distintos com mensagem específica: <code>⚠️ Sem sessão ativa</code> (AppStore/currentUser ausente), <code>⚠️ Sem UID/email</code> (uid/email não resolve), <code>⚠️ Firestore não inicializado</code> (FirestoreDB.db null). Essa separação foi o que permitiu achar o bug real na v0.16.11 — o toast apontou direto pro Firestore, e isso levou ao index.html com as tags não-fechadas.</p>' +
        '<p><b>Perfil: reescrita do zero + Place mostra todos os registrados.</b> Usuário reportou pela quarta vez que o perfil "continua não salvando" e sugeriu <i>"será que não é o caso de reescrever essa parte do programa para funcionar?"</i>. Também apontou que a seção "Registrados no scoreplace" do Place nunca aparecia entre os Preferidos e as Sugestões do Google — mesmo com dezenas de locais cadastrados. <b>Bug #1 — perfil não salvava E nem avisava:</b> auditei por que nenhum dos diagnósticos de v0.16.5-v0.16.8 aparecia pro usuário. Dois culpados independentes: (a) <code>saveUserProfileToFirestore</code> em <code>store.js</code> fazia <code>throw e</code> em erro → a exceção propagava pra fora do <code>await</code> no <code>auth.js</code>, bypassando inteiramente o bloco <code>try { var _diag = window._lastProfileSave; ... }</code> que mostraria o toast de diagnóstico; (b) <code>saveUserProfile</code> em <code>auth.js</code> chamava <code>_hintSystem.enable()</code> ou <code>.disable()</code> <b>incondicionalmente</b> em todo save — e essas funções sempre disparam toast "Dicas ativadas/desativadas". Resultado: o único toast que o usuário via era <i>"Dicas ativadas"</i> enquanto o save real morria silenciosamente em algum ponto. <b>Decisão:</b> ao invés de mais um patch, rewrite completo do <code>saveUserProfile</code>. Nova arquitetura em 8 passos explícitos: (1) ler do form com helpers <code>_v(id)</code>/<code>_chk(id, dflt)</code>; (2) fallback pro <code>displayName</code> se form vier vazio (nunca sobrescrever com string vazia); (3) construir payload <b>só com campos não-vazios</b> — omitir gender em vez de mandar <code>""</code>, deixar Firestore merge preservar o valor anterior; (4) instrumentação pro console (<code>[Profile v0.16.9] payload: ...</code>) + <code>window._lastProfileSave</code>; (5) gravar <b>direto no Firestore</b> via <code>FirestoreDB.db.collection(users).doc(uid).set(payload, {merge:true})</code> com try/catch local — a função nunca mais depende do wrapper em store.js que engolia/propagava erro; (6) re-ler o doc e comparar campo por campo usando <code>JSON.stringify</code> pra detectar mismatch real (sent vs got); (7) sincronizar <code>currentUser</code> com o que voltou do read-back — <b>single source of truth é o Firestore</b>, não o payload intermediário; (8) toast <b>sempre</b> dispara com 1 de 3 mensagens: <code>✅ Perfil salvo — N campos · v0.16.9</code>, <code>⚠️ Perfil — divergência: gender: "feminino"→"masculino" · v0.16.9</code>, ou <code>⚠️ Perfil — erro: &lt;mensagem&gt; · v0.16.9</code>. Hints continuam sendo geridos, mas <b>só mudam estado quando realmente foi alterado</b> — sem toast de hints em todo save. <b>O que o usuário vai ver agora:</b> se o save funcionar, toast diz "Perfil salvo" com a versão v0.16.9 — esse é o contrato. Se a versão no toast for diferente, é cache stale → hard refresh. Se for erro, o toast mostra a mensagem real do Firestore (não mais "tudo ok" falso). <b>Bug #2 — "Registrados no scoreplace" invisível no Place:</b> <code>VenueDB.listVenues</code> fazia <code>ref.limit(50)</code> <b>sem ordering geográfico</b>. Firestore retornava 50 docs em ordem arbitrária — se a base tem mais de 50 venues no total, os próximos ao usuário podiam simplesmente não cair nesse slice. Fix alpha-safe: limit aumentado pra 500 (caller filtra distância client-side depois). Quando a base crescer pra milhares, migrar pra GeoFirestore/sharding por geohash. <b>Regra cristalizada:</b> (a) nunca usar <code>throw</code> em caminho crítico quando o caller já tem try/catch — transforma diagnóstico em silêncio; (b) side effects secundários (hints, analytics, toast de UX não-relacionada) nunca devem mascarar o feedback da operação principal; (c) <code>limit(N)</code> em query sem ordering geográfico = garantia de resultados enganosos quando N &lt; base.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Diagnóstico de perfil que ENXERGA erro silencioso do Firestore.</b> Depois de v0.16.5, v0.16.6 e v0.16.7 tentando fechar o bug "mudo o sexo no perfil, salvo, fecho o app, reabro, continua como Não informar", usuário reportou mais uma vez que não funcionava. Parei de presumir onde o bug estava e fui atrás da evidência. Auditei as duas camadas de diagnóstico que já existiam (toast com fields persistidos + round-trip via <code>_lastProfileSave</code>) e encontrei <b>três buracos</b> que faziam o diagnóstico mentir "✅ salvou" mesmo quando nada tinha sido gravado: <b>(1) <code>firebase-db.js saveUserProfile</code> engolia silenciosamente qualquer erro do Firestore</b> via <code>try/catch → console.error()</code>. Se as regras de segurança rejeitassem o write, ou se o network caísse, ou qualquer outra coisa desse erro — o <code>await</code> resolvia normalmente e o wrapper em <code>store.js</code> marcava <code>ok=true</code>. Correção: removido o <code>try/catch</code>, erros propagam pro wrapper. O toast agora mostra <code>⚠️ Falhou: &lt;erro real do Firestore&gt;</code> quando o write não passa. <b>(2) Round-trip só checava presença de chave, não o valor.</b> Se Firestore tinha <code>gender: "masculino"</code> de uma sessão anterior e o write de <code>"feminino"</code> falhava silenciosamente, o read-back voltava com <code>gender: "masculino"</code> (valor velho, não undefined) → <code>roundtrip[k] !== undefined</code> passava → toast mostrava ✅ mesmo com a tentativa de mudar o valor sendo rejeitada. Correção: round-trip agora compara <code>JSON.stringify(payload[k]) === JSON.stringify(_roundtrip[k])</code> pra cada campo. Quando diverge, popula <code>roundtripMismatch</code> com <code>{field, sent, got}</code> e o toast mostra <code>⚠️ regrediu: gender: "feminino"→"masculino"</code>. Não tem mais como o save dizer que salvou sem o valor real ter chegado no Firestore. <b>(3) Nenhum diagnóstico em hora de carregar.</b> Se o save fosse OK mas o load reabrisse com valor errado (race de cache, guarda truthy engolindo <code>null</code>, etc.), não tinha como saber onde o buraco estava. Correção: <code>loadUserProfile</code> em <code>store.js</code> agora expõe <code>window._lastProfileLoad = {uid, version, hasProfile, gender, city, phone, birthDate, fields[]}</code> logo depois do fetch do Firestore. Dá pra abrir DevTools em qualquer momento e confirmar exatamente o que o cliente recebeu na última carga. <b>O que isso muda na prática:</b> na próxima tentativa de salvar o sexo, o toast vai dizer exatamente qual dos três caminhos quebrou: "⚠️ Falhou" (Firestore rejeitou), "⚠️ regrediu" (escreveu outra coisa / não gravou), ou "✅ v0.16.8" sem warnings + load depois volta errado (bug está no load/populate). Regra cristalizada pra memória: <b>nunca use try/catch silencioso em caminhos críticos de escrita — erros engolidos viram horas de debug especulativo. Se quiser log, <code>catch → console.error → throw</code>. Sem <code>throw</code> = bug de produção esperando pra explodir</b>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Evidência em tela quando o perfil salva + Locais preferidos sempre visível no Place.</b> Depois de v0.16.5 e v0.16.6, o usuário reportou mais uma vez que o perfil continuava não salvando. Parar de escrever fix especulativo e começar a gerar <b>evidência auditável</b> no UI, direto. <b>(1) Diagnóstico visível do salvar:</b> <code>saveUserProfileToFirestore</code> em <code>store.js</code> agora expõe <code>window._lastProfileSave = {uid, version, at, fields[], ok, roundtripMissing[]}</code> com o resultado de cada save, incluindo <b>verificação round-trip</b> — logo depois do <code>set()</code>, faz <code>loadUserProfile(uid)</code> e compara quais dos campos persistidos voltaram. Se algum não voltou, é sinal de que a regra de Firestore bloqueou silenciosamente, ou de que o cliente mandou o campo mas o merge stripou. Toast de sucesso no fim do <code>saveUserProfile</code> em <code>auth.js</code> agora mostra: <code>✅ N campos · v0.16.7</code> — ou <code>⚠️ não voltou: city, phone</code> quando há discrepância. Usuário não precisa mais abrir DevTools pra saber se o fix está rodando: a versão na notificação É o contrato. Se viu <code>v0.16.7</code> no toast, está na versão nova; se viu outra, cache está stale (hard refresh). <b>(2) Seção "⭐ Locais preferidos" sempre visível em #place:</b> v0.16.4 introduziu a seção, mas ela só aparecia quando o usuário já tinha preferidos salvos — sem preferidos, ficava invisível, criando a percepção de que a feature não foi implementada. Agora a seção sempre aparece pra usuários logados: com preferidos, lista normal; sem preferidos, placeholder tracejado âmbar com CTA <b>Adicionar no perfil →</b>. Também ajustado o early-return "Onde você está?" — ele não escondia mais a seção Preferidos quando o usuário tem locais salvos mas ainda não deu GPS. Regra cristalizada: <b>feature implementada mas invisível pra 80% dos usuários = feature percebida como ausente</b>. Seções condicionais com placeholder de onboarding > seções condicionais que somem.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil não perde mais dados — agora de verdade.</b> A v0.16.5 tentou fechar o bug de "salvo o perfil, fecho e reabro, os dados somem" mas só cobriu parte do buraco. Usuário reportou que continuava acontecendo. Causa-raiz que faltava: <code>Firestore set({merge:true}) preserva valores existentes apenas quando o campo no payload é <code>undefined</code></code> — strings vazias (<code>""</code>) e arrays vazios (<code>[]</code>) <b>ainda sobrescrevem</b>. E o <code>saveUserProfile</code> em <code>auth.js</code> escrevia cruamente o que estava no form pra <code>currentUser</code> (só <code>name</code> tinha fallback). Se o form estava vazio por qualquer motivo — race condition, dropdown não-hidratado, race entre login e populate do modal — <code>""</code> / <code>[]</code> iam direto pra <code>currentUser</code>, daí pro payload do save, daí pro Firestore, clobber completo. Uma vez clobbado, <code>loadUserProfile</code> tem guarda <code>if (profile.city)</code> que ignora strings vazias na leitura → <code>currentUser.city</code> fica <code>undefined</code> → populate renderiza vazio → próximo save repete o clobber → estado "vazio grudento" para sempre. <b>Fix em duas camadas:</b> (1) <b><code>saveUserProfile</code> em auth.js:</b> cada campo opcional agora tem a mesma guarda que <code>name</code> já tinha — se form está vazio E currentUser já tem valor, preserva o valor existente. Cobre <code>gender</code>, <code>birthDate</code>, <code>city</code>, <code>phone</code>, <code>defaultCategory</code>, <code>preferredCeps</code>, <code>preferredSports</code>, <code>preferredLocations</code>. Booleans e campos com default explícito na UI (<code>acceptFriendRequests</code>, <code>notifyPlatform/Email</code>, <code>notifyLevel</code>, <code>phoneCountry</code>, toggles de presença) continuam sendo escritos sempre — eles têm valor certo no form sempre que o modal abre. (2) <b><code>saveUserProfileToFirestore</code> em store.js — defesa em profundidade:</b> mesmo se currentUser conseguir chegar com <code>""</code> / <code>[]</code> em algum campo opcional por outro caminho, o payload antes do set() agora remove strings vazias e arrays vazios para 8 campos opcionais conhecidos. Firestore nunca mais recebe <code>""</code> pra <code>city</code>, <code>gender</code>, etc. Também adicionei <code>console.log(\'[Profile Save]\', uid, \'fields persisted:\', ...)</code> — se ainda acontecer, o console mostra exatamente quais campos foram gravados em cada save, transformando silent failure em loud failure. <b>Trade-off aceito:</b> o usuário não consegue "apagar" um campo do perfil simplesmente esvaziando o input — tem que digitar um valor novo. Na prática ele não quer apagar: reportou que dados sumindo é confuso. Regra cristalizada pra memória: <b>toda gravação de perfil em Firestore com merge:true deve strip undefined + strings vazias + arrays vazios antes do set()</b>, e <b>toda rotina de save que copia de form pra currentUser deve ter fallback pra não escrever vazio em cima de valor real</b>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil não perde mais dados entre sessões.</b> Usuário reportou: <i>"estou mudando coisas no perfil e salvando, mas sempre que encerro o app e abro novamente as informações que coloquei somem"</i>. Três bugs compondo um único sintoma foram encontrados e corrigidos. (1) <b>Race condition entre login e save:</b> após o login Google, <code>simulateLoginSuccess</code> seta <code>AppStore.currentUser = {uid, email, displayName, photoURL}</code> (só os básicos do Google) e dispara <code>loadUserProfile(uid)</code> de forma assíncrona pra mergear os campos salvos (gender, city, preferredSports, notifyLevel, etc.). Na janela de ~300-500ms entre os dois, se o usuário abria o modal de perfil e clicava Salvar, o form lia campos vazios, a rotina de save escrevia string vazia/array vazio em <code>currentUser</code>, e <code>saveUserProfileToFirestore</code> persistia isso com <code>merge: true</code> — sobrescrevendo os dados que estavam lá. Fix: <code>_openMyProfileModal</code> agora é async e faz <code>await loadUserProfile(uid)</code> antes de permitir edição, re-populando o form quando o merge chega. (2) <b>Configurações de presença nunca foram persistidas:</b> o save do perfil setava <code>currentUser.presenceVisibility</code>, <code>presenceMuteDays</code>, <code>presenceMuteUntil</code> e <code>presenceAutoCheckin</code>, mas <code>saveUserProfileToFirestore</code> (em store.js) não incluía esses 4 campos no payload. Efeito: a configuração funcionava em memória durante a sessão mas sumia ao fechar o app porque nunca chegou ao Firestore, e o <code>loadUserProfile</code> também não fazia merge deles. Fix: ambos os métodos agora tratam os 4 campos explicitamente. (3) <b>Lista de amigos podia ser apagada:</b> o payload do save incluía <code>friends: user.friends || []</code> — se o <code>loadUserProfile</code> ainda não tinha landado na hora do save, o fallback <code>[]</code> sobrescrevia a lista de amigos no Firestore. Amigos são gerenciados por fluxos dedicados (<code>sendFriendRequest</code>, <code>acceptFriendRequest</code>, <code>removeFriend</code>) usando <code>arrayUnion</code>/<code>arrayRemove</code>, e nunca deveriam ser escritos pelo save genérico do perfil. Fix: campos <code>friends</code>, <code>friendRequestsSent</code> e <code>friendRequestsReceived</code> removidos do payload de save. Defense-in-depth extra: helper que remove chaves <code>undefined</code> do payload antes de mandar pro Firestore, garantindo que campos que não foram carregados em memória nunca sejam escritos como string vazia. Regra para memória futura: <b>toda gravação de perfil deve filtrar undefined antes do set() com merge:true</b> — Firestore merge não protege contra strings vazias sobrescrevendo valores reais.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Place: modalidades em pills multi-select + seção "Locais preferidos" do perfil.</b> Três mudanças na tela <code>#place</code> que facilitam filtrar e achar locais relevantes primeiro. (1) <b>Modalidades viraram pills clicáveis não-excludentes</b>: no lugar do dropdown <select> que só aceitava 1 modalidade por vez, agora é uma linha de botões-pill (Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel, Vôlei de Praia, Futevôlei) — toque pra ativar, toque de novo pra desativar. Pode selecionar 1, várias ou todas. Sem pill ativa = todas as modalidades (comportamento equivalente à opção "Todas" do dropdown antigo). Feedback visual imediato: pill selecionada vira gradiente índigo com glow sutil. Estado persistido em localStorage com nova chave <code>sports[]</code>; migração automática do <code>sport</code> singular antigo seeda o array com a escolha prévia do usuário. (2) <b>Nova seção "⭐ Locais preferidos"</b> logo abaixo do campo de busca, mostrando os locais que o usuário marcou como preferidos no perfil. Cada preferido é resolvido contra a lista de registrados — se houver match (por <code>placeId</code> ou nome), aparece como card cadastrado completo (clicável → ficha com quadras, ratings, etc.). Se não houver match, aparece como card simples que abre no Google Maps. (3) <b>Ordem das seções reorganizada</b>: <b>Preferidos → Registrados na plataforma → Sugestões do Google</b>. Locais preferidos que também estão registrados são removidos da seção "Registrados" pra não duplicar. Locais preferidos que batem com placeId de uma sugestão do Google também são removidos de lá. A busca do Google passou a aceitar array de modalidades — quando múltiplas pills estão ativas, cada uma vira um termo de busca paralelo (merge por placeId), cobrindo clubes multi-esporte melhor que uma query única. Custo: mais API calls quando muitas pills estão ativas (5 sports selecionados = 9 termos totais), mas refresh é debounced e dentro do free tier.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Place" unifica Locais + Presença numa só ação.</b> Usuário pediu pra simplificar: em vez de dois botões separados na dashboard (<i>🏟️ Locais</i> e <i>📡 Presença</i>), um botão único <b>📍 Place</b> que cobre as duas funcionalidades — descobrir lugares para jogar e marcar presença neles. Dica do botão: <i>"Procure lugares para seus jogos e marque presença"</i>. <b>Mudanças:</b> (1) Linha hero da dashboard reorganizada em 3 botões lado-a-lado na ordem solicitada <b>⚡ Partida Casual → 🏆 Novo Torneio → 📍 Place</b> (azul cyan / azul escuro / dourado). Cada um com ícone grande em cima + label embaixo, <code>min-height:64px</code>, <code>flex:1</code> pra dividir igual. (2) Linha secundária limpou <code>btn-venues</code> e <code>btn-presence</code> — agora só <b>Convidar / Pro / Apoie</b>. (3) Welcome card (usuários novos) passou de 2 botões (Locais/Presença) pra 1 botão Place, layout 2×2: Casual → Novo Torneio → Place → Encontrar amigos. Texto descritivo atualizado: "descobre quadras próximas e marca presença". (4) Router ganhou alias <code>case \'place\':</code> fallthrough pra <code>case \'venues\':</code> — <code>#place</code> é a rota oficial do botão novo; <code>#venues</code> continua funcionando pra deep-links antigos (email, notificações, bookmarks) sem quebrar. A view renderizada é a mesma (<code>renderVenues</code>); a presença é acessada dentro da ficha de cada local (via "Estou aqui agora" / "Planejar ida", lógica da v0.16.1). Rationale: os dois botões tinham viagens de contexto sobrepostas — usuário que quer marcar presença quase sempre quer ver o local primeiro, e quem busca local geralmente quer declarar presença ali. Juntar reforça o nome do app ("scoreplace"), tira fricção e libera espaço visual na dashboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sugestões do Google agora encontram venues privadas próximas de verdade.</b> Usuário reportou (com screenshot) que em <code>#venues</code> na zona sul de São Paulo a lista "Sugestões do Google" mostrava apenas venues com "Arena" no nome (Arena BTG Pactual 2.9km, Arena Ace 9.3km, Arena Ibirapuera 9.5km…) e ignorava clubes mais próximos como <i>Play Tennis Morumbi</i>, <i>AB Academia de Tênis</i> e <i>Arena Morumbi</i> — todos a poucas quadras de casa. <b>Causa-raiz:</b> a busca usava uma única <code>Place.searchByText</code> com query conjuntiva longa (<code>"quadra esportiva clube arena tênis padel beach tennis pickleball"</code> + label da localização). O Google interpreta textos longos como match conjuntivo ranqueado por relevância — venues cujo nome não contém "arena"/"quadra" ficavam no fundo da lista ou sumiam, mesmo estando muito mais próximos que os que apareciam. <b>Correção:</b> <code>_loadGoogleNearby</code> agora roda <b>queries paralelas por termo</b> (beach tennis, padel, tênis, pickleball, vôlei de praia, futevôlei, tênis de mesa, badminton, squash, academia de tênis, escola de tênis, arena esportiva, clube esportivo, quadra de tênis, quadra de padel, quadra de beach tennis — 16 termos no modo sem filtro, 5 com modalidade selecionada). Cada query é curta e focada, o Google indexa bem cada termo em PT-BR. Merge por <code>placeId</code> remove overlap, dedup com registered venues via nome/endereço, sort final por distância. <code>state.location</code> não é mais anexado ao <code>textQuery</code> — <code>locationBias</code> já resolve geografia, e label textual ("Minha localização atual") poluía matching. Custo: ~16 API calls por refresh em vez de 1, mas refresh é debounced + só roda em mudança real de filtro → dentro do free tier do Places API. Agora academias de tênis, escolas, clubes e quadras privadas próximas aparecem sem precisar estar "Arena ____" no nome.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.16.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Ficha do local com cabeçalho padrão + cancelar presença + avatares legíveis + dashboard sincronizada.</b> Quatro bugs na ficha do local (abertura a partir do mapa/descoberta), reportados juntos por screenshot. (1) <b>Cabeçalho diferente das outras telas:</b> o overlay abria com backdrop translúcido que deixava o <code>.sticky-back-header</code> da página de fundo vazar por baixo, enquanto o modal tinha seu próprio header custom — resultado: duplicação visual e ausência do topbar com logo+hamburger. Overlay trocado pra backdrop opaco (<code>background: var(--bg-dark)</code>) começando em <code>top: 60px</code> — agora o topbar permanece visível e fixo acima. Header interno migrado pra <code>_renderBackHeader</code> (padrão de todas as outras views) com <b>Voltar à esquerda + nome do local no centro + hamburger à direita</b>, igual dashboard/torneios/perfil. CSS adicional com <code>body.venue-detail-open</code> escondendo o back-header da página de fundo como defense-in-depth. (2) <b>Botões "Estou aqui agora" e "Planejar ida" sem como cancelar:</b> após check-in, o botão ficava disabled como "✅ Presença registrada" — sem saída se o usuário mudasse de ideia ou errasse o local. Lógica reescrita: slot dinâmico <code>#venue-presence-btns-slot</code> hidratado por <code>_hydratePresenceButtonsForVenue(v)</code>, que consulta <code>PresenceDB.loadMyActive(uid)</code> e decide o que renderizar. Se não há presença ativa aqui, mostra botões verdes originais; se já tem check-in/plano ativo neste venue, mostra <b>❌ Cancelar presença</b> / <b>❌ Cancelar plano</b> em vermelho com confirmação. Cancelar re-hidrata tudo e restaura os botões originais. (3) <b>Avatares distorcidos e sem nome em "Agora no local":</b> o bloco renderizava chips sobrepostos (<code>margin-left: -6px</code>) com o nome só no <code>title</code> attribute — no mobile não há hover, então o nome nunca aparecia. Substituído por <b>chips individuais</b> (avatar 28×28 + nome visível), com glow verde pra "Você" e âmbar pros amigos. Sem overlap, sem ambiguidade. (4) <b>Presença não refletia no widget da dashboard:</b> check-in inline na ficha do local não disparava atualização do widget "Sua presença ativa" na dashboard, exigindo refresh manual. Toda ação de criar/cancelar presença agora chama <code>window._hydrateMyActivePresenceWidget()</code>, que re-renderiza as pills instantaneamente. Regra para memória futura: <b>qualquer fluxo que cria/cancela presença fora da view principal (#presence) deve notificar explicitamente o widget da dashboard</b> — não basta o onSnapshot pegar, porque o widget renderiza uma vez só no <code>render()</code>. Versão: v0.16.1-alpha (bump major-minor pra marcar saída do ciclo 0.15 focado em venue-polish).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.102-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Vôlei de Praia e Futevôlei entram como modalidades.</b> Dois novos esportes cadastráveis em torneios e locais, ambos sempre em <b>duplas</b> (dupla vs dupla, como Beach Tennis). Pontuação oficial implementada: <b>Vôlei de Praia</b> (regra FIVB 2026) — best of 3, sets 1 e 2 vão até 21 pts, set decisivo até 15 pts, sempre com margem mínima de 2 pts; <b>Futevôlei</b> (regra oficial 2025) — best of 3, sets 1 e 2 vão até 18 pts, set decisivo até 15 pts, margem mínima de 2 pts. Configuração <code>_sportScoringDefaults</code> usa o modelo GSM existente (<code>type:\'sets\'</code>, <code>countingType:\'numeric\'</code>, <code>advantageRule:true</code>, <code>superTiebreak:true</code>) — rally point encodado sem precisar de novo tipo. Padrão de dupla (<code>_sportTeamDefaults</code>) e paletas de cores dos logos (<code>sportPalettes</code>) em create-tournament.js também atualizados. Disponíveis nos pickers: quick-create (main.js), full-create (create-tournament.js), casual match (bracket-ui.js), ícones mapeados em dashboard/tournaments/venues/venue-owner/presence/landing. <b>Esportes com times de mais de 2 jogadores (vôlei indoor, basquete, futsal, futebol, handebol) ficaram de fora por enquanto</b> — o app hoje só suporta modalidades 1v1 e 2v2. Limpeza completa em todos os ícones e SPORTS arrays: o que restou são só Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel, Squash, Badminton, Vôlei de Praia e Futevôlei. Entradas antigas de vôlei indoor/basquete/futebol/futsal/handebol foram retiradas dos resolvers (bracket-ui.js, venues.js, venue-owner.js, presence.js, dashboard.js, tournaments.js, landing.js). Ordem de match importante: <b>futevôlei sempre verificado antes de qualquer match contendo "vôlei"</b> — "futevôlei" contém "vôlei" como substring, então a ordem inversa pegava o ícone errado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.101-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Quadras públicas não podem mais ser reivindicadas.</b> A lógica de acesso do local agora define quem pode reivindicar: quadras <b>públicas</b> (praias, praças, parques — acesso livre à população) são de domínio público e não fazem sentido ter proprietário declarado; quadras <b>condominiais</b> (restritas a sócios/condôminos) e <b>privadas comerciais</b> (cobrança por uso) continuam reivindicáveis normalmente pelo responsável. UI esconde o botão 🏢 Reivindicar em venues públicas (<code>accessPolicy === \'public\'</code>), e o fluxo de edição do proprietário (<code>_venueOwnerEditExisting</code> em venue-owner.js) ganhou guarda de segundo nível: se alguém chamar a função direto (via console ou link antigo) num venue público, recebe aviso "Local público — sem dono" e o formulário não abre. Edição comunitária (botão ✏️ Editar na ficha do local) continua disponível pra correções de endereço/horário/descrição em qualquer venue sem dono — inclusive nos públicos — preservando o espírito colaborativo do cadastro.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.100-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de espera reaparece entre os inscritos + substituição W.O. propaga pro parceiro.</b> Dois bugs reportados juntos via screenshot do painel de check-in. (1) <b>Lista de espera invisível na lista principal:</b> desde uma regressão recente, participantes da lista de espera só apareciam na seção própria "Lista de Espera", e não mais intercalados alfabeticamente entre os inscritos (que facilita muito a organização do check-in presencial). Causa: em <code>participants.js:511</code> o array <code>standbyParts</code> só lia <code>t.standbyParticipants</code>, ignorando <code>t.waitlist</code>. Padrão de merge com dedup por nome (mesmo já usado em <code>_declareAbsent</code>, <code>_autoSubstituteWO</code> e em bracket.js) aplicado. Agora reaparecem com badge "Lista de Espera" ao lado do nome, ordenados alfabeticamente com todos os outros. (2) <b>Card do parceiro não refletia novo time após W.O. individual:</b> quando Bot 04 levava W.O. e Rodrigo (da lista de espera) assumia a posição, o card do Bot 05 (parceiro remanescente) continuava mostrando "Bot 05 / Bot 04" em vez de "Bot 05 / Rodrigo". Causa: em <code>participants.js:_declareAbsent</code> (branch de W.O. individual), a propagação do novo nome do time era feita apenas em <code>t.matches</code> via <code>forEach</code> — rounds/groups/formatos Liga/Suíço/Rei-Rainha ficavam com referências obsoletas. Fix usa <code>_collectAllMatches(t)</code> (padrão canônico) pra iterar todos os match objects, atualiza <code>p1/p2</code> em todas as estruturas, e também toca <code>team1[]/team2[]</code> (formato Rei/Rainha). Adicionalmente, <code>.participants[]</code> aninhado (quando existe no objeto de time) tem o sub-objeto do ausente atualizado com displayName/name/uid/photoURL/email do substituto, pra que helpers como <code>_buildMatchPlayersList</code> em bracket-ui.js não continuem achando o jogador ausente. Mesma melhoria aplicada em <code>_autoSubstituteWO</code> (bracket-ui.js) pra manter consistência entre os dois caminhos de substituição.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.99-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil nunca mais some depois de reinstalar o app.</b> Usuário desinstalou o PWA do dock, reinstalou via Safari, e ao abrir o perfil viu os campos vazios — como se os dados tivessem sido apagados. <b>Não eram:</b> o Firestore (<code>users/{uid}</code>) continua com tudo intacto (nome, gênero, data de nascimento, cidade, esportes, telefone, preferências). O problema era uma <i>race condition</i> no fluxo de login: em <code>simulateLoginSuccess()</code> (auth.js), <code>AppStore.currentUser</code> é populado com dados básicos do Google (uid, email, displayName, photoURL) ANTES de <code>loadUserProfile(uid)</code> fazer o merge dos campos salvos. Na janela de ~300-500ms entre esses dois passos, se o usuário clicasse no avatar pra abrir o perfil, os campos extras apareciam em branco. Na primeira instalação do PWA ou em reinstall, o cache do <code>localStorage</code> é zerado, então não havia fallback — bastava clicar rápido. <b>Correção:</b> <code>_openMyProfileModal()</code> agora roda um re-fetch do Firestore em paralelo ao abrir o modal. Se o merge já aconteceu, a segunda leitura é idempotente (custo: 1 doc read barato); se ainda não aconteceu, os campos se auto-preenchem assim que a rede responde. Sem spinner, sem delay visível — o modal abre snappy com o que tem e completa em segundo plano. Regra para memória futura: <b>qualquer modal que lê <code>AppStore.currentUser</code> logo após login deve disparar um <code>loadUserProfile</code> como safety net</b> — listener de snapshot em <code>startProfileListener</code> só sincroniza tema/casual/amigos, não os campos principais do perfil.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.98-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Pódio do topbar em 48px.</b> 36px ainda ficava tímido no espaço disponível. Subido pra 48px — dobro do tamanho original (24px) e próximo do teto do topbar (<code>min-height: 60px</code> + <code>padding: 0.5rem</code> = ~44px úteis, mas o <code>overflow: visible</code> permite 48px sem cortar).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.97-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Pódio do topbar maior.</b> O SVG do pódio estava sendo renderizado em 24×24 porque <code>.page-title svg { width: 24px !important }</code> em <code>layout.css</code> sobrescrevia o atributo inline. Aumentado pra 36×36 — proporcional ao H1 de 1.4rem, aproveita o espaço vertical do topbar. Adicionado <code>flex-shrink: 0</code> pra garantir que não encolhe em viewports estreitos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.96-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Pódio no topbar também.</b> Completa a v0.15.95: o ícone inline do topbar continuava sendo o SVG antigo de troféu (herdava <code>stroke: var(--primary-color)</code>). Agora virou o mesmo pódio compacto 24×24 com prata/ouro/bronze + estrela — consistente com o ícone PWA, com a landing e com o novo logo da marca. Cores fixas de medalha funcionam em todos os 4 temas (Noturno, Claro, Por do Sol, Oceano) sem depender de variável CSS.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.95-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Nova identidade visual: pódio ouro/prata/bronze.</b> Os ícones do app (Chrome/Safari "adicionar à tela inicial") trocaram o <i>S amarelo em quadrado azul</i> pelo pódio com estrela sobre o 1º lugar. Arquivos: <code>icons/icon-192.svg</code> e <code>icons/icon-512.svg</code> reescritos com fundo escuro + pódio prata/ouro/bronze (cores universais de medalha — funcionam em qualquer wallpaper do SO, claro ou escuro). Novos assets pro uso no app: <code>icons/logo-wordmark.svg</code> (400×120, texto em <code>currentColor</code> pra herdar a cor do tema ativo via CSS — Noturno, Claro, Por do Sol, Oceano) e <code>icons/logo-podium.svg</code> (só o símbolo, pra combinar com texto CSS que já respeita tema). A landing page trocou o emoji 🏆 pelo pódio SVG com <code>drop-shadow</code> âmbar suave. <b>Importante:</b> usuários que já instalaram o app no iOS/Android continuam vendo o ícone antigo até reinstalar — é limitação do cache de ícones do SO, não do nosso código. Novos installs pegam o pódio direto.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.94-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Página de local: cabeçalho padronizado + Editar comunitário + Planejar ida corrigido.</b> Quatro fixes numa leva, todos direcionados pelo feedback <i>"isso foi pedido antes e ninguém fez"</i>: (1) <b>Voltar substitui Fechar</b> no canto superior esquerdo, mesmo padrão de todas as outras views do app. (2) <b>Reivindicar como dono</b> saiu do corpo da ficha e foi para o cabeçalho (direita), abreviado para "🏢 Reivindicar" — fica perto do botão de Editar e libera espaço vertical. (3) <b>Novo botão "✏️ Editar"</b> permite que qualquer pessoa logada atualize informações do local (nome, endereço, horário, descrição, faixa de preço, acesso, contatos) quando o local não tem dono reivindicado; cabeçalho troca para <i>✕ Cancelar</i> + título <i>✏️ Editando</i> + <i>💾 Salvar</i>. Cada edição grava um entry em <code>updateHistory[]</code> com <code>{uid, userName, timestamp, fields[]}</code> — mudanças só aparecem se forem de fato diferentes do baseline. Histórico público renderizado como <code>&lt;details&gt;</code> colapsável acima do corpo da ficha, formato: <i>"📝 Cadastrado por X · data"</i> ou <i>"✏️ Atualizado por Y · data · nome, endereço"</i>. (4) <b>Planejar ida corrigido em definitivo</b>: o bug de abrir diálogo de plano para "Paineiras" quando clicado em "MatchBall" era causado por <code>_venuesQuickPlan</code> navegar para <code>#presence</code> e depender de <code>state.venue</code> da outra view via sessionStorage — timing de hidratação ora pegava o local certo, ora não. Agora o overlay de plano é inline: modal próprio (<code>#venue-plan-overlay</code>, z-index 10030 acima do detail 10010) que lê o venue carregado no contexto atual, salva presence via <code>PresenceDB.savePresence</code>, re-hidrata o slot de movimento sem navegação, notifica amigos. Nunca mais cruza views para planejar.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.93-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>W.O. encontra substituto em <code>t.waitlist</code> (não só <code>t.standbyParticipants</code>).</b> Causa-raiz pela qual v0.15.92 não fechou o bug: <code>_declareAbsent</code> em participants.js lia apenas <code>t.standbyParticipants</code> para procurar substituto, mas <code>_autoSubstituteWO</code> (bracket-ui.js) e o painel de Lista de Espera (bracket.js) já mesclavam ambas as fontes (<code>standbyParticipants[]</code> + <code>waitlist[]</code>, dedup por nome). Em torneios onde o jogador está em <code>t.waitlist</code> (fluxo de inscrição com lotação cheia), o confirm handler nunca achava o Rodrigo presente → caía no fallback <i>"marca ausente e aguarda substituto"</i>, mesmo com Rodrigo já check-in. Agora <code>_declareAbsent</code> usa o mesmo padrão de merge + helper <code>_removeFromWaitlists(name)</code> que remove o substituto da fonte correta. Preview-verified com 4 cenários: Rodrigo só em waitlist ✓, só em standbyParticipants ✓, em ambos (dedup) ✓, em waitlist mas ausente (fallback correto) ✓.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.92-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Substituição W.O. completa quando o substituto chega depois.</b> Se o organizador declarou W.O. sem que o próximo da Lista de Espera estivesse presente, o jogador ausente ficava parado no time e o jogo da chave nunca era atualizado — mesmo que o substituto fosse marcado Presente mais tarde. Agora <code>_toggleCheckIn</code> detecta o cenário (jogador que acabou de chegar está na lista de espera + existe ausente pendente em jogo não decidido) e dispara automaticamente o fluxo <code>_autoSubstituteWO</code> com confirmação explicativa (<i>"W.O.: Bot 36, Parceiro: Bot 24 permanece, Substituto: Rodrigo Barth, Novo time: Rodrigo Barth / Bot 24"</i>). Preview-verified com 5 cenários: cenário positivo substitui corretamente; toggle off, jogador titular, sem ausente pendente e jogo já decidido não disparam nada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.91-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho de inscritos: espaço elegante entre <i>Voltar</i> e os filtros.</b> Na tela de inscritos com check-in ativo, o <code>extraStyle: padding-bottom:0</code> do <code>_renderBackHeader</code> deixava a linha de filtros (Todos/Presentes/Ausentes/Aguardando) colada no botão Voltar. Adicionado <code>margin-top:8px</code> no container dos filtros — mesmo gap que o <code>flex-wrap</code> já aplicava entre linhas dos próprios filtros, agora também acima deles. Puro visual, zero efeito funcional.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.90-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>W.O. unificado: sempre tenta substituir, botão "Preencher" do topo removido.</b> Registrar W.O. individual em time agora mostra sempre o mesmo diálogo (<i>"O próximo da lista de espera substituirá apenas {jogador}"</i> + botão <i>Substituir Individual</i>), independentemente de ser o primeiro ou o N-ésimo W.O. do torneio. Se houver alguém presente na lista de espera, substitui; se não houver, marca ausente e aguarda substituto — mesmo efeito, UX consistente. O botão verde <i>🔄 Preencher</i> que aparecia no topo da Lista de Espera foi removido — a operação acontece toda pelo W.O. direto no card do jogador.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.89-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel de resolução não é mais apagado por re-render em tempo real.</b> Causa-raiz do <i>"Painel removido (async)"</i> (a toast da v0.15.88 entregou a pista): o próprio <code>showUnifiedResolutionPanel</code> grava <code>t.status=\'closed\'</code> no Firestore ao abrir, o <code>onSnapshot</code> dispara <code>_softRefreshView</code> → <code>initRouter()</code> → <code>_dismissAllOverlays()</code>, e a varredura genérica (tudo que é <code>position:fixed</code> com z &gt; 101 cobrindo a viewport) apaga o painel em ~50ms. <code>_softRefreshView</code> já deferia re-render quando havia <code>.modal-overlay.active</code> ou <code>#qr-modal-overlay</code> aberto, mas não conhecia os painéis de resolução. Agora também espera por <code>#unified-resolution-panel</code>, <code>#groups-config-panel</code>, <code>#remainder-resolution-panel</code> e <code>#removal-subchoice-panel</code>. Verificado em preview com N=7, teamSize=2 (3 times + 1 resto): painel sobrevive ao soft-refresh simulado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.88-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Locais cadastrados voltaram para a descoberta + observabilidade mais granular em <i>Encerrar Inscrições</i>.</b> (1) Mesmo com a correção da v0.15.87 (filtro de sport client-side com wildcard), os locais continuavam não aparecendo entre <i>Minha localização atual</i> e <i>Sugestões do Google</i>. Causa-raiz: o filtro de <code>minCourts: 1</code> (default silencioso do state) excluía venues sem o campo <code>courtCount</code> — e todos os cadastros recentes (v0.15.51+) gravam <code>courts[]</code> como array, mas nem sempre o contador denormalizado. Agora o filtro aceita <code>courts[].length</code> como fallback para <code>courtCount</code>, e venues sem nenhuma das duas coisas (cadastro inicial sem quadras) passam como wildcard — mesmo princípio do filtro de sport. (2) Observabilidade de <i>Encerrar Inscrições</i> expandida para pinpoint stale-cache vs bug real: o toast agora embute a versão (<code>v=0.15.88</code>) — se o usuário voltar a relatar falha com uma versão antiga, sabemos que é cache do navegador. Check síncrono <i>e</i> assíncrono: se o painel não aparece imediatamente após a chamada, toast <i>"Painel não criado (sync)"</i>; se aparece e é removido depois, toast <i>"Painel removido (async)"</i>. Dois diagnósticos distintos, duas causas-raiz diferentes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.87-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Três bugs encontrados via observabilidade da v0.15.86.</b> (1) O próprio fallback <i>Painel não abriu</i> da v0.15.86 tinha um typo: buscava <code>#remainder-panel</code>, mas o ID real do overlay é <code>#remainder-resolution-panel</code>. Em caso de resto &gt; 0, o painel abria corretamente mas o toast disparava falso-positivo. Corrigido. (2) Notificação de <i>Planejar ida</i> e <i>Check-in</i> mostrava "<code>undefined</code>" no lugar do esporte ("vai jogar undefined em X"). O payload usa <code>sports[]</code> (array, plural), mas a mensagem lia <code>payload.sport</code> (singular, inexistente). Agora junta os esportes com "/" (ex: "Beach Tennis/Tênis") ou cai em "algo"/"agora" se vazio. Mesma correção no throttle key do check-in. (3) <i>Locais cadastrados sem quadras</i> não apareciam na descoberta: o filtro server-side <code>where(sports, array-contains)</code> excluía venues com <code>sports[]</code> vazio (caso comum de cadastro inicial, antes de registrar quadras). Movido o filtro de esporte para client-side com regra wildcard: venues sem <code>sports[]</code> declarado passam como coringa; venues com <code>sports[]</code> precisam incluir o esporte filtrado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.86-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Encerrar Inscrições nunca mais falha em silêncio.</b> Relatos recorrentes de que o botão <i>Encerrar Inscrições</i> em torneios Eliminatórios não mostrava o painel de resolução (resto, potência de 2, ímpar, times incompletos) em certos casos — mesmo com <code>Sortear</code> funcionando. Investigação confirmou que a lógica da v0.15.79/81 está correta para todos os formatos e contagens testadas. Para acabar com a zona cinzenta, agora o handler: (1) envelopa <code>_diagnoseAll</code> em try/catch — se um participante mal-formado quebrar o diagnóstico, surge um toast "Falha no diagnóstico" em vez de travar silenciosamente; (2) envelopa <code>showUnifiedResolutionPanel</code> em try/catch — se a função lançar ou não estiver definida (cache stale), surge um toast "Erro ao abrir painel"; (3) após disparar o painel, verifica depois de 120ms se o overlay apareceu no DOM — se não apareceu, surge um toast "Painel não abriu" com o diagnóstico completo (fmt, teams, resto, pot2, ímpar, incompletos). Se o usuário voltar a relatar o problema, o toast aponta exatamente a causa-raiz.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.85-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cadastrar local direto da descoberta.</b> Na view <i>Locais</i> (#venues), quando o usuário escreve o nome/endereço e seleciona uma sugestão do Google que ainda não está cadastrada no scoreplace, o card de detalhe mostra agora um botão <b>+ Cadastrar</b>. Um tap leva ao <i>Meus locais</i> (#my-venues) com o local já pré-preenchido (nome, endereço, coordenadas, placeId) — sem precisar re-buscar o mesmo endereço. Se por acaso outro usuário cadastrou o mesmo placeId no intervalo, o formulário cai automaticamente no modo edição colaborativa.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.84-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença: fim real da duplicata visual (onSnapshot é a única fonte de verdade).</b> Depois do in-flight registry da v0.15.83 garantir 1 doc único no Firestore, a duplicata <i>visual</i> continuava aparecendo no <i>Estou aqui agora</i> e <i>Planejar ida</i>. Causa-raiz encontrada em <code>presence.js</code>: o <code>onSnapshot</code> da coleção rebuilda <code>state.myActive</code>/<code>state.presences</code> do Firestore a cada mudança, mas os handlers <code>_presenceCheckIn</code>/<code>_presenceConfirmPlan</code> também faziam <code>state.myActive.push(payload)</code> + <code>state.presences.push(payload)</code> dentro do <code>.then()</code>. Quando o snapshot chegava ANTES do <code>.then</code> (race comum), o array ficava com 2 cópias do mesmo registro. Removidos os pushes manuais — o listener do Firestore agora é única fonte de verdade. Verificado: 1 único card por check-in/plano.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.83-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença: in-flight registry em PresenceDB (fim do double-tap duplicado).</b> Em v0.15.80/81 tentamos matar o bug de duplicata primeiro com flag síncrono em <code>presence.js</code> e depois com query de dedup em <code>PresenceDB.savePresence</code>. Mas a query sozinha tem um race clássico — duas chamadas concorrentes ambas consultam ANTES de qualquer <code>add()</code> completar, ambas veem "não tem", ambas inserem. Implementado agora um <b>in-flight registry síncrono no próprio PresenceDB</b>: chamadas concorrentes com a mesma chave lógica (uid+placeId+type+sports+janela) reusam o mesmo Promise em vez de disparar outro <code>add()</code>. Independe do caminho (presence.js, venues.js, presence-geo.js) e do modelo de dados no state. Verificado: 2 chamadas simultâneas → 1 <code>add()</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.82-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Percentuais nas estatísticas do usuário.</b> Cada número em Desempenho agora mostra o percentual entre parênteses logo abaixo do valor absoluto. Em <i>Derrotas/Vitórias, Sets, Games, Pontos, Tiebreaks</i> o % é dentro da mesma fonte (torneio 🏆 ou casual ⚡) — ex: com 0 derrotas e 3 vitórias em torneios, mostra "0 (0%)" e "3 (100%)"; 2 derrotas e 8 vitórias em casuais → "2 (20%)" e "8 (80%)". Linhas de média e mín/máx (Pontos TB Médios, TB Vencidos, TB Perdidos) não recebem % porque o par não é somável.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.81-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dedup server-side de presença + diagnóstico de Encerrar Inscrições.</b> (1) <i>Presença duplicada persistente.</i> O guard síncrono da v0.15.80 só protegia os handlers em <code>presence.js</code>, mas check-ins também são criados por <code>venues.js</code> (modal do local) e <code>presence-geo.js</code> (auto GPS) — cada um podia gerar uma duplicata antes do <code>state.myActive</code> hidratar. Movemos o dedup para dentro de <code>PresenceDB.savePresence</code>: antes de fazer <code>add()</code>, consulta Firestore por presenças ativas do mesmo uid com placeId/type iguais, sports sobrepostos (e janela de tempo sobreposta no caso de plan). Se encontra, retorna o id existente em vez de criar um novo doc. Protege double-tap, multi-tab, multi-codepath e race do cache. (2) <i>Diagnóstico em Encerrar Inscrições.</i> Quando o painel não abrir como esperado, o console mostra agora <code>[Encerrar Inscrições] diag {...}</code> com format, classificação, e todos os campos do <code>_diagnoseAll</code> (effectiveTeams, remainder, isOdd, isPowerOf2, hasIssues, hasRelevantIssues). Permite identificar rapidamente se é drift de format string, diag vazio ou classificação errada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.80-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença sem duplicidade + #venues sempre pinado.</b> Dois fixes observados em produção: (1) <i>Estou aqui agora</i> e <i>Planejar ida</i> criavam dois docs idênticos num double-tap rápido — o dup-check lia <code>state.myActive</code> antes do push, que só ocorre no <code>.then()</code> do save. Adicionado flag síncrono (<code>state._savingCheckin</code> / <code>state._savingPlan</code>) que bloqueia a segunda chamada enquanto a primeira está em voo; plan também ganhou dup-check contra planos no mesmo local com sports/horário sobrepostos. (2) Em <i>#venues</i>, após a primeira sessão a flag <code>centerFromGps</code> persistida impedia o auto-GPS nas entradas seguintes — o pin verde sumia até um click manual em 📍. Agora o auto-GPS dispara em toda entrada a menos que o usuário tenha digitado um endereço custom no campo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.79-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Encerrar Inscrições roda diagnóstico em qualquer formato não-Liga/Suíço.</b> O botão "Encerrar Inscrições" do organizador agora dispara o painel unificado de resolução (potência de 2, número ímpar, times incompletos, resto) para Eliminatórias, Dupla Eliminatória, Rei/Rainha e qualquer string de formato legada/com drift — antes só o botão Sortear checava, e a classificação por igualdade estrita (<code>format === \'Eliminatórias Simples\'</code>) deixava passar formatos com variação de acentuação/pluralização. Agora a classificação é por exclusão: Liga/Suíço ignoram potência de 2 (BYEs naturais); Grupos tem painel próprio; todo o resto cai no check completo — espelhando a lógica já usada por <code>showUnifiedResolutionPanel</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.78-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Menu empurra conteúdo também em overlays.</b> Complementa v0.15.77: em Novo Torneio, Partida Casual e modais afins, uma regra CSS com <code>!important</code> (suprimia o spacer padrão de 50px) estava anulando o margin-top inline que o _reflowChrome calcula quando o dropdown abre. Trocamos <code>element.style.marginTop = ...</code> por <code>setProperty(..., \'important\')</code> para o valor dinâmico vencer, preservando o padrão 0 quando o menu está fechado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.77-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Menu empurra conteúdo em toda página.</b> Completa a correção de v0.15.75: o hamburger aberto agora empurra o conteúdo para baixo de forma consistente em TODAS as páginas, inclusive no dashboard. A contagem de back-headers visíveis (_reflowChrome) ignora os que estão dentro de modais inativos — eles permanecem no DOM via opacity:0 + pointer-events:none e por isso falsavam a condição, impedindo o paddingTop do view-container em páginas sem overlay.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.76-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera restaurada.</b> Ajuste de escopo: o painel separado "Lista de Espera" continua visível na view de presença (traz o botão Preencher, contato, ordem de chegada). Os membros também aparecem na lista principal em ordem alfabética com rótulo âmbar — facilita só localizar nomes na lista única sem eliminar a ferramenta da lista de espera.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.75-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Hamburger em todas as páginas + lista de espera integrada.</b> (1) _reflowChrome: o modal #modal-quick-create é display:flex permanente com opacity:0 quando fechado (não display:none), então sua back-header latchava como staticBH em TODAS as páginas normais e o dropdown era posicionado em top:0px (atrás da topbar). Fix: staticBH só conta se o ancestor .modal-overlay tiver classe .active. Dashboard, Novo Torneio, Partida Casual todos funcionam agora. (2) Painel separado "Lista de Espera" removido da view de presença — standby já aparece na lista principal em ordem alfabética com rótulo "Lista de Espera" em âmbar. Uma só lista, uma só procura. O painel continua no bracket (onde o contexto é diferente).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.74-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix hamburger + W.O. normalização de nomes.</b> (1) _reflowChrome: correção crítica — o modal #modal-quick-create existe no DOM permanentemente com position:static mesmo quando invisível, fazendo getBoundingClientRect() retornar zeros e o dropdown aparecer em top:0px (atrás da topbar). Agora o staticBH ignora elementos com rect zero (display:none). Restaura hamburger no dashboard e nas páginas normais. (2) _declareAbsent: normalização do separador de duplas — times salvos como "A/B" mas partidas armazenam "A / B" (com espaços), causando matchEntry = null e o diálogo de substituição falhar silenciosamente. Agora ambos são normalizados via replace(/\\s*\\/\\s*/g, \'/\') antes da comparação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.73-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Hamburger empurra conteúdo em todos os overlays + fix W.O.</b> (1) _reflowChrome: para overlays com back-header static (Novo Torneio, Partida Casual), o dropdown agora é posicionado na borda inferior do back-header real (getBoundingClientRect), não em viewport+topbarH fixo — resolve o overlay que aparecia longe do cabeçalho no modal ou sobrepunha o conteúdo. (2) participants.js: botão W.O. sempre usa _declareAbsent() para participantes ativos, independente de ind.matchNum — remove a rota para _markAbsent() silencioso que causava o padrão diferente no primeiro W.O. Adicionado fallback else em _declareAbsent para o caso de nenhum jogo encontrado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convidar e Apoie viram páginas reais.</b> Antes abriam como card flutuante sobre a tela atual (padrão inventado). Agora navegam via hash routing (#invite, #support) e renderizam no view-container como todas as demais páginas — cabeçalho padrão com Voltar + hamburger, conteúdo que empurra o menu, comportamento idêntico a Locais, Presença etc.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.71-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho padrão unificado + hamburger empurra conteúdo.</b> Todos os overlays/modais (Partida Casual, Novo Torneio, Convidar QR, Apoie) agora usam _renderBackHeader centralizado com classe sticky-back-header. _reflowChrome atualizado: distingue back-headers fixed (páginas normais) de static (dentro de overlays) e empurra o conteúdo correto em cada contexto. Dashboard: quando o menu abre, o view-container recebe paddingTop igual à altura do dropdown — sem sobreposição. QR Convidar e Apoie: hamburger adicionado ao padrão. #qr-modal-overlay adicionado ao seletor de z-index elevado no _toggleHamburger.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.70-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix hamburger centralizado.</b> (1) CSS: hamburger do back-header oculto em páginas normais (topbar já o tem) — visível APENAS dentro de overlays fullscreen onde a topbar fica coberta (.modal-overlay, #casual-match-overlay, #modal-support-pix). (2) _toggleHamburger: eleva z-index do dropdown para 200000 quando disparado de dentro de overlays de alto z-index (casual=100002, apoie=100000) — menu efetivamente abre. (3) Modal QR (Convidar para o app): título volta para o corpo do card, header tem só o botão Voltar.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.69-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho padrão completo em todas as páginas.</b> (1) Hamburger + conteúdo se empurram: quando o menu abre, o conteúdo desce em vez de ser sobreposto. (2) Modal "Novo Torneio" (Criação Rápida): cabeçalho padrão com Voltar + título + hamburger substituindo o antigo botão avulso. (3) Overlay "Partida Casual": cabeçalho padrão com Voltar + título + Iniciar + hamburger substituindo o gradiente personalizado. (4) Modal "Convidar para o app" (QR): cabeçalho padrão com Voltar + título + hamburger.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.68-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho padrão em todas as páginas + hamburger no Voltar.</b> (1) Botão Voltar adicionado à tela de partida casual (#casual/CODE) — todas as branches (loading, offline, não encontrado, encerrado, lobby, erro) agora exibem o Voltar fixo no topo. (2) O botão Voltar agora inclui o ícone de menu hamburger no canto direito, visível em mobile — mesmo dentro de modais fullscreen onde a topbar fica coberta, o menu principal continua acessível. (3) Modais "Convidar" e "Apoie" ganham cabeçalho padrão com Voltar + hamburger substituindo o botão "Fechar" no rodapé.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.67-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Voltar no #venues.</b> Cabeçalho sticky com botão Voltar → Dashboard adicionado ao topo da página de descoberta de locais.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.66-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: página #venues abria e apagava resultados.</b> GPS re-disparava desnecessariamente quando já havia localização salva, causando race condition. Agora GPS só auto-dispara na primeira visita; nas seguintes, o centro salvo é usado diretamente.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.65-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descoberta de locais (#venues) melhorada.</b> Página abre já pinada na última localização. Locais registrados no scoreplace exibidos em box separado (máx 5 + "Mostrar mais"). Sugestões do dropdown mostram locais do scoreplace primeiro, depois Google. Dropdown de esporte com margem na seta. Clicar no campo de busca seleciona tudo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.64-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Detalhe de local redesenhado.</b> Endereço completo no cabeçalho (sem truncar). Tag "cadastrado por" mais discreta. Política de acesso com rótulo "Acesso público/restrito". Ícone por modalidade nas quadras. Box "Detalhes" com descrição livre e preços.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.63-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Removido widget "Meus locais" do dashboard.</b> Era redundante — o acesso está disponível via #venues e #my-venues na navegação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.62-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Card de local compacto em #venues.</b> O card do local selecionado agora exibe apenas nome + ✅ Score em uma linha. Clicando abre os detalhes do local (quadras, esportes, avaliações).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.61-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: resolução de potência de 2 para torneios de duplas/times.</b> O cálculo de quantos participantes remover usava "excesso em times" em vez de "excesso em participantes", causando remoção insuficiente e retorno ao painel de resto. Corrigido usando excessParticipants (= excess × teamSize) em _executeRemoval e _showRemovalSubChoice.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.60-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera: botão único "Preencher" + ordem por chegada.</b> Removidos os botões "Assumir" individuais — agora um único botão "🔄 Preencher" aparece no topo do painel quando há W.O. e alguém presente. A lista é ordenada por ordem de check-in (first come, first served): quem confirmou presença primeiro fica na posição 1 e preenche a próxima vaga.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.59-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sorteio imediato após resolução de lista de espera.</b> Ao escolher "Sorteio geral" ou "Últimos inscritos" no painel de Lista de Espera, o sorteio ocorre imediatamente — sem painel intermediário adicional.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues: card de local sem botões.</b> Card do local selecionado é um link clicável que abre o Google Maps. Sem botões — apenas nome, endereço e "✅ Cadastrado no scoreplace" quando aplicável.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.57-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues: detecta se local já está cadastrado.</b> Ao selecionar uma sugestão do Google Places, o app consulta o Firestore: se o local já existe, o card exibe "✅ Já cadastrado no scoreplace" com borda verde + botão Ver (ou Editar, se for o dono). Se não existe, mostra "+ Cadastrar no scoreplace".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.56-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues: mapa com bordas arredondadas e alinhado aos campos.</b> Mapa agora tem padding lateral, cantos arredondados e borda sutil — visual consistente com os outros cards da página.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.55-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#my-venues: lista de locais simplificada.</b> "Meus locais reivindicados" virou "Locais cadastrados". Cards mostram só nome + modalidades, sem botões Ver/Pro/Editar/✕ — clicar no card já abre o formulário de edição.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.54-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues: pin+zoom ao selecionar + resultados por distância.</b> Clicar numa sugestão do Google Places finca um pin índigo no mapa e faz zoom (nível 15) naquele ponto, mostrando imediatamente um card com nome, endereço, link para o Google Maps e CTA de cadastro. Resultados (scoreplace + Google) aparecem juntos ordenados do mais próximo ao mais distante. Campo de busca seleciona todo o texto ao clicar, facilitando substituição de "Minha localização atual".</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venues.js</code>, <code>js/store.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.53-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues: autocomplete dinâmico + modalidade como dropdown.</b> O campo de busca agora mostra sugestões do Google Places em tempo real conforme você digita — selecione um local para centralizar o mapa e carregar os venues próximos. O seletor de modalidades virou um dropdown (evita o corte no mobile). Mapa ligeiramente menor para que o campo de busca fique sempre visível na tela.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venues.js</code>, <code>js/store.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.52-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#venues redesenhado — limpo, simples e funcional.</b> A tela de Locais foi completamente refeita. Agora ela mostra: pills de modalidade no topo (rolagem horizontal), mapa de ponta a ponta logo abaixo, campo de busca + botão 📍 de GPS embaixo do mapa, e a lista de resultados aparece só depois de clicar no GPS ou digitar um endereço. Toda a poluição visual anterior (filtros de preço, distância, min. quadras, toggle mapa/lista) foi removida — ficou só o essencial. Clicar nas pills de modalidade atualiza os pins do mapa e a lista sem re-render completo da página.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.51-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Quadras: salvo e aberto sem fricção + seleção por pills.</b> O botão "Cadastrar quadras" agora funciona em qualquer estado — se o venue ainda não foi salvo, ele salva automaticamente e abre a tela de quadras na sequência, sem aviso "salve primeiro". A seleção de modalidades deixou de ser checkboxes e virou pills coloridas (roxo quando ativado) muito mais compactas e bonitas. Corrigido também o erro "Missing or insufficient permissions" que impedia salvar quadras — as entradas agora ficam dentro do próprio documento do venue, sem subcoleção separada.</p>' +
        '<p><b>Arquivos</b>: <code>js/venue-db.js</code>, <code>js/views/venue-owner.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#my-venues: mapa não duplica mais ao selecionar local.</b> A v0.15.49 introduziu o mapa no topo, mas o formulário seguia renderizando um segundo mapa menor internamente — quando você selecionava um venue via Google Places, os dois apareciam juntos. Agora o mapa é único: fica no topo e, ao selecionar, faz zoom-in (nível 16, street-level) no venue com um pin vermelho 📌 destacando a escolha. Ao cancelar, o pin some e o mapa volta a mostrar só os cadastrados.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.49-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#my-venues: mapa no topo + busca unificada (cadastrados + Google).</b> A rota agora mostra um mapa com todos os venues já cadastrados como pins no topo — você enxerga de cara o que já existe perto de você antes de cadastrar mais um. Pin âmbar = free, pin índigo = Pro. Clicar num pin abre o venue em modo edição.</p>' +
        '<p>O dropdown de busca passou a ter <b>duas seções distintas</b>: "🏢 Já cadastrados no scoreplace" (com badge ✓ oficial / 🤝 comunitário) vem no topo, e "📍 Sugestões do Google — novo cadastro" logo abaixo. Se o local que você quer cadastrar já existe, você edita direto; se não existe, escolhe a sugestão do Google e cria do zero. Evita duplicatas.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.48-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>#my-venues: preâmbulo removido, busca direta.</b> O header "🏢 Cadastre locais" + o parágrafo explicativo ("Cadastre clubes, arenas ou quadras — seu ou de qualquer lugar...") foram removidos. Quem entra na rota cai direto no input de busca + formulário. Menos atrito, menos scroll no mobile.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.47-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix crítico: cadastrar local voltou a funcionar.</b> Desde a v0.15.43 (que introduziu a grade 7×24 de horário de funcionamento) nenhum local conseguia ser salvo — o Firestore rejeitava o payload com <code>Function Transaction.set() called with invalid data. Nested arrays are not supported</code> porque o grid era persistido como array 2D (<code>[[d0h0..d0h23],[d1h0..d1h23]...]</code>) e o Firestore não aceita arrays dentro de arrays.</p>' +
        '<p>Correção: o grid agora é persistido como <b>array flat de 168 posições</b> (<code>day * 24 + hour</code>) — Firestore aceita. Em memória a UI continua trabalhando com a matriz 2D; helpers <code>_flattenGrid</code> (na gravação) e <code>_expandGrid</code> (na leitura) fazem a conversão na borda de persistência.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.46-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Nova tagline: "Jogue em outro nível".</b> A frase "Organize seus torneios" / "Plataforma de gestão de torneios esportivos e board games" foi substituída por <b>"scoreplace.app — Jogue em outro nível"</b> em todos os pontos de branding do app: <code>&lt;title&gt;</code> da página, meta description (SEO), <code>manifest.json</code> (PWA), footer de e-mail, modal "Sobre" e landing page (PT/EN).</p>' +
        '<p>A nova tagline reflete melhor os 5 pilares do produto (torneios, partidas casuais, presença, locais e stats) sem reduzir o posicionamento a "só gestão de torneios". Frases explicativas dentro do manual (ex: "o scoreplace.app notifica sobre tudo que acontece nos seus torneios") seguem intactas.</p>' +
        '<p><b>Arquivos</b>: <code>index.html</code>, <code>manifest.json</code>, <code>js/store.js</code>, <code>js/main.js</code>, <code>js/email-templates.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>, <code>sw.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.45-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: tie-break do placar ao vivo consistente com a regra do torneio.</b> O placar ao vivo disparava o tie-break quando os games empatavam em <code>gamesPerSet × gamesPerSet</code> (ex: 6-6 em set de 6 games), enquanto a regra exibida ao usuário em <code>#rules</code> e o save path do overlay de resultado tratam TB em <code>(gamesPerSet-1) × (gamesPerSet-1)</code> (ex: 5-5, final 6-5). Agora o live scorer em <code>_checkSetWon</code> dispara em 5-5, consistente com tudo.</p>' +
        '<p>Bônus: corrigido o <code>_checkSetComplete</code> do overlay de resultado set-a-set, que fazia o indicador "TB" aparecer em 6-6 (nunca alcançável na prática, já que o save fechava o set em 6-5) mas não em 5-5. Adicionado também <code>oninput</code> nos inputs de tie-break do overlay — antes, digitar pontos do TB não revalidava o estado (botão Salvar ficava preso em disabled).</p>' +
        '<p><b>Arquivos</b>: <code>js/views/bracket-ui.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.44-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Limpeza de código defensivo de compat — fase alpha.</b> Aplicando a regra persistida no <code>CLAUDE.md</code> ("dados no Firestore são descartáveis até o beta"), removidos todos os branches <code>if (legacy_shape) ...</code> que eu tinha acumulado entre venues/courts e presences:</p>' +
        '<ul style="margin:4px 0 8px 18px;font-size:0.82rem;">' +
          '<li><b>venue-db.js</b>: <code>addVenueCourt</code> e <code>updateVenueCourt</code> não gravam mais <code>sport: sports[0]</code> (mirror back-compat). Apenas <code>sports[]</code>. Aggregate <code>aggregateVenueCourts</code> drop do fallback <code>c.sport ? [c.sport] : []</code> — lê só <code>c.sports</code>.</li>' +
          '<li><b>presence-db.js</b>: <code>loadForVenueSportDay</code> era uma query dupla (uma em <code>sports[]</code> + uma em <code>sport</code> escalar) com dedup por doc id — agora só a query <code>array-contains</code> em <code>sports[]</code>. Reduz 1 round-trip por leitura de presença.</li>' +
          '<li><b>presence.js</b>: 8 sites de <code>Array.isArray(p.sports) && p.sports.length ? p.sports : (p.sport ? [p.sport] : [])</code> substituídos por <code>Array.isArray(p.sports) ? p.sports : []</code>. Campo <code>state.sport</code> (derivado redundante) deletado do objeto de estado. Payloads de <code>savePresence</code> não gravam mais <code>sport: normSports[0]</code>.</li>' +
          '<li><b>venues.js / dashboard.js / presence-geo.js</b>: todos os <code>sessionStorage._presencePrefill</code> agora gravam <code>sports: [...]</code> (array) em vez do shape antigo com string vazia. Quick check-in a partir do modal de venue também dropou o mirror singular.</li>' +
          '<li><b>venue-owner.js</b>: deletado <code>_venueCourtsRefresh</code> (função dead code pós-redesign do v0.15.43 — a inline courts list do form antigo não existe mais). Drop do fallback <code>ex.openingHoursGrid</code> (flat shape nunca usado) e <code>|| ex.website</code>. Listagem de courts na tela nova lê só <code>c.sports</code>.</li>' +
        '</ul>' +
        '<p><b>Regras ficam explícitas agora</b>: um único shape canônico por campo, zero defensive reads para shapes antigos. Se um doc no Firestore ainda tiver <code>sport</code> singular e não <code>sports[]</code>, vai aparecer quebrado — e tudo bem, é fase alpha, Rodrigo apaga e refaz.</p>' +
        '<p><b>Arquivos</b>: <code>js/venue-db.js</code>, <code>js/presence-db.js</code>, <code>js/presence-geo.js</code>, <code>js/views/venue-owner.js</code>, <code>js/views/presence.js</code>, <code>js/views/venues.js</code>, <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.43-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cadastro de locais reformulado: foco no essencial + grade de horários pintável.</b> Cinco mudanças na UX de <code>#my-venues</code>:</p>' +
        '<ul style="margin:4px 0 8px 18px;font-size:0.82rem;">' +
          '<li><b>Mapa inline + pin</b>: abaixo do nome/endereço aparece um mapa interativo de 180px centrado no local selecionado. Imports lazy de <code>maps</code> + <code>marker</code> libs quando o user selecionar o local no autocomplete.</li>' +
          '<li><b>Grade 7×24 de horários</b>: nova UI substitui o campo de texto livre. 7 linhas (Seg–Dom) × 24 colunas (h 0–23), cada célula começa vermelha (fechado); <b>arraste o dedo</b> pra pintar várias verdes (aberto) — primeiro toque decide a cor-alvo, pointermove + <code>elementFromPoint</code> aplica à célula sob o dedo. Dados do Google Places (<code>regularOpeningHours.periods</code>) já pré-preenchem a grade respeitando mudanças de dia à meia-noite.</li>' +
          '<li><b>Campos removidos</b>: modalidades (deriva de courts), contagem de quadras, faixa de preço, horário texto livre, valores, texto promocional, URLs de fotos. Dados legados preservados por <code>{merge:true}</code> — o usuário só deixa de ver/editar essas campos.</li>' +
          '<li><b>Botões grandes</b>: Reivindicar como proprietário (toggle) + Cadastrar quadras/campos — dois retângulos lado a lado acima do Salvar.</li>' +
          '<li><b>Cadastro de quadras simplificado</b>: clicar "Cadastrar quadras" abre tela nova (overlay full-screen) com nome do local, botão ➕, e lista de entradas existentes abaixo. ➕ expande form inline com <b>quantidade</b> + <b>checkboxes de modalidades</b> (multi-select = uso compartilhado, ex: 3 quadras que atendem Beach Tennis <i>e</i> Futvôlei).</li>' +
        '</ul>' +
        '<p><b>Modelo de dados</b>: <code>venues/{id}.openingHours.grid</code> (array 7×24 de booleans). <code>venues/{id}/courts/{id}.sports[]</code> (array — shared quando length > 1). Backward compat: <code>sport</code> singular continua espelhando <code>sports[0]</code>; aggregator lê ambos os shapes.</p>' +
        '<p><b>Arquivos</b>: <code>js/views/venue-owner.js</code> (reescrito form + nova tela de courts + grid helpers + Google hours parser), <code>js/venue-db.js</code> (addVenueCourt + updateVenueCourt + aggregateVenueCourts aceitam sports[]), <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.42-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rollback completo do controle pelo Apple Watch (v0.15.39 → v0.15.41).</b> Tentativa de três rodadas de controlar o placar via Apple Shortcuts foi abandonada — a UX de construir/instalar o Shortcut no iPhone era pesada demais (ações de Dicionário, variáveis, loop, menu) pro ganho marginal. Apple bloqueia instalação one-click de Shortcuts via web; tudo que sobrava era instruções de múltiplos passos no app Atalhos do iPhone, que na prática ninguém iria seguir. Decisão: abandonar o caminho Shortcuts e, quando for hora, partir direto pra app nativo watchOS (Swift + Firebase SDK).</p>' +
        '<p><b>Removido</b>: arquivo <code>js/remote-control.js</code>, botão "⌚ Apple Watch" no <code>#bracket</code>, botão <code>⌚</code> no header do placar ao vivo casual, processamento de <code>pendingRemoteActions</code> no onSnapshot, Cloud Function <code>remoteScore</code> (deletada do projeto Firebase), rules do <code>remoteSessions</code> (rollback no Firestore), precache de <code>remote-control.js</code> no SW. Docs de versão 0.15.39/40/41 consolidadas neste único bloco de rollback pra não poluir o changelog.</p>' +
        '<p><b>Mantido</b>: arquitetura planejada documentada neste parágrafo pra referência futura — se algum dia voltarmos a essa feature sem native, o padrão de <em>server enqueue → client applies</em> (fila de <code>pendingRemoteActions</code> processada pelo motor de pontuação existente) já foi validado conceitualmente e é reusável.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir locais: GPS automático na abertura + layout mais legível.</b> Dois ajustes de UX no <code>#venues</code> baseados em feedback direto do usuário: "quero que a página abra como se o clique no pin já tivesse ocorrido e coloca Modalidade logo abaixo do Local pra melhorar a leitura". <b>(1) GPS automático em TODOS os devices</b>: antes só mobile disparava o <code>getCurrentPosition</code> direto — desktop só fazia isso se a permissão já estivesse em <code>granted</code>, o que deixava a primeira visita "sem centro". Agora <code>_tryAutoGeolocate</code> chama <code>_venuesUseMyLocation(false)</code> sempre, equivalente a um clique silencioso no 📍 assim que a view carrega. Se o user negar, cai graciosamente no fallback da cidade do perfil/filtro salvo (sem toast de erro, porque <code>userTriggered=false</code>). Idempotência mantida pelo flag <code>state.centerFromGps</code> — filtros que o user ajustar depois não são sobrescritos. <b>(2) Layout empilhado</b>: Local (com botão 📍) passou a ocupar linha própria full-width, e Modalidade foi para a linha seguinte também full-width. Antes estavam lado a lado em <code>grid-template-columns:1fr 1fr</code>, o que comprimia endereços longos e forçava truncamento no mobile. Mín. quadras + Distância continuam lado a lado (esses sim cabem bem em 2 colunas). Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.37-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Follow-up v0.15.36: sync <code>sports[]</code> automático + dicas novas.</b> Duas melhorias pós-entrega do cadastro em 2 camadas. <b>(1) Sync automático do array <code>sports[]</code> no venue doc</b>: quando um usuário adiciona uma quadra de [modalidade], <code>addVenueCourt</code> agora também dispara um <code>arrayUnion(sport)</code> no doc pai via Firestore update. Garante que o filtro de modalidade na descoberta (<code>#venues</code>) capture o venue — sem esse sync, um venue com Squash só na subcoleção mas sem "Squash" no array pai ficava invisível pra quem filtrasse por Squash. arrayUnion é idempotente (adicionar duas vezes não duplica). Falha silenciosa se a rule bloquear (ex: venue claimed e user não é owner — nesse caso o user nem chegaria a adicionar court mesmo). <b>(2) 4 novas dicas</b> sobre o fluxo colaborativo cobrem contextual: política de acesso do venue, lista de quadras, botão adicionar, e reivindicação de propriedade. Arquivos: <code>js/venue-db.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.36-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cadastro de local em 2 camadas: estabelecimento + quadras colaborativas.</b> Baseado em pedido real do usuário: "Clube Paineiras do Morumby — localizo, horário, só sócios; depois cadastro tantas quadras exclusivas de Beach Tennis, tantas compartilhadas com Futvôlei; outro usuário cadastra quadras de Tênis saibro e piso duro; outro adiciona Squash e Pickleball; etc." O cadastro antigo era flat (uma modalidade + um courtCount). Agora é relacional.</p>' +
        '<p><b>Camada 1 — Estabelecimento (<code>venues/{id}</code>)</b>: nome, endereço, horário, contatos + novo campo <code>accessPolicy</code> com 4 opções:</p>' +
        '<ul style="margin:4px 0 8px 18px;font-size:0.82rem;">' +
          '<li>🌐 <b>Público</b> — aluguel aberto a qualquer pessoa</li>' +
          '<li>👥 <b>Só sócios</b> — acesso restrito</li>' +
          '<li>👥🏆 <b>Sócios + convidados de torneios oficiais</b></li>' +
          '<li>🔒 <b>Privado</b> — não aceita externos</li>' +
        '</ul>' +
        '<p>Badge correspondente aparece destacado na modal do venue — jogadores sabem de cara se podem ir jogar ali ou se precisam de convite.</p>' +
        '<p><b>Camada 2 — Quadras (<code>venues/{id}/courts/{courtId}</code>, subcoleção colaborativa)</b>: cada entrada tem <code>sport</code>, <code>count</code>, <code>shared</code> (compartilhada/exclusiva), <code>surface</code> opcional (saibro, piso duro, areia), <code>notes</code> e <code>contributorUid</code>. Novos métodos em VenueDB: <code>addVenueCourt</code>, <code>listVenueCourts</code>, <code>updateVenueCourt</code>, <code>deleteVenueCourt</code>, <code>aggregateVenueCourts</code> (resumo por modalidade).</p>' +
        '<p><b>Duas fases de edição com contrato claro:</b></p>' +
        '<ul style="margin:4px 0 8px 18px;font-size:0.82rem;">' +
          '<li><b>Colaborativa (venue sem dono)</b>: qualquer autenticado adiciona entradas. Cada um só edita/apaga as próprias. Enforced na rule Firestore + UI.</li>' +
          '<li><b>Assumida pelo dono (venue com <code>ownerUid</code>)</b>: quando alguém reivindica, "assume tudo" — inclusive entradas da comunidade ficam sob sua gestão. Dono passa a ser o único editor. Outros usuários não adicionam novas. Contributors originais veem as entradas em read-only.</li>' +
        '</ul>' +
        '<p><b>Na modal do venue</b>: nova seção "🎾 Quadras disponíveis" agrega tudo por modalidade: "<b>6</b> quadras de Beach Tennis (saibro, areia) · 🔁 compartilhada · <b>2</b> de Tênis (piso duro) · ✓ exclusiva". Rodapé cita "Informações contribuídas por X jogadores". Badge de política de acesso aparece acima. <b>Firestore rules</b>: nova match <code>/venues/{id}/courts/{courtId}</code> com create permitido só em fase colaborativa ou pelo dono; update/delete segue mesma regra. Deploy requer <code>firebase deploy --only firestore:rules</code>. Arquivos: <code>js/venue-db.js</code>, <code>js/views/venue-owner.js</code>, <code>js/views/venues.js</code>, <code>firestore.rules</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.35-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: "Você está aqui" aparecendo em duplicidade na dashboard.</b> Usuário reportou ver o mesmo check-in próprio em dois lugares. Investigação apontou duas causas possíveis:</p>' +
        '<ul style="font-size:0.82rem;line-height:1.7;margin:4px 0 8px 18px;">' +
          '<li><b>Docs duplicados em Firestore</b> — double-tap no botão de check-in antes do <code>state.myActive</code> atualizar podia criar 2 docs com mesmo venue+sport. Widget mostrava 2 pills idênticos.</li>' +
          '<li><b>Auto-amizade por dado corrompido</b> — se o uid do usuário aparecia em <code>cu.friends</code> (via migração ou bug antigo), a própria presença vinha tanto no widget "Sua presença ativa" quanto no "Amigos no local".</li>' +
        '</ul>' +
        '<p><b>Fixes:</b></p>' +
        '<ul style="font-size:0.82rem;line-height:1.7;margin:4px 0 8px 18px;">' +
          '<li><b>Dedup no my-active widget</b>: agrupa presenças por <code>type + placeId + sport</code> e mantém só a mais recente (maior <code>startsAt</code>). Elimina duplicatas mesmo se Firestore tem docs redundantes.</li>' +
          '<li><b>Filtra self do friends widget</b>: <code>cu.friends</code> filtrado pra remover o próprio uid antes da query; depois do load, filtra outra vez qualquer presença com <code>p.uid === cu.uid</code>. Defense-in-depth.</li>' +
        '</ul>' +
        '<p>A mesma presença só pode aparecer uma vez agora. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.34-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: nudge "Complete seu perfil" aparecia mesmo com perfil completo.</b> Usuário reportou a banner âmbar insistindo "faltam modalidades preferidas e locais preferidos" enquanto o perfil tinha Beach Tennis selecionado e São Paulo como cidade. <b>Duas causas:</b></p>' +
        '<ul style="font-size:0.82rem;line-height:1.6;margin:4px 0 8px 18px;">' +
          '<li><b>Sports em formato legacy (string)</b>: o nudge usava <code>Array.isArray(cu.preferredSports)</code>, mas perfis salvos antes de v0.15.19 têm <code>preferredSports</code> como string CSV ("Beach Tennis"). Array.isArray na string retorna false → contava como "faltam". Fix: nudge agora aceita array OU string não-vazia, espelhando o pattern dos outros readers (bracket-ui, explore, tournaments-organizer).</li>' +
          '<li><b>Locais exigidos quando cidade já preenchida</b>: o nudge listava "locais preferidos" como requisito separado, mas <code>city</code> sozinha já é suficiente pra torneios perto, sugestões de parceiros e base de presença. <code>preferredLocations</code> (pins específicos no mapa) é um plus pra check-in rápido, não um bloqueio. Fix: só reclama de "cidade" se tanto city QUANTO preferredLocations estão vazios. Se tem city → nudge ignora locais como campo faltando.</li>' +
        '</ul>' +
        '<p>Agora o nudge só aparece quando realmente falta algo relevante — não incomoda usuários com perfil funcional. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.33-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil: nascimento como dd/mm/aaaa + altura alinhada.</b> Dois problemas reportados no campo "Nascimento" do perfil: (1) o valor aparecia em formato longo "25 de fev. de 1974" porque o <code>&lt;input type="date"&gt;</code> nativo renderiza conforme o locale do sistema — era cortado à direita e ilegível; (2) o campo ficava mais alto que o irmão "Sexo" na mesma linha, quebrando o alinhamento visual. <b>Fix:</b> trocado pra <code>&lt;input type="text" inputmode="numeric"&gt;</code> com <b>mask JavaScript</b> que formata conforme o usuário digita (25021974 → 25/02/1974). Placeholder adapta pra língua: <b>dd/mm/aaaa</b> em PT-BR, <b>mm/dd/yyyy</b> em inglês. <b>Armazenamento</b>: Firestore continua guardando em ISO (YYYY-MM-DD) pra que queries/ordenação funcionem independente de locale; helpers <code>_isoToDisplayDate</code> e <code>_displayDateToIso</code> convertem na carga e no save. Validação no save: data inválida (ex: 32/13/1990) mantém o valor anterior em vez de apagar — protege contra typo. A altura agora bate com o Sexo porque texto não tem o chrome do date picker nativo. <code>autocomplete="bday"</code> ajuda keyboards de mobile a sugerir a data do contato. Arquivos: <code>js/views/auth.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil: preenche dados do provedor (Google/Apple) com mais robustez.</b> Usuário reportou que após login com Google, o modal de perfil mostrava o nome vazio ou pedindo pra preencher — quando o Google já tem displayName, email e foto. Investigação: cenários de race condition (loadUserProfile ainda não completou, authCache sem displayName, profile antigo com campo vazio) deixavam <code>cu.displayName</code> indefinido. <b>Fix triplo em <code>_openMyProfileModal</code>:</b></p>' +
        '<ul style="font-size:0.82rem;line-height:1.7;margin:4px 0 8px 18px;">' +
          '<li><b>Fallback pra <code>firebase.auth().currentUser</code></b> — quando <code>cu</code> não tem displayName/photoURL/email/phone, consulta direto o usuário do Firebase Auth (sempre reflete o provedor). Hidrata cu de volta pra outras partes do app usarem.</li>' +
          '<li><b>Fallback de nome em cadeia</b> — se displayName ausente, tenta <code>firstName + lastName</code>; se ausente, derive do email (ex: "rodrigo.barth@gmail.com" → "Rodrigo Barth"). Nunca mais o genérico "Usuário".</li>' +
          '<li><b>phoneCountry auto-detectado</b> do <code>navigator.language</code> — pt-BR → 55, en-US → 1. Evita que o dropdown de DDI comece vazio pra usuários de fora.</li>' +
          '<li><b>authProvider</b> gravado em cu (<code>google.com</code>, <code>apple.com</code>, <code>password</code>...) pra futuras UIs mostrarem "✓ do Google" ou similar.</li>' +
        '</ul>' +
        '<p><b>Salvar sem limpar nome:</b> em <code>saveUserProfile</code>, se o campo nome vier vazio, mantém o displayName anterior (ou busca via firebase.auth) em vez de gravar string vazia. Protege contra "apagou sem querer e salvou". Arquivos: <code>js/views/auth.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: pill "⚡ Partida casual em andamento" com botão Voltar.</b> Extensão do widget "Sua presença ativa" (v0.15.23) pra cobrir também partidas casuais ativas. <b>Cenário resolvido:</b> usuário cria ou entra numa partida casual → live scoring aberto → navega pra dashboard (back button ou hash change) → o overlay some mas a partida no Firestore permanece <code>active</code>. Antes, o usuário não tinha feedback visual desse estado e podia esquecer. Agora, o widget <code>#dashboard-myactive-widget</code> detecta <code>cu.activeCasualRoom</code> (já sincronizado via <code>startProfileListener</code>) e renderiza um pill ciano com gradient: "⚡ Partida casual em andamento · Sala ABC123" + botão "⚡ Voltar" que faz <code>hash = \'#casual/&lt;room&gt;\'</code>. Roteador já sabe abrir live scoring quando a partida está em status <code>active</code>. <b>Posição:</b> pill de casual match vai ANTES das presenças (check-in/plan) porque é o estado mais "live" — partida casual acontece ao vivo, presença é menos urgente. <b>Degradação graciosa:</b> se a query de presenças falha (rede ou rules), o pill de casual ainda aparece. Se presença silenciada (<code>presenceMuteUntil</code>) mas casual ativo, casual aparece sozinho. Nova hint <code>dash-my-active</code> atualizada pra mencionar os 3 tipos. Arquivos: <code>js/views/dashboard.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Landing page: features atualizadas pros 5 pilares (torneios + casual + presença + locais).</b> A landing de visitantes não-logados mostrava 6 features, todas focadas em torneios (Chaveamento Auto, 5 Formatos, Inscrições, <b>Modo TV</b>, <b>QR Code</b>, <b>Estatísticas</b>). Os 3 pilares novos (Partida Casual, Presença, Locais) entregues em v0.14-0.15 ficavam invisíveis pra quem ainda não tinha conta — o pitch da landing subrepresentava o app. Os 3 últimos slots foram substituídos:</p>' +
        '<ul style="font-size:0.82rem;line-height:1.7;margin:4px 0 8px 18px;">' +
          '<li>📺 Modo TV → <b>⚡ Partida Casual</b>: "Placar ao vivo em tela cheia pra partidas avulsas. Convide amigos via QR Code e jogue."</li>' +
          '<li>📱 QR Code → <b>📍 Presença no Local</b>: "Marque \'Estou aqui agora\' e amigos veem. Veja quem tá jogando nos seus clubes."</li>' +
          '<li>📊 Estatísticas → <b>🏢 Descobrir Locais</b>: "Mapa com clubes e arenas abertos ao público. Filtros por modalidade, distância e preço."</li>' +
        '</ul>' +
        '<p>Chaveamento Automático, 5 Formatos e Inscrições Online (feat1-3) permanecem — ainda são o core do app pra quem busca organizar torneios. Modo TV e QR Code seguem existindo como features, só deixam de ocupar destaque na conversão. Estatísticas também continuam (acessíveis via modal do player). i18n PT + EN atualizado; ícones na grid de cards do landing trocados (⚡ 📍 🏢 no lugar de 📺 📱 📊). Arquivos: <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>, <code>js/views/landing.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.29-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cards de torneio: badge "HOJE / Amanhã / Em Xd" na data.</b> Release notes de v0.2.27 documentaram um "Countdown de Início" (torneios com data futura mostram "Começa hoje/amanhã/em X dias"), e as i18n keys <code>tournament.startsToday / startsTomorrow / startsIn</code> existiam em <code>i18n-pt.js</code> e <code>i18n-en.js</code> — mas nada no dashboard usava elas. Feature estava silenciosamente ausente, provavelmente perdida numa refatoração. <b>Fix:</b> pill ao lado da data na linha do calendário dos cards de torneio. Lógica: diff em dias entre <code>startDate</code> e hoje (ignorando fuso horário via comparação de year/month/day local). <b>Cores semânticas:</b> verde "Começa hoje!" (16,185,129), âmbar "Começa amanhã!" (251,191,36), índigo "Começa em X dias" (99,102,241). <b>Filtros:</b> skip em torneios encerrados, sem startDate, ou que já começaram (<code>_dayDiff &lt; 0</code>). Cap em 14 dias — badge irrelevante pra torneios distantes. Apenas visual; não altera lógica existente. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Meus Locais: busca dinâmica + botão "👁️ Ver" na lista.</b> Dois ajustes na view #my-venues (cadastro de locais). <b>(1) Busca do Google Places</b>: mínimo de caracteres 3 → 2 e debounce 220ms → 150ms pra bater com a busca do profile (v0.15.19). Digitar "Ar" já começa a sugerir "Arena..." em tempo quase real. <b>(2) Botão "👁️ Ver"</b> adicionado na lista "Meus locais reivindicados" — abre a modal pública do venue (<code>#venues/&lt;placeId&gt;</code>) pra que o dono veja exatamente o que os usuários veem: movimento ao vivo, reviews, contatos, torneios. Útil antes de compartilhar o link ou reivindicar mudanças visuais. Posicionado antes de "🚀 Pro" e "Editar" pra destacar como ação principal de preview. Nova hint <code>myvenues-view</code> cobre o botão. Arquivos: <code>js/views/venue-owner.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Amigos no local: nome clicável abre stats do jogador.</b> O widget "Amigos no local" da dashboard tinha o card inteiro clicável — qualquer tap navegava pra #presence com o venue/sport prefilado. Mas não havia como ver as estatísticas do amigo (histórico, V/D, parceiros frequentes, adversários recorrentes) a partir dali. <b>Fix:</b> o nome do amigo agora tem seu próprio onclick que dispara <code>_showPlayerStats(name)</code> com <code>event.stopPropagation()</code> pra não triggerar o click do card pai. Visual: underline dashed sutil no nome pra indicar que é clicável, com tooltip "Ver estatísticas". Mesmo pattern já usado em outras partes do app (bracket, standings, player cards). Rest do card continua navegando pra #presence. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.26-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Torneio → Local: botão "🏢 Local" abre modal do venue.</b> A página de detalhes do torneio mostrava endereço + botão 🗺️ (Google Maps externo) mas não tinha link pra modal do venue DENTRO do scoreplace — onde aparece movimento ao vivo (check-ins + torneios do dia), contatos (📞 💬 📷 ✉️) e reviews. Usuário querendo saber "quem mais joga aqui" ou "qual o WhatsApp do clube" tinha que sair do torneio, ir pra #venues, filtrar, achar... 4-5 cliques. <b>Fix:</b> novo botão azul ciano "🏢 Local" na linha do endereço, entre o texto e o 🗺️. Clique chama <code>_openVenueFromTournament(tId)</code> que resolve a venueKey via <code>VenueDB.venueKey(placeId, name)</code> — mesma chave usada pelo deep-link <code>#venues/&lt;key&gt;</code> — e navega. Roteador já sabe abrir a modal do venue após renderizar a lista. <b>Compatível com torneios do discovery feed</b> (hidrata via <code>AppStore.publicDiscovery</code> quando o id não está no scoped). Nova hint <code>tournament-venue-link</code> cobre o botão. Conecta mais um nó no grafo de navegação: Torneio ↔ Local bi-direcional (modal do venue já tinha "🏆 Criar torneio aqui" desde v0.14.70). Arquivos: <code>js/views/tournaments.js</code>, <code>js/views/tournaments-sharing.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença ativa: contador "expira em" em tempo real.</b> O pill "Sua presença ativa" da dashboard (v0.15.23) mostrava "expira em Xh" mas o valor era calculado uma vez no render e ficava estático. Após 10min num check-in de 3h, o usuário continuava vendo "2h 50min" quando o real era "2h 40min" — stale até reload. <b>Fix:</b> o span do "expira em" agora usa <code>data-countdown-target="&lt;endsAt&gt;"</code> e o ticker global de store.js (setInterval 1s) atualiza o textContent automaticamente — "2h 45m 30s" conta em tempo real. Zero overhead extra (o ticker já existia pra countdowns de torneios). Quando diff &lt;= 0, vira "Agora!" — próximo renderiza do widget (ex: abrir/fechar dashboard) remove a entrada expirada. Arquivos: <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.24-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: botão "📤 Compartilhar" no result screen.</b> Tournament matches tinham <code>_shareMatchResult</code> desde v0.2.29, mas partidas casuais não tinham maneira de compartilhar o resultado. Pós-partida, usuário via o placar e podia "Jogar Novamente" mas não tinha jeito de mandar pro WhatsApp dos amigos ausentes. <b>Fix:</b> botão verde WhatsApp (📤) adicionado ao lado de "Jogar Novamente" no finish screen. <b>Layout:</b> singles ganham grid 2-col (Jogar / 📤), duplas mantêm layout existente mas com "Re-sortear" compacto e botão 📤 entre eles. <b>Payload:</b> "⚡ [Título] · 🎾 [P1] vs [P2] · 📊 [placar set-a-set com tiebreak em parênteses] · 🏆 Vitória: [vencedor] · 🔗 scoreplace.app". Usa <code>navigator.share</code> mobile (WhatsApp, Instagram DM, SMS) com fallback pra clipboard + toast "Resultado copiado". <b>Formato do placar:</b> em GSM (sets) monta "6-4 · 3-6 · 7-6(5)" com tiebreaks em parênteses; em simple monta "15 x 13" baseado em <code>currentGameP1/P2</code>. Nova hint <code>casual-share-result</code> cobre o botão no contexto <code>casual-match</code>. Manual atualizado na seção Partida Casual → Tela de Resultado. Arquivos: <code>js/views/bracket-ui.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: status "Sua presença ativa" no topo.</b> Antes, usuário fazia "📍 Estou aqui agora" num local e ao voltar pra dashboard não via feedback visual do próprio check-in — só os amigos dele viam. Adicionado novo widget <code>#dashboard-myactive-widget</code> renderizado logo abaixo dos filtros e antes de "Amigos no local". <b>Check-in ativo:</b> pill verde gradient com dot luminoso "📍 Você está em [Local] · expira em Xh" e tempo restante formatado (min ou h/min). <b>Plan futuro:</b> pill índigo "🗓️ Planejado: [Local] hoje às HH:mm". <b>Ações por pill:</b> "Ver local" (abre modal do venue com movimento + contatos) e "Cancelar" (com confirmação, dispara <code>PresenceDB.cancelPresence</code>). <b>Condicional:</b> silent quando não há presença ativa — não polui a UI. Respeita <code>presenceMuteUntil</code> — usuário silenciado nem carrega o widget. Carrega via <code>PresenceDB.loadMyActive(uid)</code> (busca <code>endsAt &gt; now</code>), filtra e ordena client-side. Mostra até 1 check-in + 2 plans mais próximos. Nova hint <code>dash-my-active</code> cobre o widget na sessão "Complete seu perfil" das dicas. Arquivos: <code>js/views/dashboard.js</code>, <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.22-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dicas visuais para todas as features novas + contextos de Venues, Presença e Casual.</b> O sistema de hints (hints.js) tinha 173 dicas mas cobria só dashboard, tournament-detail, bracket, create-tournament, explore, player-stats e algumas overlays — TODAS as features que foram adicionadas nas pilars A (Presença), B (Locais) e polish recente (calendário, compartilhar venue, nudge de perfil, widget Meus Locais, Partida Casual com convite) estavam sem dicas. Três melhorias:</p>' +
        '<p><b>(1) Contextos novos no detector</b> — <code>_getCurrentContext()</code> em hints.js agora reconhece: <code>venues</code> (rota #venues), <code>venue-detail</code> (modal inline aberta), <code>presence</code> (#presence), <code>casual-match</code> (overlay ativo ou #casual/...), <code>my-venues</code> (cadastro), <code>notifications</code> (#notifications). Overlays full-screen (casual, venue-detail) têm prioridade sobre o hash pra não cair em dashboard quando um modal está aberto.</p>' +
        '<p><b>(2) 17 hints legacy migradas</b> — entradas de Partida Casual usavam um campo <code>area: \'casual\'</code> que o engine de filtragem ignorava (só olha <code>context</code>). Todas migradas pra <code>context: \'casual-match\'</code> via sed — dicas que estavam dormentes há versões voltam a aparecer.</p>' +
        '<p><b>(3) 34 hints novas</b> distribuídas:</p>' +
        '<ul style="font-size:0.82rem;line-height:1.6;margin:4px 0 8px 18px;">' +
          '<li><b>Venues descoberta (8):</b> Local, GPS, Modalidade, Distância, toggle Mapa/Lista, Expandir raio, CTA Cadastrar</li>' +
          '<li><b>Venue detail modal (6):</b> Estou aqui, Planejar, Compartilhar, Criar torneio aqui, Avaliar, Reivindicar</li>' +
          '<li><b>Presença (7):</b> seletor de venue, pills de modalidade, Check-in, Planejar, Adicionar à agenda, Cancelar, Gráfico</li>' +
          '<li><b>Casual Match (2):</b> Avisar amigos do scoreplace, Código de sala</li>' +
          '<li><b>Dashboard widgets (5):</b> Meus locais, Amigos no local, Nudge de perfil, botões Presença/Locais do hero</li>' +
          '<li><b>Tournament detail (1):</b> Adicionar à agenda</li>' +
          '<li><b>My venues (2):</b> Autocomplete Google, Reivindicar proprietário</li>' +
          '<li><b>Notificações (3):</b> Marcar lida, Convite casual, Check-in de amigo</li>' +
        '</ul>' +
        '<p><b>Total agora: 207 dicas</b> (antes 173). Manual (seção "💡 Dicas do App") ganhou blocos dedicados pra cada contexto novo e o contador atualizou pra "mais de 200 dicas". Aparecem quando usuário fica parado 6s em qualquer tela; respeitam <code>requiresLogin</code> e <code>requiresPlan</code> igual as existentes. Arquivos: <code>js/hints.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: "Avisar amigos do scoreplace" com 1 clique.</b> O modal de convite da Partida Casual tinha QR Code, Copiar link e WhatsApp — mas nenhum jeito de notificar direto os amigos do scoreplace sem sair do app. Pra quem já tá no app, era redundante passar pelo WhatsApp. <b>Novo botão:</b> "👥 Avisar meus N amigos do scoreplace" (gradient índigo, abaixo dos botões de share tradicionais). Um clique dispara notificação <code>casual_invite</code> pra todos os amigos. <b>Mensagem:</b> "Fulano começou uma partida casual de X. Entra junto: CÓDIGO". <b>Botão de ação na notificação:</b> "⚡ Entrar na partida" (gradient ciano, identidade visual da partida casual) navega direto pra <code>#casual/&lt;code&gt;</code> — o roteador já sabe lidar com status waiting (abre lobby) ou active (abre live scoring). <b>Throttle:</b> botão desabilita após 1 clique pra evitar spam. <b>Contador adaptativo:</b> mostra o número exato de amigos no botão ("Avisar meus 3 amigos") pra reforçar que o impacto é real. <b>Catálogo:</b> <code>casual_invite</code> adicionado com ⚡ ciano, nível \'all\'. Só aparece quando o usuário tem pelo menos 1 amigo; sem amigos, o espaço fica vazio (widget não força descoberta aqui — tem outros lugares pra isso). Arquivos: <code>js/views/bracket-ui.js</code>, <code>js/notification-catalog.js</code>, <code>js/views/notifications-view.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Welcome card da dashboard: botão "⚡ Partida Casual" adicionado.</b> O welcome card pra usuários novos (v0.15.8) tinha 4 CTAs — Criar torneio, Descobrir locais, Registrar presença, Encontrar amigos — mas faltava Partida Casual, que é o caminho MAIS rápido pra começar a usar o app (sem setup de torneio, sem inscrições, só scanear QR com amigos e jogar). Adicionado como 2º botão (ciano, entre Criar torneio e Descobrir locais) pra reforçar visualmente que são os 2 caminhos principais de "jogar agora". Subtítulo do welcome atualizado pra mencionar as 5 áreas (torneios + partida casual + presença + locais + amigos). Max-width do container de botões aumentado de 540px → 600px pra acomodar o botão extra sem wrapping apertado. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Perfil: esportes preferidos como botões + busca de locais mais dinâmica.</b> Dois ajustes baseados em uso real. <b>(1) Esportes preferidos → pills toggleáveis</b> no lugar do input de texto livre. Antes o usuário precisava digitar os nomes exatamente como o app esperava ("Tênis" com acento, "Beach Tennis" com ambas as palavras); inconsistências quebravam filtros e sugestões silenciosamente. Agora 5 botões canônicos (Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel) — um clique seleciona/desseleciona, âmbar quando ativo. Fonte de verdade: <code>window._profileSelectedSports</code> (array). Dados gravados em <code>cu.preferredSports</code> como <code>array</code> (forma moderna e canônica); hidden input <code>#profile-edit-sports</code> sincroniza CSV equivalente pra readers legacy. <b>Readers blindados</b> pra aceitar array OU string: bracket-ui.js (<code>userSport</code> picker), explore.js (subtitle chip), tournaments-organizer.js (nearby sport match). presence.js e presence-geo.js já usavam <code>String(pref).split()</code> que tolera array. <b>(2) Busca de locais mais responsiva</b>: mínimo de caracteres caiu de 3 → 2, debounce de 300ms → 150ms. Agora digitar "Pi" já começa a mostrar bairros/POIs relevantes em tempo quase real. Focus/blur handlers ajustados pro novo threshold. Arquivos: <code>js/views/auth.js</code>, <code>js/views/bracket-ui.js</code>, <code>js/views/explore.js</code>, <code>js/views/tournaments-organizer.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: novo widget "🏟️ Meus locais" — check-in em 1 tap.</b> Encurta o caminho mais usado da presença: "cheguei no clube, quero marcar que tô aqui". Antes o fluxo era: dashboard → clicar hero "📍 Presença" → abrir dropdown de local → escolher venue → clicar "Estou aqui agora". 5 cliques. Agora: dashboard → botão 📍 verde no card do venue → já cai com local prefilado. 2 cliques. <b>Conteúdo do widget:</b> até 5 locais que o usuário tem relação — <code>preferredLocations</code> do perfil (prioridade máxima, explicitamente marcados) + venues dos torneios visíveis (organizador + participante). Dedup por <code>venueKey</code>. Grid responsivo <code>auto-fill minmax(200px)</code>. <b>Dois alvos clicáveis por card:</b> (1) nome do venue → abre modal do venue (<code>#venues/&lt;placeId&gt;</code>) com Movimento e contatos; (2) botão verde 📍 → prefilla <code>_presencePrefill</code> em sessionStorage e navega pra <code>#presence</code> com o local já setado no dropdown. Link "Descobrir outros →" no header leva pra <code>#venues</code> pra expandir. <b>Condicional:</b> só aparece se o usuário tem pelo menos 1 venue — fresh users veem o welcome card em vez. Posicionado abaixo do widget "Amigos no local" pra formar uma narrativa: "veja quem tá jogando onde → vá pro seu local também". Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença planejada → "Adicionar à agenda" (mesmo picker de torneios).</b> Complementa o fix de v0.15.16: agora "Planejar ida" também ganha botão 📅 ao lado de "Cancelar" na lista de presenças ativas (<code>#presence-myactive</code>). Abre o mesmo picker com Google Calendar, Outlook.com, .ics — reusa os helpers <code>_googleCalendarUrl</code>, <code>_outlookCalendarUrl</code>, <code>_icsDownload</code> e <code>window._calPick</code> de tournaments-sharing.js (zero duplicação). <b>Payload específico</b> de presença: título <code>📍 [Local] · [Modalidade]</code>, janela <code>startsAt → endsAt</code> (ou <code>startsAt + 2h</code> se openEnded, evita evento de 12h que é o cap default do presence-db), local = nome do venue, descrição com modalidade/local + link <code>/#venues/&lt;placeId&gt;</code>. <b>Só aparece em <code>type === \'planned\'</code></b> — check-in ativo não faz sentido botar na agenda (evento já está rolando). Loop completo: Planejar ida → Notifica amigos → Adiciona à tua agenda → Você + amigos chegam no local. Manual atualizado na seção Presença. Arquivos: <code>js/views/presence.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Adicionar torneio à agenda (Google Calendar / Outlook / .ics).</b> Qualquer usuário pode agora adicionar o torneio à sua agenda em 2 cliques — evita o esquecimento clássico ("eu me inscrevi mas não lembrei do horário"). Botão "📅 Adicionar à agenda" aparece no header da página do torneio (abaixo do nome, ao lado de Convidar/Compartilhar) quando <code>startDate</code> está definido. Abre picker com 3 opções: <b>🟦 Google Calendar</b> (abre em nova aba com evento pré-preenchido via <code>calendar.google.com/calendar/render</code>), <b>🔷 Outlook.com</b> (equivalente Microsoft via <code>outlook.live.com/deeplink/compose</code>), <b>📄 Apple/Outlook (.ics)</b> (gera arquivo .ics conforme RFC 5545 — iOS/macOS abre no Apple Calendar, Outlook desktop e Thunderbird importam). Payload: título <code>🏆 [Nome]</code>, janela <code>startDate → endDate</code> (se ausente, assume +4h), local <code>venue + endereço</code>, descrição com modalidade/formato + link canônico, URL do torneio. Se usuário criou torneio com só data (sem hora), default 09:00 local pra evitar evento cruzando dias por fuso horário. Disponível pra organizador e participante igual — o flow é genérico. Arquivos: <code>js/views/tournaments-sharing.js</code>, <code>js/views/tournaments.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: banner "Complete seu perfil" smart + dismissível.</b> Muitas features do app dependem de campos que o usuário pode ter pulado na primeira vez (cidade, modalidades preferidas, locais preferidos) — torneios perto da sua região, sugestões de parceiros, presença rápida nos locais que você frequenta. Sem esses dados, várias experiências ficam degradadas silenciosamente. Adicionado <code>_buildProfileNudgeHtml()</code> em dashboard.js que detecta quais campos estão faltando e renderiza um banner âmbar dismissível acima da lista de torneios. <b>Regras de exibição:</b> (1) só aparece se o usuário JÁ tem torneios (<code>allUnique.length &gt; 0</code>) — fresh user já tem o welcome card com CTAs, não empilha dois banners; (2) só aparece se pelo menos um dos três campos (<code>city</code>, <code>preferredSports[]</code>, <code>preferredLocations[]</code>) está vazio; (3) dismiss persiste em <code>sessionStorage</code> (não em localStorage, proposital) — na sessão atual some, mas no próximo login reaparece se os campos continuam vazios, reforçando o nudge sem virar spam. <b>Mensagem adaptativa:</b> texto enumera especificamente o que falta e os benefícios correspondentes ("notificar torneios na sua região", "sugerir parceiros da sua modalidade", "ativar presença rápida"). CTA "Completar perfil" abre a modal do perfil direto. Arquivos: <code>js/views/dashboard.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venue detail: botão "📤 Compartilhar" (viral growth).</b> Até agora a modal do venue tinha 5 ações (Estou aqui, Planejar ida, Ver no mapa, Ver presenças, Criar torneio aqui) mas nenhuma pra compartilhar o local em si. Adicionado botão verde "📤 Compartilhar" (cor do WhatsApp, cobrindo o use case principal). <b>Fluxo:</b> tenta <code>navigator.share</code> (Web Share API — mobile nativo abre dialog com WhatsApp, Instagram DM, SMS, Telegram, etc; Chrome/Edge desktop também). Se o browser não suporta ou o usuário cancela com erro não-Abort, cai no fallback <code>navigator.clipboard.writeText</code> + toast "Link copiado! Cole no WhatsApp ou em qualquer rede." Último fallback: <code>mailto:</code>. <b>Payload compartilhado:</b> título "🏢 [Nome do local]", até 3 modalidades, endereço, e link canônico <code>/#venues/&lt;placeId&gt;</code> — destinatário abre direto na modal do venue com movimento, torneios e contatos. Viral growth: quem receber sem conta pode explorar, quem tem conta cai na modal completa. Manual atualizado na seção Descobrir Locais. Arquivos: <code>js/views/venues.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in imediato notifica amigos (📡) + botão "Vou também".</b> Até v0.15.12 só "Planejar ida" avisava amigos; "Estou aqui agora" ficava silencioso — amigos descobriam só via refresh do widget do dashboard. Mas check-in imediato é o convite MAIS forte ("cheguei, vem jogar agora") — faz sentido notificar. <b>Implementação:</b> <code>_notifyFriendsOfCheckin</code> em <code>presence.js</code> e <code>_notifyFriendsOfQuickCheckin</code> em <code>venues.js</code> (para os dois entry points) percorrem <code>cu.friends[]</code> e disparam notificação tipo <code>presence_checkin</code>. Mensagem: "Fulano chegou em [local] pra jogar [modalidade]. Vem junto!". <b>Throttle anti-spam:</b> chave <code>localStorage</code> <code>scoreplace_checkin_notified_&lt;placeId&gt;_&lt;sport&gt;_&lt;dayKey&gt;</code> impede que múltiplos check-ins do mesmo usuário no mesmo local/modalidade/dia acionem notificação repetida (ex: cancelar e refazer). <b>Catálogo:</b> <code>presence_checkin</code> adicionado com 📡 vermelho pra distinguir visualmente do 🗓️ verde do <code>presence_plan</code>. <b>Botão de ação</b> na view de notificações: "📡 Vou também" (pra checkin) vs "🏢 Ver local" (pra plan). Ambos levam pra modal do venue onde o amigo pode marcar sua própria presença. Respeita <code>presenceVisibility: \'off\'</code> — usuários com presença desligada não disparam nem recebem. Manual atualizado nas seções Presença e Notificações. Arquivos: <code>js/views/presence.js</code>, <code>js/views/venues.js</code>, <code>js/notification-catalog.js</code>, <code>js/views/notifications-view.js</code>, <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Manual atualizado: 4 pilares + Presença + Locais + Busca rápida expandida.</b> O manual estava desatualizado em relação às features entregues nos pilares A (Presença) e B (Locais/Venues) + polimento recente. Esta versão sincroniza tudo. <b>(1) Nova seção "📍 Presença no Local"</b> — cobre os dois tipos de presença (check-in imediato / planejar ida), multi-modalidade, visibilidade (amigos/público/desligado + silenciar temporário), e a integração com torneios e modais de venue. <b>(2) Nova seção "🏢 Descobrir Locais"</b> — filtros (Local/GPS/Modalidade/Mín. quadras/Distância/Faixa de preço), map vs lista, hierarquia de pins (âmbar=cadastrado free, índigo=Pro, cinza=Google, verde=você), barra de resumo com CTA "Expandir raio", modal de detalhe com todos os CTAs inline (Estou aqui, Planejar ida, Criar torneio aqui), e explicação do cadastro colaborativo + reivindicação de propriedade + plano Pro. <b>(3) Primeiros passos agora abre com os 4 pilares</b> — Torneios, Partida Casual, Presença, Locais — dando ao usuário novo a visão completa das áreas do app em vez de cair direto no login. <b>(4) Dashboard expandida</b> — documenta o welcome card pra usuário novo, a semântica da contagem "Inscrições Abertas" (união de seus + discovery), o widget "Amigos no local" e seus empty states, e o botão de inscrição inline com hidratação defensiva do discovery feed. <b>(5) Atalhos de teclado</b> ganhou bloco explicando o que a busca rápida (Ctrl+K) encontra agora — torneios scoped + discovery, locais, jogadores de todos os torneios visíveis. <b>(6) Notificações</b> menciona <code>presence_plan</code> (amigo planejando ida) com CTA "🏢 Ver local". Arquivos: <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações: tipos faltando + ação "Ver local" em convites de presença.</b> Auditoria dos tipos de notificação revelou três gaps entre o catálogo e os eventos realmente disparados em produção. <b>(1) <code>tournament_updated</code></b> (disparado por <code>create-tournament.js</code> quando organizador salva edições) não existia no catálogo — só <code>tournament_update</code>. Notificação caía no fallback genérico 🔔 em vez do ícone 📢 âmbar. Adicionada entrada alias no catálogo mantendo ambas as chaves (não quebra notificações já persistidas em Firestore). <b>(2) <code>organizer_communication</code></b> (disparado por <code>tournaments-organizer.js</code> quando organizador manda comunicado aos inscritos) vs <code>org_communication</code> no catálogo — mesmo problema, mesmo fix de alias. Agora comunicados organizacionais aparecem com 📣 em vez do bell genérico. <b>(3) <code>presence_plan</code></b> (v0.14.70 — amigo planejando ida a um local) não estava no catálogo: adicionado com 🗓️ verde, nível \'all\'. Além disso, <b>notificações de presence_plan ganharam botão "🏢 Ver local"</b> na view de notificações — antes elas ficavam sem ação, enquanto todas as outras (tournament_*, friend_request, etc) tinham CTA. Clicar navega pra <code>#venues/&lt;placeId&gt;</code> com a modal aberta, onde o usuário pode dar "Estou aqui agora" ou "Planejar ida" pra se juntar ao amigo. Fecha o loop Presence→Notification→Ação. Arquivos: <code>js/notification-catalog.js</code>, <code>js/views/notifications-view.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Busca rápida (Ctrl+K) cobre torneios públicos + locais.</b> A busca rápida (Ctrl+K / ⌘K no desktop, botão 🔍 no header) antes só olhava em <code>AppStore.tournaments</code> (o scoped list) — torneios públicos que o usuário ainda não tinha entrado (só no <code>publicDiscovery</code>) não apareciam no resultado, mesmo com match exato de nome. Espelhava o mesmo gap do botão "Inscrever" consertado em v0.15.9. <b>Fix:</b> busca rápida agora une <code>AppStore.tournaments</code> + <code>AppStore.publicDiscovery</code> (dedup por id) antes de filtrar. Nova seção "Locais" no dropdown: pesquisa nos venues únicos extraídos dos torneios (via <code>venuePlaceId</code>) por nome e endereço, com deep-link pra <code>#venues/&lt;placeId&gt;</code>. Seção de "Jogadores" também passa a considerar o scope expandido, então nomes de participantes de torneios públicos não vinculados aparecem na busca. O que muda pro usuário: digite "beach" e a busca rápida agora mostra seus torneios de beach tennis + torneios públicos de beach tennis na região + clubes que o app conhece — 3 categorias num só dropdown. Arquivos: <code>js/main.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: botão "Inscrever" em card de descoberta falhando silenciosamente.</b> Bug descoberto durante auditoria pós-v0.15.6 (quando a dashboard passou a mostrar torneios do discovery feed na "Inscrições Abertas"): clicar em "✅ Inscrever-se" num card de torneio público que o usuário ainda não entrou não fazia nada — sem toast de erro, sem navegação, só o spinner do botão ativo. Causa: torneios do discovery feed vivem em <code>AppStore.publicDiscovery</code>, não em <code>AppStore.tournaments</code> (que é o scoped listener apenas dos torneios onde o usuário já é membro). As funções <code>_dashEnroll</code>, <code>enrollCurrentUser</code> e <code>_doEnrollCurrentUser</code> usavam <code>AppStore.tournaments.find(...)</code> — encontravam <code>undefined</code> e caíam em branches early-return que não faziam nada. <b>Fix:</b> hidratação defensiva em todas as três funções — se não achar em <code>tournaments</code>, tenta em <code>publicDiscovery</code> e injeta o doc em <code>tournaments</code> antes de prosseguir. Pattern aplicado um jeito só pra todas as portas de entrada (dashboard card, click direto via outras views). Inscrição em torneio de descoberta agora funciona com 1 clique: push otimístico do participant, notificação "Inscrição confirmada", navigate pra página de detalhes. Arquivos: <code>js/views/dashboard.js</code>, <code>js/views/tournaments-enrollment.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: welcome rico para usuário novo.</b> Antes, usuário sem nenhum torneio caía num empty state mudo "Nenhum torneio encontrado." — sem direção, sem CTA, sem sinal de que a plataforma tem mais funcionalidades além de torneios. Agora a dashboard detecta o cenário "usuário novo" (<code>allUnique.length === 0 && !curSport && !curLocation && !curFormat</code> e filtro "todos" ou default) e renderiza um card welcome no lugar: título "Seja bem-vindo ao scoreplace!" + subtítulo explicando os quatro pilares (torneios, presença, locais, amigos) + 4 CTAs coloridos um ao lado do outro: "🏆 Criar torneio" (verde, abre modal de criação rápida), "🏢 Descobrir locais" (ciano, leva pra #venues), "📍 Registrar presença" (âmbar, leva pra #presence), "👥 Encontrar amigos" (índigo, leva pra #explore). Dica no rodapé: "se já existe um torneio público na sua cidade, ele vai aparecer aqui automaticamente" — explica por que o feed pode ficar vazio inicialmente (se ninguém na cidade ainda criou). <b>Regra:</b> só mostra no cenário "fresh user" total; com filtros aplicados ou em outras abas (Organizados, Encerrados etc) mantém o empty state neutro "Nenhum torneio encontrado" porque o usuário sabe por que está vazio. Arquivos: <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: widget "Amigos no local" com empty states + cleanup do segundo call site de status.</b> Duas melhorias de consistência pós-v0.15.6. <b>(1) Empty states no widget de amigos:</b> antes, quando o usuário não tinha amigos adicionados, o widget "dashboard-presences-widget" ficava completamente vazio — sem sinal de que a feature existia. Mesmo problema quando tinha amigos mas nenhum deles tinha presença ativa hoje (dias calmos, fim de semana de Páscoa, etc.). Agora dois empty states explícitos: (a) <b>zero amigos</b> → card gradiente índigo com "👥 Veja seus amigos jogando. Adicione amigos na Explorar..." + CTA "Encontrar amigos →" que leva pra #explore; (b) <b>zero presenças</b> → linha discreta "Nenhum amigo registrou presença hoje" com link "Minha presença →" pra abrir a view de presença própria. Promove descoberta da feature sem forçar: usuário sem amigos vê um prompt ativo; usuário ocioso num dia calmo vê um acknowledgment discreto de que o widget tá funcionando. <b>(2) Segundo call site de <code>status==open</code>:</b> <code>loadOpenTournaments</code> em <code>firebase-db.js</code> (usado pelo notifier de torneios próximos/modalidade) tinha o mesmo bug de status estrito que a v0.15.5 consertou em <code>loadPublicOpenTournaments</code> — excluía docs legacy sem o campo. Mesmo fix aplicado: query sem filtro server-side de status + filtro client-side que aceita ausente OU \'open\'. Notificações "novo torneio perto de você" voltam a pegar torneios legacy. Arquivos: <code>js/views/dashboard.js</code>, <code>js/firebase-db.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix #2: "Inscrições Abertas" ainda dando zero depois do v0.15.5.</b> A v0.15.5 tinha fixado o default de <code>status: \'open\'</code> e a query pública, mas a contagem continuava errada porque o filtro <code>abertosParaVoce</code> no dashboard só consumia <code>publicDiscovery</code> — e <code>publicDiscovery</code> EXCLUI torneios onde o usuário já é membro (creator, organizer, co-host ou participante). Quando o usuário criava um torneio próprio público, ele aparecia em "Organizados" mas sumia de "Inscrições Abertas" (count zerado, como reportado). Label "Inscrições Abertas" não dá a entender "só os que você não entrou" — a semântica esperada é "total de torneios aceitando inscrição". <b>Fix:</b> <code>abertosParaVoce</code> agora é a UNIÃO de (a) torneios próprios do usuário (<code>visible</code>) com enrollment aberto + (b) torneios do discovery feed com enrollment aberto que ainda não entrou. Dedup por id. Helper extraído <code>_isOpenEnrollment(t)</code> consolida a lógica antes espalhada: checa hasDraw, ligaAberta, status closed/finished, deadline expirada. Resultado: se o usuário criou um torneio público com inscrições abertas, agora aparece no count imediatamente, como esperado. Arquivos: <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: "Inscrições Abertas" na dashboard dando zero.</b> Bug reportado: torneio criado público com inscrição aberta não aparecia no count "Abertos para você". Causa raiz: <code>create-tournament.js</code> monta o <code>tourData</code> sem setar o campo <code>status</code> — ficava undefined. Já <code>loadPublicOpenTournaments</code> em <code>firebase-db.js</code> filtrava server-side por <code>where(\'status\', \'==\', \'open\')</code>, que é estrito e exclui docs com o campo ausente. Resultado: todos os torneios criados pelo fluxo normal (Detalhes Avançados) ficavam invisíveis no feed público, inclusive pros próprios organizadores quando viam outros torneios. <b>Fix em duas camadas:</b> (1) <code>AppStore.addTournament</code> agora seta <code>status: \'open\'</code> como default no <code>Object.assign</code> — torneios novos sempre nascem com status explícito; (2) <code>loadPublicOpenTournaments</code> removeu o filtro server-side de status e passou a filtrar client-side, aceitando status ausente OU \'open\'. O custo é ler até 3× mais docs por página (pra compensar descartes de closed/finished/active na memória) mas garante que o alpha funcione pra todos os torneios legacy que ficaram em Firestore sem o campo. Quando a base crescer, um backfill de <code>status: \'open\'</code> em docs antigos permite reverter pra query estrita. Arquivos: <code>js/store.js</code>, <code>js/firebase-db.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venue detail modal: "📍 Estou aqui" inline + "🗓️ Planejar ida" com picker automático.</b> Fecha o loop descoberta→ação sem forçar navegação pra #presence. <b>(1) Check-in inline:</b> modal de detalhe do venue agora tem dois botões dedicados no topo da seção de ações (só pra usuários logados): "📍 Estou aqui agora" e "🗓️ Planejar ida". O primeiro escreve direto na collection <code>presences</code> via <code>PresenceDB.savePresence</code>, com toast de confirmação e re-hidrata o bloco "Movimento no local" pra mostrar a nova presença sem fechar a modal. Botão vira "✅ Presença registrada" após sucesso (disabled, opacity 0.65) pra evitar double-tap duplicata. <b>(2) Sport picker inteligente:</b> nova <code>_pickSportForVenue(v)</code> decide qual modalidade registrar sem friction: se venue tem 1 sport declarado → usa direto; se tem múltiplos, intersecta com <code>preferredSports</code> do usuário pra ver se desempata; se não resolve → abre overlay "Qual modalidade agora?" com os sports do venue. Se venue e perfil ambos vazios, cai pra navegação pra #presence com o picker completo. <b>(3) Planejar ida auto-abre dialog:</b> botão seta flag <code>_openPlanDialog: true</code> no prefill e <code>renderPresence</code> dispara <code>_presencePlanDialog()</code> após 450ms da renderização — o usuário cai direto no modal de agendamento com venue já selecionado. Impacto: pra reivindicar presença num venue descoberto na busca, antes era 3 navegações (modal → #presence → check-in), agora é 1 tap direto da modal. Arquivos: <code>js/views/venues.js</code>, <code>js/views/presence.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir Locais: summary acima do mapa + "expandir raio" em 1 clique.</b> Duas melhorias de orientação no map mode. <b>(1) Barra de resumo acima do mapa:</b> novo slot <code>#venues-map-summary</code> renderiza "🏢 N cadastrados · 🔎 M sugestões Google" em tempo real conforme filtros mudam. Antes o usuário tinha que scrollar até a seção "📍 Locais no Google" lá embaixo pra saber se havia algum resultado; agora a contagem é a primeira coisa que aparece abaixo dos filtros, reforçando feedback visual imediato. Durante loading exibe "Buscando locais…". <b>(2) Botão "Expandir para Xkm":</b> quando a combinação filtros + raio retorna ZERO resultados (nem cadastrados, nem Google suggestions) e o usuário tem um <code>center</code> válido, a barra mostra um CTA que multiplica o raio atual por 3× (ex: 10km → 30km, 50km → 150km, capado em 500km). Um clique re-renderiza a view com o raio expandido — sai do beco sem saída sem exigir que o usuário descubra o input de distância e edite manualmente. Se o raio já está no cap, mostra texto "Tente remover filtros". Implementação via nova função <code>window._venuesExpandRadius(nextKm)</code> que atualiza <code>state.distanceKm</code>, persiste nos filtros salvos (localStorage), e re-renderiza via <code>render(document.getElementById(\'view-container\'))</code>. Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir Locais: sugestões do Google aparecem como pins no mapa.</b> No mode "Mapa", antes só apareciam pins dos locais cadastrados na scoreplace — em áreas onde ninguém ainda reivindicou um venue, o mapa ficava vazio (só com o pin verde do usuário), mesmo havendo dezenas de sugestões do Google na lista abaixo. Agora os Google suggestions também viram pins, visualmente diferenciados: <b>cinza translúcido com ícone 🔎 e escala 0.9</b> (vs cor cheia âmbar/índigo com 🏢 dos cadastrados). Clique num pin cinza abre o local direto no Google Maps (nova aba) — o venue ainda não existe na base scoreplace, então não há modal interna pra mostrar. Deduplicação por <code>placeId</code>: se o mesmo lugar já é cadastrado, não plota pin duplicado sobre o pin colorido. <b>Heurística de zoom</b> ajustada também: com <code>state.center</code> resolvido (GPS ou geocoder de cidade), o mapa respeita o centro do usuário e ajusta zoom baseado em <code>distanceKm</code> (~13 pra raio 10km, 12 pra 15km, 11 pra 40km) em vez de dar <code>fitBounds</code> nos pins — que fazia zoom-out excessivo quando uma sugestão do Google estava longe do centro de busca. Usuário continua podendo dar pinch-zoom manualmente; a heurística só intervém quando o zoom atual está visivelmente fora do apropriado. Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir Locais: mapa com bordas laterais + busca Google mais ampla.</b> Dois fixes de UX baseados em uso real. <b>(1) Mapa "preso" no mobile.</b> O mapa do Google ocupava toda a largura da viewport com <code>gestureHandling: \'greedy\'</code> — ao tentar scrollar verticalmente a página, o dedo caía dentro do mapa e ele capturava o gesto, deixando o usuário preso sem conseguir sair a não ser fechando o app. Dois ajustes: (a) <code>#venues-map-wrap</code> ganha <code>padding: 0 10px</code> criando um gutter lateral de 10px de cada lado — zona segura de toque para scrollar a página; (b) <code>gestureHandling</code> trocado para <code>\'cooperative\'</code>, que no mobile exige pan com dois dedos (scroll vertical com um dedo passa para a página), no desktop exige Ctrl+scroll para dar zoom. Altura do mapa reduzida de 460px para 380px — ainda generosa, mas deixa o conteúdo abaixo visível sem precisar scrollar longe. <b>(2) Zero resultados do Google.</b> A busca de sugestões externas (<code>_loadGoogleNearby</code>) usava <code>locationRestriction</code> (filtro duro — só places estritamente dentro do raio) combinado com query fraca "quadra [cidade]". Em bairros com poucos venues cadastrados no Google como "quadra", retornava vazio. Duas mudanças: (a) query default quando modalidade = qualquer é agora <code>"quadra esportiva clube arena tênis padel beach tennis pickleball"</code> — cobre os termos que Google indexa para os múltiplos formatos (alguns clubes aparecem como "arena", outros como "clube esportivo"); (b) <code>locationRestriction</code> trocado por <code>locationBias</code> — o raio vira viés, não trava, então lugares muito relevantes logo fora do raio também aparecem. O filtro haversine client-side via <code>state.distanceKm</code> já cobre o raio exato para venues próprios. Resultado: área densa volta a ter sugestões sem o usuário precisar mexer nos filtros primeiro. Iteramos próximos refinamentos conforme novos casos aparecerem. Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.15.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: fix do "Iniciar" + "Fechar" não reabre mais a partida.</b> Dois bugs reportados na partida casual — o botão "Iniciar" não funcionava corretamente e, depois de "Fechar", a partida reabria sozinha — eram dois sintomas da mesma causa: o listener de perfil real-time (<code>startProfileListener</code> em <code>store.js</code>) usava o campo <code>activeCasualRoom</code> para sincronizar partidas entre dispositivos, mas as entregas de snapshot do Firestore são assíncronas e podem chegar fora de ordem. <b>(1) Primeiro snapshot:</b> quando o listener anexa (pós-login), ele faz "resume" navegando para <code>#casual/&lt;room&gt;</code> se houver uma partida ativa no perfil — útil para recuperar de crash. Mas a checagem só olhava o hash, não o DOM: se o usuário acabou de abrir o setup de uma nova partida (overlay <code>casual-match-overlay</code> visível, hash ainda em <code>#dashboard</code>), o primeiro snapshot entendia "estou fora, precisa ir pra lá" e roubava a navegação no meio do fluxo. Fix: a checagem agora também considera a presença de <code>casual-match-overlay</code> ou <code>live-scoring-overlay</code> no DOM — se qualquer um existe, o usuário já está engajado e não deve ser redirecionado. <b>(2) Snapshots stale pós-close:</b> quando o usuário clica Fechar, três writes atômicos acontecem (<code>cancelCasualMatch</code>, <code>saveUserProfile({activeCasualRoom: null})</code>, overlay removido). O Firestore pode entregar um snapshot optimístico com o valor antigo <i>depois</i> do null, e a comparação <code>newRoom !== lastCasualRoom</code> disparava navegação de volta pro match. Fix: flag <code>window._suppressCasualResumeUntil</code> com validade de 6s setada em todos os caminhos de close (<code>_casualSetupClose</code> no setup, MutationObserver quando ambos overlays saem, e <code>_closeLiveScoring</code> no live scoring). A branch de sync no listener checa essa flag antes de navegar — snapshots stale chegando na janela de 6s são descartados. <b>(3) Clear explícito no close do live-scoring:</b> antes, o clear de <code>activeCasualRoom</code> dependia do MutationObserver criado no setup. Se o usuário entrou direto na partida via URL <code>#casual/&lt;code&gt;</code> (sem passar pelo setup), não havia observer — o perfil ficava com a sala apontando mesmo depois do Fechar, e o próximo reload "ressuscitava" a partida. <code>_closeLiveScoring</code> agora limpa explicitamente <code>activeCasualRoom: null</code> + ativa a flag de supressão antes de navegar pro dashboard. Cobre ambos os caminhos (setup→live e join direto). Arquivos: <code>js/store.js</code>, <code>js/views/bracket-ui.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.81-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir Locais: filtro de distância + mapa centrado + sugestões do Google + CTA cadastrar.</b> Cinco melhorias no fluxo de descoberta para fechar o loop "quero jogar perto de onde estou / onde vou". <b>(1) Mín. quadras agora = 1 por padrão.</b> Zero ficava sem sentido — ninguém busca "locais com zero quadras". Input agora tem <code>min="1"</code> e default 1. <b>(2) Novo campo "Distância (km)".</b> Terceira coluna no grid de filtros, default 10km, range 1–500. Permite expandir (viagem para cidade grande) ou apertar (bairro específico). <b>(3) Mapa centraliza na cidade informada.</b> Novo <code>_geocodeCity(city)</code> usa <code>google.maps.Geocoder</code> (lazy via <code>importLibrary(\'geocoding\')</code>) para transformar "São Paulo" em <code>{lat, lng}</code>. Cache em <code>_geocodeCache</code> para evitar re-lookups. Fallback para <code>preferredLocations[0]</code> do perfil; último fallback é centro do Brasil. Mapa faz <code>setCenter(center)</code> + zoom 12 quando geocode resolve. <b>(4) Filtro Haversine.</b> Novo <code>_haversineKm(a, b)</code> em km — filtra venues client-side pela distância ao centro. Venues sem coordenadas passam (aparecem na lista mas não participam do raio). <b>(5) Seção "📍 Locais no Google".</b> Abaixo do mapa e da lista, renderiza até 15 sugestões externas via <code>google.maps.places.Place.searchByText</code> (API nova) com <code>textQuery = "[sport] [cidade]"</code> + <code>locationBias</code> circular centrado no raio escolhido. Cards mostram nome + endereço e abrem no Google Maps via <code>query_place_id</code>. Legenda explica os filtros aplicados ("min. 2 quadras · São Paulo · raio 10km · $$"). Disparado só com center resolvido para não queimar quota de Places. Empty state da lista também mostra essas sugestões para não ser beco sem saída. <b>(6) CTA "🏢 É dono de um local?"</b> Bloco destacado (gradiente azul-índigo) com botão "Cadastrar meu local" que abre o modal de perfil direto na seção de proprietário. Endereça a dúvida "onde cadastro meu local?" — aparece tanto na lista quanto no mapa. Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.80-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fixes: cancelar presença + Cancelar persistente + ordem dos botões hero + hint Apoie.</b> Set <code>_cancelledIds</code> guarda ids cancelados por 60s para o listener ignorar docs que ainda chegam não-cancelados do servidor. <code>state.myActive</code> agora sincroniza do listener (filtro por <code>uid === myUid</code> + <code>endsAt &gt; agora</code>) + filtro de <code>renderMyActive</code> usa <code>sports[]</code>. Plans com <code>openEnded</code> mostram "a partir de HH:mm". Ordem dos botões do hero: Convidar, Pro, Apoie, Locais, Presença. Hint Apoie: "Gostando do scoreplace?"</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.79-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença real-time + busy state em todos os escritores + ícones em Agora.</b> Três ajustes baseados em feedback. <b>(1) Real-time em <code>#presence</code>.</b> Usuário reportou: "Nelson registrou presença agora, mas não apareceu para Rodrigo". A view carregava dados só no mount. Novo listener via <code>onSnapshot</code> na coleção <code>presences</code> escopado por <code>placeId + dayKey</code> (cross-sport — filtra modalidade e visibilidade client-side). Anexado em <code>_attachListener()</code> disparado dentro de <code>refreshData()</code>; teardown em <code>_teardownListener()</code> chamado na entrada da view e quando não há venue/sports selecionados. Handle global <code>window._presenceUnsubscribe</code>. Custo: 1 listener ativo enquanto a view está aberta — reads só nas mudanças reais. <b>(2) Busy state em Salvar perfil + todos os escritores.</b> Feedback: "Kelly teve que clicar várias vezes em salvar". O clique não dava feedback visual e a usuária tentava de novo. Reutilizado o <code>_spinButton(this, label)</code> já existente (usado pelo botão Inscrever-se que o usuário elogiou — troca innerHTML por spinner + disabled, safety timeout 8s pra reverter). Aplicado em: Salvar perfil ("Salvando..."), Check-in presença ("Registrando..."), Confirmar planejamento ("Salvando..."), Reivindicar/Salvar venue ("Salvando..."), Enviar convite de amizade ("Enviando..."), Cancelar convite ("Cancelando..."), Aceitar convite ("Aceitando..."), Rejeitar convite ("Rejeitando..."). Todos os botões dão feedback imediato e bloqueiam cliques duplicados. <b>(3) Ícones de modalidade em "Agora no local".</b> Complementa a mudança de v0.14.78 que já tinha aplicado em Próximas horas. Cada card de amigo presente agora começa com os ícones das modalidades que ele indicou estar disponível para jogar (🎾 🥒 🏓 🏸 ⚽ 🏐 🏀), via mesmo helper <code>_sportsIcons</code>. Avatar ganhou <code>flex-shrink: 0 + min-width: 44px</code> para nunca distorcer. Arquivos: <code>js/views/presence.js</code>, <code>js/views/auth.js</code>, <code>js/views/explore.js</code>, <code>js/views/venue-owner.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.78-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença: multi-modalidade num único doc + hora de saída opcional + ícones nos chips.</b> Três melhorias baseadas em uso real. <b>(1) Multi-modalidade sem perguntar.</b> Antes, clicar em Check-in/Planejar com 2+ modalidades abria um overlay "Qual modalidade agora?" porque cada presença ia pra um doc com um único <code>sport</code>. O usuário corretamente notou: "estou no local disponível pra jogar qualquer das selecionadas — não faz sentido escolher uma agora". Modelo atualizado: campo novo <code>sports: [\'Beach Tennis\', \'Pickleball\', ...]</code> gravado no doc da presença, ao lado do <code>sport</code> escalar (mantido pra compat com docs legados). Um único doc representa toda a sessão multi-modal. Overlay <code>_pickSportThen</code> deletado, <code>_presenceCheckIn</code> e <code>_presencePlanDialog</code> escrevem <code>sports</code> direto. <code>loadForVenueSportDay</code> em <code>presence-db.js</code> agora faz <b>duas queries paralelas</b> em <code>where(\'sports\', \'array-contains\', X)</code> + <code>where(\'sport\', \'==\', X)</code> e deduplica client-side — docs novos são pegos pelo primeiro, antigos pelo segundo, zero miss durante a transição. Índices compostos novos requeridos em (<code>placeId asc, sports array-contains, dayKey asc</code>) — Firestore sugere link automático. <b>(2) Hora de saída opcional no Planejar ida.</b> O modal agora marca o segundo input como "Até (opcional)" com placeholder vazio e texto explicativo "Deixe em branco se não quiser fixar hora de saída". Submit com end vazio seta <code>openEnded: true</code> + <code>endsAt = startsAt + 12h</code> (cap para docs não ficarem indefinidos nas queries de <code>endsAt &gt; now</code>). O display "a partir de HH:mm" virá num PR de renderização — o storage já está pronto. <b>(3) Ícones de modalidade nos chips de Próximas horas.</b> Cada chip agora começa com os ícones das modalidades que a pessoa vai jogar (🎾 🥒 🏓 🏸 ⚽ 🏐 🏀). Map <code>_sportIcon(sport)</code> dedicado em <code>presence.js</code> (espelha o <code>getSportIcon</code> do dashboard pra consistência). Se a presença tem <code>sports: [\'Beach Tennis\', \'Pickleball\']</code>, o chip fica <code>🎾🥒 [avatar] Nome</code>. <code>title</code> no span tem os nomes completos pra acessibilidade. Arquivos: <code>js/presence-db.js</code>, <code>js/views/presence.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.77-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Descobrir Locais: Mapa como default + cidade pré-preenchida do perfil.</b> Dois ajustes de UX baseados em uso real. <b>(1) Mapa como view padrão</b> em <code>#venues</code>: <code>state.mode</code> inicial trocado de <code>\'list\'</code> para <code>\'map\'</code>. Ordem dos botões de toggle invertida — "🗺️ Mapa" agora vem antes de "▦ Lista" para reforçar a hierarquia. <code>render()</code> chama <code>_ensureMap()</code> logo após montar o DOM se o modo inicial for mapa, evitando que o usuário veja um frame escuro por 200ms antes do tile carregar. Mapa faz sentido como padrão: a descoberta é espacial por natureza ("qual clube fica perto da minha casa?" / "o que tem nessa cidade que estou visitando?"), e pins do Google com marker PRO/free já carregam a informação essencial. Lista continua acessível em 1 clique para quem quer scrollar. <b>(2) Cidade pré-preenchida a partir do perfil</b>: <code>render()</code> agora inicializa <code>state.city</code> com <code>AppStore.currentUser.city</code> na primeira entrada (guarda: só preenche se <code>state.city</code> estiver vazio, preservando edições intencionais anteriores). O usuário cai direto nos venues da cidade dele, sem precisar digitar. Mudar para outra cidade (viagem planejada) é só apagar + digitar — o filtro debounce já estava implementado. Combinado com a ordenação Pro-first do PR B5, entregamos: quem abre a descoberta vê "locais Pro da minha cidade no mapa" em 1 segundo, sem interação. Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.76-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença → "Próximas horas": chips com nome + X discreto em amigos.</b> Dois ajustes visuais. <b>(1) Chips em Próximas horas</b> — os avatares apareciam espremidos horizontalmente porque estavam dentro de um flex container sem <code>flex-shrink: 0</code>, e só mostravam foto sem nome. Refatorado para chips individuais <code>[avatar] Nome</code> com fundo âmbar suave, borda âmbar, avatar 28×28 com <code>flex-shrink: 0 + min-width: 28px</code> (círculo perfeito em qualquer largura), nome com ellipsis. Self aparece como "Você" com borda verde. <b>(2) X no card de amigo</b> — o círculo vermelho em volta do X ficava visualmente pesado. Removido o background (<code>rgba(239,68,68,0.15)</code>) e a borda redonda; agora é só o glifo ✕ em cinza (<code>var(--text-muted)</code>) com <code>opacity: 0.5</code> que sobe para <code>1</code> no hover. Arquivos: <code>js/views/presence.js</code>, <code>js/views/explore.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.75-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Explorar: cards de amigos compactos + ✕ para desfazer.</b> Os cards de "Meus Amigos" na Explorar estavam grandes demais, dominando a tela antes mesmo da busca/outros usuários aparecerem. Nova função dedicada <code>_friendCompactCardHtml(u, uid)</code> (o genérico <code>_userCardHtml</code> continua atendendo as outras seções). Layout mais enxuto: avatar 44×44 (antes 52×52), nome com <code>white-space:nowrap</code> + ellipsis em vez de wrap de 2 linhas, subtítulo com <b>cidade</b> (omitida quando igual à cidade do usuário logado — comparação case-insensitive) <b>+ modalidade</b> (primeira de <code>preferredSports</code>, normalizada sem emoji). Idade removida. Botão "Desfazer amizade" (que ocupava toda a largura) substituído por ✕ vermelho sutil (22×22) no canto superior direito do card via <code>position:absolute</code>, com cor <code>#ef4444</code> sobre fundo <code>rgba(239,68,68,0.15)</code>. Clique mantém a mesma lógica (<code>_removeFriend(uid)</code>). Grid também apertado: <code>minmax(110px, 1fr)</code> (antes 140px) + <code>gap: 8px</code> (antes 10px) — cabem ~3 cards por linha no mobile em vez de 2. Arquivos: <code>js/views/explore.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.74-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Explorar: sincronização real-time de amizades (aceitar/rejeitar).</b> Feedback de uso: quando Nelson aceita ou rejeita o convite do Rodrigo, o card "Convite enviado" na Explorar do Rodrigo continuava aparecendo até ele recarregar a página. O <code>rejectFriendRequest</code>/<code>acceptFriendRequest</code> no backend já limpavam <code>friendRequestsSent</code> do remetente corretamente (e <code>friends</code> no caso de aceite) — a rule <code>isFriendArrayDiff</code> introduzida em v0.14.72 permite essa escrita cross-user. O gap era no cliente: <code>startProfileListener</code> só sincronizava <code>theme</code> e <code>activeCasualRoom</code> entre snapshots, ignorando os arrays de amizade. O <code>AppStore.currentUser.friendRequestsSent</code> ficava preso no estado do último login. Listener agora compara os 3 arrays (<code>friends</code>, <code>friendRequestsSent</code>, <code>friendRequestsReceived</code>) a cada emissão — quando qualquer um muda, atualiza a cópia local e, se a view Explorar está aberta, dispara <code>renderExplore</code> para atualizar a UI em tempo real. Dedup de comparação via <code>slice().sort().join(\',\')</code> — O(n log n) por array, trivial mesmo com centenas de amigos. Fluxo agora: Nelson aceita → Rodrigo vê o card migrar de "Convites Pendentes" para "Meus Amigos" em segundos; Nelson rejeita → o card desaparece silenciosamente. Mesma sincronia funciona para qualquer outra ação cross-user (amigo removendo amizade, cancelando convite recebido, etc.). Arquivos: <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.73-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fixes rápidos pós-v0.14.72 + polimento em detalhe de venue.</b> <b>(1) Label do local mostrava nome + endereço concatenados.</b> O geocoder do map picker do perfil grava <code>label</code> no formato "Nome — Av. ..." — a dropdown da view de presença exibia o texto todo, truncando no meio. Novo helper <code>_cleanVenueName(label)</code> corta no primeiro separador <code>\\s[—–-]\\s</code> e retorna só o nome. Aplicado em <code>_venuesFromTournaments</code> e <code>_defaultVenue</code>. <b>(2) Botões "Estou aqui agora" e "Planejar ida" não funcionavam.</b> Bug sutil: <code>_defaultVenue()</code> lia <code>p.placeId</code>/<code>p.name</code> (ambos undefined no formato do map picker), gerava <code>venueKey(\'\',\'\')</code> vazio, e o guard <code>hasVenue = !!state.venue.placeId</code> mantinha o disabled. Corrigido com o mesmo padrão usado em <code>_venuesFromTournaments</code> (v0.14.72): fallback para <code>p.label</code>. Em venues sem placeId, a chave agora vem do slug do nome — o que dá consistência entre a chave do venue e das presenças registradas lá. <b>(3) Empty state útil na dropdown de Local.</b> Quando o usuário não tem nem <code>preferredLocations</code> no perfil, nem torneios com venue gravado, a dropdown mostrava só "— nenhum local conhecido —" + "✏️ Digitar outro local". Agora renderiza também um card 💡 apontando "Adicione seus locais em Perfil → Locais de preferência" com CTA "Abrir perfil" — evita o beco sem saída. <b>(4) Deep-link inválido em <code>#venues/&lt;placeId&gt;</code>.</b> Se o placeId for de um venue deletado ou digitado errado, a view ficava em branco silenciosa. Agora mostra modal "🧭 Local não encontrado" explicando que o link pode estar desatualizado, com botão para fechar. <b>(5) Lista de torneios no detalhe do venue.</b> Modal de detalhe agora tem seção "🏆 Torneios neste local" logo abaixo do Movimento, separada em "Próximos" (até 4) e "Recentes" (últimos 6 meses, até 4). Cada linha clicável navega para <code>#tournaments/&lt;id&gt;</code>. Usa o cache <code>AppStore.tournaments</code> (sem fetch extra). Oculta automaticamente se não há nenhum. Arquivos: <code>js/views/presence.js</code>, <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>3 fixes críticos + multi-modalidade na presença.</b> <b>(1) Convite de amizade não chegava ao destinatário.</b> Regex da análise: quando Nelson convidava Rodrigo, <code>FirestoreDB.sendFriendRequest(fromUid, toUid, fromData)</code> tentava escrever <code>friendRequestsReceived: arrayUnion(fromUid)</code> no doc <code>users/{toUid}</code>. As rules introduzidas em v0.14.60 impunham <code>allow write: if request.auth.uid == userId</code>, então Nelson (uid diferente de Rodrigo) era rejeitado com <code>permission-denied</code>, quebrando o <code>await</code> e impedindo a criação da notificação. Rule relaxada com novo helper <code>isFriendArrayDiff()</code> (<code>request.resource.data.diff(resource.data).affectedKeys().hasOnly([\'friends\', \'friendRequestsSent\', \'friendRequestsReceived\'])</code>) que permite qualquer autenticado atualizar outro user SE o diff tocar <b>apenas</b> esses 3 arrays. Mantém PII bloqueada para escrita cross-user. Também formalizadas sub-regras para <code>matchHistory</code> e <code>templates</code> (só o owner). <b>(2) Salvar perfil redirecionava para partida casual.</b> <code>startProfileListener</code> em <code>store.js</code> observava <code>data.activeCasualRoom</code> e navegava para <code>#casual/&lt;room&gt;</code> em toda emissão, inclusive quando o evento era um save de perfil unrelated (ex: editar locais preferidos). Reescrito para rastrear <code>lastCasualRoom</code> entre snapshots — só navega quando o valor <i>mudou</i>. Snapshot inicial agora semeia a variável em vez de disparar, evitando hijack no login também. <b>(3) Locais preferidos não carregavam em Presença.</b> O map picker do perfil armazena <code>{lat, lng, label}</code> — sem <code>placeId</code> e sem <code>name</code>. O <code>_venuesFromTournaments()</code> lia <code>p.name</code> (undefined) e <code>p.placeId</code> (undefined) → <code>venueKey(\'\',\'\')</code> retornava vazio → filtrado. Corrigido com fallback <code>p.name || p.label</code> e <code>p.lng != null ? p.lng : p.lon</code>. <b>(4) Multi-modalidade na presença.</b> Pedido do usuário: um local oferece várias modalidades (Beach Tennis + Tênis + Pickleball) e faz sentido ver o movimento consolidado. Select de modalidade substituído por pills toggleable (pelo menos 1 sempre selecionada como guard). Novo campo <code>state.sports[]</code> (o primário em <code>state.sport</code> é usado para writes). <code>refreshData</code> faz <code>Promise.all</code> de N chamadas <code>loadForVenueSportDay</code> (uma por modalidade selecionada) e merge deduplicado por <code>_id</code> — queries em paralelo, custo linear em número de sports selecionados. Filtro de torneios do dia usa um Set de sports permitidos. Check-in e Planejar ida, quando há mais de 1 modalidade ativa, abrem um picker overlay "Qual modalidade agora?" com botões — o usuário só joga uma por vez fisicamente, então o doc de presença continua single-sport. Pickler handler <code>_pickSportThen</code> compartilhado entre os dois fluxos. Deploy requer rules atualizadas. Arquivos: <code>firestore.rules</code>, <code>js/store.js</code>, <code>js/views/presence.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.71-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Explorar: empty state útil + destaque em "Convites Pendentes".</b> Feedback de usuário: após enviar convite de amizade, a tela de Explorar parecia "vazia" (campo de busca + "Nenhum usuário encontrado") porque a seção de convites pendentes existia mas sem destaque visual — o usuário passava batido. Duas correções. <b>(1) Empty state com usuários recentes</b>: nova primitiva <code>FirestoreDB.listRecentUsers(days, limit)</code> faz duas queries paralelas <code>.where(\'updatedAt\', \'&gt;=\', cutoff).orderBy(\'updatedAt\', \'desc\').limit(N)</code> e a equivalente em <code>createdAt</code> (perfis novos que nunca re-saved) com <code>cutoff = agora - days*24h</code>, dedup client-side. Quando a caixa de busca está vazia, <code>_performUserSearch</code> chama <code>listRecentUsers(7)</code> em vez de <code>searchUsers(\'\')</code> (que retornava vazio). Filtros de dedup contra amigos/convites existentes aplicados igual à busca normal, via helper extraído <code>_dedupeAgainstRelationships</code>. Na base do resultado, botão "🔎 Ampliar para 30 dias" (depois "90 dias", depois "ano todo") incrementa <code>window._exploreRecentDays</code> e re-dispara a busca — progressive disclosure sem retype. Se mesmo com 365 dias não achar ninguém, mostra "Nenhum usuário nos últimos X dias" em vez do antigo "Nenhum usuário encontrado" estático. <b>(2) Destaque em Convites Pendentes</b>: <code>_renderSentRequests</code> agora envolve a seção em um card âmbar (<code>rgba(245,158,11,0.08)</code> bg + borda <code>rgba(245,158,11,0.3)</code>), título em <code>#f59e0b</code> com ícone ✉️ e subtítulo instrutivo "Aguardando resposta. Clique em ✕ no card para cancelar." — substitui o bloco anterior que usava cor muted e se misturava com o fundo. O rastreio dos convites enviados agora é impossível de passar batido. Arquivos: <code>js/firebase-db.js</code>, <code>js/views/explore.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.70-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Integrações pós-pilares A+B (PR C1): fecha os loops abertos.</b> Três ligações pequenas mas de alto valor conectando funcionalidades já entregues. <b>(1) <code>_venuePrefill</code> → create-tournament</b>: o botão "🏆 Criar torneio aqui" da modal de detalhe do venue (adicionado em B2) agora efetivamente carrega o local escolhido nos campos do torneio. Dois caminhos tratados: (a) "Criar Torneio" (rápido) em <code>main.js</code> lê <code>sessionStorage._venuePrefill</code> no handler <code>btn-quick-create</code> e adiciona <code>venue</code>, <code>venuePlaceId</code>, <code>venueLat</code>, <code>venueLon</code> ao <code>tourData</code> direto; (b) "Detalhes Avançados" popula os inputs <code>#tourn-venue</code>, <code>#tourn-venue-lat</code>, <code>#tourn-venue-lon</code>, <code>#tourn-venue-place-id</code> antes do <code>openModal</code> e dispara <code>_initVenueCreateMap</code> para mostrar o mapa já posicionado. Em ambos os caminhos o prefill é consumido (<code>sessionStorage.removeItem</code>) para não vazar para próximas criações. Fluxo "descobrir → criar" agora é 1 clique e reaproveita o venue reivindicado (sem redigitar busca do Places). <b>(2) Notificação a amigos em planejamento de presença</b>: quando um usuário confirma "Planejar ida" em <code>_presenceConfirmPlan</code>, nova função <code>_notifyFriendsOfPlan(payload)</code> percorre <code>cu.friends[]</code> e chama <code>_sendUserNotification(friendUid, { type: \'presence_plan\', message, level: \'all\' })</code> para cada um. Mensagem no formato "[Nome] vai jogar [Modalidade] em [Local] às HH:mm hoje. Quer ir junto?". Reaproveita toda a infra de notificação existente — respeita <code>notifyLevel</code> de cada amigo via filtro em <code>_sendUserNotification</code> (quem marcou "só importantes"/"só fundamentais" não recebe, porque presença planejada usa <code>level: \'all\'</code>). Dispatch paralelo (<code>forEach</code>) com <code>.catch</code> silencioso por destinatário para que uma falha não bloqueie as demais. Presenças com <code>visibility: \'off\'</code> já eram bloqueadas upstream, então o notify só roda para quem está ativo — sem leakage. <b>(3) Deep-link <code>#venues/&lt;placeId&gt;</code></b>: o router passa o <code>cleanParam</code> para <code>renderVenues(container, deepLinkPlaceId)</code>; quando presente, a view renderiza normalmente e, após 150ms (para o DOM da view estar montado antes do overlay), dispara <code>_venuesOpenDetail(deepLinkPlaceId)</code> abrindo a modal automaticamente. Permite compartilhar um venue específico por URL — <code>scoreplace.app/#venues/ChIJ...</code> — útil para proprietários divulgarem seu local em redes sociais e cair direto na modal com contatos e movimento. Encerra os loops abertos que ficaram desses PRs A+B: agora descoberta, criação de torneios, presença e compartilhamento de locais formam um único grafo de navegação consistente. Arquivos: <code>js/main.js</code>, <code>js/views/presence.js</code>, <code>js/views/venues.js</code>, <code>js/router.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.69-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venues (PR B5): monetização Pro — destaque + analytics + fluxo de interesse.</b> Quinto PR do pilar B, encerrando o roadmap inicial. Três partes: <b>(B5a) Destaque visual + ordenação</b>: <code>VenueDB.listVenues</code> agora ordena resultados com Pro no topo (<code>plan === \'pro\'</code> desc → verified desc → alfabético asc) para que proprietários pagantes ganhem visibilidade natural. Cards de venue Pro na view <code>#venues</code> ganham gradiente azul-suave de fundo, borda índigo (<code>rgba(99,102,241,0.4)</code>) e glow externo (<code>box-shadow 0 0 16px</code>). Markers no mapa já eram diferenciados em B4 (PinElement índigo Pro vs âmbar free). Pro é herdado do plano pessoal do usuário no momento da reivindicação/edição: se o owner é Pro (via <code>_isPro()</code> ou <code>plan === \'pro\'</code> válido no perfil), o venue ganha <code>plan: \'pro\'</code>. Isso reaproveita a monetização pessoal existente (Stripe R$19,90/mês ativado em v0.2.41-alpha) como um ganho imediato para o dono sem cobrar separadamente neste alpha. <b>(B5b) Fluxo de upgrade venue-level</b>: lista "Meus locais" no perfil ganha botão "🚀 Pro" visível apenas para venues Free. Clique abre <code>showConfirmDialog</code> explicando benefícios do Pro venue-específico (R$49/mês, destaque em busca, marker, painel analytics, fotos ilimitadas, filtros prioritários). Confirmar dispara <code>FirestoreDB.queueEmail</code> para <code>scoreplace.app@gmail.com</code> com os dados do proprietário + <code>placeId</code> + versão — fluxo "manifestar interesse" usando a infra de e-mail já existente (extensão <code>firestore-send-email</code>). Ativação manual neste alpha; Cloud Function <code>createVenueCheckoutSession</code> + novo Stripe Price ID serão adicionados quando o primeiro proprietário interessado aparecer (evita construir infra monetária especulativa). <b>(B5c) Painel de analytics do dono</b>: modal de detalhe do venue, quando aberta pelo próprio <code>ownerUid</code>, renderiza bloco "📊 Seu painel (dono)" com 3 métricas — visualizações (<code>viewCount</code> no doc), check-ins nos últimos 7 dias (somatório de 7 chamadas de <code>loadForVenueDay</code>), e total de torneios cadastrados no local (filtrados do cache <code>AppStore.tournaments</code>). Nova primitiva <code>VenueDB.incrementViewCount(key)</code> usa <code>firebase.firestore.FieldValue.increment(1)</code> para bump atômico, disparado fire-and-forget em cada abertura de modal — exceto quando o próprio dono está visualizando (não infla as próprias métricas) e exceto para visitantes anônimos (rules exigem auth). <b>Regras Firestore atualizadas</b>: <code>allow update</code> em <code>venues</code> agora permite (a) owner editar qualquer campo OU (b) qualquer autenticado editar um diff restrito a <code>viewCount</code> + <code>lastViewedAt</code> — via helper <code>isViewCountBump()</code> que usa <code>request.resource.data.diff(resource.data).affectedKeys().hasOnly([...])</code>. Impede que um usuário qualquer edite outros campos passando-se por owner. Badge PRO mais destacado na lista "Meus locais" com gradiente azul-índigo + glow. Contador de visualizações exibido ao lado dos metadados de cada venue do dono. Deploy requer <code>firebase deploy --only firestore:rules</code> para ativar o bump de viewCount. Próximos passos (fora de B5, já fora do roadmap planejado): integração <code>_venuePrefill</code> no <code>create-tournament.js</code>; clusterização de markers quando a base crescer; Cloud Function para denormalizar <code>presencesWeek</code> em cada venue diariamente (reduz os 7 fetches do painel). Pilares A e B agora cobrem check-in/planejamento, descoberta, diretório proprietário, integração bidirecional e monetização — ciclo completo. Arquivos: <code>js/venue-db.js</code>, <code>js/views/venues.js</code>, <code>js/views/venue-owner.js</code>, <code>firestore.rules</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.68-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venues (PR B4): mapa interativo na descoberta <code>#venues</code>.</b> Quarto PR do pilar B. Nova toggle "▦ Lista / 🗺️ Mapa" sobre os resultados da descoberta com troca visual instantânea (sem re-fetch — reaproveita o mesmo <code>state.results</code> já carregado). Modo mapa carrega Google Maps (<code>maps</code> + <code>marker</code> libraries via <code>importLibrary</code>, lazy — só quando o usuário clica pela primeira vez, não no render inicial) num container de 460px de altura com <code>mapId: \'scoreplace-venues-map\'</code> e <code>gestureHandling: \'greedy\'</code> para navegação livre no mobile. Para cada venue com <code>lat/lon</code> no <code>state.results</code>, plota um <code>AdvancedMarkerElement</code> com <code>PinElement</code> customizado: glifo 🏢 em fundo âmbar (<code>#f59e0b</code>) para venues free ou índigo (<code>#6366f1</code>) para venues Pro, borda escura, escala 1.1. Clique no marker reaproveita a mesma função <code>_venuesOpenDetail(placeId)</code> que o card da lista chama — abre a mesma modal com presenças, torneios, contatos e navegações cruzadas. Depois de renderizar markers, <code>fitBounds</code> enquadra todos os pontos com padding de 60px; se só há um ponto, faz <code>setCenter</code> + <code>setZoom(14)</code> em vez do fit (que seria excessivo). Na troca de filtros (cidade/modalidade/preço/quadras), a chamada de <code>listVenues</code> também re-renderiza os markers no mapa se ele estiver aberto — mantém mapa e lista sempre sincronizados. Edge cases tratados: (1) re-render completo do view (disparado por clique em botão de preço) reseta <code>_map = null</code> para forçar re-init contra o novo DOM (o div anterior fica detached); (2) ao alternar para mapa depois de ele já ter sido inicializado oculto, dispara <code>google.maps.event.trigger(_map, \'resize\')</code> para recalcular o layout. Como a renderização de mapa depende de <code>lat/lon</code> gravados no doc do venue pela reivindicação (PR B1 pega do Places API), venues com coordenadas faltando (cenário raro — normalmente só para custom venues sem Places) não aparecem no mapa, mas continuam aparecendo na lista. Próximos passos: integração com <code>create-tournament.js</code> para ler <code>_venuePrefill</code> e pular para venue escolhido, PR B5 (monetização Pro — destaque em busca, analytics, upload de fotos), e adição futura de clustering de markers quando a base crescer (atualmente todos os pins aparecem individualmente; Firebase <code>MarkerClusterer</code> é uma dependência opcional que agregaria para 100+ venues). Arquivos: <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.67-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venues (PR B3): detalhe do local integra presenças + torneios (sinergia A↔B).</b> Fecha o ciclo entre os dois pilares. Modal de detalhe do venue (aberto clicando num card da descoberta <code>#venues</code>) agora carrega uma seção "Movimento no local" renderizada assincronamente após o overlay aparecer para não travar a abertura. Nova primitiva <code>PresenceDB.loadForVenueDay(placeId, dayKey)</code> busca todas as presenças de hoje no venue <i>cruzando todas as modalidades</i> via query composta <code>where(\'placeId\', \'==\', X).where(\'dayKey\', \'==\', Y)</code> — requer índice composto em <code>(placeId asc, dayKey asc)</code>, que o Firestore sugere via link de 1 clique no console na primeira execução. Helper interno <code>_buildMovimentoHtml(venue)</code> em <code>venues.js</code> consolida presenças + torneios do mesmo placeId/dia (filtrados do cache <code>AppStore.tournaments</code> via <code>VenueDB.venueKey</code> — zero custo extra de leitura). Dois blocos renderizados: <b>"Agora no local"</b> com avatares sobrepostos de amigos (borda dourada) + "você" (borda verde) + pill "👥 +N" agregando presenças públicas de desconhecidos e participantes de torneios em andamento; <b>"Mais tarde hoje"</b> lista até 5 eventos ordenados por horário com formato <code>HH:mm</code> e badge clicável "🏆 Torneio X" que leva direto para a página do torneio, ou linha com "Você / [nome do amigo] · [modalidade]" para presenças planejadas. Se venue não tem movimento nem torneio hoje, exibe mensagem "Ninguém registrou presença ou torneio hoje neste local" em vez da seção vazia. Privacidade herda do pilar A: presenças <code>friends</code> só contam como avatar para amigos do usuário, e presenças <code>public</code> entram apenas na contagem agregada (nunca como avatar para desconhecidos). Navegação cruzada "📍 Ver presenças" (já existia em B2) permanece disponível como o caminho completo para a view de presença com gráfico de barras por hora — o detalhe é a versão resumida. Próximos pilares pensados: B4 (mapa interativo na view de descoberta com markers posicionados pelo lat/lon dos venues reivindicados, zoom respondendo aos filtros ativos), B5 (monetização <code>plan: \'pro\'</code> — destaque em busca, analytics para o proprietário, upload de fotos ilimitadas), e integração com <code>create-tournament.js</code> para ler <code>_venuePrefill</code> deixado pelo botão "Criar torneio aqui" e pular direto para o venue escolhido. Arquivos: <code>js/presence-db.js</code>, <code>js/views/venues.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.66-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venues (PR B2): view pública de descoberta <code>#venues</code>.</b> Segundo PR do pilar B. Nova view pública <code>#venues</code> (acessível sem login — é o hook de onboarding para usuários viajando) com filtros em tempo real de <b>cidade</b> (input com debounce de 350ms), <b>modalidade</b> (dropdown das 8 esportes canônicas do app), <b>faixa de preço</b> ($/$$/$$$ via botões exclusivos) e <b>mín. quadras</b> (input numérico com debounce). Nova primitiva <code>VenueDB.listVenues(filters, opts)</code> prioriza a query server-side mais seletiva (sport via <code>array-contains</code>) e aplica os demais filtros client-side sobre a página de até 50 docs retornada — evita exigir índices compostos para todas as combinações de filtros na fase alpha. Resultados em grid responsivo (<code>auto-fill minmax(300px, 1fr)</code>) de cards com: ícone 🏢, nome + badge "✓" se verificado + badge "PRO" se plano Pro, endereço, pills de modalidades (até 4 por card), e meta com contagem de quadras + preço + cidade. Clicar em um card abre <b>modal de detalhe</b> com layout sticky header + body scrollável contendo: pills de modalidades, linha com quadras/preço/cidade, horário, descrição, botões de ação para contato (📞 Ligar com <code>tel:</code>, 💬 WhatsApp com link <code>wa.me</code> auto-prefixando 55, 📷 Instagram abrindo <code>instagram.com/@user</code>, ✉️ E-mail com <code>mailto:</code>) e três navegações cruzadas: "🗺️ Ver no mapa" (Google Maps com <code>query_place_id</code> quando disponível), "📍 Ver presenças" (prefill de sessionStorage + navega para <code>#presence</code> apontando para este local), "🏆 Criar torneio aqui" (prefill <code>_venuePrefill</code> + abre modal de criação rápida). Quando nenhum resultado bate os filtros, empty state convida o usuário a ajustar filtros ou sugerir ao dono que reivindique em Perfil. Nova rota em <code>js/router.js</code>, novo botão "🏢 Locais" ao lado de "📍 Presença" no hero do dashboard, ambos com gradiente distinto para diferenciar visualmente as duas descobertas (presença = laranja, locais = ciano). Rules não mudaram — <code>venues</code> já tinha <code>allow read: if true</code> desde PR B1 justamente para suportar descoberta anônima. Próximos passos: integração com <code>create-tournament.js</code> para ler <code>_venuePrefill</code> e pular direto para o venue escolhido com <code>venuePlaceId</code>/<code>venueLat</code>/<code>venueLon</code> preenchidos; PR B3 (sub-rota <code>#venues/&lt;placeId&gt;</code> com timeline de presenças do dia integrada no detalhe, sinergia direta com pilar A); PR B4 (mapa interativo com markers na view de descoberta, filtros adicionais por raio/geolocation); PR B5 (monetização <code>plan: \'pro\'</code> com destaque em busca e analytics para o proprietário). Arquivos: <code>js/venue-db.js</code>, <code>js/views/venues.js</code> (novo), <code>js/router.js</code>, <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.65-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Venues (PR B1): diretório de locais — reivindicação + CRUD do proprietário.</b> Primeiro PR do pilar B (marketplace de locais esportivos). Nova coleção Firestore <code>venues</code> — docs representam locais físicos onde se joga (clubes, arenas, quadras). Doc id usa a mesma chave que <code>presences</code> (<code>placeId</code> do Google ou <code>custom:&lt;slug&gt;</code>), garantindo que venue e suas presenças/torneios compartilham o mesmo identificador sem denormalização. Schema: <code>name, address, city, lat, lon, sports[], courtCount, courtType, priceRange ($|$$|$$$), hours (texto livre), description, contact { phone, whatsapp, email, instagram }, ownerUid, ownerEmail, verified (admin manual), plan (\'free\'|\'pro\'), createdAt, updatedAt, claimedAt</code>. Novo módulo <code>js/venue-db.js</code> expõe <code>VenueDB</code> com <code>venueKey(placeId, name)</code> (reusa PresenceDB), <code>loadVenue(key)</code>, <code>claimVenue(key, data)</code> em transação (previne double-claim — se outro ownerUid já reivindicou, throw <code>venue-já-reivindicado</code>), <code>updateVenue(key, updates)</code>, <code>releaseVenue(key)</code> (seta <code>ownerUid = null</code> sem apagar o doc — preserva referências de presenças/torneios), e <code>loadMyVenues(uid)</code>. Regras Firestore em <code>venues</code>: <b>read público</b> (descoberta anônima funciona sem login — este será o hook de onboarding para usuários viajando que querem achar onde jogar), <b>create</b> exige <code>request.resource.data.ownerUid == auth.uid</code> (impede forjar claim em nome de outro), <b>update/delete</b> restrito ao <code>ownerUid</code> gravado no doc. Nova UI no modal de perfil: seção "🏢 Sou dono de um local para jogar" com busca do Google Places (<code>AutocompleteSuggestion.fetchAutocompleteSuggestions</code> com <code>includedPrimaryTypes: [\'establishment\']</code> e <code>includedRegionCodes: [\'br\']</code>), reutilizando o mesmo padrão de <code>create-tournament.js</code>. Ao selecionar um resultado, o sistema checa se o venue já existe: se já reivindicado por outro usuário, mostra aviso "escreva para scoreplace.app@gmail.com se você é o dono"; caso contrário, abre formulário inline com campos para modalidades oferecidas (checkboxes para Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel, Futsal, Vôlei, Basquete), número de quadras, faixa de preço, horário, descrição, e 4 canais de contato (telefone, WhatsApp, Instagram, e-mail). Ao confirmar, <code>claimVenue</code> grava o doc com <code>ownerUid</code> do usuário atual. Lista "Meus locais reivindicados" logo abaixo com os venues que o proprietário administra; cada linha com botões Editar (repopula o formulário) e ✕ (libera ownership via <code>releaseVenue</code> após confirmação). Novo módulo encapsulado em <code>js/views/venue-owner.js</code> para não inflar <code>auth.js</code>. Próximos passos no pilar B: B2 — view pública <code>#venues</code> (descoberta por cidade/modalidade com mapa e filtros), B3 — página de detalhe do venue integrando presenças (da feature A) + torneios do local + agenda, B4 — monetização <code>plan: \'pro\'</code> com destaque em busca e analytics. Integração pensada mas não feita ainda: filtro "mín. N quadras" no picker de venue do <code>create-tournament.js</code> e sugestão de swap quando torneio excede capacidade (usuário levantou esse caso de uso). Deploy requer rules atualizadas. Arquivos: <code>js/venue-db.js</code> (novo), <code>js/views/venue-owner.js</code> (novo), <code>js/views/auth.js</code>, <code>firestore.rules</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.64-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença no local (PR A4 + ajuste UI do silenciar): auto check-in via Geolocation.</b> Terceiro PR do pilar de presença. <b>(1) Ajuste de UI no silenciar</b> conforme feedback: em vez de 6 botões exclusivos (Ativas/1d/7d/15d/30d/Sempre), agora um toggle simples "🔕 Silenciar presença temporariamente" + campo numérico de dias (1-365, default 7) que aparece quando o toggle está ativo. A opção "Sempre" foi removida — se quiser silenciar indefinidamente, o usuário digita um número alto. Semântica expandida: enquanto silenciado, o sistema não cria presenças <i>nem</i> carrega presenças de amigos (widget do dashboard e view de presença ficam vazios com banner explicativo) — consistente com "não informar e não receber". Ao recarregar, se <code>presenceMuteUntil &lt; Date.now()</code>, o toggle volta automático para off. Campo novo: <code>presenceMuteDays</code> (lembra a última duração usada para pré-preencher o input na próxima vez). <b>(2) Auto check-in via GPS</b>: novo módulo <code>js/presence-geo.js</code> com função <code>_presenceGeoCheck()</code> chamada 3s após login em <code>simulateLoginSuccess</code>, em paralelo aos outros background loaders. Ao rodar: verifica se o usuário tem <code>preferredLocations[]</code> com <code>lat/lng</code> — senão retorna sem pedir permissão de GPS. Se tem, chama <code>navigator.geolocation.getCurrentPosition</code> (prompt nativo do browser na primeira vez). Com a posição em mãos, calcula distância de Haversine para cada local preferido e pega o mais próximo dentro de <code>MATCH_RADIUS_M = 150</code> metros. Se bater, dois caminhos: <b>(a) Auto check-in ON</b> (novo toggle de perfil <code>presenceAutoCheckin</code>, default OFF): cria presença automaticamente usando a primeira modalidade preferida do perfil (ou primeira modalidade do histórico de torneios), com <code>source: \'geo\'</code> no doc. Toast "📍 Check-in automático" informa o usuário. <b>(b) Auto check-in OFF</b>: abre <code>showConfirmDialog</code> com "Você está em [Local]. Registrar presença em [Sport]?" e botões "Sim, registrar" / "Agora não". Respeita todos os gates de privacidade: <code>presenceVisibility === \'off\'</code> → skip; mute ativo → skip; sem <code>preferredLocations</code> com coordenadas → skip. Dedupe por sessão via <code>sessionStorage._presenceGeoHandled</code> com chave <code>data+placeId</code> — se o usuário já foi perguntado hoje neste local, não pergunta de novo (mas um local diferente ainda aciona). Sem permissão de GPS ou erro → log silencioso, sem retry. <b>Privacidade de localização</b>: a posição GPS do usuário nunca é gravada no Firestore — só é usada localmente para o match, e só o <code>placeId</code> do venue preferido acaba no doc de presença (mesma informação que o check-in manual já grava). Novo toggle no perfil "📡 Auto check-in ao chegar no local (usa GPS)" com descrição explicando o comportamento. Próximos passos pensados mas não implementados: pilar B (diretório de venues com perfil de proprietário), notificação push a amigos quando plano de ida é criado, integração com visão de Local para "locais preferidos próximos com pessoas jogando agora". Arquivos: <code>js/presence-geo.js</code> (novo), <code>js/views/auth.js</code>, <code>js/views/presence.js</code>, <code>js/views/dashboard.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.63-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença no local (PR A3): config de perfil — visibilidade + silenciar.</b> Segundo PR do pilar de presença. Nova seção "📍 Presença no local" no modal de perfil com dois controles. <b>Visibilidade</b>: três botões mutuamente exclusivos — 👥 Amigos (default, presenças só aparecem para amigos confirmados), 🌐 Todos (presenças entram na contagem agregada para qualquer usuário logado que consulte o local), 🚫 Ninguém (bloqueia criação de check-in/planejamento — o usuário some completamente do sistema de presença). O valor é aplicado no momento em que a presença é criada: <code>payload.visibility = cu.presenceVisibility</code> em <code>_presenceCheckIn</code> e <code>_presenceConfirmPlan</code>; guarda adicional impede criação quando <code>off</code>. <b>Silenciar sugestões automáticas</b>: seis botões — Ativas (default), 1 dia, 7 dias, 15 dias, 30 dias, Sempre. Ao salvar, <code>_presenceMuteToUntil(token)</code> traduz para timestamp absoluto em <code>presenceMuteUntil</code>; no próximo carregamento, se <code>presenceMuteUntil &lt; Date.now()</code>, o mute expira automaticamente e volta para "Ativas". Este campo prepara o terreno para o PR A4 (auto check-in por geolocation), onde será consultado antes de disparar o toast de sugestão. <b>Integração com presence view</b>: <code>_defaultVenue()</code> agora prioriza <code>preferredLocations[0]</code> do perfil (já existente — a seção de Locais de preferência com mapa) sobre o primeiro venue do histórico de torneios; <code>_defaultSport()</code> prioriza o primeiro item de <code>preferredSports</code> (CSV, também já existente no perfil). O dropdown de local agora lista primeiro os locais preferidos salvos, depois os venues de torneios — usuário novo que configurou preferências no perfil chega na view de presença já apontando para o lugar certo. Novos handlers globais: <code>_setPresenceVisibility</code>, <code>_applyPresenceVisibilityUI</code>, <code>_setPresenceMute</code>, <code>_applyPresenceMuteUI</code>, <code>_presenceMuteToUntil</code>. Novos campos no doc do usuário: <code>presenceVisibility</code>, <code>presenceMute</code>, <code>presenceMuteUntil</code>. Próximo PR planejado: A4 — auto check-in opt-in via Geolocation API com matching por raio de 150m contra <code>preferredLocations</code> (com lat/lon) ao abrir o app, respeitando o mute configurado aqui. Arquivos: <code>js/views/auth.js</code>, <code>js/views/presence.js</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.62-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Presença no local (PR A1): check-in + quem está no clube.</b> Primeira entrega do novo pilar de descoberta social. Nova coleção Firestore <code>presences</code> com docs do tipo check-in (agora → +4h) ou planejado (janela horária hoje). Cada doc guarda <code>uid</code>, <code>placeId</code> (Google Place id ou chave estável <code>custom:&lt;slug&gt;</code> para locais digitados livremente), <code>sport</code> normalizado (sem emoji), <code>dayKey</code> (YYYY-MM-DD) para queries eficientes, janela <code>startsAt</code>/<code>endsAt</code> em ms, <code>visibility</code> (<code>friends</code> default), e flag <code>cancelled</code> para soft-delete. Novo módulo <code>js/presence-db.js</code> expõe <code>PresenceDB</code> com <code>savePresence</code>, <code>cancelPresence</code>, <code>loadForVenueSportDay(placeId, sport, dayKey)</code>, <code>loadMyActive(uid)</code> e <code>loadForFriends(uids, windowMs)</code> (chunked em lotes de 10 p/ limite do operador <code>in</code>). Nova view <code>#presence</code> (<code>js/views/presence.js</code>) com seletor de Local e Modalidade, pré-preenchidos a partir do histórico de torneios do usuário — zero configuração necessária para começar. Gráfico de barras 6h–23h empilhado (dourado = amigos, cinza = outros) agrega em cada bucket: presenças de amigos + presenças públicas de desconhecidos + ocupações virtuais derivadas de torneios do mesmo local/modalidade/dia (participantes = contagem). Seção "Agora no local" mostra avatares de amigos com tempo desde o check-in + pill "+N outros". Seção "Próximas horas" lista por faixa horária com avatares sobrepostos de amigos, pill "+N" de desconhecidos e badge "🏆 Torneio X" clicável que leva ao torneio. Botões "📍 Estou aqui agora" (check-in de 4h) e "🗓️ Planejar ida" (overlay com from/to hoje) criam presenças. Privacidade client-side: presenças com <code>visibility: \'friends\'</code> de não-amigos são filtradas do display (regras Firestore serão endurecidas em PR A3 com <code>friendUids[]</code> denormalizado). Nova regra Firestore em <code>presences</code>: read autenticado, create/update/delete escopado ao <code>uid</code> do doc. Novo widget no dashboard "Amigos no local" (acima dos cards de torneios) carrega <code>loadForFriends(cu.friends, 48h)</code>, dedup por (uid+venue+sport), ordena ativos-primeiro → planejados mais próximos, limita a 6 cards; clicar em um card salva prefill em <code>sessionStorage._presencePrefill</code> e navega para <code>#presence</code> já apontando para aquele local/modalidade. Novo botão laranja "📍 Presença" no hero do dashboard para acesso direto. Nova rota <code>#presence</code> em <code>js/router.js</code>. Próximos PRs planejados: A2 (planejar ida com notificação a amigos), A3 (config de perfil: locais/modalidades preferidos, visibilidade pública/amigos/off, silenciar 1/7/15/30/sempre), A4 (auto check-in opt-in via Geolocation com raio 150m contra venues conhecidos). Pilar paralelo B (diretório de venues com perfil de proprietário reivindicável + descoberta quando viajando) começa após A. Deploy requer regras Firestore atualizadas: <code>firebase deploy --only firestore:rules</code>. Arquivos: <code>js/presence-db.js</code> (novo), <code>js/views/presence.js</code> (novo), <code>js/router.js</code>, <code>js/views/dashboard.js</code>, <code>firestore.rules</code>, <code>js/store.js</code>, <code>sw.js</code>, <code>index.html</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.61-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga unificada (PR 4): Suíço removido do seletor de formato.</b> Continuação da unificação iniciada em v0.14.52 (PR 1). A criação de novos torneios não oferece mais o formato "Suíço Clássico" como opção — o grid de formatos passa de 5 para 4 botões (Eliminatórias Simples, Dupla Eliminatória, Grupos + Elim., Liga), com layout responsivo ajustado (desktop 4 colunas em vez de 5; mobile 2 em vez de 3). Quem quiser o comportamento do Suíço (número finito de rodadas, sorteio a cada rodada) usa Liga com o toggle "Temporada contínua" em OFF — o substituto unificado introduzido na v0.14.52. A <code>option</code> <code>value="suico"</code> foi removida do <code>select</code> escondido; os campos <code>#suico-fields</code> e <code>#suico-draw-schedule-fields</code> permanecem no DOM mas ficam órfãos (não renderizados, sem entrada de UI que os ative). O Suíço continua existindo como opção de resolução no painel de Nash de potência de 2 — esse é um caminho de código separado (conversão on-the-fly de formato durante o sorteio) que não foi tocado. Código de bracket/standings que trata <code>t.format === \'Suíço Clássico\'</code> também fica intacto para os raros torneios legados que ainda tenham esse valor. Alterações em <code>js/views/create-tournament.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.60-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Segurança: regras Firestore estritas + denormalização <code>adminEmails[]</code>.</b> Auditoria das <code>firestore.rules</code>: antes deste deploy as regras eram <code>allow read: if true</code> para <code>tournaments</code>, <code>users</code> e <code>casualMatches</code>, e <code>allow write: if request.auth != null</code> em tournaments — ou seja, qualquer visitante lia todos os perfis (e-mail, telefone, <code>preferredCeps</code>, <code>fcmToken</code>) e torneios privados, e qualquer usuário autenticado podia editar/deletar qualquer torneio do banco. Reescrita completa em <code>firestore.rules</code>: <b>(1) <code>tournaments</code></b> — leitura permitida para visitante apenas se <code>isPublic == true</code>; usuário autenticado lê qualquer torneio (necessário para links de convite e transações de enroll). Criação exige que o <code>creatorEmail</code> declarado bata com o <code>auth.token.email</code> do caller. Update dividido em dois caminhos: admin do torneio faz update completo; qualquer autenticado só pode fazer um diff enrollment-shaped (campos restritos a <code>participants</code>, <code>memberEmails</code>, <code>status</code>, <code>teamOrigins</code>) — impede participante de se promover a organizador. Delete só admin. <b>(2) <code>users</code></b> — leitura agora exige autenticação; perfis deixam de vazar PII para visitantes anônimos. Write continua escopado ao próprio <code>uid</code>. <b>(3) Notificações</b> — create por qualquer autenticado (envio cross-user); read/update/delete só pelo dono. <b>(4) <code>casualMatches</code></b> — read público mantido (QR join anônimo precisa), write reforçado para autenticados. Para suportar a checagem de admin em O(1) dentro das rules, novo campo denormalizado <code>adminEmails[]</code> (creator + organizer + active coHosts, lowercased) gravado junto do <code>memberEmails[]</code> em <code>saveTournament()</code> via nova helper <code>FirestoreDB._computeAdminEmails(data)</code>. Também pulado no caminho <code>skipParticipants: true</code> (usado por <code>AppStore.sync()</code>) para não clobbar mudanças concorrentes em <code>coHosts</code>. Transações de enroll/deenroll não tocam em <code>adminEmails</code> — só em <code>memberEmails</code>. Torneios existentes sem <code>adminEmails[]</code> falham no <code>isTournamentAdmin</code> — precisam de um re-save pelo organizador (abrir modal de edição → Salvar) para popular o campo. Deploy manual das rules necessário: <code>firebase deploy --only firestore:rules</code>. Alterações em <code>firestore.rules</code> e <code>js/firebase-db.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.59-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase E): modo visitante sem listener real-time.</b> Último ponto da superfície de leitura do Firestore. Antes deste deploy, usuários não-logados (visitantes chegando pela landing page ou por um link direto de convite) disparavam, via <code>_commitSignOut</code> em <code>auth.js</code>, um listener em tempo real <code>.collection(\'tournaments\').where(\'isPublic\', \'==\', true).onSnapshot(...)</code> que baixava todos os torneios públicos do banco no snapshot inicial e, pior, recebia snapshot full a cada mudança em qualquer torneio público. Custo por visitante aberto simultaneamente: O(torneios públicos) reads de baseline + uma rajada de O(torneios públicos) por cada alteração remota. Removido: visitante agora opera sem nenhum feed — <code>AppStore.tournaments = []</code> e o router é chamado uma vez. A landing page (<code>renderLanding</code>) não depende de <code>AppStore.tournaments</code>; rotas diretas <code>#tournaments/{id}</code> já usavam fallback de <code>FirestoreDB.loadTournamentById()</code> em <code>tournaments.js:445</code> para buscar um único doc quando não está em cache, então esse caminho segue funcionando. Visitantes sem interesse específico consomem 0 reads de torneios no startup, e visitantes vindo de convite consomem exatamente 1. Fecha o último 🟡 residual do audit de escalabilidade. Alterações em <code>js/views/auth.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase D): limpeza automática de dados antigos via Cloud Functions agendadas.</b> Fecha os dois 🟡 finais do audit, ambos do tipo "crescimento lento que não dói hoje mas complica amanhã". Duas funções novas em <code>functions/index.js</code> usando <code>onSchedule</code> (Firebase Functions v2 scheduler), rodam diariamente às 3h da manhã (timezone America/Sao_Paulo): <b>(1) cleanupOldNotifications</b> faz <code>collectionGroup(\'notifications\').where(\'read\', \'==\', true).where(\'createdAt\', \'&lt;\', threshold)</code> com threshold de 90 dias, deletando em batches de até 400 docs por página até a query retornar vazia. O user mantém a caixa não-lida intacta mesmo se ficar meses fora — só o histórico lido envelhece e some. <b>(2) cleanupOldCasualMatches</b> faz <code>collection(\'casualMatches\').where(\'status\', \'==\', \'finished\').where(\'finishedAt\', \'&lt;\', threshold)</code> com threshold de 30 dias, mesmo padrão de batches. Os dados granulares de cada partida já são persistidos separadamente no perfil do jogador via <code>_buildAndPersistMatchRecord</code>, então o doc da sala de partida é descartável após 30 dias — histórico do jogador continua completo no perfil. Helper compartilhado <code>_batchDeleteQuery</code> encapsula o loop de paginar + deletar, com guarda de 100 passes para evitar loop infinito. Ambas as queries podem exigir índices compostos na primeira execução — Firebase Functions loga o link do console automaticamente no Firestore se faltarem. O <code>functions/</code> é untracked no git do projeto (deploy manual via <code>firebase deploy --only functions</code>), então este alpha bump é só para refletir o deploy arquitetural — não há mudança de cliente neste release. Status do audit: todos os 🔴 e 🟡 endereçados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.57-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase C): busca de usuários com range query.</b> Último 🔴 crítico do audit: a busca de amigos em Explore executava <code>.collection(\'users\').get()</code> sem filtro a cada tecla digitada — custo O(total de usuários do banco) por keystroke, multiplicado pelo debounce de 250ms. Reescrito para usar range queries sobre campos denormalizados. <code>saveUserProfile()</code> agora escreve <code>displayName_lower</code> e <code>email_lower</code> (lowercase) sempre que os campos originais estão presentes no payload — apenas quando presentes, para não clobbar em merge-saves parciais. <code>searchUsers(queryText, opts)</code> reescrito: query vazia retorna <code>[]</code> imediatamente (sem scan cego), e para qualquer query faz duas queries paralelas com prefix-range: <code>.where(\'displayName_lower\', \'&gt;=\', q).where(\'displayName_lower\', \'&lt;\', q + \'\\uf8ff\').limit(20)</code> e a mesma em <code>email_lower</code>. Resultados deduplicados por docId, filtro de <code>acceptFriendRequests</code> aplicado após os fetches (custa O(resultado), não O(banco)). Suporte a busca por cidade e preferredSports foi removido — recurso pouco usado e agora o padrão é prefix match em name/email. Backfill automático: em <code>simulateLoginSuccess</code> no auth.js, quando o perfil existente não tem os campos <code>_lower</code>, dispara um merge-save que os popula. Usuários antigos ganham os campos no primeiro login após este deploy, sem migration script. Custo por keystroke: 40 reads máx (20 por query, 2 queries) em vez de O(N) reads — constante. Firestore pode pedir índice automático single-field em <code>displayName_lower</code> e <code>email_lower</code> na primeira execução (sugere link no console). Alterações em <code>js/firebase-db.js</code>, <code>js/views/auth.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.56-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase B): descoberta pública de torneios com query paginada.</b> Fecha o gap deixado pela Fase A.2 — o <code>AppStore.tournaments</code> é escopado aos torneios do próprio usuário, então o filtro "🗓️ Abertos" do dashboard (que antes listava torneios públicos de inscrição aberta onde o usuário ainda não estava) passou a render sempre vazio. Corrigido com um feed de descoberta separado e escalável. Nova primitiva <code>FirestoreDB.loadPublicOpenTournaments({ limit, cursor })</code> faz <code>.where(\'isPublic\', \'==\', true).where(\'status\', \'==\', \'open\').orderBy(\'createdAt\', \'desc\').startAfter(cursor).limit(N+1)</code> — pagina por cursor (DocumentSnapshot) e usa limit+1 para detectar fim do feed sem query extra. Novo estado <code>AppStore.publicDiscovery</code>, <code>_publicDiscoveryCursor</code>, <code>_publicDiscoveryHasMore</code>; novo método <code>AppStore.loadPublicDiscovery({ append })</code> carrega página e filtra client-side os torneios que o usuário já é membro via <code>memberEmails[]</code> (Firestore não permite <i>not-array-contains</i>, então a subtração é client-side — custa O(página), não O(banco)). Disparado 3s após login em <code>auth.js</code> junto com os outros <i>background loaders</i>, em paralelo ao listener principal. Dashboard: <code>abertosParaVoce</code> agora deriva de <code>AppStore.publicDiscovery</code> em vez de filtrar <code>visible</code>. Quando o filtro "Abertos" está ativo e todos os itens da página atual já foram renderizados, aparece um botão "🔍 Descobrir mais torneios" que dispara <code>loadPublicDiscovery({ append: true })</code> para buscar a próxima página no servidor. Botão aparece em alternativa ao "Carregar mais" (que é paginação client-side) — hierarquia: primeiro esgota o cache local, depois oferece próxima query. Custo por abertura do dashboard: 1 read por membro da página de discovery (tipicamente 20), independente do tamanho do banco. Índice composto necessário em <code>(isPublic asc, status asc, createdAt desc)</code> — Firestore sugere link no console na primeira execução se não existir. Novas chaves i18n: <code>dashboard.discoverMore</code> em pt e en. Alterações em <code>js/firebase-db.js</code>, <code>js/store.js</code>, <code>js/views/auth.js</code>, <code>js/views/dashboard.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.55-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase A.2): login passa a escopar a leitura de torneios.</b> Aplicação do <code>memberEmails[]</code> introduzido na v0.14.54 aos dois pontos de entrada de leitura. O listener real-time em <code>startRealtimeListener(email)</code> agora executa <code>.collection(\'tournaments\').where(\'memberEmails\', \'array-contains\', email).onSnapshot(...)</code> em vez de escutar a coleção inteira — cada snapshot inicial e cada atualização remota passam a transferir apenas os torneios em que o usuário tem relação (criador, organizador, co-host ativo ou participante). Custo de leitura por login cai de <i>O(total de torneios no banco)</i> para <i>O(torneios do usuário)</i>, e o mesmo vale para cada mudança remota propagada pelo listener. O fallback <code>loadFromFirestore()</code> foi alinhado: usa <code>loadMyTournaments(email)</code> quando há usuário logado e mantém <code>loadAllTournaments()</code> só quando não há email (caminho degradado raro). Como o <code>AppStore.tournaments</code> passa a conter apenas os torneios do usuário, <code>_checkNearbyTournaments()</code> (notificação de torneios abertos próximos ao CEP do usuário) precisaria quebrar silenciosamente — corrigido adicionando <code>FirestoreDB.loadOpenTournaments()</code>, que faz <code>.where(\'status\', \'==\', \'open\').get()</code> em chamada única e é usado diretamente pelo nearby check. A função <code>renderTournaments()</code> já tinha fallback (tournaments.js:445) para buscar torneio por id diretamente do Firestore quando chega um link de convite fora do escopo do usuário — esse caminho segue funcionando. Requer índice automático em <code>memberEmails</code> (array-contains puro, Firestore cria sob demanda; composto não é necessário). Alterações em <code>js/store.js</code>, <code>js/firebase-db.js</code>, <code>js/views/auth.js</code>, <code>js/views/tournaments-organizer.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.54-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escalabilidade (Fase A.1): denormalização de <code>memberEmails[]</code> em cada torneio.</b> Primeiro passo do plano para eliminar o gargalo de leitura do login — hoje cada usuário baixa a coleção inteira de torneios, o que não escala para milhares de usuários. Este deploy é aditivo e não muda comportamento: todo torneio agora ganha um campo denormalizado <code>memberEmails[]</code> contendo os e-mails (lowercase, deduplicados) de todo mundo com relação com o torneio — <code>creatorEmail</code>, <code>organizerEmail</code>, <code>coHosts[]</code> ativos e <code>participants[].email</code>. O campo é mantido em sincronia em todos os pontos de escrita: <code>saveTournament()</code> recomputa em toda gravação (exceto com <code>skipParticipants: true</code>, que ignora o campo para não sobrescrever enrollments concorrentes); as transações <code>enrollParticipant()</code> e <code>deenrollParticipant()</code> recomputam dentro da própria transação, preservando atomicidade contra race conditions. Duas funções novas ficam disponíveis lado a lado com <code>loadAllTournaments()</code>, ainda não usadas no fluxo de login: <code>loadMyTournaments(email)</code> faz <code>.where(\'memberEmails\', \'array-contains\', email)</code> — substituta escalável para o login, troca O(total de torneios no banco) por O(torneios do usuário); <code>loadTournamentById(id)</code> busca um torneio por id, usada futuramente pelo router quando um link direto de convite apontar para um torneio fora do escopo do usuário. Próximos passos (não neste deploy): backfill do campo em torneios existentes via re-save (torneios nunca tocados após este deploy permanecem sem o campo e precisam de um migration script, que vai ser um <code>migration.html</code> dedicado rodado uma vez); criação do índice composto <code>memberEmails</code> no console do Firebase; swap do <code>loadAllTournaments()</code> por <code>loadMyTournaments()</code> no <code>loadFromFirestore()</code> de <code>store.js</code>. Alterações em <code>js/firebase-db.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.53-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual: controle de saque restaurado ao clicar em "Jogar Novamente".</b> Em partidas casuais, ao clicar em "🔄 Jogar Novamente" (ou no botão "↺ Resetar" do cabeçalho), o sistema limpava <code>state.serveOrder</code> mas não repopulava a ordem a partir das duplas atuais. Resultado: a bolinha de saque desaparecia e a rotação de sacadores deixava de ser controlada na segunda partida. Agora ambos os handlers chamam <code>_reinitServeOrderForNewMatch()</code>, que reconstrói a ordem proposta a partir dos arrays <code>p1Players</code>/<code>p2Players</code> atuais (importante quando o toggle "Re-sortear duplas" foi ativado antes do restart) e auto-preenche <code>state.serveOrder</code> com a mesma lógica usada na abertura do overlay. A partida seguinte já começa com o sacador definido e a rotação ativa, e a bolinha volta a ser arrastável antes do 2º game para ajuste manual, respeitando o hard-lock após 2 games. Alteração em <code>js/views/bracket-ui.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.52-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga unificada: novos toggles Temporada + Equilibrado na criação do torneio (PR 1 de 4).</b> Primeira etapa da unificação dos formatos Liga e Suíço em um único formato Liga. Dois novos toggles aparecem no topo do bloco "⚽ Configurações da Liga" no modal de criação/edição: <b>(1) Temporada contínua</b> (padrão ON): com ativado, a liga segue por temporada com rodadas periódicas (comportamento atual da Liga); desativado, número de rodadas é determinado dinamicamente (futuro substituto do Suíço puro). <b>(2) Sorteio equilibrado</b> (padrão ON): com ativado, cada rodada sorteia jogos dentro de <i>clusters</i> (grupos) de jogadores de nível próximo em vez de sortear livremente entre todos os ativos. Quando o toggle está ligado, expande um bloco de configuração com dois campos: <b>Tamanho do cluster</b> (input numérico, padrão 8, min 2, max 32 — potências de 2 como 4, 8, 16 permitem formatos como Rei/Rainha com 2 grupos de 4) e <b>Equilíbrio baseado em</b> (botões exclusivos: Jogador individual [padrão] ou Time). Novos campos salvos no Firestore: <code>t.temporada</code> (boolean), <code>t.equilibrado</code> (boolean), <code>t.clusterSize</code> (number), <code>t.balanceBy</code> (<code>\'individual\'</code> | <code>\'team\'</code>). Handlers adicionados: <code>window._onLigaBalancedToggle()</code> (mostra/oculta bloco de config quando o toggle é alterado) e <code>window._selectLigaBalance(btn)</code> (seleciona individual/time com estilo visual ativo). Load block em <code>create-tournament.js</code> reidrata toggles e botões ao abrir um torneio existente — torneios antigos sem esses campos assumem os padrões (<code>temporada: true</code>, <code>equilibrado: true</code>). Esta PR implementa apenas a UI e a persistência. A lógica do <i>cluster engine</i> (sorteio dentro de grupos, sit-outs com média de pontos, ativos zerados) vem na PR 2. A renderização dos sit-outs/inativos no bracket vem na PR 3. A remoção do formato Suíço + migração de torneios existentes (<code>format: \'Suíço\'</code> → <code>format: \'Liga\'</code> com <code>temporada: false</code>) vem na PR 4. i18n: 9 chaves novas em pt e en (<code>create.ligaSeasonToggle</code>, <code>create.ligaSeasonDesc</code>, <code>create.ligaBalancedToggle</code>, <code>create.ligaBalancedDesc</code>, <code>create.ligaClusterSize</code>, <code>create.ligaClusterSizeHint</code>, <code>create.ligaBalanceBy</code>, <code>create.ligaBalanceIndividual</code>, <code>create.ligaBalanceTeam</code>). Alterações em <code>js/views/create-tournament.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.51-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sistema de Pontos Avançado para Liga e Suíço puro.</b> Novo critério de pontuação opcional, configurável por torneio, que funciona como critério de desempate adicional (e prioritário quando ativo) nos formatos Liga e Suíço puro. O organizador escolhe quais eventos pontuam e define o valor de cada um. Sete categorias agrupadas em dois blocos: <b>(A) Derivados do placar</b> — Participação (+150 por jogo disputado), Vitória na partida (+150 bônus), Game ganho (+50 × N), Game perdido (−20 × N) e Ponto em tie-break (+2 × N); e <b>(B) Requerem pontuação ao vivo</b> — Killing point 40-40 (+10) e Ponto feito (+1), ambos dependem de live scoring ponto a ponto e ficam desativados por default. Os valores padrão foram calibrados para garantir seis invariantes simultâneos: (1) dominância sempre recompensada — vencer 6-0 (610 pts) supera vencer 7-6 (574 pts); (2) nenhum total de partida fica negativo — piso de segurança em 0 por partida; (3) granularidade nas derrotas — perder 0-6 (30) < 1-6 (80) < 2-6 (130) … < 5-6 (280) < 6-7 (370); (4) qualquer vitória > qualquer derrota; (5) anti-farming — game perdido custa 40% do game ganho, desincentiva deixar o adversário pontuar para inflar o próprio placar; (6) tie-break vencido nunca supera jogo decidido em sets. Configuração UX: seção "⚡ Sistema de Pontos Avançado" aparece no modal de criação/edição apenas quando formato = Liga ou Suíço (exclui Rei/Rainha e Monarch), logo após a seção de Formato da Partida (GSM). Master toggle liga/desliga o bloco inteiro; cada categoria tem seu próprio toggle + campo numérico editável. Na lista arrastável de critérios de desempate aparece o 7º item "⚡ Pontos Avançados" que só fica visível quando o sistema está ativo; quando habilitado, é automaticamente inserido no topo da prioridade de desempate (antes de confronto_direto/saldo_pontos). Visualização na classificação: nova coluna "⚡ PA" (âmbar) logo após Saldo em todas as tabelas do bracket; clique no valor abre modal de detalhamento por partida com breakdown de cada item que contribuiu para o total (ex.: "R3 ✅ vs João — Participação × 1 = +150, Vitória × 1 = +150, Game ganho × 6 = +300, Game perdido × 4 = −80 → Total 520"). Dados novos salvos no Firestore: <code>t.advancedScoring = { enabled, categories: { participation: {enabled, value}, match_won: {...}, game_won: {...}, game_lost: {...}, tiebreak_point: {...}, killing_point: {...}, point_scored: {...} } }</code>. Novas funções: <code>window._calcAdvancedPoints(t, playerName, category)</code> em <code>js/views/bracket-logic.js</code> (retorna <code>{ total, breakdown[] }</code> com iteração sobre <code>t.rounds[].matches[]</code>, suporte a individual/duplas/monarch via <code>_playerInSide</code>, extração de games e tie-break points de <code>m.sets[]</code>, integração opcional com <code>m.liveStats</code> para categorias ao vivo); <code>window._showAdvancedPointsBreakdown(tId, playerName, category)</code> em <code>js/views/bracket-ui.js</code> (modal com resumo + tabela por partida). Integração em <code>_computeStandings</code>: quando <code>t.advancedScoring.enabled === true</code>, cada standing recebe <code>s.advancedPoints</code> e <code>pontos_avancados</code> entra como caso no switch de tiebreaker. i18n: ~22 chaves novas em pt e en cobrindo títulos, descrições e notas de uso. Alterações em <code>js/views/create-tournament.js</code> (UI + load/save), <code>js/views/bracket-logic.js</code> (<code>_calcAdvancedPoints</code> + tiebreaker), <code>js/views/bracket.js</code> (coluna PA), <code>js/views/bracket-ui.js</code> (modal de detalhamento), <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga: tabela da rodada agora fica ENTRE os jogos do usuário e os demais jogos da rodada, e Liga+Rei/Rainha sorteia aleatoriamente entre os ativos.</b> Dois ajustes encadeados no fluxo Liga. <b>(1)</b> O split "Seu jogo" / "Demais jogos da rodada" ganhou uma terceira peça no meio: agora o HTML final do torneio Liga segue a ordem <i>⭐ Seus jogos</i> → <i>Classificação — Rodada N</i> (tabela da rodada, colapsável) → <i>▸ Demais jogos da rodada</i> (card colapsável fechado por padrão) → rodadas futuras → estatísticas → H2H → rodadas anteriores. O card "Demais jogos" foi promovido de <code>&lt;details&gt;</code> inline dentro do card da rodada atual para um <code>.card</code> próprio, intercalado depois da tabela de classificação. Variável <code>ligaOtherMatchesHtml</code> em <code>renderStandings</code> carrega esse HTML entre a geração do split e o <code>return</code> final. <b>(2)</b> <code>_generateReiRainhaRoundForPlayers</code> agora embaralha os ativos antes de formar os grupos de 4 em modo Liga — cada rodada forma grupos fresca, em vez de seguir a ordem fixa de standings. Torneios puros de Rei/Rainha da Praia (não-Liga) preservam a ordem de standings, que serve como balanceamento inicial por nível. Sit-outs continuam escolhidos pela lógica de fairness em <code>_chooseSitOutPlayers</code> (quem jogou mais rodadas seguidas sai). Alterações em <code>js/views/bracket.js</code> e <code>js/views/bracket-logic.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.49-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Grupos do Rei/Rainha: tabela de classificação do grupo agora fica DEPOIS dos cards das partidas, coroa removida do 1º lugar e critérios de desempate expandidos.</b> Três correções no bloco "R<i>N</i> Grupo <i>X</i>" do formato Rei/Rainha (e de Liga com rodada <i>rei_rainha</i>): <b>(1)</b> A ordem dentro de cada card de grupo foi invertida: antes mostrava cabeçalho + lista de jogadores + tabela mini de standings + cards de partida; agora mostra cabeçalho + lista de jogadores + <b>cards de partida (primeiro)</b> + tabela mini de standings (ao final). O raciocínio é que, quando a rodada está em andamento, o que interessa é lançar resultado — então os cards precisam estar visíveis no topo do bloco do grupo; a tabela de posições vive melhor como consequência, logo abaixo. <b>(2)</b> A coroa (👑) que marcava o 1º lugar do grupo foi removida. O símbolo 👑 é convenção do app pra indicar organizador do torneio em outros contextos (dashboard, cards de participante), e usá-lo dentro da tabela de um grupo para indicar líder confundia. Agora todas as posições usam o mesmo formato "1º / 2º / 3º / 4º" com a mesma cor neutra, sem destaque especial para o primeiro colocado. <b>(3)</b> Tabela de classificação do grupo agora expõe critérios de desempate muito mais ricos que apenas V/D/Saldo. Novas colunas sempre visíveis: <b>V, D, Pts (pontos pró em todos os jogos — contam mesmo em game perdido), Saldo (pró − contra), %</b> (aproveitamento = V/J). Em torneios GSM (sistema Game-Set-Match), a tabela expande automaticamente com: <b>Sets (V-D), ±S, Games (V-D), ±G, TB (V-D)</b>. A função <code>_computeMonarchStandings</code> em <code>js/views/bracket-logic.js</code> foi reescrita: cada jogador do grupo agora acumula <code>setsWon, setsLost, gamesWon, gamesLost, tiebreaksWon, tiebreaksLost</code> extraídos de <code>m.sets[]</code>, além de <code>pointsFor, pointsAgainst</code> já existentes. A ordenação passa a seguir uma cadeia completa de 11 critérios: vitórias → saldo de sets → sets vencidos → saldo de games → games vencidos → saldo de tie-breaks → tie-breaks vencidos → saldo de pontos → pontos pró → aproveitamento → número de jogos (asc, quem jogou menos sobe no empate total — reflete registro mais confiável). Alterações em <code>js/views/bracket.js</code> (render dos grupos em <code>renderStandings</code>) e <code>js/views/bracket-logic.js</code> (<code>_computeMonarchStandings</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.48-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga: rodada atual agora renderizada ANTES da tabela de Classificação — seu jogo fica literalmente no topo da página.</b> Na v0.14.47 o split "⭐ Seu jogo" / "Demais jogos da rodada" já existia, mas o bloco da rodada corrente continuava sendo desenhado DEPOIS da seção "Classificação — Rodada N" no output de <code>renderStandings</code>. O usuário pedia o bloco no topo mesmo; agora a ordem final do HTML em torneios Liga passa a ser: banner de fase, barra de progresso, banner de "partidas prontas", <i>rodada atual</i> (com seu jogo + demais colapsados), tabela de classificação (ainda colapsada por default), rodadas futuras, estatísticas, confrontos diretos, rodadas anteriores. Para os demais formatos (Eliminatórias, Grupos, Suíço como qualifier), a ordem legada é preservada — classificação antes da rodada atual — porque nesses contextos a tabela de standings é mais relevante para entender o estado do torneio do que o card individual de uma partida. Alteração em <code>js/views/bracket.js</code> (retorno de <code>renderStandings</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.47-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga: seu(s) jogo(s) da rodada atual sobem para o topo do chaveamento, os demais ficam colapsados em "Demais jogos da rodada".</b> Em torneios de formato Liga (e também no branch <i>standings</i> do Suíço quando o usuário joga Liga-like), a renderização da rodada corrente em <code>renderStandings</code> deixou de ser um único grid flat com todas as partidas em ordem de sorteio. Agora, se houver um usuário logado cujo nome/email aparece em <code>m.p1</code> ou <code>m.p2</code> (comparação direta para individual; split por " / " e comparação elemento-a-elemento para duplas) de pelo menos uma partida da rodada, a rodada é dividida em duas regiões: (a) um cabeçalho "⭐ Seu jogo" (ou "Seus jogos" no plural) em destaque indigo seguido apenas do(s) card(s) do próprio usuário em tamanho normal e com o mesmo border glow indigo que já marcava a partida do usuário; (b) um <code>&lt;details&gt;</code> colapsado intitulado "Demais jogos da rodada (<i>N</i>)" abaixo, contendo todos os outros jogos da rodada. O usuário pode abrir o <code>&lt;details&gt;</code> quando quiser acompanhar a rodada inteira. Detecção reutiliza o mesmo padrão de <code>_isMyMatch</code> já presente em <code>renderMatchCard</code> (bracket.js:1355), então é 100% consistente com o destaque visual do card individual. A numeração das partidas (prefixo "Jogo <i>N</i>") continua refletindo a posição absoluta dentro da rodada — ou seja, o primeiro card exibido pode ser "Jogo 3" se foi o terceiro sorteado; isso é intencional, pra não quebrar referências que já circulam em notificações/compartilhamentos. Fallback preservado: se o usuário não está logado, ou se a rodada não contém nenhuma partida do usuário, ou se TODAS as partidas são dele, ou se o formato não é Liga (e.g. Eliminatórias, Grupos), o render cai para o grid flat original sem overhead. Rei/Rainha (formato de rodada <code>rei_rainha</code>) ignora o split — seu agrupamento por grupo já é a estrutura natural. Alteração em <code>js/views/bracket.js</code> (<code>renderStandings</code>, bloco de current-round matches).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.46-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Quatro correções relacionadas: modal de criação coerente, card do participante com toggle alinhado ao nome, sort por disponibilidade voltando a funcionar, e sorteio automático da Liga aparecendo na hora em que ocorre</b>.</p>' +
        '<p><b>(1) Modal "+ Novo Torneio → Detalhes Avançados" agora abre sempre consistente.</b> Antes, ao abrir o modal de criação após ter acabado de editar um torneio Liga, o botão "Liga" aparecia visualmente destacado (borda azul, fundo colorido, texto em cor de destaque) mas os campos visíveis abaixo eram os de Eliminatória Simples — os dois estados divergiam. Causa raiz: o handler do botão "Detalhes Avançados" em <code>js/main.js</code> chama <code>form.reset()</code>, que restaura os valores dos elementos de formulário (incluindo o <code>&lt;select id="select-formato"&gt;</code> oculto para <code>elim_simples</code>), mas o estado visual dos botões de formato e de modo de sorteio é mantido via estilos <i>inline</i> (<code>style.border</code>, <code>style.background</code>, <code>style.color</code>, <code>style.boxShadow</code>) aplicados por <code>_selectFormato</code>/<code>_selectDrawMode</code>. Estilos inline sobrevivem a <code>form.reset()</code>, então o botão Liga continuava "pintado de azul" enquanto o <code>select-formato</code> voltava para <code>elim_simples</code>. Correção: antes de <code>openModal(\'modal-create-tournament\')</code>, o handler agora força o select + chama <code>_selectFormato</code> no botão padrão (Eliminatória Simples) e <code>_selectDrawMode</code> no padrão (Sorteio). Alteração em <code>js/main.js</code> (handler <code>btn-quick-advanced</code>).</p>' +
        '<p><b>(2) Toggle ativado/desativado da Liga movido para cima, alinhado ao nome do participante.</b> Antes, em cada card de inscrito, o toggle ficava em uma linha inferior misturado com o rótulo "Inscrição Individual" e logo ao lado dos botões VIP/Apagar — visualmente confuso e deslocado para baixo, longe da identidade do jogador. Agora o toggle vive na MESMA linha do nome/avatar, ancorado à direita do card — posição fixa, acima do badge VIP e do botão de apagar (que continuam no rodapé do card). O rótulo dinâmico ("<span style="color:#34d399;font-weight:700;">ativado</span>" em verde ou "<span style="color:#f87171;font-weight:700;">desativado</span>" em vermelho) fica à esquerda do próprio toggle: quando o texto aumenta de 7 para 10 caracteres, o rótulo expande para a esquerda mantendo o toggle no mesmo ponto de alinhamento, então o "botão físico" da pílula não se mexe visualmente quando o estado muda. Alteração em <code>js/views/tournaments.js</code> (markup do card do participante).</p>' +
        '<p><b>(3) Sort por "🟢 Ativos primeiro" / "🔴 Inativos primeiro" volta a ordenar de verdade.</b> Bug: o comparador principal (<code>_sortedParts</code>) só tinha casos para <code>alpha_asc/alpha_desc/chrono_desc</code> e caía para <code>return 0</code> quando o modo era <code>active_asc/active_desc</code>. Como <code>parts.slice().sort(...)</code> roda DEPOIS de uma pré-ordenação por atividade (aplicada em <code>parts</code> direto), o sort principal com organizer-first acaba sobrepondo e/ou o <code>return 0</code> preserva apenas a ordem de chegada — resultado: o usuário clicava no botão, a flecha virava, mas a lista visível não mudava. Agora o comparador principal tem um primeiro branch explícito para Liga + <code>active_asc/active_desc</code> que (a) ordena por <code>ligaActive !== false</code> (ativos vs de fora), (b) usa <code>parts.indexOf</code> como desempate para manter ordem de inscrição dentro de cada grupo, e (c) <b>ignora a regra "organizadores primeiro"</b> para essa ordenação específica — faz sentido, porque se o usuário pediu para ver os inativos primeiro, a ordem pedida é a que deve valer. Alteração em <code>js/views/tournaments.js</code> (comparador de <code>_sortedParts</code>).</p>' +
        '<p><b>(4) Sorteio automático da Liga: agora aparece imediatamente na tela em que o usuário está, não só no próximo F5.</b> O poller <code>_checkLigaAutoDraws</code> já disparava a cada 60s, mas depois de executar <code>_fireLigaAutoDraw</code> (criar standings + chamar <code>_generateNextRound</code> + notificar + salvar no Firestore) a única atualização de UI era <code>_rerenderBracket</code> — ou seja, só renovava a tela se o usuário estivesse exatamente dentro da página do bracket desse torneio. Se o organizador estava no dashboard ou na página de detalhes, a nova rodada só aparecia após reload manual. Agora, após cada sorteio bem-sucedido, o poller detecta a rota atual (<code>window.location.hash</code>): em <code>#tournaments/&lt;id&gt;</code> chama <code>renderTournaments(container, id)</code>, em <code>#bracket/&lt;id&gt;</code> chama <code>_rerenderBracket(id)</code>, em <code>#dashboard</code> (ou rota raiz) chama <code>renderDashboard(container)</code>. Além disso, um toast de notificação <code>showNotification(\'🎲 Sorteio automático realizado…\')</code> / <code>\'🎲 Nova rodada sorteada…\'</code> é exibido para o próprio organizador, avisando imediatamente que a rodada acabou de ser gerada. Como bônus, a checagem de "sou organizador deste torneio?" foi reescrita para testar diretamente <code>t.organizerEmail</code> / <code>t.creatorEmail</code> / <code>t.coHosts</code> contra <code>store.currentUser.email</code> (antes flipava <code>store.viewMode</code> temporariamente — abordagem frágil). Alteração em <code>js/views/bracket-logic.js</code> (<code>_checkLigaAutoDraws</code> e <code>_fireLigaAutoDraw</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.45-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Toggle de disponibilidade da Liga volta a salvar (e filtrar) de verdade + card mais compacto</b> — Três correções no toggle "ativado/desativado" integrado ao card do participante: <b>(1)</b> o handler <code>window._toggleLigaActive</code> acessava <code>window.AppStore.state.tournaments</code>, mas <code>AppStore.state</code> nunca existiu — a propriedade correta é <code>AppStore.tournaments</code>. Como resultado, toda vez que um participante clicava no próprio toggle, o callback disparava um <code>TypeError</code> silencioso, o estado não era persistido no Firestore e o valor visual revertia ao original no próximo render. Isso também explicava o sintoma reportado de que "o botão de sort parece ativar o toggle do jogador": o sort só re-renderizava a página, e como o toggle nunca havia sido salvo, o re-render exibia o estado antigo — parecia que o sort tinha desfeito a alteração. Agora o handler usa <code>AppStore.tournaments</code>, compara IDs via <code>String(x.id) === String(tId)</code>, e salva via <code>syncImmediate</code> (quando organizador) ou <code>FirestoreDB.saveTournament</code> (quando participante). <b>(2)</b> Com a persistência funcionando, o filtro <code>_getActiveLigaPlayers</code> em <code>_generateNextRoundForPlayers</code> passa a receber de fato o estado atualizado — participantes com <code>ligaActive=false</code> são removidos do pareamento e registrados como folga (<code>isSitOut: true</code>, <code>sitOutReason: \'inactive\'</code>), com label "R<i>N</i> • Folga (inativo)". <b>(3)</b> Card do participante ficou mais compacto: o toggle saiu de uma linha dedicada e foi movido para a mesma linha do rótulo de tipo ("Individual" / "Time"), com o texto do estado agora em minúsculas dinâmicas — "<span style="color:#34d399;font-weight:700;">ativado</span>" (verde) / "<span style="color:#f87171;font-weight:700;">desativado</span>" (vermelho) — sem emoji de bolinha e sem pílula com padding extra. Outros participantes continuam visíveis em estado <code>disabled</code> (opacity 0.6). Alterações em <code>js/views/tournaments-enrollment.js</code> (handler <code>_toggleLigaActive</code>) e <code>js/views/tournaments.js</code> (markup do card).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.44-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sorteio automático em torneios Liga com data/hora do primeiro sorteio + intervalo de repetição</b> — Em torneios Liga onde o toggle "Sorteio manual" está desligado e os campos "Data/hora do primeiro sorteio" e "Repetir a cada X dias" estão preenchidos, o sorteio, a geração do chaveamento e a notificação aos inscritos passam a acontecer automaticamente, sem precisar de clique do organizador. O navegador do organizador (ou de um co-host ativo) executa um poller (<code>window._checkLigaAutoDraws</code>) a cada 60s: identifica torneios Liga agendados, calcula a última janela de sorteio que já passou (<code>firstDraw + N * interval</code>), compara com <code>t.lastAutoDrawAt</code>, e se não houve disparo nesta janela, executa. A primeira rodada embaralha os inscritos, semeia o <code>t.standings</code> e chama <code>_generateNextRound(t)</code>; rodadas subsequentes chamam direto <code>_generateNextRound(t)</code>, que já filtra via <code>_getActiveLigaPlayers(t)</code> — ou seja, somente participantes com <code>ligaActive !== false</code> são pareados, e os "de fora" ficam registrados em <code>sitOutHistory</code> como informativo (sem penalidade). Após cada rodada, a notificação "Nova Rodada" é enviada a todos os inscritos via <code>_notifyTournamentParticipants</code> (nível <code>important</code>), e o Firestore é atualizado via <code>syncImmediate</code>. O poller para quando <code>t.endDate</code> é atingido (torneio já encerrado) ou quando o status for <code>finished</code>, ou se há menos de 2 ativos. <b>Idempotência:</b> só o organizador/co-host dispara (evita corrida entre múltiplos navegadores); <code>t.lastAutoDrawAt</code> (ISO da hora agendada) evita duplicação. Implementação em <code>js/views/bracket-logic.js</code> (nova função <code>_checkLigaAutoDraws</code> + helper interno <code>_fireLigaAutoDraw</code>), <code>js/views/auth.js</code> (kick inicial 3s após login), <code>js/main.js</code> (setInterval 60s). O helper existente <code>_calcNextDrawDate(t)</code> já é usado para exibir o countdown na UI — agora também é consistente com o momento em que o sorteio efetivo ocorre.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.43-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Toggle de disponibilidade integrado ao card do participante + novo sort "ativos/de fora"</b> — O painel público dedicado "Disponibilidade para o próximo sorteio" (introduzido na v0.14.42) foi removido. O toggle agora vive dentro do próprio card de cada inscrito na seção "Inscritos Confirmados", em uma pílula compacta logo abaixo do nome com o texto de estado ("🟢 Ativo" / "🔴 De fora"). Continua valendo: só o próprio usuário edita seu toggle (os demais aparecem em <code>disabled</code>, visíveis mas não clicáveis); todos os inscritos enxergam o estado de todos; o filtro de sorteio (<code>_getActiveLigaPlayers</code>) continua excluindo inativos da próxima rodada e <code>_recordSitOut</code> registra os de fora como informativo. Adicionado também um terceiro botão de ordenação ao lado de "A-Z↓" e "🕐↓" na barra de cabeçalho da seção (só aparece em torneios Liga): "🟢↓" inverte para "🔴↑" no segundo clique, ordenando os cards por status de disponibilidade (ativos primeiro ou de fora primeiro). Por fim, novas inscrições em Liga agora gravam <code>ligaActive: true</code> explicitamente no objeto do participante (para quem já estava inscrito antes da feature, o valor <code>undefined</code> continua sendo interpretado como ativo pela lógica de display <code>p.ligaActive !== false</code>). Implementação em <code>js/views/tournaments.js</code> (toggle no card + novo botão de sort), <code>js/views/tournaments-enrollment.js</code> (default <code>ligaActive: true</code> nos 4 pontos de inscrição), <code>js/i18n-pt.js</code> e <code>js/i18n-en.js</code> (5 chaves novas: <code>clickToInactive</code>, <code>clickToActive</code>, <code>othersReadOnly</code>, <code>sortActiveFirst</code>, <code>sortInactiveFirst</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.42-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel público de disponibilidade na Liga: cada participante tem um toggle de "ativo" visível a todos, mas editável só por si mesmo</b> — Em torneios de formato Liga, o sorteio da próxima rodada agora respeita um toggle individual de disponibilidade por participante (<code>p.ligaActive</code>, default <code>true</code>). Quando alguém desativa o próprio toggle, fica de fora do pareamento da próxima rodada gerada — o sistema filtra via <code>_getActiveLigaPlayers(t)</code> antes de chamar <code>_generateNextRoundForPlayers</code>, e quem fica de fora é registrado em <code>_recordSitOut</code> como "de fora" (informativo, sem penalidade). Antes, esse toggle existia apenas como controle interno do organizador, sem visibilidade pública. Agora, na página do torneio, logo abaixo da seção Organização, aparece um novo painel "🎲 Disponibilidade para o próximo sorteio" com uma lista de todos os participantes mostrando: avatar, nome, status visual (🟢 Ativo / 🔴 De fora), e um toggle à direita. Dois comportamentos-chave: <b>(a) transparência total</b>, todos os inscritos veem o estado de cada um (permite planejar rodadas, saber quem está disponível na semana, evitar ficar esperando alguém que já sinalizou que não joga); <b>(b) autoridade individual</b>, o toggle é interativo só para o próprio usuário (cada um controla apenas sua disponibilidade — o toggle dos outros aparece em estado <code>disabled</code>, visível mas não clicável). Participantes inativos vão para uma subseção separada "De fora do próximo sorteio" dentro do painel, com contagem ao lado do título. A própria linha do usuário tem badge "VOCÊ" ao lado do nome. O handler <code>window._toggleLigaActive(tId, isActive)</code> só altera a entrada do usuário corrente (match via <code>_userMatchesParticipant</code>), nunca de outro. Se você não está inscrito no torneio, o painel aparece em modo somente-leitura. Strings adicionadas em pt/en via <code>_t(\'liga.availability*\')</code>. Implementação em <code>js/views/tournaments.js</code> (render do painel), <code>js/views/tournaments-enrollment.js</code> (handler de toggle), <code>js/i18n-pt.js</code> e <code>js/i18n-en.js</code> (8 chaves novas).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.41-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificação "Convite enviado" é marcada como lida automaticamente quando o convidado aceita ou recusa</b> — Antes, quando o organizador convidava alguém para co-organizar um torneio, ele via duas notificações distintas: "✉️ Convite de co-organização enviado para X" (com botão "Cancelar Convite") e, logo depois que o convidado respondia, "✅ X aceitou ser co-organizador" (ou "❌ recusou"). A primeira continuava marcada como não-lida e ainda oferecia o botão "Cancelar Convite", mesmo depois que o convite já tinha sido resolvido — permitindo que o organizador clicasse em cancelar um convite que já não existia, o que levava a comportamento confuso. O código já tentava marcar essa notificação como lida via <code>_markInviteNotifsRead(orgUid, ...)</code>, mas falhava silenciosamente nos casos em que o campo <code>t.creatorUid</code> não estava populado no documento do torneio (<code>orgUid</code> virava string vazia e o <code>if (orgUid)</code> pulava a chamada). Agora <code>_markInviteNotifsRead</code> aceita UID ou email: se recebe email, faz lookup via <code>users.where(email)</code> e marca as notificações corretas; se recebe UID, verifica que o documento existe antes de operar. Os handlers de aceite e recusa de cohost passam <code>t.creatorUid || t.creatorEmail || t.organizerEmail</code> — ou seja, sempre há um identificador válido, independente de quando o torneio foi criado. Com isso, a notificação "Convite enviado" passa a estado lido e o botão "Cancelar Convite" desaparece assim que a resposta do convidado chega. Correção em <code>js/views/host-transfer.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.40-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Ao resolver "resto" em formação de times ou potência de 2, o sorteio efetivo acontece imediatamente</b> — Antes, depois que o organizador selecionava uma solução nos painéis de decisão (Reabrir / BYEs / Play-in / Lista de Espera / Exclusão / Suíço / Dissolver / Enquete) e confirmava o preview no painel de simulação, o app ainda abria uma <i>Revisão Final</i> com checklist verde ("Inscrições encerradas ✅", "Times consolidados ✅", "Estrutura da chave ✅") + histórico de resolução + botão "🎲 Rolar Sorteio Agora" — obrigando um clique extra para finalmente executar o sorteio. Essa tela era redundante: o organizador já tinha revisado o preview do cenário no painel de simulação e confirmado explicitamente clicando em "Confirmar". Agora, logo após a confirmação da resolução (ou do executar da remoção pelo painel de remainder, ou do save do painel de dissolve, ou quando o <code>showUnifiedResolutionPanel</code> detecta que não há mais issues a resolver), o app chama <code>window.generateDrawFunction(tId)</code> diretamente — o sorteio efetivo acontece imediatamente e o organizador é levado para o bracket pronto. O painel de Revisão Final não foi deletado (continua acessível via fallback se <code>generateDrawFunction</code> não estiver disponível), mas não aparece mais no fluxo normal. Benefício adicional: quando o torneio já está em estado "pronto para sortear" (sem remainder, sem times incompletos, sem desbalanço de potência de 2), o próprio botão "Sortear" vai direto para o sorteio — antes mostrava a Revisão Final como uma confirmação extra. Correção em <code>js/views/tournaments-draw-prep.js</code> (duas chamadas <code>showFinalReviewPanel</code> substituídas por <code>generateDrawFunction</code> em <code>showUnifiedResolutionPanel</code> e <code>_confirmP2Resolution</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.39-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Organizadores sempre visíveis em "Organização" e bloqueio de dupla inscrição quando já se está em um time</b> — Dois bugs relacionados corrigidos em conjunto: (1) <b>Organizador sumia da seção Organização ao se inscrever para competir:</b> quando o organizador (ou co-organizador) marcava presença como competidor, seu card desaparecia da lista "Organização" no topo da página do torneio (a lógica escondia quem estivesse no array <code>participants</code>). Agora organizador e co-organizadores <i>ativos</i> aparecem sempre na seção Organização, independente de também estarem inscritos como competidores — as duas listas são ortogonais: Organização mostra quem administra o torneio, Inscritos Confirmados mostra quem joga. (2) <b>Botão "Inscrever-se" reaparecia para quem já estava dentro de um time pré-formado e, se clicado, criava uma duplicata:</b> a checagem <code>isParticipating</code> só reconhecia entradas individuais — entradas do tipo <code>"Nelson Barth / Rodrigo Barth"</code> (team string), objetos com array <code>participants[]</code> de membros, ou objetos cujo <code>displayName</code>/<code>name</code> contenha <code>" / "</code> eram ignoradas. Resultado: Rodrigo, já membro do time com Nelson, via o botão aparecer e clicá-lo o adicionava como participante individual separado, aparecendo duas vezes (posição #1 como individual, posição #2 dentro do time). Agora três camadas detectam membros de time: (a) render do botão <code>isParticipating</code> em <code>tournaments.js</code>; (b) check otimista <code>alreadyIn</code> em <code>tournaments-enrollment.js</code>; (c) transação atômica <code>enrollParticipant</code> em <code>firebase-db.js</code> (safety net no servidor). Todas usam o novo helper compartilhado <code>window._userMatchesParticipant(user, p)</code> / <code>window._isUserEnrolledInTournament(user, t)</code> em <code>store.js</code>, que entende todos os formatos: string individual, string com barra separadora, objeto com email/uid/displayName/name, e objeto-time com array de membros. Correção em <code>js/store.js</code>, <code>js/firebase-db.js</code>, <code>js/views/tournaments.js</code>, <code>js/views/tournaments-enrollment.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cards de times formados antes do sorteio agora mostram os dois jogadores com tipografia e tamanho de avatar iguais</b> — Antes, dentro de um card de equipe (nome com "/" separando os integrantes, como <code>Nelson Barth / Rodrigo Barth</code>) o primeiro jogador aparecia com fonte 0.95rem, peso 700, cor <code>var(--text-bright)</code> e avatar 24x24, enquanto o parceiro aparecia menor (0.85rem), mais leve (peso 500), mais apagado (<code>var(--text-muted)</code>) e com avatar 20x20 — passando a impressão visual de que um era "o titular" e o outro era só um anexo. Em times formados (inscritos como dupla, sorteados ou montados manualmente) os dois jogadores têm peso equivalente dentro do time, então ambos devem ser apresentados com a mesma hierarquia visual. Agora os dois integrantes renderizam com <code>font-weight:700</code>, <code>font-size:0.95rem</code>, <code>color:var(--text-bright)</code> e avatar 24x24 — tamanho, cor e peso idênticos. A coroa 👑 de organizador/co-organizador continua sendo colocada ao lado do jogador correspondente, e o número de inscrição/badge VIP do card não foi alterado. Correção em <code>js/views/participants.js</code> (branch "Normal mode: team cards") e <code>js/views/tournaments.js</code> (render de participantes dentro da view de detalhes do torneio).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.37-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Textos explicativos dos toggles "W.O." e "Inscrições Após Encerramento" agora refletem o estado atual</b> — Antes, cada descrição trazia as duas frases juntas (ligado + desligado) no mesmo parágrafo, obrigando o organizador a ler tudo e mentalmente filtrar qual comportamento estava ativo no momento. Agora cada <code>.toggle-desc</code> exibe apenas a frase correspondente ao estado atual do toggle, atualizada em tempo real ao alternar. Três casos: (1) <b>W.O. — Individual:</b> ligado mostra "W.O. afeta só o jogador ausente. O parceiro continua em quadra com um substituto da lista de espera."; desligado mostra "Time inteiro eliminado em caso de W.O. Adversário avança automaticamente." (2) <b>Inscrições Após Encerramento — Fechadas:</b> ligado mostra "Ninguém mais pode se inscrever após o prazo."; desligado mostra "Novos inscritos continuam sendo aceitos e vão para a lista de espera como suplentes." (3) <b>Novos Confrontos:</b> quando Fechadas está ligado (toggle inerte), mostra aviso "Indisponível enquanto \'Fechadas\' estiver ligado — não há suplentes para formar novos confrontos." e a linha ganha opacidade 0.55; quando Fechadas está desligado e Novos Confrontos ligado, explica o auto-expand; quando ambos desligados, explica que suplentes só entram em caso de desistência. Implementação: novas chaves i18n <code>create.woIndividualOnDesc</code>/<code>OffDesc</code>, <code>create.lateEnrollClosedOnDesc</code>/<code>OffDesc</code>, <code>create.lateEnrollExpandOnDesc</code>/<code>OffDesc</code>/<code>DisabledDesc</code> em pt e en; IDs <code>wo-indiv-desc</code>, <code>late-closed-desc</code>, <code>late-expand-desc</code> adicionados nos <code>.toggle-desc</code>; <code>_syncWoScope</code> e <code>_syncLateEnrollment</code> agora escrevem <code>textContent</code> correto e aplicam <code>opacity:0.55</code> na linha de Novos Confrontos quando inerte. Sincronizações em edit-mode (restore) e apply-template continuam chamando os sync functions, então as descrições refletem o estado salvo automaticamente. Correção em <code>js/views/create-tournament.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.36-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auto-scroll da tela ao arrastar cards próximos das bordas do viewport</b> — Antes, ao arrastar um card de jogador, de categoria, ou de critério de desempate, se o destino estivesse fora da área visível (por exemplo, a coroa 👑 de co-organizador, uma categoria no fim da lista, ou um time lá embaixo no painel de sorteio), era impossível chegar até ele — o arrasto ficava preso dentro da janela visível e soltar o card no meio do caminho cancelava a operação. Agora, quando o ponteiro/dedo se aproxima das bordas superior ou inferior do viewport durante qualquer arrasto, a tela (ou o container rolável mais próximo — inclusive dentro de modais como o Gerenciador de Categorias e o modal de Criar/Editar Torneio) rola automaticamente na direção do movimento. Velocidade proporcional à proximidade da borda (até 18px/frame no limite) e zona de ativação de 80px. Implementação: helper global <code>_dragAutoScrollOnTouchMove</code> / <code>_dragAutoScrollStop</code> em <code>store.js</code> que escuta <code>dragover</code>/<code>dragend</code>/<code>drop</code> no document para arrastos HTML5 (cobre pre-draw, sorteio de times, coroa de co-organizador) e expõe entrypoints para os arrastos com clone por touch (Gerenciador de Categorias em <code>tournaments-categories.js</code> e reordenação de critérios de desempate em <code>create-tournament.js</code>). Usa <code>requestAnimationFrame</code> com parada automática após 150ms sem eventos — sem listeners persistentes consumindo recursos quando não há arrasto.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.35-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Seções "W.O." e "Inscrições Após Encerramento" simplificadas para menos toggles</b> — (1) <b>W.O. (Ausência):</b> antes existiam dois toggles mutuamente exclusivos ("Individual" e "Time Inteiro") que se comportavam como radio buttons. Agora existe um único toggle "Individual" — ligado (padrão) elimina apenas o jogador ausente e o parceiro continua com substituto da lista de espera; desligado elimina o time inteiro e o adversário avança. O valor gravado em <code>t.woScope</code> continua sendo <code>\'individual\'</code> ou <code>\'team\'</code>, mantendo compatibilidade total com <code>bracket-ui.js</code> e <code>participants.js</code>. (2) <b>Inscrições Após Encerramento:</b> antes existiam três toggles radio-like ("Fechadas", "Lista de Espera", "Novos Confrontos"). O toggle "Lista de Espera" foi removido porque seu comportamento já é coberto pelo toggle "Fechadas" desligado. Agora são dois toggles independentes: "Fechadas" (padrão ligado) e "Novos Confrontos" (padrão ligado). Fechadas ligado → <code>lateEnrollment = \'closed\'</code>; Fechadas desligado + Novos Confrontos desligado → <code>\'standby\'</code> (suplentes acumulam na lista de espera); Fechadas desligado + Novos Confrontos ligado → <code>\'expand\'</code> (suplentes viram novos confrontos automaticamente quando acumulam o suficiente). O consumidor <code>_allowsLateEnrollment</code> em <code>tournaments-enrollment.js</code> continua funcionando sem alteração (aceita ambos <code>\'standby\'</code> e <code>\'expand\'</code>). Sincronizadores <code>_syncWoScope</code> e <code>_syncLateEnrollment</code> reescritos; restore em edit-mode e apply-template atualizados. Descrições i18n <code>create.woIndividualDesc</code>, <code>create.lateEnrollClosedDesc</code> e <code>create.lateEnrollExpandDesc</code> atualizadas em pt e en para explicar os novos comportamentos ligado/desligado. Correção em <code>js/views/create-tournament.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.34-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Barra do botão Voltar no modal de Estatísticas agora usa a largura padrão da página</b> — O cabeçalho do modal <code>📊 Estatísticas</code> (aberto via <code>window._showPlayerStats</code>) estava sendo renderizado com <code>max-width:520px</code>, ficando visualmente constrito ao tamanho do card e dando a impressão de ser um header "de modal" em vez do header padrão do app. O <code>extraStyle</code> passado para <code>window._renderBackHeader</code> foi simplificado para apenas <code>top:&lt;topbarHeight&gt;px</code>, deixando todo o resto (posicionamento <code>fixed</code>, <code>left:0/right:0</code>, <code>max-width:1400px</code>, <code>margin:0 auto</code>, <code>padding:10px 2rem</code>, <code>background</code>, <code>backdrop-filter</code>, <code>border-bottom</code>) cair sobre os defaults da classe <code>.sticky-back-header</code> — exatamente o mesmo tratamento dos outros botões Voltar do app (Bracket, Regras, Explorar, Notificações, Criar/Editar Torneio). Resultado: barra plena em todos os breakpoints, Voltar alinhado à esquerda, X de fechar alinhado à direita, consistência visual total. Correção em <code>js/views/tournaments-analytics.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.33-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Barra colorida ao lado do nome da rodada padronizada em todos os torneios + tag BYE só aparece em partida BYE real</b> — (1) <b>Barra colorida:</b> o affordance visual de barra vertical à esquerda do nome da rodada/fase (ex.: <code>OITAVAS DE FINAL</code>, <code>QUARTAS DE FINAL</code>, <code>FINAL</code>, <code>GRAND FINAL</code>) agora é padrão em <b>todo e qualquer torneio</b> — Eliminatórias, Dupla Eliminatória, Grupos, Liga, Suíço, Rei/Rainha. A cor da barra varia pela natureza da fase: <code>primary</code> para rodadas normais, <code>#fbbf24</code> para Final/Grand Final, <code>#f59e0b</code> para 3º lugar, <code>#38bdf8</code> para classificação, <code>#94a3b8</code> para rodadas anteriores, além das cores existentes em Upper/Lower bracket da Dupla Eliminatória. Headers corrigidos: rótulo de rodada na coluna da Dupla Eliminatória (<code>bracket.js:1027</code>), Grand Final (<code>bracket.js:1046</code>), rodada dentro de grupo (<code>bracket.js:1616</code>), "Jogos" no card de grupo (<code>bracket.js:1645</code>), Rodada atual Liga/Suíço (<code>bracket.js:1867</code>), Classificação (<code>bracket.js:1942</code>), Rodadas Anteriores (<code>bracket.js:1990</code>), Estatísticas (<code>bracket.js:2058</code>), Rodada Liga no layout legado (<code>bracket.js:2341</code>). (2) <b>Tag BYE removida para times que venceram:</b> antes, um time que avançava via BYE numa rodada anterior continuava exibindo o badge "BYE" nas rodadas seguintes mesmo após vencer partidas reais (ex.: Bot 14/Bot 17 venceu 6-3 nas oitavas mas ainda aparecia rotulado como "BYE" por ter passado de BYE na primeira rodada). A lógica de carry-forward (<code>_p1Bye</code>/<code>_p2Bye</code> com detecção via <code>_collectAllMatches</code>) foi removida de <code>renderMatchCard</code>. Agora o badge "BYE" aparece <b>apenas</b> na partida BYE real, onde não houve adversário (<code>m.isBye === true</code> ou <code>m.p2 === \'BYE\'</code>) — com o texto "BYE — Avança Direto" no rodapé do card. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>"Minhas Estatísticas" do dashboard consolidada dentro do modal do botão 📊 Estatísticas</b> — Antes o dashboard renderizava dois lugares para ver estatísticas do usuário: (1) o modal que abre pelo botão <code>📊 Estatísticas</code> no hero (via <code>window._showPlayerStats</code>, com partidas casuais vs torneios, títulos, pódios, parceiros, adversários, timings) e (2) a seção colapsável "Minhas Estatísticas" logo abaixo do hero (via <code>window._buildAnalyticsSection</code>, organizador-only, com Torneios, Participantes Únicos, Média por Torneio, Mês Mais Ativo, gráficos Por Formato e Por Esporte). Agora <b>existe um único lugar</b>: o modal do hero. A seção de analytics do organizador foi incorporada ao modal como um bloco próprio (<code>_buildOrganizerAnalyticsForModal</code> em <code>tournaments-analytics.js</code>) que aparece apenas quando o usuário está vendo seu próprio perfil E organizou 2+ torneios — renderizado com os mesmos helpers visuais do modal (<code>_sectionShell</code> + <code>_boxStat</code>), 4 stat-boxes lado-a-lado (Torneios, Participantes Únicos, Média por Torneio, Mês Mais Ativo) e as duas barras horizontais (Por Formato, Por Esporte) em versão compacta. A seção colapsável que existia antes no dashboard foi removida para evitar duplicação. O acesso à <code>window._buildAnalyticsSection</code> permanece exportado no <code>dashboard.js</code> caso algo ainda dependa — apenas não é mais renderizado no layout do dashboard. Correção em <code>js/views/tournaments-analytics.js</code> (novo helper + injection no <code>_showPlayerStats</code>) e em <code>js/views/dashboard.js</code> (remoção do render).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão QR Code removido dos cards de detalhes de torneio</b> — O botão <code>📱 QR Code</code> foi removido das quatro posições em que aparecia no card de detalhes de torneio: (1) linha principal de ações ao lado de Convidar/Compartilhar, (2) grid inferior de ações pós-sorteio (Regras, Inscritos, QR Code, Imprimir, Exportar CSV, Modo TV) em 3 variantes (organizador com sorteio, organizador sem sorteio/sorteio feito, participante com chaveamento). O acesso ao QR Code permanece disponível na página do chaveamento (bracket view). Correção em <code>js/views/tournaments.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Label "RODADA -1" agora aparece como "REPESCAGEM" na Dupla Eliminatória</b> — Quando o organizador escolhia a opção <i>Repescagem</i> para resolver o problema de potência de 2 em um torneio de Dupla Eliminatória, os jogos da repescagem apareciam no Chaveamento Superior com o título "RODADA -1", confundindo o usuário (que relatou não conseguir encontrar a repescagem na tela). Causa raiz: as partidas de repescagem são criadas com <code>round: -1</code> por design (slot entre R1 e R2 — R1 todos jogam, Repescagem é segunda chance para melhores perdedores, R2 recebe vencedores de R1 + qualifiers da Repescagem), mas dois sites de renderização usavam <code>_t(\'bracket.round\', {n: col.round})</code> cegamente, produzindo literalmente "Rodada -1". Correção em três lugares: (1) <code>bracket.js:1028</code> agora prefere <code>col.label</code> do modelo unificado e, como fallback, checa <code>col.round &lt; 0</code> para renderizar "Repescagem" (ou "Repescagem N" para rodadas de repescagem &gt; 1). (2) <code>bracket-model.js:230-237</code> agora gera o <code>label</code> correto quando <code>phase === \'repechage\'</code>, usando a chave i18n <code>bracket.repechage</code>. (3) Novas chaves i18n <code>bracket.repechage</code> e <code>bracket.repechageN</code> adicionadas em <code>i18n-pt.js</code> e <code>i18n-en.js</code>. Verificado no preview: coluna na posição correta (entre Rodada 1 e Rodada 2) com label "Repescagem".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.29-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho Voltar centralizado no modal de criar/editar torneio — fim do Voltar duplicado</b> — Antes o modal de criar/editar torneio renderizava um cabeçalho customizado com botão Voltar próprio no topo do modal, enquanto a view por baixo (<code>.sticky-back-header</code> em z-index <code>1001</code>) continuava visível acima do overlay do modal (z-index <code>1000</code>). O resultado era <b>dois botões Voltar empilhados</b> na tela, o topo da página aparecendo cortado, e funções duplicadas entre os dois cabeçalhos. Agora o modal usa o helper centralizado <code>window._renderBackHeader({onClickOverride, rightHtml})</code> via um placeholder <code>&lt;div id="create-tournament-header-host"&gt;</code>, exatamente o mesmo helper usado pelas views do app. Os botões de ação (Carregar Template 💾, Salvar Template ⭐, Descartar, Salvar) agora vivem no slot <code>rightHtml</code> do back-header — uma única linha horizontal com o Voltar à esquerda e as ações à direita. Um IIFE intercepta <code>openModal</code>/<code>closeModal</code> para suspender todos os <code>.sticky-back-header</code> existentes quando <code>modal-create-tournament</code> abre, e restaurá-los quando fecha — garantindo que apenas UM Voltar apareça na tela. O clique no Voltar do modal chama <code>_discardCreateTournament()</code> (equivalente ao botão Descartar), que por sua vez aciona <code>closeModal()</code> consistentemente — acionando o restore dos back-headers subjacentes. Sintaxe validada via <code>node --check</code> em todos os JS modificados. Correção em <code>js/views/create-tournament.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Campos de Estimativas de Tempo alinhados em uma mesma linha</b> — Os três inputs do box "Estimativas de Tempo" (Chamada dos Jogadores, Aquecimento, Duração Média do Jogo) agora ficam rigorosamente alinhados na mesma baseline, em uma única linha elegante. O label "Chamada dos Jogadores" quebrava em duas linhas por ser mais longo que os demais, empurrando seu input para baixo e desalinhando a linha. Fix: labels ganharam <code>white-space:nowrap; overflow:hidden; text-overflow:ellipsis</code>; o grid ganhou <code>align-items:end</code>; e os inputs receberam <code>width:100%</code>. Correção em <code>js/views/create-tournament.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho do criar/editar torneio: todos os botões em uma linha só, sem Voltar duplicado</b> — O cabeçalho sticky do modal de criação/edição de torneio foi consolidado em uma única linha contendo, da esquerda para a direita: <code>← Voltar</code>, <code>💾 Carregar</code> (template), <code>⭐ Salvar Template</code>, espaçador, <code>Descartar</code>, <code>Salvar</code>. Antes haviam duas linhas com o Voltar em cima e o botão Template na linha de baixo (e não havia um Salvar Template no header de criação — só quando o torneio já existia, via "Ferramentas do Organizador"). Agora: (1) <b>Um único Voltar</b> no cabeçalho do modal — alinhado com todos os outros botões de ação. (2) <b>Novo botão "⭐ Salvar Template"</b> — a qualquer momento durante a criação/edição do torneio o usuário pode salvar a configuração atual como template (nome, esporte, formato, pontuação GSM, categorias, local, duração, modo de inscrição, teamSize). O botão chama <code>window._saveCurrentFormAsTemplate()</code> que lê os valores do form e persiste via <code>window._saveTemplate(template)</code> (Firestore com fallback para localStorage). Respeita o limite de 10 templates do plano Free. (3) <b>Botão "Carregar" (antes chamado "Template")</b> — mesma função de antes, abre o picker de templates salvos para preencher o form. (4) <b>Responsivo:</b> em viewports ≤ 600px os labels dos botões ficam ocultos (só ícones visíveis) para caber tudo em uma única linha sem wrap. Ajustes em <code>js/views/create-tournament.js</code> (header HTML + <code>window._saveCurrentFormAsTemplate</code>) e novas chaves <code>create.loadTemplate</code> / <code>create.saveTemplate</code> / <code>template.loginRequired</code> em <code>i18n-pt.js</code> / <code>i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.26-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Criar/Editar torneio: botões "Descartar" e "Salvar" sempre visíveis no topo</b> — O cabeçalho sticky do modal de criação/edição de torneio agora carrega os três botões de ação em uma única linha no topo: <code>← Voltar</code> à esquerda, e <code>Descartar</code> + <code>Salvar</code> à direita, alinhados horizontalmente e sempre visíveis enquanto o usuário rola o formulário. Antes o botão de salvar ficava numa segunda linha junto ao Template, e não havia um botão explícito de "Descartar" — só o Voltar fazia o mesmo papel implicitamente. Agora: (1) <b>Voltar</b> (pill azul com seta) e <b>Descartar</b> (botão vermelho-ghost) ambos chamam <code>window._discardCreateTournament()</code> que fecha o modal sem salvar — sair sem mudar nada do estado em que estava quando o usuário entrou na página. (2) <b>Salvar</b> (botão azul primário, label curto "Salvar" — não mais "Salvar Torneio") persiste as alterações no Firestore, exibe notificação e fecha o modal (comportamento existente). (3) O botão <code>💾 Template</code> desceu para a segunda linha ao lado do título <i>"Criar Novo Torneio"</i> / <i>"Editar Torneio"</i>, ficando com visual mais discreto (ação secundária). Layout testado em 375px (mobile) e 1280px (desktop) — todos os três botões cabem inline em ambos os breakpoints. Correção em <code>js/views/create-tournament.js</code> (header + <code>window._discardCreateTournament</code>) e novo key <code>btn.discard</code> em <code>i18n-pt.js</code> / <code>i18n-en.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tie-break dispara em 5-5 (não mais em 6-6) + box "Estimativas de Tempo" compacto</b> — Duas mudanças. (1) <b>Tie-break padrão em 5-5:</b> em sets de 6 games, o tie-break agora é acionado quando os jogadores empatam em <code>5-5</code> (não mais em 6-6), com o set finalizando em <code>6-5</code>. É a regra mais comum no Beach Tennis, Padel e Tênis amador. Alterada a lógica de gatilho em três pontos do <code>bracket-ui.js</code> — <code>_trigger</code> no preview dos campos de TB (linha ~739), <code>_tbAt</code> no salvamento das partidas via Set Scoring Overlay (linha ~1059) e <code>tbTrigger</code> no <code>_saveResultInline</code> (linha ~1203) — todos agora calculam <code>gamesPerSet - 1</code>. Isso deixa o código consistente com o label do modal de configuração GSM ("Tie-break em 5-5") e com a lógica de partidas casuais (que já acionava o tie-break em <code>g - 1</code> via <code>tieRule</code> em <code>bracket-ui.js:2493</code>). A tabela de regras do torneio (<code>rules.js</code>) também foi corrigida para mostrar <code>5-5</code> como ponto de gatilho e <code>6-5</code> como placar final após TB. O organizador ainda pode alterar o número de games por set no modal GSM — a regra "TB em (games − 1)" se aplica consistentemente a qualquer valor. (2) <b>Box "Estimativas de Tempo" condensado:</b> os três campos de tempo (Convocação, Aquecimento, Duração da Partida) que ocupavam três linhas empilhadas full-width agora estão lado-a-lado em um grid 3-colunas, com o resumo da duração estimada exibido inline no header do box (em vez de bloco separado abaixo). Mesma funcionalidade, muito menos espaço vertical — especialmente útil no mobile.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Novo Torneio" na dashboard voltou a funcionar</b> — Clicar em <code>🏆 Novo Torneio</code> (ou em qualquer botão que abra <code>modal-quick-create</code>) não fazia absolutamente nada: o modal não aparecia. Causa raiz: <code>window._dismissAllOverlays</code> em <code>store.js</code> (chamado pelo router a cada navegação) faz um sweep genérico de <i>qualquer</i> direct-child do <code>&lt;body&gt;</code> com <code>position:fixed</code>, <code>z-index &gt; 101</code> e bounding-rect &gt; 50% da viewport — tratando-os como "overlays full-screen esquecidos". Mas <code>.modal-overlay</code> é <code>position:fixed</code> com <code>top/left/right/bottom:0</code> (cobre o viewport inteiro) — mesmo com <code>opacity:0</code> e sem a classe <code>.active</code>. Resultado: os modais <code>modal-quick-create</code>, <code>modal-create-tournament</code>, <code>modal-login</code>, <code>modal-profile</code>, <code>modal-result</code> eram TODOS removidos do DOM na primeira navegação após terem sido injetados pelos setup funcions. Quando o botão chamava <code>openModal(\'modal-quick-create\')</code>, <code>getElementById</code> retornava <code>null</code> e o <code>console.warn</code> silencioso era a única pista. Correção: safe-list <code>.modal-overlay</code> no passo 2 do sweep (<code>store.js:429</code>). Modais são gerenciados separadamente no passo 4 via <code>classList.remove(\'active\')</code> — não precisam ser removidos do DOM. Outros botões afetados pela mesma falha (agora também corrigidos): Login, Perfil, Resultado de partida, Inscrição, Criação avançada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.22-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Classificação final: 2º lugar não aparecia mais</b> — No bracket de Eliminatórias, a lista <code>🏅 Classificação (N posições definidas)</code> pulava do 1º direto para o 3º — o 2º lugar (perdedor da final) sumia. Causa raiz: <code>_updateProgressiveClassification</code> em <code>bracket-logic.js</code> processava as rodadas em ordem crescente (R1 → SF → Final). No loop, o loop da Final atribuía diretamente <code>classification[finalLoser] = 2</code>, mas os grupos de posições de rodadas anteriores eram salvos em <code>positionGroups</code> e aplicados <b>depois</b> do forEach. Se o perdedor da final também aparecia como perdedor em uma rodada anterior (dado corrompido — um resultado editado pode deixar o mesmo jogador listado como perdedor em R1 e também avançando para a Final), o <code>positionGroups.forEach</code> sobrescrevia <code>classification[finalLoser]</code> com a posição da rodada anterior (ex: 9º). Correção: rodadas agora são processadas em ordem <b>reversa</b> (Final → SF → QF → R1), com um objeto <code>placed[name]</code> rastreando jogadores já posicionados. A Final define posições 1 e 2 primeiro, depois o 3º lugar (se existir), depois as semis (se não houver 3º lugar), depois cada rodada anterior — sempre pulando qualquer jogador já em <code>placed</code>. Bônus: a aplicação canônica de 1/2/3/4 do 3º lugar no final do processamento continua para garantir precedência absoluta. Correção em <code>js/views/bracket-logic.js</code> (<code>_updateProgressiveClassification</code>, ~linha 740).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Topbar + hamburger + Voltar: layout do topo centralizado e sempre pinado</b> — O menu hamburger e a barra "Voltar" agora ficam fixos no topo da viewport e nunca saem de vista com o scroll. Três mudanças relacionadas. (1) <b>Dropdown do hamburger agora é <code>position: fixed</code>:</b> antes era <code>position: relative</code>, então quando o usuário já tinha rolado a tela e abria o menu, o dropdown aparecia DENTRO do fluxo do documento (posição original) — a centenas de pixels acima da viewport visível. Agora o dropdown é <code>fixed</code> com <code>top</code> calculado dinamicamente para colar logo abaixo da topbar, independente de scroll. (2) <b>Layout unificado via <code>_reflowChrome()</code>:</b> a posição de topbar + dropdown + back-header era gerenciada por duas funções diferentes (<code>_syncBackHeaderSpacer</code> e <code>_adjustBackHeaderForHamburger</code>) que se desincronizavam e calculavam offsets a partir de estado obsoleto. Substituídas por uma única função <code>window._reflowChrome()</code> que mede a topbar, posiciona o dropdown (se aberto) logo abaixo, e posiciona todas as <code>.sticky-back-header</code> logo abaixo do último elemento de chrome. É o único ponto que toca os offsets do topo. Alias <code>_syncBackHeaderSpacer = _adjustBackHeaderForHamburger = _reflowChrome</code> mantidos para compatibilidade. (3) <b>Triggers completos:</b> <code>_reflowChrome()</code> é chamado em <code>resize</code>, <code>scroll</code> (passive), <code>ResizeObserver</code> em topbar + dropdown + cada back-header, <code>MutationObserver</code> em <code>#view-container</code> (render de nova view), abrir/fechar hamburger (com rAF duplo para medir após paint) e no boot inicial. Efeito: nenhuma ordem de ações — scroll primeiro, depois abrir menu / abrir menu primeiro, depois scrollar / navegar para outra view / redimensionar a janela — deixa o dropdown ou o Voltar escondidos ou fora de posição. Correções em <code>js/store.js</code> (<code>_reflowChrome</code>, <code>_toggleHamburger</code>, <code>_closeHamburger</code>, observer block, scroll listener) e <code>css/components.css</code> (<code>.hamburger-dropdown</code> → <code>position:fixed</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Voltar no chaveamento (e em todas as views) corrigido de uma vez por todas</b> — O Voltar continuava "quebrado" mesmo após a v0.14.19: clicar no botão mudava o hash corretamente, mas o usuário aterrissava parado na posição 623px do meio da tela anterior, como se o Voltar não tivesse feito nada. Três causas raiz foram resolvidas. (1) <b>Scroll restoration do navegador competindo com o router:</b> o Chrome/Safari aplicam <code>history.scrollRestoration = \'auto\'</code> por padrão, restaurando a posição salva da URL AO hashchange — depois dos <code>window.scrollTo(0,0)</code> do router. Agora <code>initRouter()</code> começa com <code>history.scrollRestoration = \'manual\'</code>, desabilitando a restauração automática globalmente. (2) <b>Visitor CTA auto-scroll re-disparando:</b> em <code>tournaments.js</code> havia um <code>setTimeout(500ms)</code> que fazia <code>ctaEl.scrollIntoView({behavior:\'smooth\'})</code> sempre que um visitante não-logado abria o detalhe de um torneio — inclusive quando o usuário voltava via Voltar. O smooth scroll animava até ~623px DEPOIS do jump-to-top do router, matando o efeito. Agora o scroll é idempotente via <code>sessionStorage[\'_ctaScrolled_\' + tournamentId]</code>: dispara só na primeira visita ao torneio dentro da sessão. (3) <b>Centralização definitiva do Voltar:</b> todas as views (bracket, tournament detail, participants, pre-draw, rules, explore, notifications, player stats) já chamavam <code>window._renderBackHeader(...)</code>, mas o clique usava uma string inline de <code>onclick</code> construída por concatenação — frágil a bugs de escaping (ids com aspas/barras quebravam a string JS inteira e o botão virava silenciosamente no-op). Reescrito: o botão agora carrega atributos <code>data-back-nav</code>, <code>data-back-href</code> e opcionalmente <code>data-back-cb</code>/<code>data-back-inline</code>; um único listener delegado em <code>document.body</code> (capture-phase) atende todos os cliques de Voltar do app. Há UM caminho de clique para manter. Overrides via função agora são suportados além do string legacy (registro em <code>window._backNavCallbacks</code>). Ganhou também um fallback: se <code>href</code> coincidir com o hash atual, o listener alterna via <code>#dashboard</code> e volta para forçar re-render. Correções em <code>js/router.js</code>, <code>js/store.js</code> (<code>_renderBackHeader</code> + <code>_installBackNavDelegate</code>) e <code>js/views/tournaments.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Voltar que parecia quebrado + numeração de jogos contínua Suíço→Eliminatória + Ocultar/Mostrar para rodadas Suíças concluídas</b> — Três correções relacionadas ao bracket. (1) <b>Voltar quebrado:</b> ao clicar Voltar em qualquer view (bracket → tournament detail, participants → tournament detail, etc) o <code>router.js</code> chamava apenas <code>window.scrollTo({top:0, behavior:\'smooth\'})</code>. O smooth scroll era cancelado por layouts shifts da própria view nova (ex: <code>_buildSwissPastColumns</code> + <code>scrollIntoView</code> de cards) e o usuário aterrissava no meio da página — parecendo que o Voltar não tinha funcionado. Agora o jump-to-top usa <code>window.scrollTo(0,0)</code> instantâneo + <code>documentElement.scrollTop=0</code> + <code>body.scrollTop=0</code>, repetido em <code>rAF</code>, <code>setTimeout 50</code>, <code>150</code> e <code>350ms</code> — sobrevive a qualquer scroll-into-view tardio. O padrão antes existia só para o case <code>dashboard</code>; agora aplica a todas as navegações. (2) <b>Numeração contínua:</b> em torneios Suíço que avançam para a eliminatória, tanto as colunas Suíças passadas quanto as colunas Eliminatórias começavam a contar "Jogo 1". Havia dois Jogo 1, dois Jogo 2, etc — o usuário não conseguia se referir a um jogo unicamente pelo número. Agora a eliminatória continua a contagem do Suíço: se o Suíço teve 20 jogos (R1 + R2), as Oitavas começam em Jogo 21, Quartas em 29, Semis em 33, 3º Lugar em 35, Final em 36. Implementado via <code>swissPastMatchOffset</code> (soma das <code>matches.length</code> das colunas <code>phase === \'swiss-past\'</code>) aplicado a <code>globalMatchNum</code> inicial, <code>thirdPlaceMatchNum</code> e <code>finalMatchNum</code> em <code>renderSingleElimBracket</code>. (3) <b>Ocultar/Mostrar para rodadas Suíças concluídas:</b> as colunas "RODADA SUIÇA N/M ✓" ganharam botão "Ocultar" individual no header (como as colunas Eliminatórias) e pill vertical <code>◀ Mostrar Suíço (N)</code> sticky à esquerda do scroll quando há rodadas ocultas. Estado persistido em <code>window._hiddenSwissPast[tId]</code> Set separado de <code>window._hiddenRounds</code> para evitar colisão de números de rodada entre Suíço e Eliminatória (ambas podem ter R1, R2). Helpers <code>window._toggleSwissPastVisibility(tId, roundNum)</code> e <code>window._showAllHiddenSwissPast(tId)</code> em <code>bracket-ui.js</code> (LIFO, igual ao do elim). Correções em <code>js/router.js</code>, <code>js/views/bracket.js</code> e <code>js/views/bracket-ui.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rodadas Suíças concluídas mantêm os mesmos match cards de quando estavam em andamento, agora que o torneio passou para a eliminatória</b> — Quando um torneio com classificação Suíça avança para a fase eliminatória (<code>t.currentStage === \'elimination\'</code>), as colunas "RODADA SUIÇA N/M ✓" à esquerda do scroll horizontal do bracket eram renderizadas por <code>_buildSwissPastColumns</code> em um layout compacto próprio: uma linha por jogo com <code>jogador1</code> · <code>placar</code> · <code>jogador2</code> em ellipsis, sem avatar, sem cabeçalho "Jogo N", sem badge de vencedor — totalmente diferente dos match cards que o usuário vinha vendo durante o Suíço. Agora <code>_buildSwissPastColumns</code> usa <code>renderMatchCard(m, false, t.id, matchNum)</code> (com <code>canEnterResult=false</code> para ficar read-only) — as mesmas colunas que <code>_completedColsHtml</code> renderiza durante a fase de classificação. Resultado: as rodadas Suíças concluídas mantêm exatamente a apresentação visual que tinham enquanto estavam ativas (avatar, nome, score, "Jogo N", borda verde no vencedor, "🏆 troféu" etc), só que sem os inputs de placar. Correção em <code>js/views/bracket.js</code> (função <code>_buildSwissPastColumns</code>, ~linha 1697).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Suíço: caixa "🎯 Fase Classificatória" agora vem acima de "Progresso do Torneio" no topo do bracket</b> — Ao abrir o chaveamento de um torneio com fase Suíça, o usuário via primeiro a barra "PROGRESSO DO TORNEIO — X/Y partidas (Z%)" e só depois o banner azul "🎯 Fase Classificatória — Suíço — N rodadas → 🏆 Fase Eliminatória (Top M)". A ordem foi invertida: agora o banner contextual aparece primeiro (explicando o formato e o corte para a eliminatória) e a barra de progresso vem logo abaixo, antes da tabela de classificação. O <code>progressBarHtml</code> gerado no header do bracket agora é passado como quinto parâmetro de <code>renderStandings(t, isOrg, canEnterResult, readyBannerHtml, progressBarHtml)</code> e injetado entre <code>_phaseBannerHtml</code> e <code>standingsTablesHtml</code>, mantendo o mesmo HTML em ambos os layouts (coluna-por-rodada de Suíço e stacked de Liga/Ranking). Vale só para a view de classificação — outras views (grupos, monarch, eliminatórias puras) continuam com a barra de progresso no topo, pois não têm o banner de fase classificatória. Correção em <code>js/views/bracket.js</code> (linha 186 e assinatura de <code>renderStandings</code> + retorno no final).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Voltar agora sobrevive a qualquer overlay esquecido + pill "◀ Mostrar" segue o scroll</b> — Dois ajustes relacionados. Primeiro, o <code>_dismissAllOverlays()</code> da v0.14.15 só removia 6 overlays nomeados, mas o app tem 40+ pontos de criação de overlay em z-index 9999–999999 (host-transfer, re-auth, categorias, draw-prep, confirmação de alterações, etc). Como <code>.sticky-back-header</code> vive em z-index <b>101</b>, qualquer overlay órfão passava por cima dele — o usuário via o Voltar mas o clique era absorvido pelo overlay invisível. Agora o sweep é genérico: varre todo filho direto do <code>&lt;body&gt;</code> que seja <code>position:fixed</code>, com z-index &gt; 101 e com bounding box cobrindo mais de 50% da viewport, pulando uma safe-list (topbar, hamburger-dropdown, sticky-back-header, notification-banner, toast-notification, offline-banner, view-container, skip-link, aria-live-region, qualquer ID começando com <code>notification</code> ou <code>toast</code>). O router chama esse sweep em todo <code>hashchange</code>, e o próprio onclick do Voltar chama antes de setar o hash — então o overlay morre no mesmo clique. Segundo, em torneios Suíço/Eliminatórias com rodadas ocultas, o pill vertical <code>◀ Mostrar (N)</code> no canto esquerdo do scroll do chaveamento agora usa <code>position:sticky;top:120px</code>: acompanha o scroll vertical e fica sempre visível abaixo do topbar + back-header, seja no topo, no meio ou no pé do bracket. Correção em <code>js/store.js</code> (<code>_dismissAllOverlays</code>) e <code>js/views/bracket.js</code> (pill Eliminatórias ~linha 748 e pill Suíço coluna-por-rodada ~linha 2189).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Voltar unificado — fim dos travamentos após TV mode, placar por sets e overlays</b> — O botão "Voltar" que fica fixo no topo (<code>.sticky-back-header</code>) tinha 10 implementações espalhadas por <code>tournaments.js</code>, <code>bracket.js</code>, <code>participants.js</code>, <code>rules.js</code>, <code>pre-draw.js</code>, <code>explore.js</code>, <code>notifications-view.js</code> e <code>tournaments-analytics.js</code>, cada uma com seu próprio SVG, onclick e padding. Pior: overlays fullscreen de alto z-index (<code>#tv-mode-overlay</code> z 99999, <code>#set-scoring-overlay</code> z 100001, <code>#qr-modal-overlay</code>, <code>#player-stats-overlay</code>, <code>#scan-qr-overlay</code>) podiam sobreviver a uma navegação hash e passar a cobrir o Voltar, deixando o usuário travado ("o botão para de funcionar"). Reescrita unificada: novo helper <code>window._renderBackHeader(opts)</code> em <code>store.js</code> emite o HTML canônico, e <code>window._dismissAllOverlays()</code> remove todos os overlays de alto z conhecidos + sai de fullscreen + limpa <code>.modal-overlay.active</code>. O router (<code>router.js</code>) chama <code>_dismissAllOverlays()</code> em todo <code>hashchange</code>, e o próprio onclick do Voltar chama o dismiss antes de setar o hash — então qualquer overlay órfão morre no mesmo clique que navega. As 10 call sites foram migradas para o helper: rules, pre-draw, explore, notifications-view, tournaments (list e detail), participants (com título + badges + barra de check-in via <code>middleHtml</code>/<code>rightHtml</code>/<code>belowHtml</code>), bracket (com nav de grupos + toggle "Só meus jogos"), tournaments-analytics (modal player-stats com close ×). Correção em <code>js/store.js</code>, <code>js/router.js</code> e nos 8 arquivos de view.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "◀ Mostrar" do chaveamento agora revela uma rodada por clique (LIFO), não todas de uma vez</b> — Ao ocultar múltiplas rodadas concluídas (seja em Suíço/Liga ou em Eliminatórias), o pill vertical <code>◀ Mostrar (N)</code> no canto esquerdo do scroll estava chamando <code>_toggleRoundVisibility(tId, maxHiddenRound)</code>, que internamente executa <i>"unhide this round AND all rounds before it"</i>: um único clique desempilhava todas as rodadas ocultas de uma vez, ignorando a contagem do pill. Corrigido para usar <code>_showAllHiddenRounds(tId)</code> (cujo comentário já declara "Revela apenas a rodada oculta mais recente a cada clique"), que remove apenas a rodada de maior número do <code>window._hiddenRounds[t.id]</code> Set por clique. Ciclo agora: hide R1 → hide R2 → pill "◀ Mostrar (2)" → clique revela R2 → pill "◀ Mostrar (1)" → clique revela R1 → pill some. Aplica-se ao bracket de Eliminatórias (linha 748) e ao bracket de Suíço coluna-por-rodada (bloco <code>useColumnLayout</code>). O botão "Ocultar" individual de cada rodada continua usando <code>_toggleRoundVisibility</code> — é o pill de "desempilhamento" que precisava ser LIFO. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Suíço/Liga: rodadas concluídas voltam a aparecer como colunas no scroll do chaveamento, com botão "Ocultar"</b> — Em torneios Suíço (e Eliminatórias com fase classificatória suíça), as rodadas já concluídas estavam sendo recolhidas em um card <code>📜 Rodadas Anteriores (N)</code> no rodapé, isolado do scroll horizontal principal. Isso fugia do padrão usado pelas Eliminatórias, onde cada rodada anterior aparece como uma coluna à esquerda da rodada corrente e traz um botão <code>Ocultar</code> no header. Agora o comportamento é unificado: cada rodada suíça concluída é renderizada como coluna normal do scroll horizontal (com os mesmos cards reais de partida, já com placar/vencedor), à esquerda da rodada em andamento, e cada coluna traz um botão <code>Ocultar</code> wired em <code>window._toggleRoundVisibility(tId, roundNum)</code>. Ocultar uma rodada apenas a remove visualmente — o estado é mantido em <code>window._hiddenRounds[t.id]</code> (Set) compartilhado com o bracket eliminatório. Quando há rodadas ocultas, um pill vertical <code>◀ Mostrar (N)</code> aparece no canto esquerdo do scroll (mesmo padrão do Eliminatórias, ~linha 748 em <code>bracket.js</code>). O card colapsado de "Rodadas Anteriores" foi removido do layout de colunas para evitar duplicação (o layout legado stacked-card de Liga/Ranking/Rei-Rainha continua usando o card colapsável, pois não tem colunas). Correção em <code>js/views/bracket.js</code> (bloco <code>useColumnLayout</code> dentro de <code>renderStandings</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga/Suíço: reordenação da view e reagrupamento do banner "Jogos Prontos"</b> — Na view de classificação de torneios Liga e Suíço, a ordem dos blocos foi alterada para priorizar a informação de contexto e deixar o banner de jogos prontos mais acionável. Antes: (1) banner "Jogos Prontos" misturado no topo, (2) classificação, (3) caixa azul "Fase Classificatória — Suíço… → Fase Eliminatória". Agora: (1) caixa azul "Fase Classificatória" primeiro, (2) classificação logo abaixo, (3) banner "Jogos Prontos". Além disso, o próprio banner foi reagrupado: em vez de uma grade única misturando cards verdes (ambos presentes) e âmbar (presença parcial), agora a frase "X pronto(s) para chamar" vem seguida apenas dos cards verdes, e depois a frase "Y aguardando presença" vem seguida apenas dos cards âmbar — fica visualmente claro qual jogo pode ser chamado imediatamente e qual ainda espera. A função <code>renderStandings</code> passou a aceitar um quarto parâmetro <code>readyBannerHtml</code> injetado entre a tabela de classificação e os cards de rodadas, e <code>_renderReadyMatchesBanner</code> passou a emitir duas grades separadas com seus próprios headers. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Banner "Jogos Prontos" volta para o topo do chaveamento e passa a cobrir Liga/Suíço/Grupos/Monarch</b> — Na v0.8.24 (abr/2026) o banner de jogos prontos para chamar foi removido do bracket e deixado apenas na Lista de Presença. Voltou ao topo do chaveamento (acima de classificação/grupos/chaves) em todos os formatos. Além disso a função <code>_renderReadyMatchesBanner</code> tinha um early-return em <code>!t.matches</code>, que silenciava o banner inteiro em torneios Liga/Suíço (matches ficam em <code>t.rounds[].matches</code>) e Fase de Grupos/Rei-Rainha (em <code>t.groups[].rounds[].matches</code>). Agora usa <code>window._collectAllMatches(t)</code> para iterar todas as estruturas. O banner lista em verde os jogos com ambos os lados 100% presentes ("pronto para chamar") e em âmbar os jogos com pelo menos um presente mas não todos ("aguardando presença"). Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Presença: rótulo "Jogo N" colorido conforme presença dos jogadores daquela partida</b> — Na lista de check-in (<code>js/views/participants.js</code>), o rótulo "Jogo N" que aparece ao lado do nome de cada participante passa a refletir visualmente o status de presença da partida inteira, não apenas do indivíduo. Regra: se todos os jogadores envolvidos na partida (time + adversário) já marcaram "Presente", o rótulo fica <span style="color:#4ade80;font-weight:800;">verde</span> (#4ade80); se pelo menos um mas não todos, fica <span style="color:#fbbf24;font-weight:800;">âmbar</span> (#fbbf24); se nenhum, fica no cinza padrão de opacidade 0.55. Assim, o organizador bate o olho e sabe quais jogos já estão prontos para começar. A lista de membros é coletada de <code>ind.teamName</code> (split por <code>/</code>) + <code>ind.opponent</code>, deduplicada, e cruzada com o mapa <code>checkedIn</code>. Correção em <code>js/views/participants.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera: badge "PRÓXIMO" removido e nome completo do participante sempre legível</b> — Dois ajustes visuais na Lista de Espera (<code>_renderStandbyPanel</code>, <code>js/views/bracket.js</code>). Primeiro, o badge laranja "PRÓXIMO" que aparecia ao lado do primeiro da fila foi removido — a ordem continua sendo preservada pelo número circulado à esquerda e pela borda lateral dourada do primeiro item, então o badge era redundante e apenas roubava espaço horizontal. Segundo, o nome do participante deixou de ser truncado: o span do nome não tem mais <code>overflow:hidden</code>, <code>text-overflow:ellipsis</code> nem <code>white-space:nowrap</code>. Agora usa <code>min-width:0</code> + <code>word-break:break-word</code> + <code>overflow-wrap:anywhere</code>, permitindo que nomes longos (ou emails) quebrem em linhas extras dentro do próprio item em vez de virarem "Bot..." ou "João da Silva Pere...". A altura do item cresce conforme necessário. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera unificada: seção "Substituição" removida, botão "Assumir" por participante, painel visível em todos os formatos</b> — Dois ajustes combinados na Lista de Espera (<code>_renderStandbyPanel</code>, <code>js/views/bracket.js</code>). Primeiro, a subseção "Substituição" inteira foi apagada: o cabeçalho, o texto descritivo e o botão agregado "🔄 Substituir W.O. pelo próximo presente" não existem mais. O único controle de substituição agora é o botão "🔄 Assumir" que aparece ao lado de cada participante presente na lista quando há slot de W.O. aberto — mais direto e sem duplicação de ação. Segundo, o painel passou a ser renderizado ao final de <i>todos</i> os caminhos do bracket (Liga/Suíço, Fase de Grupos, Rei/Rainha, Eliminatórias simples e dupla), não apenas nas Eliminatórias. Antes, em torneios Liga/Suíço/Grupos/Monarch a Lista de Espera aparecia só na tela de check-in (<code>participants.js</code>); agora também aparece na view do chaveamento/classificação, facilitando ao organizador ver quem está na lista sem trocar de aba. A declaração <code>const standbyHtml = _renderStandbyPanel(t, isOrg)</code> foi movida para antes do branching de formato, e cada <code>container.innerHTML = ...</code> tem <code>+ standbyHtml</code> appended ao final. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: Lista de Espera volta a renderizar mesmo se a detecção de W.O. falhar</b> — Em v0.14.6 a detecção de slot W.O. foi trocada para <code>window._collectAllMatches(t)</code> a fim de cobrir Suíço/Liga/Grupos. Mas quando o torneio tinha estruturas inesperadas em alguma partida (valores não-string em <code>m.p1/m.p2</code>, entradas <code>null</code> em arrays, shapes legadas), a chamada a <code>name.includes(\' / \')</code> estourava um TypeError síncrono e, como o bloco não estava em try/catch, derrubava <i>toda</i> a função <code>_renderStandbyPanel</code> — resultado: até a Lista de Espera desaparecia. Agora o bloco de detecção de W.O. está encapsulado em try/catch (com guard extra <code>typeof name === \'string\'</code> antes de <code>.includes</code>), de modo que qualquer glitch no coleta-e-escaneia cai em zero candidatos e a lista continua sendo renderizada normalmente. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: botão "🔄 Assumir" na Lista de Espera agora aparece em torneios Suíço/Liga/Grupos</b> — A v0.14.3 havia adicionado um botão <code>🔄 Assumir</code> por participante presente na lista de espera, visível apenas quando existe um slot de W.O. aberto no bracket. Mas o detector de W.O. (<code>hasWOSlot</code> em <code>_renderStandbyPanel</code>, <code>js/views/bracket.js</code>) olhava somente <code>t.matches</code> — o array de partidas achatado usado por Eliminatórias/Dupla Eliminatória. Em torneios cujas partidas ficam em <code>t.rounds[].matches</code> (Suíço, Liga, Ranking) ou em <code>t.groups[].rounds[].matches</code> (Fase de Grupos), nenhuma partida era encontrada e o botão nunca aparecia, mesmo com W.O. declarado pelo organizador. Agora a detecção usa <code>window._collectAllMatches(t)</code> (mesma função usada pelo próprio <code>_autoSubstituteWO</code>), que cobre todas as estruturas (flat, rounds, groups, rodadas, thirdPlaceMatch). Como bônus, o botão agregado "🔄 Substituir W.O. pelo próximo presente" também volta a aparecer nesses torneios, já que depende do mesmo <code>woPlayers[]</code>. Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: "RODADA SUIÇA N/M" agora aparece no header do bracket, não apenas no column-model</b> — A v0.14.4 introduziu o rótulo <code>RODADA SUIÇA N/M</code> em <code>js/views/bracket-model.js</code>, mas o cabeçalho real da rodada corrente no bracket é renderizado em <code>js/views/bracket.js</code> (linhas 1858, 2176, 2204, 2284), que usa <code>_t(\'bracket.round\', {n})</code> + concatenação manual de <code>/ maxRounds</code>. Resultado: o usuário via "RODADA 1 / 2" mesmo em torneios Eliminatórios com fase suíça. Agora <code>bracket.js</code> calcula <code>isSwissQualifier = isSuico && t.format !== \'Suíço\' && t.format !== \'Suíço Clássico\' && !isLigaFmt</code> no topo de <code>renderBracket</code> e, quando verdadeiro, usa o helper <code>_swissQualifierLabel(n)</code> (que traduz <code>bracket.swissRoundFull</code>) em todos os 4 pontos de renderização — header do card principal, coluna atual do scroll, colunas de rodadas futuras (TBD) e cards legados de rodadas pendentes. Torneios puramente Suíço/Liga/Ranking continuam com o rótulo simples "Rodada N / M". Correção em <code>js/views/bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rodadas Suíças de classificação agora marcadas "RODADA SUIÇA N/M" em torneios eliminatórios</b> — Em torneios com formato "Eliminatórias", "Dupla Eliminatória" ou "Fase de Grupos + Eliminatórias" que usam Suíço como fase classificatória (Swiss-as-qualifier), as rodadas suíças eram exibidas apenas como "Rodada 1", "Rodada 2" no bracket, indistinguíveis das rodadas eliminatórias que viriam depois. Agora, em tais torneios, as rodadas suíças recebem o rótulo "RODADA SUIÇA N/M" (e.g., "RODADA SUIÇA 1/2", "RODADA SUIÇA 2/2") — onde M vem de <code>t.swissRounds</code> (o total configurado pelo organizador). Isso vale tanto para rodadas ativas (<code>t.rounds[]</code>, em <code>_buildSwissColumns</code>) quanto para rodadas históricas já consumidas (<code>t.swissRoundsData[]</code>, em <code>_buildSwissPastColumns</code>). Torneios "Suíço", "Suíço Clássico", "Liga" e "Ranking" continuam com o rótulo simples "Rodada N", já que a fase suíça é o próprio torneio. Helper novo <code>_isSwissQualifierTournament(t)</code> e chaves i18n <code>bracket.swissRoundFull</code> em pt (<i>RODADA SUIÇA N/M</i>) e en (<i>SWISS ROUND N/M</i>). Correção em <code>js/views/bracket-model.js</code>, <code>js/i18n-pt.js</code>, <code>js/i18n-en.js</code>.</p>' +
        '<p><b>Estatísticas Detalhadas agora somam torneios jogo a jogo, persistentes mesmo após exclusão</b> — Até a v0.14.3, somente partidas casuais entravam em <code>users/{uid}/matchHistory</code>. Resultados de torneios lançados pelo scoring inline (botão "Confirmar" no card de partida) ou pelo overlay GSM set-a-set atualizavam <code>m.scoreP1/scoreP2/sets</code> no torneio mas não gravavam um registro independente no histórico do jogador — de modo que, se o torneio fosse apagado ou encerrado, os dados evaporavam da aba "📊 Estatísticas Detalhadas" no perfil. Agora ambos os caminhos (<code>window._saveResultInline</code> e <code>window._saveSetResult</code> em <code>js/views/bracket-ui.js</code>) chamam dois helpers novos — <code>_persistInlineTournamentMatchRecord</code> e <code>_persistGSMTournamentMatchRecord</code> — que constroem um record mínimo compatível com <code>_renderPersistentMatchStats</code> (matchType=<code>tournament</code>, players com uid/photoURL resolvidos de <code>t.participants</code>, winnerTeam, sets com tiebreak, stats.team1/team2 com games e sets) e gravam uma cópia em cada <code>users/{uid}/matchHistory/{recordId}</code> via <code>FirestoreDB.saveUserMatchRecords</code>. Como inline scoring não tem pointLog/gameLog, as métricas por ponto (holds, breaks, deuces, streaks, time-per-point) ficam em zero — mas vitórias, derrotas, saldo de sets e saldo de games passam a somar normalmente entre casual e torneio, sobrevivendo à exclusão do torneio. Também simplificada a legenda abaixo de "📊 Estatísticas Detalhadas" para <i>"Partidas casuais ⚡ e torneios 🏆."</i> em <code>js/views/tournaments-analytics.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera: botão "Assumir" por participante para substituir jogador com W.O.</b> — Antes, a substituição de um jogador declarado W.O. dependia de um único botão agregado no painel de Lista de Espera, que escolhia automaticamente o primeiro presente da fila. Agora cada participante presente na lista de espera tem seu próprio botão <code>🔄 Assumir</code> (ao lado dos botões de presença e W.O.), visível para o organizador quando há ao menos um slot de W.O. disponível. Isso permite ao organizador decidir QUEM assume a posição do ausente (útil quando o próximo da fila não é quem deveria substituir). A função <code>window._autoSubstituteWO(tId, overrideReplacementName)</code> agora aceita um segundo argumento opcional que força uso de um substituto específico; o botão agregado continua disponível e usa o primeiro presente quando nenhum override é passado. Correção em <code>js/views/bracket.js</code> (painel de Lista de Espera) e <code>js/views/bracket-ui.js</code> (substituição com override).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in ainda mais compacto: card em 2 linhas com "Jogo N" colado ao nome</b> — A v0.14.1 já havia reduzido o card para 2 linhas no desktop, mas o rótulo <code>Jogo N</code> ficava em coluna separada e os times desciam uma linha abaixo do nome, produzindo um espaço morto abaixo da identificação do jogador. Agora o rótulo <code>Jogo N</code> é impresso imediatamente à direita do nome (sem ocupar coluna do grid), e o bloco de jogadores sobe para ancorar em <code>align-items:start</code> — o nome+Jogo passa a compartilhar a primeira linha vertical com o time 1, e o time 2 se encaixa logo abaixo, sem desperdiçar linha. Grid reduzido de 4 para 3 colunas (<code>auto 1fr auto</code>): célula 1 = nome+Jogo, célula 2 = stack vertical dos times, célula 3 = "vs". No mobile (&lt;768px) o fallback continua colocando nome em cima e matchup abaixo para evitar truncamento. Correção em <code>js/views/participants.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.14.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in no desktop: card encolhe para 2 linhas alinhando o nome do jogador com o time 1</b> — A v0.12.101 já empilhava os times, mas o nome do jogador ficava em uma linha própria no topo, totalizando 3 linhas visuais por card (nome / time1+vs / time2). Agora, em telas ≥768px, o nome vai para a mesma linha do time 1 e do rótulo "vs", reduzindo o card para apenas 2 linhas (linha 1: <code>Bot 03 &nbsp; Jogo 2 &nbsp; ● Bot 03 / ● Bot 05 &nbsp; vs</code>; linha 2: <code>● Bot 12 / ● Bot 07</code>). A coluna central do grid mantém o alinhamento vertical dos jogadores (Bot 03 acima de Bot 12, Bot 05 acima de Bot 07). Em telas <768px, o fallback preserva o nome em linha própria com o bloco de matchup abaixo, evitando truncamento em mobile. Implementação via grid de 4 colunas (<code>auto auto auto auto</code>) com <code>align-items:center</code>, detecção de viewport em JS (<code>window.innerWidth &lt; 768</code>). Correção em <code>js/views/participants.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.101-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in: "vs" volta para a linha do time 1 e players alinhados verticalmente entre os dois times</b> — A v0.12.100 empilhou os times mas colocou o "vs" em uma linha própria entre eles, gastando uma terceira linha vertical. Voltamos ao padrão mais denso e elegante: <code>Jogo X &nbsp; Bot yy / Bot zz &nbsp; vs</code> em uma linha, <code>Bot aa / Bot bb</code> em outra — com Bot yy diretamente acima de Bot aa e Bot zz diretamente acima de Bot bb. Layout implementado via CSS grid de 3 colunas (<code>auto auto auto</code>, <code>align-items:start</code>): célula esquerda = "Jogo N", célula central = stack vertical dos dois times (flex column), célula direita = "vs" ancorado no topo. Como a célula do grid mede pelo conteúdo máximo, os dois times compartilham a mesma métrica de largura e os jogadores caem alinhados coluna a coluna sem precisar de <code>&lt;table&gt;</code>. Quando não há adversário (standby puro), a grid vira duas colunas sem "vs". Correção em <code>js/views/participants.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.100-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in mais elegante: times empilhados verticalmente com separador <i>vs</i></b> — Na tela "Torneio em andamento — marque a presença abaixo", cada card de participante mostrava o confronto em uma única linha inline ("Jogo 2 • Bot 03 / Bot 07 vs • Bot 12 / Bot 05"), o que ficava apertado e dificultava a leitura quando os nomes eram longos. Agora o bloco de informações é dividido verticalmente: (1) header compacto com "Jogo N" (e, se aplicável, tag "Lista de Espera"), (2) time do próprio participante em uma linha, (3) separador <i>vs</i> em itálico maiúsculo discreto, (4) time adversário em outra linha. Cada dupla/jogador continua com as bolinhas de status de presença (verde/vermelho/cinza). Quando não há adversário (standby puro, ausência sem substituto, etc.), exibe apenas o time disponível sem o <i>vs</i>. Correção em <code>js/views/participants.js</code> (~linha 617): variável <code>vsLine</code> removida, lógica de montagem reescrita para produzir bloco <code>flex-direction:column</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.99-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auto-saneamento da lista de espera quando inscrições estão abertas</b> — A v0.12.96 passou a drenar <code>t.standbyParticipants</code> e <code>t.waitlist</code> no momento em que o organizador clicava "Reabrir Inscrições". Mas torneios cujo reopen aconteceu antes desse fix, ou cujo caminho de reopen foi disparado por outra rota (importação, edição direta no Firestore, rollback manual), continuavam com entradas residuais — o stat-box "LISTA DE ESPERA" persistia exibindo <code>1</code> ou mais mesmo com as inscrições claramente abertas. Agora um helper idempotente <code>window._drainWaitlistsIfOpen(t, { save: true })</code> em <code>tournaments.js</code> roda no próprio render da página de detalhes do torneio: se o torneio está realmente aberto (<code>status !== "closed"</code>, <code>status !== "finished"</code>, sem sorteio/bracket/rounds/groups) e tem qualquer coisa em <code>standbyParticipants</code> ou <code>waitlist</code>, promove tudo para <code>participants</code> (dedupe por email/uid/displayName/name), zera as duas listas, persiste no Firestore e loga em <code>AppStore.logAction</code>. A verificação é gratuita quando as listas já estão vazias (early-return). Quando o organizador encerrar novamente as inscrições, o fluxo de stand-by volta a popular normalmente — reavalia do zero. Correção em <code>js/views/tournaments.js</code> (helper no topo + chamada após o bloco de auto-close, antes do cálculo de <code>statusText</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.98-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Toasts deduplicados e limitados a 3 simultâneos</b> — Usuários relatavam pilhas de 3+ toasts idênticos ("Inscrito!", "Sucesso") aparecendo em sequência quando o mesmo evento disparava múltiplos fluxos (auto-enroll + notificação do organizador + re-render do dashboard). Isso enchia a tela e acabava não comunicando nada. Agora <code>showNotification(title, message, type)</code> em <code>notifications.js</code> faz duas coisas: (1) <b>dedup</b> — se já existe um toast visível com a mesma chave <code>type|title|message</code>, incrementa um contador exibido como badge <code>×N</code> no título e reseta o timer de auto-dismiss, em vez de empilhar outro card; (2) <b>cap</b> — mantém no máximo 3 toasts visíveis no container; ao adicionar um quarto, os mais antigos saem imediatamente (slide-out + remoção). Cada toast carrega <code>data-toast-key</code>, <code>data-toast-count</code> e uma referência ao próprio timer (<code>_dismissTimer</code>) para permitir que o dedup reinicie o countdown. Sem mudanças em call sites — a política nova vale para todas as ~200+ chamadas de <code>showNotification</code> no app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.97-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>BYE não aparece mais como jogo na rodada atual; tag BYE mostrada no time que avançou na próxima rodada</b> — Usuários relataram que, em torneios com número de inscritos não-potência-de-2, a rodada 1 exibia um "JOGO N" com o rótulo <code>BYE — Avança Direto</code>, poluindo a listagem de jogos realmente disputados. Havia um filtro em <code>bracket.js</code> tentando ocultar esses cards (<code>!(m.isBye || m.p2 === \'BYE (Avança Direto)\')</code>), mas ele não cobria matches gerados por <code>bracket-logic.js</code> que usam <code>p2: \'BYE\'</code> (string curta) — então esses vazavam para a view. Agora um helper único <code>window._isByeMatch(m)</code> reconhece <code>m.isBye</code>, <code>m.p2 === \'BYE\'</code> e <code>m.p2 === \'BYE (Avança Direto)\'</code> e é aplicado em todos os lugares de filtro (count pré-finais + layout mirror + layout linear) e na detecção de BYE winners para a tag da próxima rodada. Resultado: a rodada atual mostra apenas jogos disputados; o time/dupla que passou de BYE aparece na rodada seguinte com badge verde <b>BYE</b>. A informação (match com <code>isBye:true</code> e <code>winner</code>) permanece persistida no torneio — pronta para ser consumida quando o ajuste de posicionamento/semeadura de BYE for refeito.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.96-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Reabrir inscrições também esvazia a <code>t.waitlist</code> (lista de espera de inscrição tardia)</b> — A v0.12.95 promovia apenas <code>t.standbyParticipants</code> (lista pré-sorteio) ao reabrir inscrições, mas o stat-box "LISTA DE ESPERA" soma também <code>t.waitlist</code> (onde caem inscrições feitas com o torneio já fechado, via <code>_addStandbyParticipant</code>). Resultado: um torneio com 41 inscritos + 1 na <code>waitlist</code> continuava mostrando "1 Lista de Espera" após reabertura. Agora, a lógica de promoção foi extraída para um helper <code>_promoteList(list)</code> aplicado tanto a <code>t.standbyParticipants</code> quanto a <code>t.waitlist</code> — com dedupe por email/uid/displayName/name e suporte a participantes armazenados como string. Ambas as listas são explicitamente zeradas (<code>= []</code>) ao final, garantindo que qualquer entrada futura em qualquer uma delas seja drenada. A notificação de sucesso e o log de atividades somam os promovidos das duas origens.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.95-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Reabrir inscrições promove automaticamente quem estava na lista de espera</b> — Antes, se o organizador encerrasse as inscrições e o sorteio movesse participantes sem time para <code>t.standbyParticipants</code> (lista de espera), reabrir as inscrições deixava essa lista intacta — os participantes continuavam em "espera" mesmo com vagas abertas e acabavam esquecidos. Agora, ao clicar "Reabrir Inscrições", todos os participantes em <code>t.standbyParticipants</code> são movidos de volta para <code>t.participants</code> (com checagem de duplicata por email/uid/displayName), a lista de espera é esvaziada e o contador de promovidos é registrado em <code>AppStore.logAction</code> (Histórico de Atividades) e anexado à notificação de sucesso via a nova chave i18n <code>draw.standbyPromoted</code>. Se não havia ninguém na lista de espera, o fluxo continua igual (nenhuma ação extra). Correção em <code>js/views/tournaments-draw-prep.js</code> dentro de <code>window.toggleRegistrationStatus</code> (~linha 2290).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.94-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Destaque forte para a opção selecionada na tela Classificatória Suíça</b> — Antes, a opção selecionada ganhava apenas uma borda roxa discreta e um glow suave — fácil de confundir com as outras opções coloridas (Nash) ao lado. Agora, ao selecionar uma opção de número de rodadas, o card ganha: (1) fundo em gradiente roxo <code>linear-gradient(135deg, rgba(139,92,246,0.28), rgba(139,92,246,0.12))</code>, (2) borda roxa grossa de 3px (vs 2px nas outras), (3) glow externo forte + glow interno, (4) escala <code>1.04x</code>, e (5) badge roxo <b>✓ Selecionado</b> ancorado no rodapé (distinto do badge verde <b>⭐ Recomendado</b> no topo — os dois podem coexistir se a opção escolhida for a recomendada). A lógica aplicada tanto no render inicial quanto em <code>window._selectSwissRounds(r)</code>. Hover desabilitado na opção já selecionada para não sobrescrever o destaque. Nova chave i18n <code>predraw.nashSelected</code> em pt e en.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.93-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Spinner de processamento nos botões Inscrever-se / Desinscrever-se</b> — Usuários relatavam que a inscrição/desinscrição "demora demais" porque o botão ficava estático enquanto o Firestore completava a transação (a UI otimista já atualiza o estado local imediatamente, mas o feedback visual no botão era nulo). Agora, ao clicar Inscrever-se ou Desinscrever-se no dashboard ou na página do torneio, o conteúdo do botão é substituído por um ícone em rotação + "Processando…" e o botão fica desabilitado até a view re-renderizar. Implementação: helper global <code>window._spinButton(btn, label)</code> em <code>store.js</code> troca o <code>innerHTML</code> por <code>&lt;span class="btn-spinner"&gt;</code> e seta <code>data-spinning="1"</code> + <code>disabled</code>. CSS <code>@keyframes sp-btn-spin</code> + classe <code>.btn-spinner</code> em <code>components.css</code>. Safety timeout de 8s restaura o botão caso o fluxo seja abortado (ex: usuário cancela o confirm dialog de desinscrição) sem re-renderizar. Nova chave i18n <code>enroll.processing</code> em pt e en.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.92-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Comparação dos Times: vencedor à direita, perdedor à esquerda</b> — Na tela de resumo pós-partida, as barras "⚖ Comparação dos Times" (Sets, Games, Pontos, % no saque, Quebras etc.) agora mostram as estatísticas positivas (azuis, do vencedor) à direita e as negativas (vermelhas, do perdedor) à esquerda. Antes era o oposto — o que invertia a leitura natural "perdeu × ganhou". Trocados: posição dos dois <code>span</code> numéricos, direção de crescimento dos dois <code>div</code> de barra (a barra da esquerda cresce da direita para a esquerda via <code>justify-content:flex-end</code>; a da direita cresce da esquerda para a direita), e os gradientes (esquerda: <code>loseClr44 → loseClr</code>; direita: <code>winClr → winClr44</code>). Os cantos arredondados e o separador central continuam iguais. Correção em <code>js/views/bracket-ui.js</code> ~linha 3580 (função <code>_compareBar</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.91-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão ✕ Fechar volta a aparecer no placar ao vivo ao fim do torneio</b> — Usuários relatavam "botão Voltar não funciona em torneio concluído com todos os resultados". A causa era o overlay de placar ao vivo (<code>#live-scoring-overlay</code>, <code>z-index:100002</code>) que, ao terminar a última partida de um torneio, mostrava a tela de resumo final — e nesse estado o bloco inteiro de ações do header (<code>#live-score-header-actions</code>) era ocultado com <code>display:none</code>, levando junto o botão <code>✕ Fechar</code>. Como o overlay cobre o sticky back-header (que está em <code>z-index:1001</code>, ver v0.12.88), não havia como dispensá-lo a não ser clicando <code>Confirmar Resultado</code>. Agora, na tela de resumo do torneio, mantemos o <code>✕ Fechar</code> visível e ocultamos apenas o botão <code>Reset</code> (que desfaria o estado finalizado). O handler <code>_closeLiveScoring()</code> já prompta para salvar/confirmar o resultado antes de fechar, então não há risco de perder dados. Correção em <code>js/views/bracket-ui.js</code> ~linha 3822.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.90-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Scroll ao ocultar rodada pousa o título da próxima bem abaixo da topbar</b> — Ajuste da v0.12.89: o <code>scrollIntoView({block:\'start\'})</code> colocava o topo da coluna exatamente em <code>y=0</code>, mas a topbar (sticky, ~94px) e o back-header fixo (~63px) juntas cobriam ~158px do topo — o título da rodada ("QUARTAS DE FINAL", "SEMIFINAIS" etc.) ficava atrás delas. Agora <code>_toggleRoundVisibility</code> mede dinamicamente a altura dos headers fixos via <code>getBoundingClientRect()</code> e calcula <code>scrollY = target.top + pageYOffset − (topbar+back-header+8px)</code>, chamando <code>window.scrollTo({top, behavior:\'smooth\'})</code>. Assim, o título da próxima rodada fica posicionado logo abaixo das barras fixas (no exemplo do teste: topo da coluna em y=165, logo abaixo dos headers que terminam em y=158). Em paralelo, se existir um container com overflow-x scroll (bracket lateral), também faz scroll horizontal para alinhar a coluna na borda esquerda. Aplicado a todas as "Ocultar" — chaveamento normal, mirror/World Cup, e Swiss/Liga.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.89-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Scroll automático para a próxima rodada ao ocultar uma anterior</b> — Quando o organizador clica "Ocultar" em uma rodada concluída no chaveamento, agora a tela rola suavemente até o topo da rodada seguinte (a que passa a ser a primeira coluna visível). Antes, ocultar uma rodada mantinha o scroll na posição anterior — se o usuário estava olhando R3 e ocultava R1, R3 continuava ancorada onde estava (visualmente o bracket "pulava" para a esquerda sem reposicionar), forçando um scroll manual. Agora <code>_toggleRoundVisibility(tId, roundNum)</code> localiza a próxima <code>.bracket-round-column</code> com <code>data-round-num</code> maior que a oculta e chama <code>scrollIntoView({behavior:\'smooth\', block:\'start\', inline:\'start\'})</code> após o rerender (50ms de delay para o DOM assentar). Cada coluna agora carrega <code>data-round-num</code> em ambos os layouts do bracket (normal e mirror/World Cup) para permitir a seleção. O comportamento inverso (clicar "Mostrar") não muda scroll — o usuário já sabe onde a rodada restaurada aparece.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.88-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Voltar sempre clicável — agora passa por cima de modais abertos</b> — O botão "Voltar" fixo no topo (<code>.sticky-back-header</code>) estava em <code>z-index:101</code>, logo abaixo de modais (1000). Resultado: quando qualquer modal do app abria (confirmar ação, editar categoria, gerenciar organização, etc.), o overlay escuro cobria o botão e cliques caíam no <code>.modal-overlay</code> em vez de navegar. Se o modal estava em estado "ativo mas congelado" (ex: após um erro de rede ou interação travada), o usuário ficava preso — o botão estava visível mas morto. Correção em duas camadas: (1) z-index hierarchy revisto em <code>css/style.css</code> — <code>--z-back-header</code> subiu para <code>1001</code> (acima de modal <code>1000</code>) e <code>--z-hamburger-dropdown</code> para <code>1002</code>. Overlays full-screen (live-scoring 100002, TV mode 99999, player stats 100001) continuam cobrindo o back — eles têm botões próprios de ✕ Fechar. (2) <code>router.js</code> passa a limpar <code>.modal-overlay.active</code> a cada hashchange — assim clicar Voltar nunca deixa um modal fantasma visível sobre a nova view. Testes de z-index em <code>tests.html</code> atualizados: nova invariante <code>zBackHeader > zModal</code> substituiu a antiga <code>zBackHeader === zTopbar + 1</code>. Arquitetura documentada inline nos comentários.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.87-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Mostrar rodadas ocultas uma de cada vez</b> — Ajuste da v0.12.86: o botão "Mostrar rodadas ocultas" restaurava todas de uma vez. Revisado para revelar apenas a rodada mais recente a cada clique — assim o usuário controla quanto contexto traz de volta. Ex: com R1, R2 e R3 ocultas, primeiro clique revela R3, segundo revela R2, terceiro revela R1. Label atualizado para "👁️ Mostrar rodada anterior (N oculta/s)" para refletir o comportamento. Contador entre parênteses sempre mostra quantas ainda faltam. Verificado em preview: <code>_hiddenRounds[tId]</code> passa de [1,2,3]→[1,2]→[1]→[] ao clicar 3 vezes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.86-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Mostrar rodadas ocultas" visível na toolbar do bracket</b> — O botão "Ocultar" em cada rodada concluída do chaveamento já existia, mas ao clicar a coluna inteira sumia junto com o botão. A única forma de revelar era via um botão vertical estreito (<code>writing-mode:vertical-lr</code>) que ficava no meio das colunas de rodadas — fácil de passar despercebido. Adicionado botão horizontal explícito na toolbar do bracket: "👁️ Mostrar rodadas ocultas (N)" aparece em destaque (borda lilás, mesma linha dos controles de zoom e layout) sempre que pelo menos uma rodada está oculta, mostrando o total entre parênteses. Um clique restaura todas de uma vez via novo helper <code>window._showAllHiddenRounds(tId)</code>. O botão vertical antigo permanece para quem quiser restaurar rodadas uma a uma. Verificado em preview com fixture de 3 rodadas (todas decididas) com R1 e R2 marcadas como ocultas: botão aparece com texto "👁️ Mostrar rodadas ocultas (2)"; clique limpa <code>window._hiddenRounds[tId]</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.85-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão W.O. também sempre visível para o organizador</b> — Complemento da v0.12.84. O botão W.O./Reverter tinha o mesmo gate <code>canAct</code> que o toggle de presença e sumia quando o jogo já havia sido decidido ou estava com W.O. declarado. Removido o gate — o botão agora renderiza sempre que o visualizador é organizador, independente do estado da partida. Continua alternando rótulo corretamente: "W.O." quando o participante ainda não foi marcado ausente, "Reverter" quando já foi. Verificado em preview com fixture de 4 times (um jogo decidido normal + um jogo fechado por W.O. com 2 jogadores já ausentes): os 8 participantes têm toggle + botão, e os que já estavam ausentes mostram "Reverter" (azul) ao invés de "W.O." (vermelho).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.84-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Toggle de presença visível para todos os participantes</b> — Na lista de inscritos em modo check-in, participantes cujo jogo já havia sido decidido (ou estavam marcados como W.O.) apareciam sem o toggle "Presente"/"Ausente" — o gate <code>canAct = !ind.matchDecided && !isWO</code> em <code>participants.js</code> escondia o controle. Resultado: se o organizador quisesse marcar presença retroativamente (ou corrigir um erro), não tinha como. Exemplo reportado: em torneio com Bot 03/Bot 08 num jogo já lançado, os cards de Bot 03 e Bot 08 não tinham o toggle, enquanto vizinhos em jogos pendentes tinham. Corrigido: o toggle agora é sempre renderizado — presença é independente do resultado do jogo. O botão W.O. continua gated (só faz sentido para jogos pendentes sem W.O. ainda). Verificado em preview com fixture de 4 times (um jogo decidido + um pendente): 8/8 participantes têm toggle, e clicar no toggle de um jogador cujo jogo já foi decidido persiste em <code>t.checkedIn</code> normalmente.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.83-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Editar resultado 7-6 já salvo agora revela os campos do tie-break</b> — Follow-up da v0.12.82: quando um jogo já estava lançado como 7-6 e o organizador clicava em ✏️ Editar para incluir (ou corrigir) os pontos do tie-break, os campos <code>tb1</code>/<code>tb2</code> não eram renderizados. Ao tentar confirmar, a validação rodava contra o DOM vazio e caía no alerta "Informe o placar do tie-break" — sem ter onde digitar. A causa: <code>_editResultInline</code> em <code>bracket-ui.js</code> só recriava os inputs principais de games; a v0.12.81 tinha adicionado os inputs <code>tb1</code>/<code>tb2</code> apenas ao render inicial do card em <code>bracket.js</code>, não ao swap de Edit. Corrigido: o Edit agora (1) detecta torneios com <code>scoring.type===\'sets\'</code> e tie-break habilitado, (2) renderiza os inputs <code>tb1</code>/<code>tb2</code> ao lado de <code>s1</code>/<code>s2</code> pre-preenchidos com <code>m.sets[0].tiebreak.pointsP1/P2</code> se já existirem (caso comum: o jogo foi salvo pelo placar ao vivo e já tem TB; usuário está só conferindo/editando), e (3) chama <code>_highlightWinner(matchId)</code> no final do swap para que, se o placar editado for 7-6 ou 6-7, os campos apareçam imediatamente sem precisar digitar de novo. Verificado em preview com fixture de match salvo como 7-6(7-5): clicar Edit mostra s1=7, s2=6, tb1=7 (visível), tb2=5 (visível). Em match 6-4 (set normal sem TB): tb1/tb2 existem no DOM mas ficam escondidos até o placar virar 7-6.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.82-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tie-break manual: digite 7-6 (não mais 6-6) para abrir os campos dos pontos</b> — Mudança de UX no lançamento manual de set decidido no tie-break. O comportamento da v0.12.81 (digitar 6-6 e o sistema bumpar para 7-6 depois que o TB era preenchido) tinha dois problemas: (1) ficava ambíguo quando o usuário digitava 7-6 diretamente — o sistema aceitava como vitória normal sem pedir o TB, silenciando o registro de pontos do tie-break; (2) na metade das entradas reais os usuários já digitam o placar final do set (7-6) pensando que não há TB envolvido. Nova sistemática: digite o placar final do set como ele apareceria em qualquer tabela de resultados — <b>7-6 ou 6-7 quando o set foi decidido no tie-break</b>. Como 7-6 só ocorre via TB (7-5 é fim normal, 7-6 significa que foi 6-6 e o TB aconteceu), o sistema reconhece esse placar e revela dois campos menores ao lado para você informar quantos pontos cada time fez no tie-break. O vencedor do set já está determinado pelos games (quem tem 7 ganhou), então validamos que o time com mais games tem mais pontos no TB (rejeita inconsistências tipo "ganhou 7-6 mas fez 5 no tb contra 7"). Resultado armazenado sem bump posterior: <code>{gamesP1:7, gamesP2:6, tiebreak:{pointsP1:7, pointsP2:5}}</code> — mesma shape que o placar ao vivo já produz. Rules view atualizada: "Disputado em 6-6" (quando o TB começa) permanece, e a instrução de lançamento manual passa a dizer "digite 7-6 para registrar set decidido no tie-break". i18n: novas chaves <code>result.tbWinnerMismatch</code> + detalhe para validação, <code>rules.scoringHowManualTb</code> reescrita.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.81-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tie-break lançado no chaveamento: digite 6-6 e dois campos de pontos aparecem</b> — A documentação adicionada em v0.12.80 descrevia o comportamento, mas o código não existia: digitar 6-6 disparava o alerta "Empate não permitido em eliminatórias" e o usuário ficava sem como registrar um tie-break. Agora implementado de fato. (1) <b>Input rendering</b> em <code>bracket.js</code>: ao lado de cada campo de games (<code>s1</code>/<code>s2</code>), um segundo campo menor (<code>tb1</code>/<code>tb2</code>) é renderizado com <code>display:none</code> quando o torneio usa GSM com tie-break habilitado. (2) <b><code>_highlightWinner</code></b> em <code>bracket-ui.js</code> agora lê o <code>gamesPerSet</code> do torneio e revela/oculta os campos de TB em tempo real: quando ambos os placares atingem o gatilho (ex: 6-6) os campos aparecem; se um dos placares muda, somem e são zerados. A coloração verde/vermelho também passa a considerar o TB quando preenchido. (3) <b><code>_saveResultInline</code></b> detecta entrada de TB quando <code>s1 === s2 === trigger</code>, exige os dois campos de pontos, rejeita empate no TB, concede o jogo extra ao vencedor do TB (<code>gamesP1/P2</code> se torna <code>trigger+1</code>) e grava <code>sets[0].tiebreak = {pointsP1, pointsP2}</code> — mesmo formato usado pelo overlay "Lançar Sets" e lido por <code>_formatSetForPlayer</code>, garantindo que o placar final aparece como <code>7⁽⁷⁾-6⁽⁵⁾</code> nas 7 superfícies de display. Com isso a restrição "empate não permitido em eliminatórias" agora só dispara para 0-0, 1-1, etc. — o gatilho do TB passa a aceitar empate de games porque sabe que o TB decidirá.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.80-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convidado preso em "Partida não encontrada" após encerramento + regras do torneio documentam lançamento manual de tie-break</b> — Dois problemas observados em partida casual real. (1) Fluxo do convidado após encerramento: quando o anfitrião fechava o placar ao vivo (mesmo com resultado salvo), o código chamava <code>cancelCasualMatch</code> que <i>deletava</i> o doc Firestore. Qualquer convidado que abria o link depois via "Partida não encontrada" e ficava preso numa tela sem contexto. Agora o doc é preservado com <code>status=\'finished\'</code> quando a partida é concluída — só é deletado quando o anfitrião abandona <i>antes</i> de finalizar. Resultado: convidados que abrem o link depois caem na tela de resumo/estatísticas do casual view (que já existia mas era inalcançável) com botão para o dashboard. Convidados que estavam assistindo ao vivo agora também transicionam automaticamente para essa tela quando o listener detecta <code>status=\'finished\'</code>, em vez de serem expulsos com notificação genérica. (2) Regras do torneio não documentavam como lançar tie-break manualmente. Adicionada nova seção "🎾 Sistema de Pontuação" na página de regras mostrando tipo de placar (simples ou Game/Set/Match), sets para vencer, games por set, tipo de contagem (tênis 15-30-40 ou numérica), regra de vantagem, configuração do tie-break (placar de disparo calculado do <code>gamesPerSet</code> — ex: 6-6, pontos até atingir, diferença mínima), super tie-break e instrução passo-a-passo: "digite os games; ao atingir 6-6 o sistema abre dois novos campos para os pontos do tie-break de cada time". Para torneios sem tie-break, mensagem alternativa explicando o critério de decisão do set.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.79-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Placar do tie-break por time como sobrescrito entre parênteses</b> — Em todos os displays de resultados (chaveamento, placar ao vivo, log do torneio, histórico casual), quando um set é decidido no tie-break agora o placar do TB de <i>cada</i> time aparece como sobrescrito entre parênteses ao lado dos games. Exemplo: <code>7⁽⁷⁾-6⁽⁵⁾</code> (vencedor fez 7 games e 7 pontos no TB; perdedor 6 games e 5 pontos). Antes o comportamento era inconsistente entre os 7 sites de exibição: alguns mostravam só o <code>Math.max</code> (ambíguo), outros só do perdedor (convenção do tênis), outros combinavam <code>(7-5)</code>. Criados dois helpers compartilhados em <code>bracket-model.js</code>: <code>window._formatSetForPlayer(set, playerNum, opts)</code> devolve <code>7⁽⁷⁾</code> para uma coluna de jogador, e <code>window._formatSetCombined(set, opts)</code> devolve <code>7⁽⁷⁾-6⁽⁵⁾</code>. Ambos normalizam as duas shapes de dado existentes (<code>{p1,p2}</code> em partidas casuais e <code>{pointsP1,pointsP2}</code> em torneios). <code>opts.html=true</code> usa <code>&lt;sup&gt;</code>; sem opts usa dígitos Unicode sobrescritos para texto puro (notificações, logs, CSV). Aplicado em: <code>bracket.js</code> (coluna por jogador + resumo do vencedor + fixed-set com TB), <code>bracket-ui.js</code> (log de torneio, <code>scoreSummary</code> do record, summary da partida casual e display HTML inline ao vivo).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.78-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sexta onda de migrações canônicas — 6 silent-miss corrigidos em bracket/analytics/draw/categories</b> — Após v0.12.77 concluir os core helpers (<code>_findMatch</code> + <code>_showPlayerHistory</code>), este release varre os sites restantes. (1) <code>_byeWinners</code> em <code>bracket.js</code> (L1237): detecção de jogadores que avançaram via BYE em R1 só olhava <code>t.matches</code>; agora usa o collector. (2) Champion detection em <code>tournaments-analytics.js</code> (L197): títulos/pódios de eliminação liam só <code>t.matches[last]</code> — se o elim foi escrito via canonical writer em <code>t.rounds[].matches</code>, o campeão era invisível. Agora prefere o adapter (busca última coluna de fase <code>grandfinal</code>/<code>elim</code>) com fallback legado. (3) <code>generateDrawFunction</code> em <code>tournaments-draw.js</code> (L101): a proteção contra re-sorteio só detectava resultados em <code>t.matches</code> e <code>t.rounds</code> — em Grupos/thirdPlace/rodadas, um redraw apagava resultados reais sem aviso. Migrado. (4) <code>_executeMerge</code> em <code>tournaments-categories.js</code> (L1100): ao mesclar categorias, referências <code>m.category</code> em <code>t.groups[].matches</code>, <code>t.thirdPlaceMatch</code> e <code>t.rodadas</code> não eram atualizadas; agora o collector mutate-by-reference aplica a mudança em todas as 7 shapes. (5) <code>finishTournament</code> em <code>tournaments-draw-prep.js</code> (L2202): cálculo de <code>hasResults</code> e <code>pendingMatches</code> (usado na confirmação de encerramento manual) também migrado. (6) Substituição W.O. e desqualificação de times em <code>bracket-ui.js</code> (L142/L202): o rewrite de <code>p1</code>/<code>p2</code>/<code>winner</code> de <code>oldTeamName</code>→<code>newTeamName</code> só alcançava <code>t.matches</code> e <code>t.rounds</code> — em Grupos/thirdPlace/rodadas, o nome antigo sobrevivia. Migrado. Bonus: <code>_showPlayerHistory</code> agora também cobre <code>c.subgroups[].matches</code> (Groups/Monarch) que ficaram fora da v0.12.77. Verificado em preview: fixture com dados em 4 shapes devolve 5/5 matches, substituição propaga para as 5 shapes, campeão é detectado via adapter quando <code>t.matches</code> está vazio. Fallback legado preservado em todos os sites.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.77-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Core <code>_findMatch</code> e Player History migrados para o helper canônico</b> — Duas migrações internas aos módulos <code>bracket-*</code> (antes deixadas fora do escopo). (1) <code>_findMatch</code> em <code>bracket-logic.js</code>, a função utilitária mais chamada do sistema (14 call sites, incluindo <code>_advanceWinner</code>, save de resultado, repescagem e merge do lower bracket), tinha um silent miss duplo: omitia <code>t.groups[].matches</code> (partidas diretas no grupo, fora de sub-rounds) e <code>t.rodadas</code> (tanto <code>.matches</code> quanto array direto). Resultado: operações que dependiam de encontrar um match por ID — advance de vencedor, save de placar — falhavam silenciosamente se o match estivesse numa dessas shapes. Migrado para <code>window._collectAllMatches(t)</code>, cobrindo as 7 shapes. Verificado em fixture com todas as shapes: 8/8 matches encontráveis por ID (antes: 5/8); mutação via referência preservada (crítico para advance-winner). (2) <code>_showPlayerHistory</code> em <code>bracket-ui.js</code> — o popup de histórico que abre ao clicar no nome de um jogador — tinha silent miss em <code>t.thirdPlaceMatch</code>, <code>t.rodadas</code> e <code>t.groups[].rounds</code>. Migrado para <code>_getUnifiedRounds(t)</code>, que além de cobrir todas as shapes entrega rótulos semânticos. Resultado visível: jogador que disputou semifinal + 3º lugar agora aparece com "Semifinais" e "Disputa de 3º Lugar" na coluna Fase (antes: só "Rodada 1" da semi, o 3º lugar nunca aparecia). Em elim de 4 jogadores: Alice (campeã) mostra "Semifinais"/"Final"; Bob (3º) mostra "Semifinais"/"Disputa de 3º Lugar" — antes o 3º lugar era invisível no histórico. Fallback legado preservado em ambos os sites.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.76-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>W.O. e check-in de Liga/Suíço/Grupos ganham lookup funcional</b> — Dois scans em <code>participants.js</code> que só olhavam <code>t.matches</code> foram migrados para <code>window._collectAllMatches(t)</code>. (1) <code>_declareAbsent</code> (W.O.) em L253: antes, ao declarar ausência em torneio Liga/Suíço/Grupos, a busca do match retornava null silenciosamente — a dialog mostrava "Partida ?" sem saber o adversário. Agora a busca atravessa todas as 7 shapes e encontra a partida em qualquer formato. (2) O builder do mapa <code>nameToMatch</code>/<code>nameToOpponent</code> no check-in (L506) tinha o mesmo silent miss: em formatos não-elim, o mapa ficava vazio e nenhum participante mostrava número/adversário da próxima partida. Agora popula corretamente para Liga, Suíço, Grupos e Rei/Rainha. <b>Regressão zero em elim:</b> a ordem do helper começa com <code>t.matches</code>, preservando o índice amigável ("Partida N") para chaves eliminatórias — verificado em fixtures (Alice vs Bob mantém index 1, Alice vs Charlie mantém index 3). Em Liga/Grupos, o número é o índice flat na lista coletada, o que pode não ser tão semântico quanto "Rodada 2 / Partida 1" mas é um <i>strict improvement</i> sobre o "?" anterior — e habilita toda a lógica downstream (detecção de decidido, opponent lookup) para esses formatos. Fallback legado preservado em ambos os sites. Com esta migração, <b>todos os scans multi-shape fora de <code>bracket-*</code></b> passam pelo helper canônico: a refatoração da fase 3 está funcionalmente completa.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.75-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rotina AutoFixNames (3 scans) consome helper canônico</b> — <code>_scanForStaleNames</code> em <code>auth.js</code>, que roda em toda abertura de sessão para detectar e corrigir nomes desatualizados de participantes (quando usuário editou <code>displayName</code> no perfil entre sessões), tinha 3 scans multi-shape espalhados pela rotina: <code>_scanMatch</code> (coleta de uid/email, L1875), <code>_scanMatchStale</code> (detecção de nome stale em partidas, L1985) e <code>_collectFromMatch</code> (coleta de strings para matching contra <code>previousDisplayNames</code>, L2033). Os 3 seguiam o mesmo padrão de 5 branches (<code>t.matches</code>, <code>t.rounds</code>, <code>t.groups.matches</code>, <code>t.groups.rounds</code>) — todos com o mesmo silent miss: <b>omitiam</b> <code>t.thirdPlaceMatch</code> e <code>t.rodadas</code>. Resultado: se um usuário mudava de displayName e só tinha partidas em fase de 3º lugar ou em rodadas legadas, o AutoFix simplesmente não detectava a divergência — o propagate (já migrado em v0.12.74) tinha consertado a correção, mas a <i>detecção</i> ainda escapava. Com esta migração, os 3 scans delegam para <code>window._collectAllMatches(t)</code>, cobrindo as 7 shapes. <code>g.players</code> (roster) segue tratado separadamente em cada scan que precisa dele. Verificado: helper devolve 7/7 matches da fixture (antes: 4/7). Fallback legado preservado em cada site. Com estas migrações, <b>todos os 4 scans multi-shape do auth.js</b> (AutoFix × 3 + propagate × 1) passam pelo helper canônico — a detecção/correção de nomes stale agora cobre todas as shapes de storage.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.74-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Propagação global de rename consome helper canônico</b> — <code>_propagateNameChange</code> em <code>auth.js</code> (usado sempre que o usuário edita o <code>displayName</code> no perfil, espalhando a mudança por todos os torneios onde ele aparece como <code>p1</code>/<code>p2</code>/<code>winner</code>/<code>team1</code>/<code>team2</code>) era a última cópia viva do padrão de 5 branches manuais que já havia sido migrado em <code>participants.js</code> (v0.12.72). O scan agora delega para <code>window._collectAllMatches(t)</code>, cobrindo as 7 shapes de armazenamento (<code>t.matches</code>, <code>t.rounds[].matches</code>, <code>t.groups[].matches</code>, <code>t.groups[].rounds[]</code> — array direto e <code>.matches</code>, <code>t.thirdPlaceMatch</code>, <code>t.rodadas</code> — array direto e <code>.matches</code>). O campo <code>g.players</code> (roster, não match) segue tratado separadamente. <b>Correção de bug silencioso:</b> nomes que só apareciam em <code>t.thirdPlaceMatch</code> antes não eram propagados — verificado em fixture onde "Zulu" aparecia apenas em <code>thirdPlaceMatch</code>: antes o rename ficava órfão, agora p1/team1/winner são atualizados corretamente. Fixture com todas as 7 shapes: 10/10 campos propagados corretamente. Fallback legado preservado caso <code>bracket-model.js</code> não carregue. Com essa migração, todos os 3 scans globais de rename (participants, dashboard widget, propagate) passam pelo helper canônico.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.73-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Pódio de eliminatória e scan <code>_nameInDraw</code> consomem adaptador/collector canônicos</b> — Mais duas leituras diretas de storage legado migradas. (1) O cálculo do pódio em torneios encerrados (<code>tournaments.js</code> L883) agora prefere as colunas <code>grandfinal</code> e <code>elim</code> devolvidas por <code>_getUnifiedRounds(t)</code> para localizar a partida final; cai na última coluna <code>elim</code> caso não haja <code>grandfinal</code>, e só então recorre ao scan legado <code>Math.max(m.round)</code> em <code>t.matches</code>. O 3º lugar vem da coluna canônica <code>thirdplace</code>. Verificado em 3 fixtures: eliminatória simples de 4 jogadores com 3º (A/D/B), elim sem 3º (X/Z/null), e dupla eliminatória com grande final (A/D/null via prioridade <code>grandfinal</code>). (2) O scan <code>_nameInDraw</code> dentro de <code>_executeMerge</code> em <code>tournaments-utils.js</code> — que verifica se um nome aparece em alguma partida antes de permitir merge de categorias — tinha uma falha silenciosa: olhava <code>t.matches</code>, <code>t.rounds[].matches</code> e <code>t.groups[].matches</code>, mas <b>omitia</b> <code>t.thirdPlaceMatch</code>, <code>t.groups[].rounds</code> e <code>t.rodadas</code>. Resultado: participantes que só apareciam nessas 3 shapes eram considerados "fora do chaveamento" e podiam ter a categoria mudada mesmo já tendo partida marcada. Agora delega para <code>window._collectAllMatches(t)</code>, que cobre todas as 7 shapes. Verificado: nomes em <code>thirdPlaceMatch.team1</code>, <code>rodadas[].matches</code> e <code>rodadas[].jogos</code> agora retornam <code>true</code> corretamente (antes: <code>false</code>). Fallback legado preservado em ambos os sites caso <code>bracket-model.js</code> não carregue.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rename de jogador e widget "Próximas Partidas" consomem helper canônico</b> — Duas varreduras multi-shape migradas para <code>window._collectAllMatches(t)</code>: (1) O scan de renomeação de jogador em <code>participants.js</code> (que acompanha toda mudança de nome no perfil, propagando para <code>p1</code>/<code>p2</code>/<code>winner</code>/<code>team1</code>/<code>team2</code> em todas as partidas do torneio) antes tinha 5 branches explícitas (<code>t.matches</code>, <code>t.thirdPlaceMatch</code>, <code>t.rounds[].matches</code>, <code>t.groups[].matches</code> + <code>g.rounds</code>, <code>t.rodadas</code>); agora é uma única chamada ao helper. O campo <code>g.players</code> (roster, não match) segue tratado separadamente. (2) O widget "Próximas Partidas" na dashboard — que coleta todas as partidas pendentes do usuário em torneios ativos — tinha a mesma duplicação de 5 branches. Idem. Fallback legado preservado em ambos os sites caso <code>bracket-model.js</code> não esteja carregado. Verificado em fixtures cobrindo as 7 variantes de armazenamento (elim, 3º lugar, swiss, groups.matches, groups.rounds sub-array, rodadas array-direto, rodadas.matches): helper coleta todas as 7 refs, rename aplicado via refs atualiza o estado original por completo (0 leftovers); widget coleta 1 match pendente por fixture em cada shape. Com estas duas migrações, 7 dos ~8 sites remanescentes de varredura "flat-across-all-shapes" fora de <code>bracket-*</code> agora passam pelo helper canônico.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.71-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>CSV de resultados ganha rótulos semânticos e passa a incluir Suíço/Liga; progresso de torneio consome helper canônico</b> — Duas migrações focadas em caminhos de leitura remanescentes fora de <code>bracket-*</code>. (1) <code>_getTournamentProgress(t)</code> em <code>tournaments-utils.js</code>, que alimenta as barras de progresso no dashboard e na página de detalhes, agora delega sua varredura de 5 shapes legados para <code>window._collectAllMatches(t)</code>. Paridade byte-a-byte verificada em 6 fixtures (elim com 3º lugar, Suíço, Grupos com sub-rounds, Rei/Rainha, <code>t.rodadas</code> com <code>rd.jogos</code>, e elim com BYE). (2) <code>_exportTournamentCSV</code> em <code>tournaments-sharing.js</code>, que antes duplicava um scan incompleto (<b>silenciosamente omitia <code>t.rounds</code> e <code>t.rodadas</code> por inteiro</b> — ou seja, exports de Suíço/Ranking vinham sempre vazios na seção de matches), agora consome as colunas canônicas de <code>_getUnifiedRounds(t)</code>. Resultado: a coluna "Rodada/Fase" do CSV agora traz rótulos humanos do adaptador em vez de números genéricos — "Semifinais", "Final", "Quartas de Final", "Grupo A - Partida 1", "Disputa 3º lugar", "Rodada N". Em fixture de 4 jogadores com 3º lugar, o CSV passa de cabeçalho nu (0 matches exportados, bug existente) para 4 linhas rotuladas "Semifinais", "Semifinais", "Final", "Disputa 3º lugar". Fallback legado preservado em ambos os caminhos caso o adaptador não carregue; inclui agora também <code>t.rounds</code> e <code>t.rodadas</code> (fix defensivo).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.70-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Histórico de Atividades ganha rótulos semânticos + analytics consome helper canônico</b> — Duas migrações em <code>tournaments-analytics.js</code>: (1) O helper local <code>_getAllMatches(t)</code>, que duplicava a lógica de varredura multi-shape em ~22 linhas, agora delega para <code>window._collectAllMatches(t)</code>, com fallback legado preservado caso <code>bracket-model.js</code> não esteja carregado. Antes era uma das últimas cópias vivas do padrão "flat-every-match-across-shapes"; agora resta apenas o helper global. (2) <code>_buildActivityLog</code> passa a ler matches via <code>_getUnifiedRounds(t)</code>, aplicando rótulos humanos do adaptador em cada partida listada. Efeito visual: o Histórico de Atividades na página de detalhes do torneio agora mostra "(Semifinais)", "(Final)", "(Quartas de Final)", "(Grupo A)" e "(Disputa 3º lugar)" em vez do genérico "(Rodada 1)", "(Rodada 2)" etc. Em um torneio eliminatório de 4 jogadores, o card de atividade passa de "<i>Alice vs Bob → Alice (Rodada 1)</i>" para "<i>Alice vs Bob → Alice (Semifinais)</i>". Fallback legado mantido: se o adaptador não carregar ou retornar colunas vazias, o scan antigo (t.matches/t.rounds/t.groups/t.rodadas/t.thirdPlaceMatch) é usado. <code>_collectAllMatches</code> também foi estendido para reconhecer o campo legado <code>rd.jogos</code> dentro de <code>t.rodadas</code>. Verificado em fixture de torneio de 4 jogadores com 3 matches resolvidas + disputa de 3º lugar — HTML resultante contém "Semifinais" (2x), "Final" e "Disputa 3º lugar", sem nenhum "Rodada 1".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.69-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Modo TV — "Próximos Jogos" ganha rótulos semânticos via adaptador canônico</b> — O quarto scan pendente (<code>_tvBuildNextMatches</code>) foi migrado para consumir as colunas devolvidas por <code>_getUnifiedRounds(t)</code>. Cada coluna já carrega um rótulo humano ("Final", "Semifinais", "Quartas de Final", "Oitavas", "Rodada N", "Grupos"), e o scan usa esse rótulo como <code>_roundLabel</code> da partida. Resultado visível para o usuário: em torneio eliminatório de 4 jogadores, as partidas da Rodada 1 agora aparecem rotuladas como "Semifinais" e a Rodada 2 como "Final" (antes, partidas de <code>t.matches</code> não tinham rótulo algum no Modo TV); em 8 jogadores, "Quartas"/"Semifinais"/"Final"; em 16, "Oitavas"/"Quartas"/etc. Formatos Suíço/Liga preservam "Rodada N" (adaptador usa o mesmo rótulo) e grupos preservam "Grupo A"/"Grupo B"/etc. por subgrupo. Fallback legado mantido quando adaptador não carrega ou retorna colunas vazias (ex: torneio sem <code>currentStage</code>). Verificado em 5 fixtures: elim 4 jogadores, elim 8 jogadores, Suíço 2 rodadas, Grupos com <code>currentStage=\'groups\'</code>, e Grupos sem currentStage (exercitando o fallback). Com isso, todos os 4 sites de varredura multi-shape em <code>bracket-ui.js</code> agora passam por helpers canônicos (3 por <code>_collectAllMatches</code>, 1 por <code>_getUnifiedRounds</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.68-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Helper canônico de leitura <code>_collectAllMatches</code> unifica 3 varreduras duplicadas em <code>bracket-ui.js</code></b> — Extraído <code>window._collectAllMatches(t)</code> em <code>bracket-model.js</code>: devolve um array flat com todas as partidas espalhadas pelas 5 formas legadas (<code>t.matches</code>, <code>t.rounds[].matches</code>, <code>t.groups[].matches</code>, <code>t.groups[].rounds[].matches</code>, <code>t.thirdPlaceMatch</code>, e o campo legado <code>t.rodadas</code> com suas 2 shapes). Três sites em <code>bracket-ui.js</code> migrados: <code>_substituteWO</code> (detecção de jogador ausente para W.O., L248), <code>_shareMatchResult</code> (busca de match por id para compartilhar, L1124) e <code>_tvBuildAttendance</code> (scan de presença no modo TV, L1283). Byte-parity test confirma que o helper é um <i>strict superset</i> de cada scan legado — extras (3º lugar, sub-rounds de grupos, rodadas legadas) são filtrados pelos guards existentes em cada chamador (<code>!m.winner</code>, <code>!m.isBye</code>, <code>p1 &amp;&amp; p2</code>, TBD guard). Como efeito colateral benigno, o scan de W.O. e o share agora também encontram matches em sub-rounds de grupos e no 3º lugar, o que antes escapava silenciosamente. Quarto scan em <code>_tvBuildNextMatches</code> (L1233) <b>não</b> migrado — ele pina <code>_roundLabel</code> como metadado em cada match durante a enumeração, um side-effect específico que não cabe no helper genérico. Parity verificada em fixtures elim/swiss/groups/rodadas via 6 casos unitários (helperPresent, empty, elim, swiss, groups com sub-rounds, rodadas legadas, null/undef safe), todos passaram.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.67-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Helper canônico <code>_appendCanonicalColumn</code> corrige branch <code>thirdplace</code> e sites de inicialização migrados</b> — O helper tinha uma branch morta para <code>phase=\'thirdplace\'</code> que erroneamente empurrava o match para <code>t.matches[]</code> (caminho legado de fases elim/grand). Como a legacy storage do 3º lugar é o campo único <code>t.thirdPlaceMatch</code>, chamar o helper com essa fase corrompia o estado. Corrigido com early-return dedicado: <code>if (phase === \'thirdplace\') { t.thirdPlaceMatch = desc.matches[0]; return; }</code>. Com a branch funcional, os 2 sites de inicialização de <code>t.thirdPlaceMatch</code> foram migrados: <code>bracket-logic.js</code> L887 (em <code>_maybeAdvanceWinner</code>, ao detectar que faltava o match de 3º lugar) e <code>bracket.js</code> L569 (em <code>_ensureFutureRounds</code>, garantindo o match ao renderizar). Ambos passam a criar o match via <code>_appendCanonicalColumn(t, {phase:\'thirdplace\', matches:[{...}]})</code>. Zero mudança comportamental — a saída é literalmente a mesma atribuição <code>t.thirdPlaceMatch = {...}</code> feita antes, mas agora roteada pelo mesmo helper usado pelas demais fases. Verificado por testes unitários (helper define campo corretamente e não toca <code>t.matches</code>; matches vazio não crasha) e teste ao vivo (renderizar torneio sem 3º lugar → card aparece automaticamente com placeholder TBD × TBD, conforme política "always on"). Com isso, 100% das escritas canônicas em campos legados (elim, grand, thirdplace, swiss, monarch, liga) passam pelo helper — a Fase 3 está essencialmente completa.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.66-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Leitura do 3º lugar no renderer de eliminatória simples migrada para o adaptador</b> — <code>renderSingleElimBracket</code> agora obtém o match de 3º lugar da coluna canônica <code>thirdplace</code> emitida por <code>_getUnifiedRounds(t)</code>, com fallback para <code>t.thirdPlaceMatch</code> e depois para o placeholder vazio. Antes o renderer lia <code>t.thirdPlaceMatch</code> diretamente; agora segue o mesmo padrão dos demais campos (colunas elim, grand, groups, swiss-past, etc.). Verificado em 3 cenários: (1) com <code>thirdPlaceMatch</code> configurado, card renderiza com participantes corretos; (2) sem <code>thirdPlaceMatch</code>, placeholder "3º LUGAR" aparece conforme política "always on" (project memory); (3) com adaptador desabilitado, fallback legado para <code>t.thirdPlaceMatch</code> funciona. A/B byte-parity: com adaptador vs sem adaptador, saída idêntica (19978 bytes vs 19978 bytes, 0 delta). Essa é a última leitura direta de <code>t.thirdPlaceMatch</code> no renderer — futuras mudanças visuais no 3º lugar podem ser feitas em um único lugar (emissão da coluna no adaptador) e propagam para o renderer.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.65-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Geradores de eliminatória migrados para o helper canônico — Fase 3 contínua</b> — 8 sites de <code>t.matches.push</code> espalhados por 3 arquivos agora passam pelo helper <code>_appendCanonicalColumn</code>: 6 em <code>tournaments-draw.js</code> (single-elim shell rounds 2+, dupla eliminatória upper bracket, lower R1, lower merge rounds, lower battle rounds, grande final), 1 em <code>bracket.js</code> (rebuild de rodadas faltantes no advance-winner) e 1 em <code>bracket-ui.js</code> (transição Rei/Rainha → eliminatória). Cada chamada marca explicitamente a fase (<code>elim</code> ou <code>grandfinal</code>) e, quando aplicável, o bracket (<code>upper | lower | grand</code>). Zero mudança comportamental — o helper para fases elim apenas concatena em <code>t.matches[]</code> preservando ordem e semântica legada. Verificado em fixtures de 4 jogadores: single-elim (2 R1 + 1 Final, links nextMatchId corretos) e dupla eliminatória (2 upper R1, 1 upper R2, 1 lower R1, 1 grand final, todos com bracket tag). Adaptador <code>_getUnifiedRounds</code> consome corretamente a saída migrada — 4 colunas para dupla (upper R1, upper R2, lower R1, grand final) com labels "Rodada 1", "Rodada 2", "Grande Final". Com isso, ~90% dos writes para <code>t.matches/t.rounds</code> passam pelo helper. Sites remanescentes são caminhos raros (seed de times via drag-drop em <code>tournaments-draw.js</code>) ou operações de mutação in-place (advance winner), a migrar sob demanda.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.64-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Geradores Swiss/Liga/Rei-Rainha migrados para helper de escrita canônico — início da Fase 3</b> — Extraído <code>window._appendCanonicalColumn(t, desc)</code> em <code>bracket-model.js</code>: a função encapsula o conhecimento de "qual campo legado receber o append dada a fase" (<code>elim | grandfinal | thirdplace</code> → <code>t.matches[]</code>; <code>swiss | liga | monarch</code> → <code>t.rounds[round-1]</code>, com merge correto quando a posição já existe). Os 3 sites de push quase-idênticos em <code>bracket-logic.js</code> foram substituídos por chamadas ao helper: <code>_generateReiRainhaRoundForPlayers</code> (rodada de Rei/Rainha com grupos e <code>format: \'rei_rainha\'</code>), <code>_generateNextRound</code> na branch Liga (matches acumulam na rodada existente quando chamado por categoria), e <code>_generateNextRound</code> na branch Swiss. Nenhuma mudança comportamental — o helper reproduz exatamente a sequência legada (incluindo o sobrescreve-sempre em <code>.format</code>). Verificado por testes unitários diretos do helper (6 casos de shape) e testes end-to-end chamando os geradores ao vivo com fixtures Swiss (4 jogadores), Liga (8 jogadores, formato duplas), e Rei/Rainha (4 jogadores com grupo AB/CD/AC/BD/AD/BC): todas as saídas têm shape canônico correto (<code>round</code>, <code>status</code>, <code>matches</code>, <code>format</code> e <code>monarchGroups</code> quando aplicável). Primeiro passo em unificar a escrita — próximos candidatos: sites <code>t.matches.push</code> em <code>tournaments-draw.js</code> (geração de eliminatória), rebuild de <code>bracket-ui.js</code>, e advance-winner em <code>bracket.js</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.63-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Renderer de Classificação (Liga/Suíço/Rei-Rainha) migrado para o adaptador canônico — fim da Fase 2</b> — <code>renderStandings</code> agora deriva o array de rodadas a partir das colunas <code>swiss | monarch | liga</code> devolvidas por <code>_getUnifiedRounds(t)</code>, achatando cada coluna de volta para o shape legado <code>{matches, status, format, monarchGroups, round}</code> que a renderização consome. Todas as seções do render (classificação principal com categorias, rodada atual, rodadas anteriores, estatísticas de torneio, matriz de confrontos diretos, layout em colunas do Suíço) continuam funcionando sem alteração. Verificado por teste A/B byte-a-byte em 4 fixtures (Suíço 3 rodadas, Liga com 2 categorias, Rei/Rainha com grupo de 4 jogadores, Suíço 2 rodadas): <b>0 bytes de diferença</b> entre a saída com e sem adaptador. Com isso, os 5 renderers (eliminatória simples, dupla, grupos, Rei/Rainha, Liga/Suíço) agora consomem a mesma forma canônica — qualquer refinamento visual feito uma vez propaga para todos os formatos. Próxima fase: unificar também os geradores (draw logic) para gravar direto no shape canônico.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.62-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Renderer de Dupla Eliminatória migrado para o adaptador canônico</b> — <code>renderDoubleElimBracket</code> agora consome as colunas devolvidas por <code>_getUnifiedRounds(t)</code>, particionadas pelo novo campo <code>bracket</code> (<code>upper</code>, <code>lower</code>, <code>grand</code>). O adaptador foi enriquecido para emitir uma coluna por combinação (bracket, rodada) em vez de uma coluna por rodada: chaveamento superior e inferior aparecem como seções separadas com suas próprias rodadas, e a grande final sai etiquetada como fase <code>grandfinal</code> com rótulo correto em PT/EN. Single-elim continua intocado (sem campo <code>m.bracket</code>, o adaptador roteia pelo caminho <code>bracketOrder = [null]</code> preservando rótulos Semifinais/Final/etc.). Verificado em fixture dupla (upper R1+R2, lower R1, grand R1) e em fixture single-elim (regressão). Zero regressão. Quarto renderer migrado — falta apenas <code>renderStandings</code> (Liga/Suíço).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.61-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Renderer de Fase de Grupos migrado para o adaptador canônico</b> — <code>renderGroupStage</code> agora consome os subgrupos devolvidos por <code>_getUnifiedRounds(t)</code> (fase <code>groups</code>). O adaptador ficou um pouco mais rico: agora cada subgrupo preserva também o array <code>rounds[]</code> com <code>{round, status, matches}</code>, de modo que o renderer iterando sobre as rodadas continua tendo acesso aos rótulos "Rodada 1 — Concluído ✓", "Rodada 2 — Em andamento" etc., sem precisar reler <code>t.groups[i].rounds</code>. Verificado em fixture de 2 grupos × 4 jogadores × 3 rodadas (Grupo A totalmente concluído, Grupo B com rodada 2 em andamento e 3 pendente): mesmos nomes, mesmas cores por grupo (laranja/roxo), mesmas classificações com badge CLASSIF. nos top-2, mesmos rótulos de rodada, botão "Avançar para Eliminatória" corretamente oculto quando ainda há partidas pendentes. Zero regressão. Terceiro renderer migrado — faltam <code>renderDoubleElimBracket</code> e <code>renderStandings</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.60-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Renderer Rei/Rainha migrado para o adaptador canônico</b> — <code>_renderMonarchStage</code> agora itera sobre os subgrupos devolvidos por <code>_getUnifiedRounds(t)</code> (fase <code>monarch</code>) em vez de ler <code>t.groups</code> diretamente. O cálculo de classificação via <code>_computeMonarchStandings</code> continua exatamente o mesmo, assim como o HTML de cada grupo (status, tabela, cards de partida). Segundo renderer consumindo o modelo canônico — verificado em fixture de 2 grupos × 4 jogadores (Grupo A finalizado, Grupo B em andamento): mesmos rótulos, mesmas bordas coloridas, mesmas tabelas de classificação, mesmos cards de partida. Zero regressão. Faltam <code>renderDoubleElimBracket</code>, <code>renderGroupStage</code> e <code>renderStandings</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.59-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Primeiro renderer migrado para o adaptador canônico</b> — O renderer da eliminatória simples (<code>renderSingleElimBracket</code>) agora deriva a estrutura de colunas (rodadas eliminatórias + colunas históricas de Suíço classificatório) a partir de <code>_getUnifiedRounds(t)</code> em vez de ler <code>t.matches</code> e <code>t.swissRoundsData</code> diretamente. A saída visual é <b>idêntica</b> à versão anterior (verificado em dois cenários: torneio Suíço→Elim de 20 jogadores e eliminatória pura de 8 jogadores — mesmas 6 e 3 colunas, mesma largura de scroll, mesmas marcas de avanço ✓). Zero regressão, mas este é o primeiro renderer consumindo o modelo canônico. Nos próximos passos os outros 4 renderers (dupla eliminatória, grupos, Rei/Rainha, Liga/Suíço) também serão migrados, e a partir daí qualquer refinamento visual ou correção feita uma vez propaga para todos os formatos automaticamente.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Adaptador unificado de chaves (fundação arquitetural)</b> — Novo módulo <code>bracket-model.js</code> expõe <code>window._getUnifiedRounds(t)</code>, que lê qualquer um dos três formatos internos (<code>t.matches</code>, <code>t.rounds</code>, <code>t.groups</code>) e devolve uma única forma canônica de "colunas da tira unificada". Cada coluna já traz fase (<code>swiss</code>, <code>elim</code>, <code>groups</code>, <code>monarch</code>, <code>thirdplace</code> etc.), rótulo, status e partidas — prontos para o renderer consumir de um jeito só. É código novo, inerte nesta versão (nenhum renderer o chama ainda), mas é a base para unificarmos, nos próximos deploys, os 5 renderers e os N geradores numa única pipeline — eliminando o problema de ajustes visuais e correções precisarem ser reaplicados formato a formato. Verificado com 5 fixtures (elim, Suíço→elim, Liga, Grupos+Elim, Rei/Rainha). Disponível para inspeção via <code>window._bracketModelSanityChecks()</code> no console.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.57-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tira única: Suíço + Eliminatória num só chaveamento horizontal</b> — Na v0.12.56 a fase classificatória Suíço virava um card separado <i>abaixo</i> do bracket eliminatório, com botão Mostrar/Ocultar. Agora as rodadas Suíço já disputadas aparecem como <b>colunas à esquerda do bracket</b>, na mesma tira horizontal das Quartas/Semi/Final — uma chave única sincrética, adaptada por caso. Cada coluna Suíço leva o selo "Suíço R1 ✓", "Suíço R2 ✓" etc. com os placares compactos, e o ✓ verde marca quem avançou para a eliminatória. O organizador/participante enxerga a linha do tempo completa do torneio num só scroll, sem troca de contexto ou abrir/fechar acordeões. Primeiro passo de uma refatoração maior para unificar os caminhos de geração/renderização de todos os formatos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.56-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Correção: após o Suíço classificatório, bracket eliminatório aparece continuamente</b> — Em torneios que usavam Suíço como resolução de potência de 2 (ex: 20 inscritos → 2 rodadas Suíço → top 16 eliminatória), assim que a última rodada Suíço era fechada, a tela caía num estado "Nenhuma rodada gerada ainda / 🎲 Iniciar Primeira Rodada" por uns instantes — apagando a sensação de continuidade do torneio. A transição para a eliminatória agora faz o bracket (já desenhado pelo sorteio automático) aparecer imediatamente na tela.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.55-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>"3º Lugar" com ordinal sobrescrito</b> — As chaves i18n <code>bracket.thirdPlace</code> e <code>bracket.thirdPlaceLabel</code> em português estavam escritas como "3o Lugar" (letra "o" minúscula), o que em maiúsculas virava "3O LUGAR" e podia ser lido como "30 LUGAR". Trocado pelo caractere ordinal masculino (º), deixando "3º Lugar" — legível em qualquer casing.</p>' +
        '<p><b>Classificação colapsável (oculta por padrão)</b> — A tabela de classificação do Suíço/Liga agora aparece recolhida por padrão, com um cabeçalho clicável "▸ Mostrar classificação" no card. Ao clicar, expande para a tabela completa com todos os critérios de desempate e vira "▾ Ocultar classificação". Tira carga visual da tela inicial (os cards de partida e o bracket continuam em destaque) e dá acesso rápido a quem quer ver a tabela detalhada.</p>' +
        '<p><b>Rodadas Anteriores abertas por padrão</b> — O bloco "📜 Rodadas Anteriores" do Suíço/Liga volta a aparecer expandido por padrão (como já acontecia na eliminatória simples), mas mantém o cabeçalho clicável para ocultar quando o histórico fica longo.</p>' +
        '<p><b>Botão "Ocultar" padronizado nas Rodadas Anteriores</b> — Substituímos o antigo <code>&lt;details&gt;</code> do bloco "📜 Rodadas Anteriores" pelo mesmo botãozinho pill "Ocultar" / "Mostrar" já usado inline nas rodadas da eliminatória linear. Assim a UX fica consistente entre os formatos e o organizador/jogador identifica o controle de imediato.</p>' +
        '<p><b>Botão "Voltar" no fluxo de Novo Torneio</b> — O modal rápido de criação e o formulário avançado agora trazem o botão "← Voltar" (estilo pill padrão do app) fixo no topo, sempre visível enquanto o usuário rola o formulário. Consistente com a navegação de outras telas e oferece uma saída previsível para quem abriu por engano ou mudou de ideia.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.54-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>3º Lugar sempre visível no chaveamento futuro</b> — Na v0.12.53 o card "🥉 3º Lugar" só aparecia quando o torneio tinha flags legadas (thirdPlace/elimThirdPlace) ligadas. Como a disputa de terceiro lugar é obrigatória no Scoreplace há tempos (não é mais opção do organizador), o card agora é sempre exibido abaixo da Final — em todo torneio eliminatório.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.53-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Disputa de 3º Lugar no chaveamento futuro</b> — A coluna da Final ganhou, logo abaixo do card da partida final, um card tracejado "🥉 3º Lugar" para a disputa de terceiro colocado. Antes só aparecia a Final — agora o organizador e os participantes veem desde o início que haverá disputa de 3º lugar e quantos confrontos no total (mesmo padrão do bracket linear já existente, onde o 3º lugar fica empilhado na coluna da Final).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.52-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cada rodada numa coluna (sistema linear) no chaveamento Suíço</b> — Na v0.12.51 as rodadas futuras apareciam empilhadas verticalmente como cards de largura total, uma embaixo da outra. Agora a visualização segue o mesmo sistema do chaveamento eliminatório linear: cada rodada ocupa uma coluna vertical, e as colunas ficam lado a lado numa tira horizontal com scroll. A rodada atual (com inputs de placar, botões de confirmar, Encerrar Rodada) fica na primeira coluna; as rodadas Suíço pendentes seguem à direita; e, quando o Suíço é resolução de potência de 2, as rodadas eliminatórias (Oitavas → Quartas → Semi → Final, tracejadas em dourado) seguem depois. O cabeçalho "🎯 Fase Classificatória → 🏆 Fase Eliminatória" fica acima da tira, unificado. Assim o organizador enxerga o fluxo completo da esquerda pra direita, igual ao bracket linear, sem ter que scrollar pra baixo pra ver as próximas rodadas.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.51-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rodadas futuras com slots "A definir" no lugar dos times</b> — As rodadas ainda não disputadas apareciam como um único card de texto ("Aguardando rodada anterior"), sem dar ideia de quantos jogos teriam nem como seriam visualmente. Agora cada rodada futura (Suíço classificatório remanescente + toda a fase eliminatória) é renderizada como um card completo, com o mesmo número de partidas que a rodada vai ter e cada partida mostrando dois slots "? A definir" em estilo tracejado. Conforme os adversários são definidos (Suíço: pela classificação da rodada anterior; eliminatória: pelos top N classificados), os slots são preenchidos. Assim o organizador e os participantes enxergam desde o primeiro jogo a estrutura completa do torneio — não só quantas rodadas, mas também quantos confrontos em cada uma.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Correção crítica: Confirmar placar da Rodada 2/2 do Suíço não salvava</b> — Em torneios Eliminatórios com Suíço como resolução de potência de 2 (ex: 20 inscritos → 2 rodadas Suíço → top 16 na eliminatória), ao encerrar a Rodada 1 o app disparava o fechamento dela por <i>dois caminhos duplicados</i> — o auto-close do último placar (v0.12.47) e a rede de segurança do render (v0.12.48) — no mesmo tick. O primeiro fechava a Rodada 1 e gerava a Rodada 2; o segundo, chamado imediatamente depois com o mesmo índice, via <code>rounds.length = 2</code>, achava que as 2 rodadas Suíço já estavam cumpridas e <i>transicionava direto para a eliminatória</i>, apagando a Rodada 2 recém-gerada. O organizador via os confrontos da Rodada 2 sumirem antes mesmo de poder lançar placares. Adicionado guard em <code>_doCloseRound</code>: ignora chamadas com <code>roundIdx</code> obsoleto (já não é a última rodada) ou para rodadas já marcadas como <code>complete</code>. O fechamento da rodada agora é idempotente — qualquer número de disparos produz o mesmo resultado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.49-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fase eliminatória também visível quando Suíço é apenas a resolução de potência de 2</b> — Continuação da v0.12.48. Em torneios onde o formato é Eliminatórias mas o Suíço foi escolhido como resolução para o problema de potência de 2 (ex: 20 inscritos → 2 rodadas Suíço → top 16 entra na chave eliminatória), o chaveamento só mostrava as rodadas Suíço — parecia um torneio Suíço puro sem eliminatória. Agora o chaveamento exibe a estrutura completa desde o início: um cabeçalho azul <i>"🎯 Fase Classificatória — Suíço — N rodadas"</i> acima da rodada atual, e depois dos placeholders Suíço um cabeçalho dourado <i>"🏆 Fase Eliminatória (Top N)"</i> seguido de cards tracejados para cada rodada eliminatória (Oitavas, Quartas, Semifinais, Final — com nomes apropriados ao número de classificados). Assim o organizador e os participantes veem desde o primeiro dia todas as fases do torneio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.48-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Todas as rodadas Suíço visíveis desde o início + desbloqueio de torneios travados</b> — O chaveamento Suíço mostrava só a rodada atual; o usuário tinha que deduzir pelas contagens quantas rodadas faltavam. Agora as rodadas futuras aparecem como cards tracejados ("Rodada 2/2 • Aguardando rodada anterior") abaixo da rodada em andamento — os confrontos são sorteados automaticamente quando a rodada anterior termina (pareamento Suíço precisa das classificações do round anterior pra ser justo, então não dá pra sortear tudo de uma vez). Também foi adicionada uma rede de segurança no render do chaveamento: se uma rodada está com todos os resultados lançados mas não foi encerrada (caso de torneios criados antes da v0.12.47), o app dispara o fechamento automaticamente, gerando a próxima rodada. Dashboard: a seção "Torneios Encerrados" agora abre fechada por padrão — clique na seta para expandir.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.47-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Suíço: avanço automático para a próxima rodada</b> — Em torneios com Suíço como resolução de potência de 2 (e em Suíço puro também), ao lançar o último resultado de uma rodada, o app não gerava a próxima rodada automaticamente — o organizador precisava clicar em "Encerrar Rodada" manualmente, e isso não estava óbvio. Relatado em torneio de 2 rodadas Suíço: após registrar todos os placares da Rodada 1, a Rodada 2 simplesmente não aparecia. Agora, quando o último resultado de uma rodada é registrado, o app dispara automaticamente o fechamento da rodada e gera a próxima (ou transiciona para a fase eliminatória, se Suíço estava sendo usado como classificação). O botão manual "Encerrar Rodada" continua disponível caso se queira encerrar antes da hora.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.46-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel "Tudo Pronto para o Sorteio!": botões sempre visíveis + registro no histórico</b> — O painel final estava cortando embaixo em telas altas: o botão "Voltar e Revisar" aparecia parcialmente atrás da barra de endereço do iOS. Reestruturado em flex-column com rodapé fixo — os botões "Rodar Sorteio Agora" e "Voltar e Revisar" ficam sempre visíveis, o meio (checklist e histórico) rola internamente se precisar. A resolução do resto (Lista de Espera, Exclusão) agora também é gravada no Histórico de Resoluções, com indicação do método (sorteio geral ou últimos inscritos) e os nomes dos afetados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.45-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel do resto: cobertura total e descrição dinâmica do toggle</b> — Em telas altas (iPhone com barra de endereço recolhida), o overlay escuro deixava aparecer o conteúdo do torneio abaixo do painel. Trocado para cobertura absoluta (<code>inset:0</code> + <code>100dvh</code>) com opacidade 0.96 e blur de fundo, garantindo isolamento visual total. A descrição do toggle "Sorteio Geral" agora alterna em tempo real: com ele ativo mostra "Sorteio entre todos os inscritos."; desligado, "Aplica aos últimos inscritos.".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.44-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Resolução do resto: painel único com toggle "Sorteio Geral"</b> — Ao clicar em <b>Sortear</b> num torneio em que sobram participantes sem formar time/chave completa, o app mostrava <i>dois painéis diferentes</i> dependendo do caminho: um amarelo ("Times Incompletos") com Repescagem/Lista de Espera/Exclusão, e outro roxo ("Participantes Restantes") com Reabrir/Lista de Espera/Exclusão. Além disso, escolher Lista de Espera ou Exclusão abria <i>mais um painel</i> perguntando se deveria sortear entre todos ou remover os últimos inscritos — 3 telas para uma decisão.</p>' +
        '<p>Agora é tudo num painel só (o roxo). Acima das opções aparece um toggle <b>Sorteio Geral</b>, ligado por padrão: com ele ativo, o app sorteia aleatoriamente entre todos os inscritos; desligado, retira os que se inscreveram por último. Clicar em Lista de Espera ou Exclusão aplica a ação imediatamente segundo o toggle, sem tela intermediária. O painel amarelo antigo foi removido, assim como o painel de sub-escolha.</p>' +
        '<p>Correção de layout: o painel estava cortando embaixo em telas estreitas (iOS Safari com a barra de endereço dinâmica). Trocado <code>100vh</code> por <code>100svh</code> e <code>90vh</code> por <code>94svh</code> para que a última opção apareça inteira.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.43-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Nomes tradicionais de rodada só até Oitavas</b> — "Dezesseis avos" e "Trinta e dois avos" não são nomes usados na prática — torneios reais começam a nomear rodadas a partir das Oitavas de Final (8 jogos). Rodadas anteriores voltam a ser exibidas como "Rodada 1", "Rodada 2", etc. Chaveamentos com mais de 16 participantes agora mostram: Rodada 1 → Oitavas → Quartas → Semifinais → Final.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.42-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Voltar" parou de funcionar no chaveamento depois que todos os resultados foram lançados</b> — Sintoma: com o torneio finalizado (todos os jogos com placar e o banner "🏆 Campeão" no topo da chave), clicar em "Voltar" na barra sticky no alto da tela não tinha efeito. Causa raiz: em telas estreitas a topbar pode quebrar em duas linhas (ficando ~90px de altura em vez de 60px), mas a <code>.sticky-back-header</code> estava fixada em <code>top: 60px</code> no CSS — então a parte baixa da topbar pintava <i>por cima</i> do botão Voltar. Mesmo com <code>z-index: 101</code> no back-header (acima do 100 da topbar), <code>position:sticky</code> com backdrop-filter acaba vencendo <code>position:fixed</code> quando os retângulos se cruzam, em vários browsers. Correção: o helper <code>_syncBackHeaderSpacer</code> agora mede a altura real da topbar e seta <code>top</code> do back-header para esse valor — header fica empurrado pra baixo até onde a topbar terminar, sem sobreposição. ResizeObserver agora também observa a topbar para recalcular na hora que ela quebra/desdobra (login/logout, filtros, redimensionamento). Não era um bug ligado a "finalizar torneio" em si — ficava aparente nessa tela porque o torneio finalizado era exibido em contextos de viewport mais estreito (tablet/mobile landscape) onde a topbar quebrava.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.41-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Rodadas com nomes tradicionais — Oitavas de Final e além</b> — Antes, só as três últimas rodadas de um chaveamento eliminatório recebiam nomes tradicionais: <i>Final</i>, <i>Semifinais</i> e <i>Quartas de Final</i>. Rodadas anteriores caíam no genérico "Rodada N". Agora a nomenclatura segue até onde os torneios de verdade nomeiam: rodada com 8 jogos = <b>Oitavas de Final</b> (em inglês, <i>Round of 16</i>), rodada com 16 jogos = <b>Dezesseis Avos de Final</b> (<i>Round of 32</i>), rodada com 32 jogos = <b>Trinta e Dois Avos de Final</b> (<i>Round of 64</i>). O cálculo continua sendo por distância da final (fromEnd), então funciona tanto em eliminatória simples quanto em repescagem/double elimination, respeitando o tamanho real do chaveamento (torneios menores pulam direto para Quartas/Semis/Final).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.40-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in da view de Inscritos — boxes de jogos uniformes e botão "Limpar" removido</b> — Duas correções visuais na tela de check-in do torneio: (1) O botão <b>"Limpar"</b> (vermelho, ao lado da barra de progresso) foi removido — ele chamava <code>_resetCheckIn</code> e zerava todos os check-ins e ausências do torneio sem distinguir entre "corrigir engano" e "recomeçar do zero", o que era perigoso e sem atalho de desfazer. Controle fino continua disponível via toggle individual por participante. (2) Os <b>boxes de jogos</b> (cartões "Jogo X" com os jogadores e dots de presença) eram renderizados com <code>flex-wrap</code> e <code>flex:1</code>, o que fazia o último box de cada linha esticar para preencher a largura disponível — inconsistente quando sobrava um jogo no final da linha. Agora usam CSS grid com <code>grid-template-columns:repeat(auto-fill, minmax(200px, 220px))</code>: boxes sempre com o mesmo tamanho fixo (~200–220px, o tamanho de 2 colunas no mobile), e mais colunas aparecem conforme a tela fica mais larga, nunca esticando um box isolado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.39-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Placar ao vivo em torneio — agora faz check-in, avança o vencedor e usa "Semifinais" no plural</b> — Três correções na gravação de resultado via placar ao vivo de um card do chaveamento: (1) <b>Check-in automático</b>: antes, só o fluxo do lançamento inline (<code>_saveSetResult</code>) marcava ambas as duplas como presentes; o <code>_saveResult</code> do live-scoring salvava o placar mas deixava perdedores sem <code>checkedIn</code>, o que disparava WO e outras heurísticas de ausência mesmo após o jogo ter sido disputado. Agora o live-scoring também popula <code>t.checkedIn</code> para os 4 jogadores (ou 2 no individual), limpa <code>t.absent</code>, e marca <code>t.tournamentStarted</code> se ainda não estava — suportando tanto o separador <code>A / B</code> do fluxo padrão quanto <code>A/B</code> do live-scoring. (2) <b>Avanço antes do re-render</b>: a ordem era <code>syncImmediate → saveTournament → _rerenderBracket → _advanceWinner → _maybeFinishElimination</code>. O re-render lia o estado <i>antes</i> do avanço, então o próximo card da rodada seguinte continuava com "TBD" até o próximo tick de sync. A nova ordem é <code>_advanceWinner → _maybeFinishElimination → syncImmediate → saveTournament → _rerenderBracket</code>, e o card da próxima rodada já mostra o vencedor imediatamente. (3) <b>"Semifinais" no plural</b>: a label <code>bracket.semiFinal</code> em pt e en estava como "Semifinal"/"Semifinal" mas agrupa <i>múltiplos</i> jogos da mesma rodada (como "Quartas de Final" / "Quarterfinals" já estão). Corrigida para "Semifinais" (pt) e "Semifinals" (en).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tela final do placar ao vivo em partida de torneio — só mostra "Confirmar Resultado"</b> — Quando o placar ao vivo é aberto a partir de um card do chaveamento, a tela de vitória exibia os mesmos botões da Partida Casual: "↺ Resetar" e "✕ Fechar" no cabeçalho, e "🔄 Jogar Novamente" + toggle "Re-sortear duplas" pinados no topo. Nenhum deles faz sentido num torneio — resetar jogaria fora o resultado que já vai para o chaveamento, re-sortear misturaria as duplas do torneio, e o próprio "Fechar" era ambíguo (salva? descarta?). Agora, ao terminar a partida em modo torneio, <b>todos esses botões somem</b> e no lugar aparece um único botão verde <b>"✓ Confirmar Resultado"</b>. Clicar grava o resultado na partida, avança o vencedor pelo chaveamento (via <code>_advanceWinner</code> e <code>_maybeFinishElimination</code>), fecha o overlay e o usuário cai de volta no bracket já rolado para o card daquela partida (ancoragem existente em <code>_rerenderBracket(tId, matchId)</code>). Modo Partida Casual continua com os botões originais.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.37-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho fixo não corta mais o primeiro card em telas estreitas</b> — O cabeçalho fixo no topo das views (Inscritos, Explorar, Notificações, Regras, etc.) usava um espaçador CSS de altura fixa (<code>margin-top: 50px</code>) para que o conteúdo abaixo não ficasse por trás dele. O problema: em telas estreitas o cabeçalho podia ter 2 ou 3 linhas (título + pílulas de filtro Todos/Presentes/Ausentes/Aguardando + barra de progresso + botão Limpar) e ficar com 140–160px de altura, fazendo o primeiro card de participante desaparecer atrás do cabeçalho. Agora um helper global (<code>_syncBackHeaderSpacer</code>) mede a altura real do cabeçalho via <code>getBoundingClientRect()</code> e aplica um <code>margin-top</code> correspondente no próximo elemento, mais 8px de folga. ResizeObserver no cabeçalho e MutationObserver no <code>view-container</code> mantêm o espaçador sempre em sinc — se o usuário redimensiona a janela, ativa um filtro que adiciona/remove linhas, ou navega entre views, o espaçador recalcula automaticamente.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.36-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Placar ao Vivo em partidas de torneio — organizador, participante ou árbitro podem lançar pontos ao vivo</b> — Antes o botão "📡 Ao Vivo" só aparecia no card de partida para o <i>participante</i> (quando a regra "Jogadores lançam" estava ativa) e em jogos que eram dele. Agora o mesmo botão fica disponível para qualquer um que a regra de lançamento permita: <b>organizador</b> (sempre, inclusive em jogos que não são dele), <b>participante</b> (quando "Jogadores" estiver ativo e o jogo for dele — comportamento preservado) e <b>árbitro</b> (quando a regra "Árbitro" estiver ativa, em qualquer jogo). Clicando abre exatamente a mesma tela de placar ao vivo das Partidas Casuais — com contagem de sets, games, pontos, tiebreak, log de rally, controle de saque — e ao final grava o resultado no card, avança o vencedor no chaveamento e sincroniza com o Firestore como se fosse um lançamento manual. Configurar quem pode lançar continua no campo "Lançamento de Resultado" das regras do torneio (organizador / jogadores / árbitro ou qualquer combinação).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.35-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sup do tie-break agora mostra o placar real do vencedor, não a base 7</b> — Quando um tie-break é decidido com a regra de 2 pontos de vantagem, pode terminar além dos 7 pontos nominais (9-7, 11-9, 13-11, etc.). O sup entre parênteses exibido no placar do set passava o <b>menor</b> dos dois números do TB — se o TB terminava 9-7, aparecia <code>(7)</code>, fazendo parecer que o vencedor marcou só 7 pontos e mascarando a extensão do TB. Agora o sup mostra o <b>placar real do vencedor do tie-break</b> (o maior dos dois números): 9-7 vira <code>(9)</code>, 11-9 vira <code>(11)</code>, 7-5 vira <code>(7)</code>, etc. Alterado em quatro pontos de renderização: resumo textual ao salvar o resultado casual, string de <code>scoreSummary</code> persistida por partida, sup na tela de Estatísticas Detalhadas, e sup no card de partida do bracket de torneios. Isso corresponde ao que o usuário efetivamente viu na quadra e torna imediata a leitura de TBs prolongados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.34-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual — toggle "AD" agora é respeitado de verdade (migração remove chave legada)</b> — Correção do bug que deixava a vantagem no game (AD) sendo aplicada mesmo quando o usuário desligava o toggle em Beach Tennis. Causa raiz: a migração de <code>advantageRule</code> → <code>deuceRule</code> introduzida na v0.12.32 <i>adicionava</i> <code>deuceRule</code> ao objeto salvo em <code>localStorage</code>, mas <i>não removia</i> a chave legada <code>advantageRule</code>. Na inicialização do estado da partida, o fallback <code>sc.deuceRule === true || sc.advantageRule === true</code> fazia o OR com a chave velha — então mesmo após o usuário desligar o toggle (novo <code>deuceRule: false</code>), o <code>advantageRule: true</code> remanescente forçava o modo AD a se reativar. Correção em duas camadas: (1) <code>_getConfig()</code> agora <code>delete stored.advantageRule</code> assim que migra, e reescreve o <code>localStorage</code> imediatamente para que o objeto persistido fique limpo; (2) a inicialização do estado (<code>_startLiveScoring</code>, linha 1885) passou a preferir <code>deuceRule</code> quando definido — só cai no <code>advantageRule</code> quando <code>deuceRule</code> está de fato ausente. Assim tanto jogadores que abriram o painel após a v0.12.32 (e já têm o objeto migrado) quanto os que nunca abriram (e ainda têm só <code>advantageRule</code>) terminam no comportamento correto. Resolve o relato "continua aplica a AD quando está desligado e o jogo é beach tennis".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.33-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual — AD desligado agora fecha o game no primeiro a 4 pontos (sem vantagem)</b> — Ao desligar o toggle "AD" (vantagem no game, golden point) em modalidades como Beach Tennis, o game não terminava — ficava preso em 40-40 porque a função <code>_checkGameWon</code> continuava exigindo 2 pontos de vantagem mesmo com <code>deuceRule: false</code>. Agora a lógica é explícita: se <code>deuceRule</code> está desligado, o primeiro lado a chegar em 4 pontos vence o game independentemente da diferença — comportamento correto de golden point em tennis-style scoring.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual — separação clara entre "AD" (game) e "Vantagem de 2 pontos" (set); remoção de Placar Simples e da seção Tipo de Placar</b> — Limpeza e precisão no painel de configuração da Partida Casual. (1) <b>Removido "Placar Simples"</b>: a modalidade <code>_simple</code> saiu da lista de esportes — não era uma modalidade, era um modo de placar disfarçado. (2) <b>Removida a seção "Tipo de Placar" (Simples × Game Set Match)</b>: a escolha era redundante, já que o modo "simples" não existia como modalidade de verdade e todo jogo casual agora usa o modelo Game-Set-Match. (3) <b>Novo toggle "AD" logo abaixo da Contagem (15-30-40 / 1-2-3)</b>: representa a <i>regra de deuce no game</i> — ao empatar em 40-40, exige 2 pontos de vantagem (AD → jogo) em vez de ponto de ouro. Padrão por modalidade: Beach Tennis <b>desligado</b> (golden point), Pickleball <b>desligado</b>, Tênis <b>ligado</b>, Tênis de Mesa <b>desligado</b>, Padel <b>ligado</b>. (4) <b>Toggle "Vantagem de 2 pontos" agora é claramente set-level</b>: não se confunde mais com o AD do game. Ligado (padrão em todas as modalidades), o set não acaba em 5-6 — prorroga para 7 (extend) ou decide em tie-break, perguntando a cada novo empate que impossibilite a vantagem de 2 games. Desligado, o set termina no primeiro a chegar em g games, sem extensão e sem tie-break. O toggle de Tie-break só aparece quando a vantagem de 2 pontos está ligada (caso contrário não tem efeito). (5) Internamente: <code>advantageRule</code> foi renomeado para <code>deuceRule</code> no estado, com shim de migração que lê valores antigos de <code>localStorage</code> e de torneios salvos. <code>twoPointAdvantage</code> é novo, default <code>true</code>, e gate o <code>_checkSetWon</code> para terminar sets cedo quando desligado. GSM de torneios permanece inalterado — a mudança é só do painel casual.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas — TB Perdidos agora mostra o placar do adversário (não o seu)</b> — Correção semântica nas linhas "TB Perdidos" e "Pontos TB Médios (lado perdidos)". Antes, quando um TB era perdido, o app mostrava o placar que <i>você</i> tinha feito naquele TB (sempre o menor dos dois números, já que quem perdeu tem menos pontos). Isso era informativo mas não casava com a simetria desejada: "TB Vencidos mín/máx" = os seus pontos, "TB Perdidos mín/máx" = os pontos do <i>adversário</i>. Agora a regra é uniforme: cada linha mostra o <b>placar do vencedor</b> daquele TB. À direita (vencidos, verde) = os seus pontos quando venceu. À esquerda (perdidos, vermelho) = os pontos do adversário quando venceu contra você. Assim o <b>máximo</b> dos TB Perdidos vira a "maior surra que tomou" (maior placar do adversário), o <b>mínimo</b> vira a "derrota mais apertada" (menor placar do adversário, geralmente 7), e a média do lado perdidos do bloco "Pontos TB Médios" vira a média dos placares vencedores contra você. A seção de agregação em <code>_aggregate</code> passou a registrar <code>_oppPts</code> em vez de <code>_myPts</code> nos três trackers de TB perdidos (<code>tbLostPointsSum</code>, <code>tbLostMax</code>, <code>tbLostMin</code>), então os valores recalculam automaticamente com os dados históricos já salvos. Manual também atualizado para descrever cada métrica com a nova leitura.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dicas contextuais nas Estatísticas, Voltar fixo no manual e manual atualizado</b> — Três ajustes focados em descoberta e navegação: (1) <b>Dicas na tela de Estatísticas Detalhadas</b>: criado o contexto <code>player-stats</code> no sistema de hints com nove dicas específicas (Voltar, avatar, barras divergentes, linhas de tiebreak, barras casuais vs torneios, Top 3 Parceiros/Adversários, Torneios Disputados). Diferente dos outros overlays que bloqueiam dicas globais, o <code>player-stats</code> permite que as dicas do menu (topbar) continuem aparecendo junto com as dicas da tela — como o overlay das estatísticas fica abaixo da topbar (<code>z-index: 90</code> vs 100), o menu segue interativo e merece sua própria orientação visual. Dicas da dashboard foram removidas enquanto o modal está aberto, exatamente como pedido. (2) <b>Botão Voltar fixo no topo do manual</b>: a Central de Ajuda ganhou o mesmo padrão das sub-views (<code>position: sticky</code> no cabeçalho do modal): pill "← Voltar" à esquerda com ícone SVG, título centralizado e × à direita, com a barra de busca logo abaixo. Tudo permanece visível ao rolar pelo conteúdo das 20+ seções. (3) <b>Manual atualizado</b>: a seção "Partida Casual" ganhou menção explícita da <i>vantagem de 2 pontos ligada por padrão</i> (v0.12.29) e do <i>glow pulsante no botão de tie-break durante prorrogação</i> (v0.12.26). A seção "Estatísticas Detalhadas" foi reescrita para refletir a nova ordem das barras (1-5 são V/D/Sets/Games/Pontos/Tiebreaks, 6-8 são Pontos TB Médios/TB Vencidos mín-máx/TB Perdidos mín-máx, todas logo após Tiebreaks), com explicação de cores e do bloco Top 3 Adversários / Parceiros. Botão de Voltar do modal de estatísticas agora aparece documentado como "fixo no topo, sempre visível durante o scroll".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.29-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Pacote com quatro ajustes pedidos: tiebreaks detalhados, Voltar fixo, Explorar agrupado por data e vantagem de 2 pontos por padrão</b> — (1) <b>Estatísticas Detalhadas — três novas linhas de tiebreak mais informativas</b>: as três últimas linhas da seção "📊 Estatísticas Detalhadas" (antes "TB Média", "TB Máximo", "TB Mínimo", todas barras casuais ⇄ torneios) foram substituídas por três linhas divergentes que separam os tiebreaks ganhos dos perdidos: <i>"Pontos TB Médios"</i> (média de pontos que o jogador fez em TBs perdidos à esquerda em vermelho · média em TBs vencidos à direita em verde, ícones ⚡ casuais e 🏆 torneios nas extremidades), <i>"TB Vencidos"</i> (mínimo e máximo de pontos que o jogador marcou dentro de um único TB vencido — ambos os lados em verde, refletindo que os dois valores vêm do mesmo grupo semântico) e <i>"TB Perdidos"</i> (mínimo e máximo em um único TB perdido — ambos em vermelho). A seção inteira só aparece se pelo menos um tiebreak já foi jogado. Internamente, <code>_aggregate</code> passou a trackear <code>tbWonPointsSum</code>/<code>tbLostPointsSum</code>, <code>tbWonMin</code>/<code>tbWonMax</code> e <code>tbLostMin</code>/<code>tbLostMax</code>, e <code>_diffBarRow</code> recebeu parâmetro opcional <code>opts.leftClr</code>/<code>opts.rightClr</code> para renderizar barras com mesma cor nos dois lados quando a semântica é "range dentro do mesmo grupo" em vez de vitória×derrota. (2) <b>Estatísticas Detalhadas — botão "← Voltar" agora fica fixo no topo</b>: antes o botão Voltar do v0.12.28 era parte do corpo do modal e desaparecia ao rolar para ver as estatísticas de tiebreak ou os Top 3. Agora a barra superior (Voltar à esquerda, × à direita) usa <code>position: sticky; top: 0</code> dentro do container de scroll do overlay, com margens negativas para estender flush até as bordas arredondadas do modal e <code>backdrop-filter: blur(12px)</code> para manter legibilidade quando o conteúdo desliza por trás. Mesma promessa do <code>.sticky-back-header</code> das outras sub-views do app: o Voltar está sempre visível, não importa o quanto rolou. (3) <b>Explorar — cards agrupados por data</b>: na lista "Outros usuários" da tela Explorar, quando o modo de ordenação é Data (padrão), as pessoas encontradas em torneios ou com última atividade recente agora aparecem agrupadas por dia. Cada grupo tem um cabeçalho com emoji 📅 e a data legível (ex: "15 abr, 2026") seguido pelo grid de cards daquele dia. Sem data registrada? Vai para um bucket final "Sem encontros registrados". A data foi removida da parte inferior de cada card individual já que agora está centralizada no cabeçalho do grupo, deixando os cards mais limpos. No modo alfabético, os cards seguem num grid único sem grupos, como antes. (4) <b>Nomes longos quebram em duas linhas nos cards de Explorar</b>: o <code>text-overflow: ellipsis</code> dos nomes e chips foi substituído por <code>-webkit-line-clamp: 2</code> com <code>word-break: break-word</code> — nomes compostos ou com sobrenome longo agora quebram naturalmente em duas linhas antes de serem truncados, melhorando a legibilidade sem alargar os cards. Aplicado tanto em <code>_userCardWithEncounterHtml</code> (resultados principais) quanto em <code>_userCardHtml</code> (convites enviados). (5) <b>Partida Casual — vantagem de 2 pontos ligada por padrão</b>: o toggle "Regra de vantagem" do painel de configuração de Partida Casual agora vem ativado por padrão em todas as modalidades (Beach Tennis, Pickleball, Tênis de Mesa e Padel — Tênis já vinha ligado). A regra "ao empatar em 40-40, exige 2 pontos de vantagem" espelha melhor o que acontece em quadra, então faz mais sentido ser o default e o usuário desativar se quiser jogar sem advantage. Preferências salvas em <code>scoreplace_casual_prefs</code> continuam respeitadas — a mudança afeta apenas novos usuários ou quem ainda não personalizou a config.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Voltar" na tela de Estatísticas Detalhadas</b> — O modal de estatísticas ganhou uma barra superior com botão pill "← Voltar" à esquerda (mesmo padrão visual do Gerenciador de Categorias e demais sub-views: ícone SVG de seta + texto, borda e fundo via variáveis de tema, hover-lift). O × de fechar continua à direita, então o usuário tem dois caminhos redundantes para sair — clicar fora do modal, apertar ESC, usar o × ou o novo "Voltar".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas Detalhadas: menu visível, tema aplicado, contraste melhorado e listas enxutas</b> — A tela de "📊 Estatísticas Detalhadas" (modal aberto ao clicar no nome do jogador) recebeu quatro ajustes pedidos: (1) <b>Menu do app agora permanece visível</b> — o overlay deixou de cobrir a topbar (começa em <code>top: 60px</code> com <code>z-index: 90</code>, abaixo da topbar que é 100), assim o usuário pode navegar para outra seção a qualquer momento sem precisar fechar o modal primeiro. (2) <b>Tema escolhido é aplicado na tela</b> — fundos, bordas e tracks de barras usam agora variáveis CSS (<code>--bg-card</code>, <code>--info-box-bg</code>, <code>--stat-box-bg</code>, <code>--border-color</code>, <code>--info-pill-bg</code>) em vez de <code>rgba(255,255,255,0.xx)</code> hardcoded; em modos Claro/Pôr do Sol/Oceano as caixas e barras ganham o tom correto do tema em vez de sumirem por baixa opacidade. (3) <b>Cores de texto com contraste adequado</b> — títulos "Top 3 Parceiros/Adversários" trocaram o cinza sobre escuro pelo <code>--text-bright</code>, nomes dos jogadores nas listas usam <code>--text-main</code>, e V/D/aproveitamento usam <code>--success-color</code> / <code>--danger-color</code> (tons adaptativos por tema) em vez de #22c55e/#ef4444 fixos, garantindo legibilidade no tema Claro. (4) <b>Top 5 virou Top 3</b> — as quatro listas (Parceiros Casuais, Adversários Casuais, Parceiros Torneios, Adversários Torneios) agora mostram apenas os 3 primeiros colocados ordenados por partidas disputadas, deixando a tela mais compacta e o "top" realmente entre os mais relevantes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.26-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas suplementares viraram barras comparativas casuais ⇄ torneios</b> — As 14 métricas de valor único da seção "📊 Estatísticas Detalhadas" (Aproveitamento, % Saque, % Recepção, Games Mantidos, Quebras, Maior Seq. de Pontos, Maior Seq. de Vitórias, Tempo Total, Média por Ponto, Ponto Mais Longo, Ponto Mais Curto, TB Média, TB Máximo e TB Mínimo) deixaram de aparecer como grade de caixinhas compactas e agora seguem o mesmo padrão de barras divergentes das métricas pareadas acima. Cada métrica ocupa uma linha: no cabeçalho, rótulo azul-claro "CASUAIS" à esquerda, nome da métrica centralizado, rótulo azul-escuro "TORNEIOS" à direita; na linha de valores, o ícone ⚡ e o número das partidas casuais ficam colados na borda esquerda da tela, o número e o ícone 🏆 de torneios ficam colados na borda direita; entre eles, duas barras partem do centro e crescem para fora, escaladas proporcionalmente ao maior dos dois valores. <b>Cores unificadas com os botões da dashboard</b> — o azul-claro <code>#38bdf8</code> (idêntico ao gradiente do botão "Partida Casual") é usado em todas as barras e números de casuais; o azul-escuro <code>#1e40af</code> (idêntico ao botão "+Novo Torneio") é usado no lado de torneios. Agora toda a seção tem leitura visual consistente: basta correr o olho pela coluna esquerda para ver o perfil em partidas casuais e pela direita para ver o perfil em torneios, sem precisar comparar caixinha por caixinha.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Barras divergentes: ícone casual ⚡, rótulos no lugar dos "V/P" e grupos afastados às extremidades</b> — Três ajustes pedidos na seção "📊 Estatísticas Detalhadas" após o redesign do v0.12.24: (1) <b>Ícone de partidas casuais unificado com o botão da dashboard</b> — era 📡 (antena), agora é ⚡ (raio), o mesmo símbolo do botão "Partida Casual" da tela inicial. Aplicado no badge da seção, nos cabeçalhos das barras divergentes, nas caixas de estatísticas duplas e nos títulos "Top 5 Parceiros/Adversários — Casuais". (2) <b>"V/P" removido dos rótulos</b> — antes cada barra trazia "Sets V/P", "Games V/P" etc.; agora o nome da métrica aparece sozinho no centro do cabeçalho ("Sets", "Games", "Pontos", "Tiebreaks") e a palavra "perdidos" (vermelho, à esquerda) e "vencidos" (verde, à direita) substituem a abreviação nos lados. A primeira barra, que <i>é</i> o próprio par vitórias/derrotas, ganhou rótulos "Derrotas" à esquerda e "Vitórias" à direita (sem métrica central). (3) <b>Ícones e números empurrados para as extremidades</b> — antes o par 🏆+⚡ ficava centralizado perto do divisor central; agora cada grupo encosta na borda externa da sua metade (⚡🏆 grudados à direita do lado verde, 🏆⚡ grudados à esquerda do lado vermelho), deixando o meio da linha livre para as barras divergentes e criando simetria visual clara entre os dois lados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.24-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas Detalhadas redesenhadas como barras divergentes</b> — A seção "📊 Estatísticas Detalhadas" do perfil do jogador deixou de ser duas grades separadas (uma para Casuais e outra para Torneios, cada uma com 18 caixinhas) e virou uma única visão unificada, mais parecida com o gráfico de "Comparação dos Times" ao final de partida casual. As métricas pareadas (Vitórias/Derrotas, Sets V/P, Games V/P, Pontos V/P, Tiebreaks V/P) aparecem como linhas horizontais com rótulo centralizado no topo, ícones e números espelhados dos dois lados (📡 casuais e 🏆 torneios), e barras coloridas que divergem do centro — verde para a direita (vitórias) e vermelho para a esquerda (derrotas), proporcionais ao total. As métricas de valor único (Aproveitamento, % Saque, % Recepção, Games Mantidos, Quebras, Maiores Sequências, Tempos, TB Média/Máx/Mín) virão abaixo, num grid responsivo de caixas compactas onde cada caixa mostra o valor de casuais e de torneios lado a lado, separados por um divisor sutil. Top 5 Parceiros e Top 5 Adversários permanecem separados por origem (casuais 📡 e torneios 🏆) já que as listas diferem entre os dois contextos. Mesma informação, apresentação mais comparável e ocupando menos rolagem vertical.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: "Parceiro"/"Adversário" por perspectiva agora sobrevive ao sync remoto</b> — O fix do v0.12.21 fazia o remap de rótulos uma vez na abertura da tela ao vivo, mas o snapshot listener do Firestore sobrescrevia <code>p1Players</code>/<code>p2Players</code> com a versão do host a cada atualização (ponto marcado, troca de saque, etc.), voltando "Parceiro" para todos. Ou seja: por meio segundo Kelly e Nelson viam "Adversário 2", então o host pontuava, o Firestore propagava, e eles voltavam a ver "Parceiro" — o oposto do que se esperava. Correção: (1) <code>_localizeRoleLabels</code> deixou de ser IIFE e virou função nomeada que muta <code>p1Players</code>/<code>p2Players</code> in-place; (2) os dois handlers de <code>remote.p1Players</code>/<code>p2Players</code> (o listener em tempo real da partida casual e o <code>_applyRemoteState</code> do modo multi-device) agora chamam <code>_localizeRoleLabels()</code> imediatamente após copiar os nomes remotos, antes do <code>_render</code>. A lógica é idempotente: cada cliente sempre converge para ver "Parceiro" apenas nos slots do seu próprio time e "Adversário N" nos slots do time oposto, independente de qual cliente escreveu por último no Firestore.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.22-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Safari/iOS: fim do flicker entre lobby e tela de login + ícone do tema atualiza no menu hambúrguer</b> — Duas correções distintas: (1) <b>Lobby de partida casual parava de piscar no Safari</b>. O v0.12.20 já esperava o Firebase Auth rehidratar antes de renderizar, mas em alguns iPhones o <code>onAuthStateChanged</code> continuava emitindo eventos <i>transientes</i> com <code>user=null</code> (típico de ITP/cookies de terceiros), o que limpava o <code>scoreplace_authCache</code>, zerava <code>AppStore.currentUser</code>, disparava <code>_softRefreshView()</code> → re-roteava → renderizava a tela de login. Segundos depois o auth voltava com o usuário, outra refresh, de volta ao lobby. O usuário via lobby → login → lobby → login. Correções: (a) o handler de null em <code>onAuthStateChanged</code> agora <b>espera 2,5s</b> antes de comprometer o logout — se o auth reaparecer com um usuário nesse intervalo, o logout é <i>cancelado silenciosamente</i>; (b) logout manual (botão "Sair") bypassa esse grace via flag <code>_manualLogoutInProgress</code>, então quem clica em Sair vai embora na hora; (c) <code>_renderCasualJoin</code> agora faz fallback ao <code>scoreplace_authCache</code> quando <code>currentUser</code> está null temporariamente, mantendo a identidade do viewer estável; (d) a identidade é <i>re-lida</i> em cada render do lobby em vez de capturada em closure, pegando qualquer transição de auth em tempo real. (2) <b>Ícone do tema agora atualiza no menu hambúrguer</b>. O dropdown do hambúrguer <i>clona</i> o botão de tema da topbar, criando dois elementos com o mesmo <code>id="theme-toggle-btn"</code>. <code>getElementById</code> devolve só o primeiro, então o ícone do clone ficava preso no tema antigo mesmo após o clique. Agora <code>_applyThemeIcon</code> usa <code>querySelectorAll</code> e atualiza todas as cópias vivas.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: Duplas Mistas automáticas e rótulos "Parceiro/Adversário" por perspectiva</b> — Duas melhorias que trabalham juntas na configuração de Partida Casual em duplas: (1) <b>Toggle "Duplas mistas"</b> — quando o sistema detecta 2 homens + 2 mulheres no lobby (via campo <code>gender</code> do perfil dos 4 jogadores), aparece um novo toggle rosa abaixo de "Sortear Duplas", ligado por padrão. Com ele ativo, o shuffle ao iniciar garante que cada time tenha exatamente 1 homem + 1 mulher, com o usuário logado sempre na Team 1. Gêneros faltantes são buscados do Firestore (<code>FirestoreDB.loadUserProfile</code>) assim que alguém entra no lobby. (2) <b>Rótulos por perspectiva</b> — antes, o slot sem nome do time do usuário era salvo globalmente como "Parceiro" e todos os clientes viam o mesmo rótulo; mas "Parceiro" só faz sentido para quem está no <i>mesmo</i> time. Agora a tela ao vivo remapeia os rótulos localmente com base em quem está olhando: a mesma pessoa anônima é "Parceiro" para o viewer do seu próprio time e "Adversário N" para o viewer do time oposto, numerada pela posição na equipe dele. Nomes reais (quem já entrou na sala com conta) passam intactos — só slots genéricos são remapeados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convite de partida casual não pede login desnecessário no Safari/iOS</b> — Quando um usuário já logado abria um link <code>#casual/CODE</code> (ex: via QR code escaneado no Safari), a tela de lobby decidia "logado ou não" lendo <code>AppStore.currentUser</code> de forma <i>síncrona</i> no carregamento da página. Mas o Firebase Auth no Safari/iOS precisa de algumas centenas de ms para rehidratar a sessão do IndexedDB — nesse intervalo, <code>currentUser</code> ainda é <code>null</code> e o usuário caía na tela "Entre para entrar na sala". Se ele escolhia Google, o OAuth pedia senha + 2FA de novo. Agora <code>_renderCasualJoin</code> detecta a presença do <code>scoreplace_authCache</code> no localStorage e espera pela flag <code>_authStateResolved</code> (polling de 200ms, timeout de 6s) antes de renderizar, exibindo apenas o spinner de "carregando". Quem realmente não tem conta/cache passa direto para a tela de login como antes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: nomenclatura de times corrigida</b> — No início de uma Partida Casual em duplas, os rótulos automáticos estavam trocando de lado. O usuário logado às vezes acabava como "Adversário" na equipe vermelha, e a equipe azul ficava com dois "Parceiro". Causa: <code>var cu</code> estava declarado depois do uso (hoisting deixava ele <code>undefined</code>), então o swap "usuário sempre na Team 1" nunca rodava, e o rename nomeava <b>todos</b> os slots sem nome da Team 1 como "Parceiro". Agora: (1) <code>cu</code> é lido antes do shuffle, garantindo que o usuário logado seja movido para a Team 1 se acabar sorteado na Team 2; (2) o usuário <b>nunca</b> é renomeado para "Parceiro" — ele mantém o primeiro nome do perfil; (3) apenas um slot da Team 1 vira "Parceiro" (o parceiro de verdade); (4) adversários são numerados pela posição dentro da Team 2 ("Adversário 1" + "Adversário 2"), não mais por índice absoluto. Vale tanto no modo Sortear ON quanto OFF.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Explorar reestruturado: busca ao vivo, seções de amigos/convites unificadas, ordenação dos outros usuários</b> — A página Explorar ganhou quatro melhorias que trabalham juntas: (1) <b>Busca ao vivo</b> — o input filtra a lista à medida que você digita (debounce de 250ms), sem precisar clicar em Buscar. Enter ainda dispara imediatamente. (2) <b>Seções reorganizadas</b> em 4 blocos lógicos: <i>Convites Recebidos</i> (pessoas que te convidaram — precisam da sua resposta), <i>Meus Amigos</i> (amizades confirmadas), <i>Convites Pendentes</i> (pessoas que você convidou e ainda não responderam), e <i>Outros Usuários</i> (todos os demais). (3) <b>Unificação de Conhecidos e Outros</b> — a seção antes separada de Conhecidos foi fundida em Outros Usuários. Usuários com os quais você compartilhou torneios mantêm o destaque âmbar (borda + badge "N torneio(s) em comum" e data do encontro mais recente). (4) <b>Ordenação por setas de direção</b> — duas pílulas-toggle (<code>📅 Data</code> e <code>A–Z</code>) no cabeçalho de Outros Usuários, alinhadas à direita na mesma linha do título. Clicar na pílula ativa inverte a direção (↑/↓), clicar na inativa ativa com direção natural. Padrão: data decrescente (encontro mais recente primeiro). Botão Buscar enxuto (<code>btn-outline btn-sm</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Explorar: botão Buscar compacto e ordenação de Conhecidos com setas de direção</b> — Na página Explorar, o botão "Buscar" foi enxugado para <code>btn-outline btn-sm</code> (largura natural com borda, não mais bloco azul full-width ao lado do input). A seção <b>Conhecidos</b> (pessoas encontradas em torneios compartilhados) agora mostra os encontros mais recentes no topo por padrão. As três pílulas antigas de ordenação (Recentes/Antigos/A–Z) foram substituídas por <b>duas pílulas-toggle com setas de direção</b>: <code>📅 Data</code> e <code>A–Z</code>. Clicar em uma pílula ativa ou inverte sua direção — ↓ para decrescente (mais recente primeiro, ou Z→A), ↑ para crescente (mais antigo primeiro, ou A→Z). A pílula inativa mostra ⇅ indicando que pode ser usada. Os modos internos viraram <code>date-desc</code>, <code>date-asc</code>, <code>alpha-asc</code>, <code>alpha-desc</code>, com compat reversa dos rótulos legados recent/oldest/alpha.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tela de resultado simplificada: confirmação implícita ao Jogar Novamente ou Fechar</b> — Na tela de resultado da partida casual, o botão verde "✅ Confirmar Resultado" foi removido. O botão de recomeçar agora aparece em verde com o rótulo "🔄 Jogar Novamente" (sozinho no singles, ou lado-a-lado com o toggle "Re-sortear duplas" no doubles). Tanto o clique em <b>Jogar Novamente</b> quanto em <b>✕ Fechar</b> passam a tratar o resultado como confirmado — ambos persistem o registro da partida (localStorage v2 + Firestore + matchHistory por jogador) antes de recomeçar ou encerrar. Refatoração: <code>_saveResult()</code> aceita <code>{keepOpen, silent}</code> e usa guarda <code>_resultSaved</code> para ser idempotente — impede gravações duplicadas mesmo se o usuário clicar rapidamente em múltiplos caminhos de saída. Diálogos de confirmação agora informam explicitamente que "O resultado será salvo como confirmado".</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas de Tie-break no modal do jogador</b> — A grade de estatísticas detalhadas (Partidas Casuais e Torneios) ganhou uma sexta linha com 4 métricas dedicadas a tie-breaks: <b>TB V/P</b> (tiebreaks vencidos/perdidos), <b>TB Média</b> (média de pontos do jogador por tiebreak), <b>TB Máx</b> (máximo de pontos feitos em um TB) e <b>TB Mín</b> (mínimo de pontos feitos em um TB). Agregação itera cada <code>r.sets[].tiebreak</code> do histórico persistente (aceita ambos os schemas <code>{p1,p2}</code> e <code>{pointsP1,pointsP2}</code>), identifica o lado do jogador via <code>myTeam</code> e acumula vitórias/derrotas, soma e extremos dos pontos do jogador. Quando não há tiebreaks registrados, os cards exibem "—" mas continuam visíveis para sinalizar o que é rastreável.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual: Sortear Duplas auto-dirigido + toggle Misto removido</b> — O toggle "Sortear Duplas" no setup da partida casual agora reflete automaticamente o estado de formação de duplas: começa <b>ON</b> por padrão (sem times formados), passa para <b>OFF</b> assim que o organizador pareia dois jogadores via drag-and-drop (ícone de corrente), e volta para <b>ON</b> quando a dupla é desfeita. Se um participante sai da sala e os cards precisam ser rearranjados, o toggle também volta para ON. Os cards agora são arrastáveis o tempo todo (antes só eram quando Sortear estava OFF) — o organizador pode formar times com o toggle ON e ele é automaticamente desligado. Toggle <b>Misto</b> removido completamente (era puramente cosmético — não tinha efeito em nenhuma parte do fluxo de sorteio/start).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convidado da partida casual agora sai de verdade ao clicar "Sair"</b> — Antes, o jogador clicava Sair, via a dashboard por uma fração de segundo e era arremessado de volta ao lobby, ficando preso até o organizador cancelar a sala. A causa: o polling do lobby (<code>setInterval</code> a cada 3s) tinha um <code>if (_hasLeft) return;</code> só no topo do callback, mas o <code>await FirestoreDB.loadCasualMatch()</code> que vinha em seguida já estava em voo quando o usuário clicava em Sair. Quando o await resolvia, o callback continuava, chamava <code>_renderLobby()</code> e sobrescrevia o HTML da dashboard com o lobby. Adicionadas duas guardas adicionais: uma logo após o <code>await</code> e outra dentro do próprio <code>_renderLobby()</code>, para impedir qualquer renderização após a saída. Isso também protege contra outras in-flights (ex: <code>_autoJoin</code>).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convidado da partida casual volta à dashboard ao sair ou quando o criador cancela + ícone ⚡ padronizado</b> — Antes, quando o convidado clicava em "Sair" no lobby (ou quando o criador clicava "✕ Fechar" e a sala era deletada), o app fazia apenas <code>window.location.hash = "#dashboard"</code>. Em navegadores embutidos (scanner de QR do iOS, webview do WhatsApp) o evento <code>hashchange</code> é engolido e o convidado ficava preso na tela do lobby indefinidamente. Agora um helper <code>_evacuateToDashboard</code> faz dupla evacuação: limpa o <code>container</code> e chama <code>renderDashboard</code> diretamente <b>e</b> seta o hash, garantindo atualização de UI mesmo sem <code>hashchange</code>. Ao sair, o <code>sessionStorage._pendingCasualRoom</code> também é limpo para evitar auto-rejoin. Os três caminhos de saída (botão "Sair" logado, "Voltar ao Dashboard" não-logado, detecção de cancelamento pelo polling) usam o novo helper. Ícones do lobby e do cabeçalho da partida casual trocados de 📡 para ⚡ para casar com o botão "Partida Casual" da dashboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Top 5 Parceiros/Adversários ignora nomes genéricos de slot vazio</b> — Quando o organizador iniciava uma partida casual sem renomear os cards "Jogador 2", "Jogador 3", "Jogador 4" (ou quando o nome gravado era "Parceiro", "Adversário 1", "Adversário 2" etc.), esses rótulos apareciam no ranking de parceiros e adversários do modal de estatísticas, poluindo a lista com entradas sem jogador real. Agora <code>_computeH2hAndPartners</code> filtra por uma regex de nomes placeholder (<code>jogador|player|parceiro|partner|oponente|opponent|adversário|adversario</code> sozinho ou seguido de número) quando o participante não tem <code>uid</code> — jogadores reais identificados por uid continuam sendo contabilizados mesmo que o nome coincida.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard: botões "Novo Torneio" e "Partida Casual" lado a lado + botão "Iniciar" no topo do setup casual</b> — Na hero box da dashboard, os dois botões principais agora ficam na mesma linha, com tamanhos iguais (altura 64px, fonte 0.95rem), ícones atualizados (🏆 para Novo Torneio, ⚡ para Partida Casual) e layout compacto sem quebra em telas estreitas (<code>flex-wrap:nowrap</code>). Na tela de setup de partida casual, o botão "Iniciar Partida" que ocupava a base da tela foi substituído por um botão verde "Iniciar" (<code>linear-gradient(135deg,#10b981,#059669)</code>) fixo no cabeçalho, ao lado do "✕ Fechar" — fica sempre visível sem precisar rolar pelo QR code, código de sala e campo de convite.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas do jogador sempre mostram o grid completo de métricas</b> — Antes, quando o jogador tinha só 1 partida casual salva no cache legado <code>scoreplace_casual_history</code> (ou quando o <code>_buildAndPersistMatchRecord</code> fazia early-return por ausência de <code>FirestoreDB.saveUserMatchRecords</code>), o modal exibia apenas Vitórias / Derrotas / Aproveitamento. Agora o grid completo é sempre renderizado — Sets V/P, Games V/P, Pontos V/P, % Saque, % Recep., Games Mantidos, Quebras, Maior Seq. Pontos/Vitórias, Tempo total/médio/mais longo/mais curto — mesmo que zerados, para mostrar ao jogador o que é rastreável e incentivá-lo a jogar mais partidas no app. Correção: <code>_buildAndPersistMatchRecord</code> não faz mais early-return; o cache <code>scoreplace_casual_history_v2</code> agora é sempre gravado, mesmo quando a escrita no Firestore falha (offline, sem permissão, uid ausente). Fluxo unificado: <code>_showPlayerStats</code> sempre usa <code>_renderPersistentMatchStats</code> como renderer primário, seedando com registros locais v2 para render inicial instantâneo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login com Google: popup em todas as plataformas (incluindo iOS)</b> — Antes o iOS e o Safari eram forçados a usar <code>signInWithRedirect</code>, mas o redirect cross-origin falha no iOS moderno por causa do ITP (cookies de terceiros em <code>firebaseapp.com</code> são bloqueados e o <code>getRedirectResult</code> retorna <code>null</code>). Agora todas as plataformas tentam <code>signInWithPopup</code> primeiro — o iOS Safari 16+ suporta popup com <code>postMessage</code> sem depender de cookies cross-origin. Se o popup for bloqueado (código <code>auth/popup-blocked</code> ou similar), o handler de erro faz fallback para <code>signInWithRedirect</code> automaticamente. Log do userAgent adicionado para diagnóstico.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login com Google no celular (iOS/Safari) volta a funcionar</b> — No desktop o v0.12.6 resolveu o login via popup, mas no celular (iOS Safari e iOS Chrome, que também usa WebKit) o fluxo usa <code>signInWithRedirect</code> por causa do ITP da Apple. O handler <code>getRedirectResult</code> que recebe o usuário após o redirect também dependia exclusivamente do <code>onAuthStateChanged</code> — que pode não disparar no iOS com cookies 3rd-party bloqueados contra o <code>authDomain</code> cross-origin. Agora o próprio <code>getRedirectResult</code> chama <code>simulateLoginSuccess</code> diretamente e grava o <code>scoreplace_authCache</code>, com o mesmo guard contra execução dupla.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login com Google — aciona a dashboard diretamente do popup</b> — Antes o fluxo dependia exclusivamente do <code>onAuthStateChanged</code> do Firebase Auth para completar o login após o popup. Com a depreciação de cookies de terceiros no Chrome e o <code>authDomain</code> cross-origin (<code>firebaseapp.com</code>), esse callback pode não disparar de forma confiável, deixando o usuário preso na landing mesmo após o popup fechar com sucesso. Agora o sucesso do popup já executa <code>simulateLoginSuccess</code> diretamente (com proteção contra execução dupla via <code>_simulateLoginInProgress</code>) e gravação síncrona do <code>scoreplace_authCache</code>. Adicionados logs de diagnóstico (prefixo <code>[scoreplace-auth]</code> / <code>[scoreplace-router]</code>) no console para rastrear cada etapa.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login com Google volta a funcionar</b> — Removidos os escopos sensíveis do OAuth do Google (<code>user.gender.read</code>, <code>user.birthday.read</code>, <code>user.addresses.read</code>, <code>user.phonenumbers.read</code>) que exigem verificação formal do app pelo Google. Sem essa verificação, o Google mostrava uma tela de alerta "app não verificado" que interrompia silenciosamente o login para muitos usuários — o popup fechava, parecia que ia logar e voltava à landing. Agora o provider Google usa apenas escopos padrão (profile, email) e o fluxo de login volta a funcionar normalmente. Gênero, data de nascimento, endereço e telefone podem ser preenchidos manualmente no perfil.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações por e-mail com o conteúdo real + toggle de volta no perfil</b> — O toggle "E-mail" voltou à seção "Canais de notificação" do perfil (ao lado de "Plataforma"), permitindo ligar/desligar o envio de e-mails independentemente das notificações dentro do app. Além disso, os e-mails foram reescritos: o corpo do e-mail agora carrega a <i>mensagem real</i> da notificação (ex.: "O resultado do seu jogo foi registrado: Ana 6-4 6-3 Beto") em vez de um genérico "você tem uma nova notificação no app". Cabeçalho e ícone mudam conforme o tipo (resultado, chaveamento, comunicado, convite de co-organização, etc.), partidas exibem um placar formatado, atualizações listam as alterações e torneios encerrados mostram o campeão. O botão "Ver no scoreplace.app" continua ao final para quem quiser abrir o app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Versão do app na landing page + correção do retorno à landing após login</b> — A landing page (visitante não logado) agora exibe a versão atual (<code>vX.Y.Z-alpha</code>) logo abaixo do nome "scoreplace.app", tornando mais fácil identificar a build em uso sem precisar logar. Também corrigido: após login (Google, e-mail, etc.), se qualquer operação assíncrona de pós-login falhasse (ex.: regra do Firestore bloqueando uma leitura) a chamada final ao <code>initRouter()</code> era pulada e o usuário voltava a ver a landing. Agora o modal de login é fechado e o router é reexecutado imediatamente após setar <code>AppStore.currentUser</code>, dentro de um <code>try/catch</code> — a dashboard passa a renderizar antes mesmo de qualquer carga de perfil.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão "Login" no menu hambúrguer voltou a funcionar</b> — O menu mobile clona o conteúdo da topbar via <code>cloneNode(true)</code>, mas listeners registrados por <code>addEventListener</code> não sobrevivem à clonagem. Mudamos o botão <code>#btn-login</code> para usar um <code>onclick</code> inline (fecha o hambúrguer e abre o modal de login com todas as opções: Google, e-mail/senha, link mágico, SMS). Aplicado também ao reset pós-logout e pós-exclusão de conta.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.12.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>CTA da landing page agora abre o modal de login completo</b> — O botão verde "Crie seu torneio grátis" (e o "Comece agora — é grátis!" no rodapé) deixaram de disparar o login direto com o Google e agora abrem o modal de login padrão, com todas as opções de autenticação disponíveis (Google, e-mail/senha, link mágico por e-mail, SMS). O visitante pode escolher o método que preferir.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.76-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Campo de e-mail e botão "Enviar" do link mágico redesenhados</b> — Na página de login, o campo de e-mail para envio do link mágico ficou maior (fonte 1rem, padding 12px 14px) e ganhou um placeholder de exemplo ("ex: joao.silva@gmail.com"), tornando-o mais visível e amigável. O botão "Enviar" foi reduzido para caber apenas a palavra com uma borda fina na cor primária (outline transparente 1px, fonte 0.72rem, padding 6px 10px), liberando mais espaço para o campo de e-mail.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.75-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botões principais da partida casual movidos para o topo</b> — "Iniciar Partida" (na tela de ordem de saque) e "Confirmar Resultado" (na tela de fim de partida) agora ficam fixos no topo da página, não mais no rodapé. O conteúdo (cards de saque, estatísticas, gráfico de momentum, comparativos, tempo) passou a rolar abaixo dos botões. O toggle "Re-sortear duplas" continua junto do botão "Jogar Novamente" no cabeçalho fixo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.74-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações por E-mail e WhatsApp removidas do perfil (temporariamente)</b> — Os toggles de canal de notificação por E-mail e WhatsApp foram removidos do formulário de perfil, pois esses canais ainda não estão realmente implementados. O despacho automático para os dois canais também foi desligado (em <code>_sendUserNotification</code>). Quando o backend estiver pronto, reativamos tanto a UI quanto o envio. O canal 🔔 Plataforma (push in-app) permanece ativo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.73-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas detalhadas também para partidas casuais no hero box</b> — Corrigido: o modal de estatísticas do dashboard mostrava apenas Vitórias, Derrotas e % de Aproveitamento para partidas casuais. Agora, toda partida casual encerrada grava também no cache local (<code>scoreplace_casual_history_v2</code>) o mesmo registro completo que é salvo no Firestore — com stats de times, pontos, games, sets, saque/recepção, quebras, tempo por ponto, duração e jogadores. O modal de "Estatísticas" agora combina os registros do Firestore com os do cache local, renderizando a mesma seção ultra-detalhada (🏅 Sets V/P, 🎾 Games V/P, 🎯 Pontos V/P, 🚀 % Saque, 🎯 % Recep., 📊 Games Mantidos, 💥 Quebras, 🔥 Maior Seq. Pontos, 🏆 Maior Seq. Vitórias, ⏱ Tempo total, ⏲ Média/ponto, 📏 Ponto + longo, ⚡ Ponto + curto, 🤝 Top 5 Parceiros, ⚔ Top 5 Adversários) para casuais — não só para torneios.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas pessoais ultra-detalhadas por categoria</b> — Cada seção (📡 Partidas Casuais e 🏆 Torneios) agora mostra TODOS os metrics pedidos: Vitórias, Derrotas, % de Aproveitamento, 🏅 Sets V/P, 🎾 Games V/P, 🎯 Pontos V/P, 🚀 % Pontos no Saque, 🎯 % Pontos na Recepção, 📊 Games Mantidos (saque), 💥 Quebras de Saque, 🔥 Maior Sequência de Pontos, 🏆 Maior Sequência de Vitórias, ⏱ Tempo total de jogo, ⏲ Tempo médio por ponto, 📏 Ponto mais longo, ⚡ Ponto mais curto, 🤝 Top 5 Parceiros e ⚔ Top 5 Adversários. Os dados são lidos de <code>users/{uid}/matchHistory</code> no Firestore, onde cada partida é gravada automaticamente para cada jogador registrado ao final do jogo — persistem mesmo se o torneio ou a partida casual forem apagados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.71-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas pessoais viraram "Casual vs Torneios" com barras comparativas</b> — O modal do botão "Estatísticas" foi redesenhado para o padrão de comparação lado a lado da tela de fim de partida casual: quem tem histórico nos dois tipos vê agora barras horizontais comparando 📡 Casual (azul, à esquerda) contra 🏆 Torneios (dourado, à direita) em Partidas, Vitórias, Aproveitamento, Sets, Games, Pontos, % Pontos no Saque/Recepção, Games Mantidos, Quebras, Killer Points, Maior Sequência e Maior Vantagem. Quando só há um dos lados, cai num card compacto daquele lado. Funciona tanto com os dados persistentes do Firestore quanto com o fallback do histórico local — o visual é o mesmo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.70-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas do hero box no padrão da partida casual</b> — O modal aberto pelo botão "Estatísticas" do hero box foi totalmente reescrito para usar a mesma linguagem visual da tela de fim de partida casual: duas seções claramente separadas — 📡 <b>Partidas Casuais</b> (ciano) e 🏆 <b>Torneios</b> (dourado) — com cards de Vitórias/Derrotas/Aproveitamento, Sets/Games/Pontos, Saque%/Recepção%/Killer/Quebras, Maior Sequência/Maior Vantagem e Títulos. Quando há histórico detalhado do Firestore, os cards são enriquecidos automaticamente; quando não há, o fallback lê o histórico local das partidas casuais e mostra as duas seções mesmo assim. E para reduzir duplicação, a aba de estatísticas que ficava dentro do perfil foi <b>removida</b> — agora há um único ponto canônico: o botão "Estatísticas" do dashboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.69-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas do perfil estruturadas como partida casual</b> — A página de estatísticas dentro do perfil (no hero box do dashboard) foi totalmente reescrita para usar a mesma linguagem visual da tela de fim de partida casual. Agora com duas seções paralelas claramente separadas — 📡 <b>Partidas Casuais</b> e 🏆 <b>Torneios</b> — cada uma com seu próprio badge, cartões de estatísticas (V/D/Aproveit.) e cores. Quando há histórico detalhado (sets, games, saque%, recepção%, killer points, breaks, maior sequência, maior vantagem), os cards são enriquecidos automaticamente. Quem tem ambos os tipos de partida vê a barra comparativa "Casual vs Torneio" e tabelas de confrontos diretos / parcerias logo abaixo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.68-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Desfazer times propaga para todos + fechar encerra para todos</b> — Quando o organizador desfaz os times formados na tela de montagem da partida casual, a mudança agora aparece em tempo real para todos os jogadores no lobby (antes os convidados continuavam vendo "Times Formados" mesmo depois de o organizador quebrar a dupla). E quando o organizador clica em "Fechar" — tanto na tela de montagem quanto durante o placar ao vivo — a partida casual é encerrada para todos: os demais jogadores são automaticamente desconectados e voltam para o dashboard, em vez de ficar presos numa sala fantasma.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.67-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cards de jogador uniformes + duplas por padrão</b> — Os 4 cards de jogador na montagem da partida casual agora têm altura sempre igual (alinhamento automático à maior), e nomes longos quebram em múltiplas linhas dentro do card em vez de vazar ou serem cortados. Fluxo também ficou mais previsível: ao abrir a partida casual, lembramos o último esporte e modalidade (simples/duplas) escolhidos; usuários sem preferência configurada caem em Beach Tennis (duplas) em vez de Placar Simples — corrige o caso em que alguns usuários viam jogo single sem querer.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.65-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login no Safari estável + QR de convite para o app</b> — Safari e iOS (incluindo Chrome no iOS, que usa WebKit) agora usam o fluxo <code>signInWithRedirect</code> em vez de popup, fugindo do ITP que derrubava a sessão após leitura do QR code; persistência LOCAL é aplicada explicitamente e <code>getRedirectResult</code> é capturado no carregamento da página, trazendo o usuário de volta logado. Popup-blocked em outros navegadores também cai automaticamente para redirect. Novo botão 📱 "Convidar amigos" no hero box abre um QR code apontando para <code>scoreplace.app</code> (com <code>?ref</code> automático de quem convida) — ideal para passar o link direto para alguém que ainda não está no app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.64-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Avatares + refresh ao vivo na partida casual</b> — A tela de montagem do organizador agora mostra a foto de perfil e o nome cadastrado de cada jogador que entra na sala, atualizando os cards em tempo real. Quando um jogador sai, seu nome desaparece instantaneamente dos demais, os inputs dos convidados são reabertos e as duplas já formadas são desfeitas — a vaga fica livre para outro jogador ocupar, com aviso ("Fulano saiu da sala — vaga liberada") para o organizador.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.63-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Desfazer duplas e sair da partida casual</b> — Times formados por arrastar-e-soltar agora exibem um ícone de corrente 🔗 entre os parceiros; um toque quebra a dupla para reformar ou sortear de novo antes de iniciar. Sair da partida casual é efetivo: o organizador que fecha a tela de montagem cancela a partida no Firestore (remove-se da sala e libera os convidados), e jogadores convidados cuja sala foi cancelada são redirecionados automaticamente ao dashboard com aviso — ninguém fica preso em sala fantasma nem visível aos demais.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.62-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Estatísticas do usuário reescritas</b> — Tela de estatísticas no perfil adota o padrão visual do pós-jogo da partida casual: cards <code>_boxStat</code> com ícone + valor em cor de destaque + label pequeno uppercase. Grade principal expandida (Partidas, Vitórias, Derrotas, Aproveitamento, Títulos, Torneios) com códigos de cor por faixa de aproveitamento. Seções detalhadas (Casual 📡 e Torneio 🏆) agora com ícones em cada métrica (saque 🚀, recepção 🛡, killer ⚡, quebras 💥, sequência 🔥, vantagem 📈). Nova seção comparativa "⚖ Casual vs Torneio" com barras lado a lado (partidas, vitórias, aproveitamento, % saque, % recepção) usando o mesmo padrão <code>_compareBar</code> da tela de comparação de times. Dados vêm do <code>matchHistory</code> persistente por usuário no Firestore — sobrevive à exclusão do torneio ou partida casual de origem.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.68-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Arrastar e soltar em toda a partida casual</b> — Ordem de saque: arraste cards para reordenar (desktop + touch). Formar times: arraste um jogador sobre outro → "Formar time?" → os 2 restantes formam o outro time automaticamente. Botão "Refazer times" para recomeçar. Nome completo do perfil em todas as telas.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.65-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual completa + Manual atualizado</b> — Placar ao vivo ponto a ponto em tela cheia (sets, games, tie-break, prorrogação). Lobby em tempo real via QR Code e código de sala. Montagem de times por drag-and-drop ou sorteio automático. Fotos de perfil em todas as telas. Ordem de saque com slots fixos por time e correção durante a partida. Empate oferece Prorrogar ou Tie-break. Badge ⭐ PRO e 📊 Estatísticas na dashboard. Nova seção "Partida Casual" no manual de ajuda. 8 novas dicas contextuais para partidas casuais (152 total).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Hamburger + Voltar coexistem</b> — Menu hamburger renderiza FORA do header como elemento irmão. Quando aberto, o botão Voltar é empurrado para baixo automaticamente — ambos visíveis e clicáveis ao mesmo tempo. Z-index hierárquico: topbar (100) < Voltar (101) < dropdown (102). Estrutura à prova de regressão.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: simulação Suíço pareamento correto</b> — Rodadas 2+ agora mostram pareamento por adjacência (1º vs 2º, 3º vs 4º, etc.) em vez de metade superior vs inferior. Antes, 8 participantes mostravam "1º vs 5º, 2º vs 6º..." o que sugeria haver 16 colocados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Beta Testers</b> — Lista de emails com acesso Pro completo sem necessidade de assinatura Stripe. Testadores têm todas as funcionalidades desbloqueadas automaticamente ao fazer login.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Exclusão de torneio instantânea</b> — Apagar torneio agora usa UI otimista: remove da tela e navega ao dashboard imediatamente. Notificações aos participantes e exclusão do Firestore rodam em background sem bloquear a interface.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Escolha de rodadas no Suíço classificatório</b> — Ao selecionar Suíço como resolução de potência de 2, o organizador agora escolhe quantas rodadas jogar (de 2 até N). Cada opção exibe pontuação de equilíbrio de Nash com 4 critérios: Precisão (35%), Justiça (30%), Esforço (20%) e Velocidade (15%). A opção recomendada é destacada com badge. Ao clicar numa opção, a simulação visual atualiza em tempo real mostrando rodadas, partidas e fase eliminatória.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: Suíço como classificatória para Dupla Eliminatória</b> — Quando o Suíço é escolhido como resolução de potência de 2 (p2Resolution), ele agora funciona corretamente como fase classificatória: gera rodadas suíças, e ao encerrar a última rodada, os top N classificados (potência de 2 mais próxima) avançam automaticamente para a fase eliminatória do formato original (Eliminatórias, Dupla Eliminatória, etc.). Antes, o Suíço-como-resolução era tratado como torneio Suíço puro, ignorando o formato real.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: sorteio Suíço não gerava confrontos</b> — Torneios Suíço e Liga agora pulam direto para o sorteio sem passar pelo painel de resolução de potência de 2 (que não se aplica a esses formatos). Antes, o painel bloqueava o fluxo exigindo resolução de "número ímpar" ou "não é potência de 2" — problemas que o Suíço resolve naturalmente com BYE.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: flickering nos painéis de resolução</b> — Removido <code>backdrop-filter:blur</code> de todos os 10 overlays em draw-prep e draw (resolução de potência de 2, simulação, enquete, reabrir, revisão final). Scroll do body travado enquanto painel está aberto e restaurado ao fechar. Background opaco (92-95%) substitui o blur problemático.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Inscrição/desinscrição instantânea</b> — UI otimista: ao clicar em Inscrever-se ou Desinscrever-se, a tela atualiza imediatamente sem esperar a resposta do servidor. A transação Firestore roda em background; se falhar, o estado é revertido automaticamente com aviso ao usuário.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix estrutural: detecção de Suíço como classificação</b> — Corrigido problema recorrente onde torneios com formato Suíço aplicado via resolução de potência de 2 renderizavam tela em branco no bracket. A causa raiz era que 9 arquivos verificavam apenas <code>t.format === \'Suíço Clássico\'</code>, ignorando <code>classifyFormat</code> e <code>currentStage</code>. Agora todos os pontos de detecção incluem as 3 condições. Removido hack temporário de troca de formato no sorteio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: Suíço como ajuste de potência de 2</b> — Ao selecionar formato Suíço na resolução de potência de 2, o sorteio agora gera corretamente a Rodada 1 com standings, pairings e notificações (antes apenas marcava status sem gerar partidas). Corrigido flickering na tela de simulação: painel de resolução é escondido quando a simulação abre e restaurado ao voltar.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações de torneio encerrado</b> — Todos os participantes agora recebem notificação quando um torneio é encerrado, seja manualmente pelo organizador, automaticamente ao completar todas as partidas, ou por expiração de temporada da Liga. Corrigidos 3 pontos de encerramento que não enviavam notificação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: torneios encerrados visíveis</b> — Torneios encerrados públicos e de participação agora aparecem corretamente no dashboard. Seção "Torneios Encerrados" aberta por padrão (não mais colapsada). Filtro "Encerrados" inclui todos os encerrados visíveis.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Torneios encerrados com destaque</b> — Seção "Torneios Encerrados" agora separa os torneios em que você participou ou organizou (com badge 🏆) dos demais. Seus torneios aparecem primeiro com sub-título destacado. No filtro "Encerrados" a mesma priorização é aplicada. Canais de notificação (Plataforma, E-mail, WhatsApp) movidos para dentro da seção Social no perfil, entre filtros de comunicação e locais de preferência.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.52-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments.js</b> — 28 novas chaves <code>tourn.*</code> cobrindo mensagens e labels do orquestrador principal. Notificações: friendInvitedMsg/aFriend, noInvitesSent. Diálogos: removeParticipantTitle/Msg, splitTeamTitle/Msg. Datas: dateTo ("A"), dateTbd ("A DEFINIR"), singleCat. Banner de convite de organização: inviteTransfer/Cohost + descs. Eventos Liga: ligaStart, nextDraw, seasonEnd. Inscritos: teamEnrolled/Drawn/Formed, individualEnroll. VIP: removeVip/markVip. Ordenação de inscritos: sortAlphaAsc/Desc, sortChronoAsc/Desc.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.53-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — dashboard.js</b> — 15 novas chaves <code>dashboard.*</code>: monthAbbrevs (meses abreviados), statEnrolled/Teams/Waiting (contadores), labelFormat/Access/Progress, inProgress ("Em andamento"), votes/remaining (enquete), tournamentName (fallback), andMore (paginação), clearFilters, statistics, casualMatch.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.54-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — participants.js</b> — 16 novas chaves <code>participants.*</code>: diálogos de ausência (4 variantes de título/mensagem/botão), teamIndividual, splitTeam, drawDoneMsg, inProgressBadge, startTournament, defaultFormat. Nova chave <code>common.opponent</code>. Reutiliza tourn.teamEnrolled/Drawn/Formed e tourn.removeVip/markVip.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.55-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-categories.js</b> — 22 novas chaves <code>cat.*</code>: estimatedDuration, matchesSuffix, endTimePrefix, noCategory, dragToAssign, dragInstructions, oneCourtLabel/nCourtsLabel, mergeDialogTitle/Msg, removeFromCatTitle/Msg, unmergeDialogTitle/Msg/MsgInferred, assignedDialogMsg, sourceProfile/Organizer, questionOrg.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.56-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-draw.js</b> — 16 novas chaves <code>tdraw.*</code>: painel de revisão final (readyTitle/Subtitle, enrollClosed/Desc, teamsConsolidated/Desc, bracketStructure, p2AchievedVia, resolutionHistory, rollDrawNow, backAndReview), notificações (round1NotifTitle/Msg, swissNotifTitle/Msg).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.57-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-organizer.js</b> — 6 novas chaves <code>org.*</code>: cloneSuffixFull, reminder7d/2d/0d, nearbyMsg, commFullMsg. Lembretes de torneio e mensagens de torneio próximo totalmente traduzíveis.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — pre-draw.js</b> — 32 novas chaves <code>predraw.*</code> e <code>btn.goDashboard</code>: janela pré-sorteio (notFound/Msg, emptyDrop, standbySection, title, enrolledCount, addCategory, runDraw, statEnrolled/Categories/Bracket/ResultBy, pow2Exact/Gap, resultPlayers/Referee/Organizer, changeFormat), diálogos (renameCat, mergeCat, newCat, catExists, formatChanged).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.61-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-draw-prep.js (simulações e enquetes)</b> — ~200 novas chaves <code>predraw.*</code> cobrindo toda a tela de simulação de resolução e o sistema de enquetes. Simulações: BYE (título/subtítulo/seções R1/Byes/R2/footer), Play-in (título/stats/R1/Repescagem/R2/tiebreak/classificados), Standby/Lista de Espera (header/modos teams/indiv/badge/lista/subtítulos com teamSize), Suíço Classificatório (título/subtítulo/colunas Rodadas/Classificados/Partidas/legenda Nash/preview de fases/labels de rodada/partidas/empate/classificação). Enquetes (pollSystem): títulos/subtítulos de dialog/countdown/notificações de enquete aberta/encerrada, contextos (Ajuste de Chaveamento/Times Incompletos), labels de voto e minutos. Helpers: simTeam/Participant/Player/Match, votesLabel, backLabel/confirmLabel/simNote, gauge (centerTeams/Parts), P2 confirm (lastTeams/Parts, options titles/descs). Todas as strings hardcoded de <code>showResolutionSimulationPanel</code>, <code>_showPollCreationDialog</code>, <code>_showPollVotingDialog</code>, <code>_renderPollBanner</code>, <code>_renderClosedPollBanner</code>, <code>_checkPollNotifications</code>, <code>_handleP2Option</code> e <code>_showReopenPanel</code> conectadas ao <code>_t()</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.60-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — create-tournament.js</b> — 85 novas chaves <code>create.*</code> cobrindo todo o formulário de criação/edição: cabeçalho (modalTitle, saveTournament), nome/esporte/logo (nameLabel, sportLabel, logoSection, noLogo, genLogo), acesso (publicLabel, publicDesc), configurações de formato (gruposConfig/Count/DistDesc/Classified, suicoConfig/Rounds/Recommendation, ligaConfig/NewScore/InactRule, drawSchedule, manualDraw/Desc), datas (enrollDeadline, tournamentStart/End), local (venueSection/Label/Placeholder, accessOpen/Desc, courtsLabel/Names/Sep/Hint), tempo (weatherSection, timeEstSection, callTime/Desc, warmup/Desc, gameDur/Desc, estDuration), placar (matchFormat, gsmAdvantage/Desc), participantes (maxParticipants, noLimit, simples/DuplasSideDesc), inscrições (enrollIndividual/Desc, enrollTeam/Desc, autoClose/Desc), WO (woSection, woIndividual/TeamDesc), inscrição tardia (lateEnrollSection, lateEnrollClosed/Standby/Expand e Descs), categorias (catSection, genderCatLabel, catFem/Masc/MistoAle/Obr, skillCatLabel/Placeholder/Hint, catPreview), resultado (resultSection, resultOrg/Players/Referee e Descs), classificação (classificationSection, rankingPersonalized/Blocks/TypeHint), desempate (tiebreakerSection/Desc, tbHeadToHead/PointDiff/Wins/Buchholz/Sonneborn/Random), umidade (humidity).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.59-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-enrollment.js</b> — 9 novas chaves <code>enroll.*</code>: mensagens de notificação ao organizador (orgEnrollMsg, orgTeamEnrollMsg, orgUnenrollMsg), inscrição tardia (lateAddTitle/Msg, lateTeamTitle), campos de time (memberPlaceholder, memberLabel, anonParticipant).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.51-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — auth.js</b> — 75+ strings do perfil, autenticação e estatísticas conectadas ao <code>_t()</code>. 21 novas chaves <code>auth.*</code>: provedores de login (providerPassword/Phone), notificações (friendAcceptedMsg, participantEnrolledMsg), botão Google (signInGoogle), defaults (defaultUser/someone/someParticipant/orgShort), localização (myLocation/noLocationAdded/mapUnavailable/locationSearchError), diálogo comunicações fundamentais, categorias de gênero (genderFem/Masc/MistoAl/Ob). 52 novas chaves <code>profile.*</code>: modal completo (myProfile, labels de todos os campos, sexo, cidade, categoria, esportes, WhatsApp), social/notificações (socialCommsTitle/Desc, acceptFriends, receiveComms, notifAll/Important/Fundamental, notifChannels/Platform), locais (labelLocations/Desc/searchLocation), aparência (labelAppearance, themeNight/Light/Sunset/Ocean, visualHints/hintsDesc, language), desempenho (myPerformance, calculating, deleteAccountPerm), estatísticas (statWins/Losses/Rate/Points/Serve/Recv/Breaks, matchOne/Many, detailedStats/Desc, casualMatches/tournamentsSection, h2h/partners Casual/Tournament).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — bracket-ui.js e bracket-logic.js</b> — 22 novas chaves <code>bui.*</code> cobrindo UI interativa do bracket e lógica de rodadas. bracket-ui.js: overlay de sets (fixedSet/setResult), desempate tie-break (drawTiebreak), diálogo editar resultado (editResultTitle/Confirm), placar compartilhado (resultShareTitle/drawResult), Modo TV (nextGames, waitingPresence/Count, groupLabel). bracket-logic.js: label BYE (byeLabel), 3º Lugar (thirdPlaceMatch), diálogo rodada incompleta (incompleteRound/Msg), notificação de classificação suíça encerrada (swissFinishedNotif), notificação nova rodada (newRoundTitle/NotifMsg), roundClosed.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.49-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — bracket.js</b> — 50+ strings do chaveamento conectadas ao <code>_t()</code>. Labels de jogo/rodada: matchNum (Jogo {n}), playIn, round, final, semiFinal, quarterFinal, grandFinal, lowerBracket. Check-in: checkedIn/notCheckedIn/partial, ready badge, liveBtn/liveScore. Standings: standingsTitle/Cat (Classificação — Rodada {n}), draw/pending, tournamentFinished, closeRound, startFirstRound. Estatísticas: statMostWins/statStreak/statTotal/statsTitle. Confrontos Diretos: h2hTitle/Simple/Legend. Layout: layoutLinear/Mirror, classified ({n} posições definidas), monarchMatchRotation. Estados vazios: noRounds/noRoundsDesc/noGroups/noActiveRounds. Avançar para eliminatória. <code>var _t</code> adicionado a renderGroupStage. 37 novas chaves: <code>bracket.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.48-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — tournaments-draw-prep.js</b> — 75 strings do painel de resolução, enquetes e encerramento conectadas ao <code>_t()</code>. Painel unificado: 8 títulos/descrições de opções (Reabrir, BYE, Play-in, Lista de Espera, Exclusão, Suíço, Ajuste Manual, Enquete), gauge (Inferior/Superior/Times Atuais/Inscritos, sobram/faltam), badge "⭐ Recomendado", "Excluídas:". Lista de problemas: Times incompletos/Resto/Número ímpar/Não é potência de 2. Painel de realocação manual: título/descrição/botões. Dialog de votação de enquete: contexto (Ajuste de Chaveamento/Número Ímpar/Times Incompletos), Tempo restante, instrução, Fechar, ⚖️ Recomendado, Seu voto. Banners de enquete: ENQUETE/ENQUETE ENCERRADA, Votar Agora/Ver Alterar Voto, Você já votou/Aguardando, Encerrar Agora, restante, suspensas, Aplicar Resultado, Reabrir Enquete, Ver Detalhes. Dialog encerrar enquete. Encerrar Torneio (🏁). Painel Reabrir Inscrições: título/labels/botões. 79 novas chaves: <code>predraw.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.47-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — GSM, nomes de campos e tabela P2</b> — Strings finais de create-tournament.js conectadas ao <code>_t()</code>: _buildP2Table (inscritos, tempo/partida, cabeçalho), okCapacity (com interpolação), objeto _checkFields (8 labels de validação). Resumo curto GSM (7 chaves: fixed/Tb, points, 1set/Tb, bestOf, decider, tb, advantage). Resumo detalhado GSM (8 chaves: title, desc, tieWithTb/NoTb, sets, counting/Adv, tbDetail, superTb). 34 novas chaves: <code>create.field*</code>, <code>create.okCapacity</code>, <code>create.gsm*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.46-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — _p2Resolution e sugestões</b> — 30+ strings do sistema de estimativa de duração conectadas ao <code>_t()</code>: _p2Resolution (potência de 2 não é, classificatórias/BYEs, recomendação mais rápido), 4 sugestões de overflow (limitar/potência de 2, limitar com classificatórias, limitar simples, estender horário, +1 dia, trocar formato), 2 sugestões de near-limit (encerrar inscrições). Novas chaves: <code>create.notPow2Title</code>, <code>create.playinRow</code>, <code>create.matchesTime</code>, <code>create.byeRow</code>, <code>create.playinFaster</code>, <code>create.byeFaster</code>, <code>create.limitEnroll*</code> (3), <code>create.applyN</code>, <code>create.extendTime*</code>, <code>create.addExtraDay</code>, <code>create.switchTo</code>, <code>create.closeEnrollAt</code>, <code>create.nearLimitCap</code> + auxiliares (30 novas chaves).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.45-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — estimativa de duração e labels de acesso</b> — 20 strings em create-tournament.js conectadas ao <code>_t()</code>: estimativa de duração (quadra/s, Tempo disponível, min/partida, rodada/s, sequenciais, jogos, classificatória/s, chave, aviso de overflow, máximo), visibilidade de acesso ternária (🌐/🔒 na restauração de template), lançamento de resultado (Organizador/Jogadores/Árbitro, "Quem lança: {list}"), labels curtos de acesso. 17 novas chaves: <code>create.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.44-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — inline HTML em create-tournament</b> — 9 strings inline conectadas ao <code>_t()</code>: Sem logo, 🌐 Aberto ao Público, 🔒 Acesso Restrito, Carregando API do Google, Nenhum resultado encontrado, Mapa indisponível (2x), Previsão apenas para próximos 5 dias, X min por partida (com interpolação), detalhe de duração Chamada/Aquecimento/Jogo (com 3 interpolações). 9 novas chaves <code>create.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.43-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — labels descritivos e UI</b> — strings descritivas de interface conectadas ao <code>_t()</code> em 3 arquivos. bracket-ui.js: <code>Tie-break!</code> e <code>🔄 Atualizado</code>. tournaments-draw-prep.js: <code>Encerrada</code> (countdown de enquete) e <code>Jogadores em Espera</code>. create-tournament.js: 11 strings — tipos de jogo (simples/duplas/misto), visibilidade (público/privado), acesso (aberto/restrito), dica de quadras, título de editar torneio, labels de tie-break com interpolação, descrição de games fixos. 19 novas chaves: <code>bui.*</code> (2), <code>predraw.*</code> (2), <code>create.*</code> (9).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.42-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — textContent/innerText</b> — todas as atribuições diretas de DOM hardcoded em português conectadas ao <code>_t()</code> em 5 arquivos. bracket-ui.js: 10 strings de scoring de sets/games (Enter games, Total deve ser N, Empate + tie-break completo/preencher, Jogador 1/2, vence, TB, Em andamento, Para o Set N, Auto-refresh). auth.js: 3 strings do fluxo de exclusão de conta. store.js: 2 strings de checkout Pro. main.js: 2 títulos do modal de criação. tournaments.js: 1 string de envio. 19 novas chaves: <code>bui.*</code> (10), <code>auth.*</code> (2), <code>store.*</code> (2), <code>create.*</code> (2), <code>tourn.sending</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.41-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — botões de diálogo e labels</b> — todos os <code>confirmText</code>/<code>cancelText</code> hardcoded em português conectados ao sistema <code>_t()</code> em 9 arquivos. 22 novas chaves <code>btn.*</code>: remove, merge, mergeFuse, unmerge, group, keepSeparate, undo, keepTeam, finishTourn, finishAndDraw, keepOpen, finishAnyway, removeAndContinue, directDraw, playoff, voteNow, later, waitMore, deleteReedit, dqSub. Corrigidos também: inline onclick de QR Code (tournaments-sharing.js), label "Participante sem partida" (tournaments-utils.js).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.40-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — diálogos</b> — todas as strings hardcoded em português em <code>showAlertDialog</code>, <code>showConfirmDialog</code> e <code>showInputDialog</code> conectadas ao <code>_t()</code> em 7 arquivos: tournaments-draw.js (6 calls: sorteio já realizado, refazer, inscritos insuficientes, sem categoria, modo individual, agrupar), tournaments-draw-prep.js (5 calls: reabrir enquete, não permitido, reabrir/encerrar inscrições), create-tournament.js (4 calls: nome obrigatório/duplicado, datas/prazo inválido), tournaments-utils.js, pre-draw.js, dashboard.js e bracket-ui.js (8 calls: jogadores insuficientes, substituir W.O., resultado copiado, torneio não encontrado, confrontos, insuficiente). Novas chaves: <code>draw.*</code> (14), <code>create.*</code> (8), <code>bui.*</code> (8), <code>predraw.*</code> (3), <code>utils.*</code> (1), <code>btn.back</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.39-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — zero strings hardcoded em português</b> — últimos 6 arquivos conectados ao sistema <code>_t()</code>: tournaments-organizer.js (1 call, erro ao salvar template), tournaments-utils.js (2 calls, merge de participantes, correção de nomes), pre-draw.js (1 call, formato alterado), notifications.js (2 calls, FCM ativado/bloqueado), main.js (1 call, torneio criado), tournaments-draw-prep.js (1 call, torneio encerrado). Nenhuma string hardcoded em português restante em <code>showNotification</code> em todo o codebase.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo — 10 arquivos em uma release</b> — todas as strings hardcoded em português conectadas ao sistema <code>_t()</code> em: tournaments-enrollment.js (20 calls, incl. lista de espera, cancelamento, liga ativo/inativo), tournaments-categories.js (12 calls, merge/unmerge/atribuição), participants.js (9 calls, check-in, substituição, W.O.), tournaments-sharing.js (8 calls, copiar link, QR, CSV), tournaments-draw.js (8 calls, duplicatas, torneio iniciado, grupos, suíço, sorteio), tournaments.js (10 calls, amigos, convites, bots, onclicks inline), store.js (6 calls, login Pro, pagamento, sync error, notificações), bracket-logic.js (5 calls, torneio encerrado, classificatória, nova rodada), hints.js (6 calls, ativar/desativar/resetar dicas), enroll-modal.js (3 calls, copiar link). Variáveis locais redundantes <code>_tFn/_tFn2/_tFn2a/_tFn3/_tFnRem/_tFnSeason</code> removidas. ~30 novas chaves adicionadas em <code>bui.*</code>, <code>enroll.*</code>, <code>cat.*</code>, <code>store.*</code>, <code>hints.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.37-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo de Criação de Torneio e Bracket UI</b> — todas as strings hardcoded em português de create-tournament.js (14 calls) e bracket-ui.js (13 calls) conectadas ao sistema <code>_t()</code>. Cobertura: logo, erros de local, torneio criado/salvo, auto-atribuição de categorias, template aplicado, substituição de jogadores, fase eliminatória, lançamento de resultado, leitura de QR code. Novos namespaces adicionados: <code>sub.*</code>, <code>bui.*</code>, <code>share.*</code>, <code>tdraw.*</code>, <code>cat.*</code>, <code>tourn.*</code>, <code>store.*</code>, <code>enroll.*</code> com ~90 novas chaves em pt/en.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.36-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo da Preparação de Sorteio</b> — todas as 25 strings hardcoded em português de tournaments-draw-prep.js conectadas ao sistema <code>_t()</code>. Cobertura: ajuste de times, reabertura de inscrições, BYE rotativo, exclusão de participante, restauração de inscrições, enquetes (criação, votação, encerramento, reabertura), remoção para potência de 2, reabrir torneio, encerrar inscrições. 53 novas chaves adicionadas no namespace <code>draw.*</code>. Variáveis locais redundantes <code>_tFnFin/_tFnReopen/_tFnClose/_tFnClose2</code> removidas — substituídas pelo alias IIFE <code>_t</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.35-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo da Autenticação</b> — todas as ~80 strings hardcoded em português das funções de login/perfil (auth.js) conectadas ao sistema <code>_t()</code>. Cobertura: login Google, vinculação de conta, link por e-mail, SMS/telefone, e-mail+senha, cadastro, redefinição de senha, auto-inscrição pós-login, logout, exclusão de conta, propagação de nome, locais de preferência e geolocalização, salvamento de perfil. 105 novas chaves adicionadas em i18n-pt.js e i18n-en.js no namespace <code>auth.*</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.34-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n completo da Partida Casual</b> — todas as ~80 strings hardcoded em português da feature de partida casual (bracket-ui.js) conectadas ao sistema <code>_t()</code>. Cobertura: lobby setup, tela de espera, preview de times, status de ingresso, notificações de shuffle/ingresso/saída/início, telas de erro offline/not-found/closed, display de resultado final. 7 novas chaves adicionadas em i18n-pt.js e i18n-en.js: <code>casual.draw</code>, <code>casual.offlineMsg</code>, <code>casual.notFoundMsg</code>, <code>casual.playerFallback</code>, <code>casual.someone</code>, <code>casual.liveTitle</code>, <code>casual.liveConnectedMsg</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.33-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Limpeza de código — resultado-modal removido e console.log de produção eliminados</b> — <code>result-modal.js</code> (marcado como deprecated desde v0.4.0, nunca chamado) removido de index.html e sw.js. Logs de debug <code>console.log</code> em host-transfer.js (6 chamadas de rastreamento de notificação), auth.js (AutoFixNames + PropageName, disparados a cada login), bracket-ui.js (save casual) e tournaments-utils.js (Merge/Dedup/FixOrphans) rebaixados para <code>console.debug</code> — visíveis apenas quando o nível de log do DevTools incluir Verbose.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Correção: parseInt com radix e NaN guard no auto-encerramento de inscrições</b> — <code>parseInt(maxParticipants)</code> sem radix podia interpretar valores com prefixo <code>0x</code> como hexadecimal; sem NaN guard, um valor malformado como <code>"unlimited"</code> retornaria NaN e desabilitaria silenciosamente o auto-encerramento ao atingir capacidade. Agora usa <code>parseInt(..., 10)</code> com validação <code>!isNaN</code> e <code>&gt; 0</code>.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de segurança — XSS em atributos data- dos cards de participante</b> — Os atributos <code>data-participant-name</code> e <code>data-merge-name</code> nos cards de participante em tournaments.js usavam apenas <code>replace(/"/g, \'&amp;quot;\')</code>, deixando <code>&amp;</code>, <code>&lt;</code> e <code>&gt;</code> sem escape. Substituído por <code>_safeHtml()</code> para escape completo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de segurança — XSS em nomes de jogadores na lista de participantes</b> — Nomes de jogadores individuais e membros de equipe no participants.js agora passam por <code>_safeHtml()</code> antes de serem injetados em atributos <code>title</code>, <code>data-player-name</code> e no conteúdo do span. Corrige potencial XSS via nome contendo <code>&lt;</code>, <code>&gt;</code> ou <code>&quot;</code>. Complementa a correção de escaping de onclick da v0.11.29.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.29-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de segurança — escaping completo em participants.js e host-transfer.js</b> — Três variáveis de escape de nome de jogador em participants.js (_nmSafe, _pSafe, safeP) e o tId em host-transfer.js passaram a escapar barra invertida antes de aspas simples. Completa a varredura de todos os onclick handlers do codebase — agora todos os arquivos JS seguem o padrão correto: escapar \\\\ antes de \'.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de segurança — correção de escaping em onclick handlers</b> — (1) <b>Ordem errada corrigida</b>: pre-draw.js escapava aspas antes de barras invertidas, o que podia quebrar categorias com backslash. Corrigido para sempre escapar <code>\\</code> antes de <code>\'</code>. (2) <b>Backslash ausente</b>: dashboard.js (filtros de esporte/local/formato e botão Estatísticas), bracket.js (nomes de jogadores nos cards e na classificação), bracket-ui.js (seletor de esporte e URL de partida casual) e tournaments-sharing.js (URL de cópia de link) — todos passaram a escapar também a barra invertida, não apenas aspas simples. (3) <b>Memory leak do hamburger</b>: handler de clique externo agora sempre remove listener anterior antes de adicionar novo, evitando acumulação em double-open rápido. (4) <b>Null check</b>: doc.data() validado antes de push em loadAllTournaments do Firestore.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Modal de estatísticas do dashboard completamente reescrito com histórico persistente como dado primário</b> — O modal "📊 Estatísticas" agora exibe diretamente as estatísticas ricas do <code>users/{uid}/matchHistory</code> como conteúdo principal (o mesmo formato da tela de resultado de partida casual): seções separadas para Partidas Casuais e Torneios com vitórias/derrotas/aproveitamento, sets, games, pontos, %saque, %recepção, killer points e quebras. Tabelas de Confrontos Diretos (H2H) e Parcerias com foto e winrate. Os números mostrados sempre refletem partidas reais registradas — não mais o scan do AppStore que podia retornar zeros. Fallback para stats do AppStore apenas quando uid não é resolvível.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.26-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Foto + estatísticas detalhadas no modal de estatísticas do dashboard + botão Perfil do menu hambúrguer consertado</b> — Ao clicar no botão "📊 Estatísticas" do dashboard, o modal agora: (1) carrega a foto real do perfil quando é você mesmo (via currentUser.photoURL) ou de um amigo conhecido (AppStore.friends); (2) resolve o uid do jogador e carrega o histórico persistente em <code>users/{uid}/matchHistory</code>; (3) renderiza seções separadas para Partidas Casuais e Torneios com estatísticas ricas (vitórias, derrotas, aproveitamento, sets/games/pontos, %saque, %recepção, killer points, quebras, duração média, tempo/ponto médio); (4) exibe tabelas de Confrontos Diretos (H2H) e Parcerias separadas para casuais e torneios — registro preservado mesmo se o torneio for apagado ou a sala casual encerrada. O botão de perfil dentro do menu hambúrguer agora abre o modal corretamente — antes o addEventListener era perdido quando o menu clonava o nav; agora usa inline onclick + funções globais (<code>window._onProfileBtnClick</code> e <code>window._openMyProfileModal</code>) que sobrevivem ao cloneNode.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho do perfil blindado</b> — O render do cabeçalho de avatar + nome + e-mail dentro do bloco de estatísticas foi envolvido em try/catch para garantir que nenhuma falha (photoURL inválida, nome ausente, etc.) impeça a abertura do modal de perfil ou o carregamento das estatísticas abaixo. Fallback da inicial usa charAt() seguro. Bump força auto-update client-side para usuários com cache antigo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.24-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Cabeçalho de usuário nas estatísticas do perfil</b> — Ao abrir "Meu Desempenho" no perfil pelo dashboard, o conteúdo do bloco de estatísticas agora começa com avatar (foto real ou fallback com inicial colorida), nome e e-mail do usuário — mesmo estilo do cabeçalho de perfil. Fornece contexto imediato de que os dados exibidos são do próprio usuário.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tempo por ponto + dicas + manual atualizados</b> — Cada ponto agora registra timestamp (t: Date.now()) no pointLog. Nova seção "⏱ Tempo" na tela de resultado exibe Duração, Tempo médio/ponto, Intervalo mais longo e Intervalo mais curto. Dados persistidos em matchHistory como timeStats (avgPointMs, longestPointMs, shortestPointMs) para análises futuras. Novas dicas contextuais cobrem: bolinha de saque travada, botão Sair libera vaga, Fechar partida, cards de jogador clicáveis, gráfico Momentum com Replay, seção Comparação, botão Jogar Novamente + toggle Re-sortear, tie-break 7 com 2 de margem, times visíveis no lobby, e seção de Estatísticas Detalhadas no perfil (H2H + parcerias). Manual de ajuda ganhou seção "📊 Estatísticas Detalhadas" e expandiu "Partida Casual" com as sub-seções Placar ao Vivo, Tela de Resultado, Lobby e Compartilhamento.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.22-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Histórico persistente por usuário + estatísticas detalhadas no perfil</b> — Toda partida (casual ou torneio) passa a gravar um registro detalhado (stats de time, serve %, killer points, quebras, sequência, vantagem, per-player serve stats) em <code>users/{uid}/matchHistory</code> para cada jogador registrado envolvido. Os dados sobrevivem mesmo se o torneio for apagado ou a partida casual terminada. Nova seção "📊 Estatísticas Detalhadas" no perfil carrega esse histórico e mostra agregações separadas para Partidas Casuais vs Torneios, tabelas de Confrontos Diretos (H2H) e de Parcerias para cada modalidade — com W-L-E e aproveitamento por oponente/parceiro.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Momentum logo abaixo do vencedor + toggle elegante</b> — Box "Momentum da Partida" movido para logo abaixo do box Vencedor na tela de resultado, de forma que a animação de desenho das linhas seja visível no momento de abertura. Botão "🔄 Jogar" ficou compacto para dar espaço ao toggle "Re-sortear duplas" que agora renderiza como switch pill elegante (toggle-switch toggle-sm) no lado direito — cabe sem espremer em viewports de 303px+.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Gráfico Momentum redesenhado: linhas duplas animadas</b> — Substituído o gráfico de diferença com preenchimento por duas linhas cumulativas distintas: azul para o Time 1 e vermelha para o Time 2, ambas crescendo da esquerda para a direita. Y-axis com gridlines e rótulos (0, 5, 10, ...). Ao abrir a tela, as linhas se desenham progressivamente ao longo de 2.8s via stroke-dashoffset + pathLength="100". Marcadores finais (círculos + pontuação) aparecem ao final da animação. Botão ↻ Replay re-executa a animação. Linhas tracejadas verticais marcam o fim de cada set (S1, S2...).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botões da tela de vitória sem cortes no navegador</b> — Overlay agora usa 100dvh (altura dinâmica do viewport) para respeitar o URL bar que aparece/desaparece em Safari e Chrome mobile. O rodapé com Confirmar Resultado, Jogar Novamente e toggle Re-sortear duplas agora tem padding-bottom = 12px + env(safe-area-inset-bottom), garantindo que o home-indicator (iPhone X+) e a barra do navegador não cobrem os botões. Também removido o gate de "usuário envolvido" — os botões aparecem para todos os usuários na tela de resultado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Sair do lobby agora funciona de verdade</b> — Ao clicar Sair (ou Voltar ao Dashboard) no lobby da partida casual, o handler agora: (1) marca _hasLeft para bloquear auto-join concorrente, (2) para o interval de refresh, (3) libera a vaga no Firestore (fire-and-forget) e (4) navega imediatamente para o dashboard — o usuário não fica mais preso no lobby esperando Firestore responder.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Times formados visíveis no lobby</b> — Quando o organizador monta os times na tela de setup (via drag-and-drop ou renomeia jogadores), as mudanças são sincronizadas no Firestore em tempo real (debounced). Os outros usuários cadastrados que já estão na sala passam a ver "⚔ Times formados" com Time 1 vs Time 2 em vez de só "Aguardando jogador". Auto-refresh agora atualiza o snapshot completo da partida (não só participants).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Abandonar partida libera a vaga</b> — Ao clicar em Fechar e confirmar o abandono da partida casual, agora a vaga do jogador é efetivamente liberada (uid removido do slot em players, além de participants e playerUids). O jogador sai da tela e é redirecionado ao dashboard em vez de ficar preso na tela de lobby.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Decisões de jogo restritas aos jogadores</b> — Botão "Jogar Novamente" e toggle "Re-sortear duplas" (agora lado a lado) só aparecem para usuários cadastrados que estão jogando a partida. O mesmo vale para a escolha de prorrogar/tie-break e para o botão de ir pro tie-break durante prorrogação — não-jogadores veem "Aguardando decisão dos jogadores". Guard também no handler _liveResolveTie para impedir bypass via console. Tie-break continua a 7 pts com margem de 2 (se não houver 2 pts de vantagem, segue até ter).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dashboard sempre no topo</b> — Ao navegar de volta para o dashboard, a página agora sempre pula instantaneamente para o topo (window + documentElement + body), cancelando qualquer smooth-scroll em andamento e cobrindo reflows pós-render.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Analytics completa de partida + cards clicáveis</b> — pointLog enriquecido com sacador, contexto do game e flag de tiebreak para cada ponto. Nova seção "Comparação dos Times" lado a lado com barras: Sets, Games, Pontos, % Pontos no Saque, % Pontos na Recepção, Games Mantidos (saque), Quebras de Saque, Killer Points (40-40), Maior Sequência, Maior Vantagem. Cards de jogador clicáveis abrem modal com estatísticas individuais detalhadas: saque por game (servidos, mantidos, aproveit., maior sequência) e por ponto (pts servidos, ganhos, % no saque).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Gráfico Momentum ponto a ponto na tela de vitória</b> — SVG do desenvolvimento da partida exibe a diferença cumulativa de pontos ao longo de todo o jogo. Área azul acima do zero indica vantagem do time 1, vermelha abaixo indica vantagem do time 2. Linha amarela suave sobre as áreas com pontos maiores nos extremos (maior vantagem de cada time) e marcador final. Linhas tracejadas verticais com rótulos S1, S2, ... marcam o final de cada set.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Travamento do saque reforçado + UI</b> — Após 2 jogos, ordem de saque fica travada em todos os canais (drag, _liveSetServer, _liveSwapServerInTeam) com guards explícitos. A bolinha de saque mostra um cadeado 🔒 quando travada, glow dimmer e tooltip "Ordem de saque travada". Campo "Sala de um amigo" agora tem mesma altura (44px) do botão Entrar.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tela de vitória com aproveitamento de saque por jogador</b> — Estatísticas do vencedor em destaque no topo (troféu, nomes, placar, sets/games/pontos) e as do perdedor abaixo. Cada jogador mostra seu aproveitamento de saque (% de games mantidos como sacador) calculado a partir do histórico de saques. Tiebreaks não entram no cálculo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Layout da tela de resultado ajustado</b> — Botões "Confirmar Resultado" e "Jogar Novamente" agora fixos no rodapé (não ficam mais cortados). Estatísticas em área com scroll interno quando necessário. Toggle "Sortear duplas" reposicionado abaixo do botão Jogar Novamente seguindo padrão toggle-switch do app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Games visíveis em portrait</b> — Box de games movido para acima das placas de placar em portrait, garantindo visibilidade em qualquer tamanho de tela. Corrigido bug de variáveis leftTeam/rightTeam usadas antes da declaração (cores dos games ignoravam lado da quadra).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: partida de 1 set não encerrava</b> — Partidas configuradas com 1 set (ex: Beach Tennis 1×6) não encerravam após o primeiro set, iniciando incorretamente um segundo set. Causa: _setsWon() excluía sempre o último set da contagem. Corrigido com parâmetro includeAll em _setsWon e _checkMatchWon. Tie-break agora aparece corretamente em prorrogação de 1 set.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Games refletem lado da quadra</b> — Cores dos games agora acompanham qual time está em cada lado (ao trocar lados, os números trocam também). Layout portrait sobe para caber tudo sem cortar o box de games. Gaps e paddings reduzidos para otimizar espaço vertical.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.11.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida Casual v2</b> — Box unificado: QR code maior (88px), código da sala, Convidar, campo "Sala de um amigo" e botão Entrar em um único bloco compacto. Landscape otimizado. Sync em tempo real entre dispositivos (tema e sala casual via Firestore onSnapshot). Sem zoom/scroll nas telas de partida.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.92-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auto-update e sync completo</b> — App detecta automaticamente novas versões e recarrega, limpando caches antigos. SW sem cache HTTP e check a cada 5 min. Sync em tempo real de todo o estado (placar, saque, lado de quadra) entre dispositivos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Partida casual: cores de time e ordem de saque</b> — Cards de jogadores na tela de setup agora sempre mostram cores de time (azul para Time 1, vermelho para Time 2). Ordem de saque no picker alterna obrigatoriamente entre times (T1-T2-T1-T2); arrastar e soltar restrito a troca dentro do mesmo time para manter a alternância.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: email e WhatsApp nas notificações</b> — Canais de email e WhatsApp estavam completamente inativos (função _dispatchChannels existia mas nunca era chamada). Agora, _sendUserNotification e _notifyTournamentParticipants auto-disparam email e WhatsApp automaticamente para TODAS as notificações. Novos métodos queueEmail e queueWhatsApp no FirestoreDB escrevem na collection mail (Firebase Extension) e whatsapp_queue. Flag _skipDispatch evita duplicação em broadcasts batch.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga Rei/Rainha sem eliminatória</b> — Painel "Classificados por grupo" (1 ou 2) agora escondido quando formato é Liga com rodada Rei/Rainha (pontos corridos, sem fase eliminatória). Guard extra impede auto-advance para eliminatória em torneios Liga.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações de co-organização completas</b> — Ao aceitar ou recusar convite de co-organização ou transferência, ambos (convidante e convidado) recebem notificação. Notificação original de convite é marcada como lida automaticamente para ambos os lados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: notificação ao deletar torneio</b> — Callback de exclusão agora é async e aguarda (await) o envio de todas as notificações antes de remover o torneio do Firestore. Antes, a notificação era fire-and-forget e podia ser perdida na race condition com a exclusão.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fix: notificações do app</b> — Notificações agora usam o uid do participante diretamente em vez de buscar por email no Firestore (mais rápido e confiável). Novo helper _resolveOrganizerUid usa creatorUid do torneio com fallback para email. Corrigido em: inscrição individual, inscrição de time, cancelamento de inscrição, auto-inscrição por convite, e broadcast para todos os participantes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dicas e manual atualizados</b> — Dica do toggle Liga corrigida: descreve participação nos sorteios e regras de pontuação (inativo = 0 pts, folga por falta de jogadores = média). Manual de ajuda atualizado nas seções Formatos, Resultados e Referência Rápida com detalhes do toggle ativo e pontuação diferenciada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga: pontuacao diferenciada para inativos</b> — Jogador que desativa o toggle de participacao recebe 0 pontos na rodada (antes recebia media). Jogador que fica de fora por falta de participantes suficientes continua recebendo a media de seus pontos. Card de folga diferenciado: inativo com icone vermelho e 0 pts, remainder com icone amarelo e media.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.10.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sistema de notificações reescrito</b> — Todos os eventos agora geram notificações classificadas em 3 níveis: fundamental, importante e geral. Novos eventos cobertos: torneio apagado, sorteio realizado (todos os formatos), torneio encerrado, inscrições encerradas/reabertas, participante removido, torneio criado (notifica amigos). Catálogo centralizado de notificações (notification-catalog.js). Resultado de partida agora classificado como fundamental. Validação de perfil: WhatsApp sem telefone ou email sem email cadastrado bloqueia o save com alerta. Novos templates de email para todos os tipos. View de notificações refatorada com ícones/cores do catálogo. Navegação inteligente: draw/resultado levam ao bracket, torneio apagado não mostra botão.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.9.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convites de co-organização corrigidos</b> — Notificações de convite agora entregues corretamente via sistema unificado. Dashboard detecta convites pendentes e redireciona automaticamente para o torneio. Banner dourado pulsante com botões Aceitar/Recusar na página do torneio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.9.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Placares sempre visíveis na copa</b> — Todas as partidas com ambos os jogadores definidos agora exibem campos de placar permanentes, inclusive partidas já decididas (pré-preenchidas com valores existentes para edição direta). Não é mais necessário clicar "Editar" para alterar um resultado.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.9.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga com duplas aleatórias</b> — Rodadas da Liga agora formam duplas aleatórias a cada sorteio e confrontam dupla vs dupla. Parceiros são sorteados a cada rodada para máxima variedade. Pontuação individual mantida (vitória = 3pts, empate = 1pt). Jogadores excedentes (quando não múltiplo de 4) recebem folga com pontuação média.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.9.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dicas do App no manual</b> — Nova seção "Dicas do App" no manual de ajuda com todas as 144 dicas visuais do sistema organizadas por área (Barra Superior, Dashboard, Criar Torneio, Ferramentas do Organizador, Chaveamento, Perfil, etc.) em boxes coloridos para consulta rápida.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.79-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Dicas visuais expandidas</b> — Sistema de hints ampliado de 67 para 144 dicas contextuais cobrindo absolutamente todos os botões, toggles, campos e funcionalidades do app: formulário de criação (formato, esporte, modo de sorteio, inscrição, GSM, categorias, Liga, quadras, datas, W.O., desempate, templates), ferramentas do organizador (editar, comunicar, sortear, categorias, co-organização, encerrar, excluir), bracket (zoom, imprimir, fechar rodada, avançar eliminatória, placar inline, GSM overlay), perfil (gênero, idioma, notificações), check-in (filtros, W.O., substituição, ordenação) e explore (amizade, busca).</p>' +
        '<p><b>Manual de ajuda reescrito</b> — Todas as 20 seções do manual completamente reescritas com cobertura completa de cada funcionalidade. Nova seção "Chaveamento e Classificação" com documentação de zoom, navegação por grupos, Modo TV, standings, confrontos diretos, rodadas anteriores e exportação. Seções existentes expandidas com boxes visuais passo a passo cobrindo todos os campos do formulário, fluxo do organizador, formatos detalhados com descrições coloridas, check-in com W.O./substituição, resultados GSM, e co-organização.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Configuração de Grupos</b> — Formato "Fase de Grupos + Eliminatórias" agora exibe painel dedicado com todas as distribuições possíveis de grupos. Mostra grupos iguais e mistos (ex: 3 grupos de 4 + 1 grupo de 3), classificados por grupo configurável (1-4), e destaque para configurações com potência de 2 na eliminatória. BYE, Suíço e Lista de Espera não são permitidos neste formato.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Voltar sempre fixo</b> — Botão "Voltar" usa position: fixed para visibilidade permanente no topo. Desde v0.10.29, coexiste com o menu hamburger (ambos visíveis).</p>' +
        '<p><b>Voltar no Explorar</b> — Página "Explorar" agora inclui botão Voltar fixo no topo.</p>' +
        '<p><b>Badge BYE compacto</b> — Badge "BYE" no chaveamento posicionado acima do campo de placar para não aumentar a altura do card.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.46-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>BYE brackets consistentes</b> — Chaves com BYE agora distribuem os byes intercalados com partidas reais, garantindo que a segunda rodada cruze vencedores reais com classificados por BYE. Corrige brackets onde BYE jogava contra BYE.</p>' +
        '<p><b>Toasts sem duplicata</b> — Inscrição e desinscrição não geram mais notificações duplicadas.</p>' +
        '<p><b>Divulgação imediata</b> — Sorteio sempre com divulgação pública imediata, sem diálogo de visibilidade.</p>' +
        '<p><b>Header sticky corrigido</b> — Botão Voltar e toggle "Só meus jogos" no chaveamento e na página de detalhes agora ficam fixos no topo ao rolar a página.</p>' +
        '<p><b>Box Equipes</b> — Cards do dashboard e detalhes do torneio mostram box "Equipes" com contagem de times formados (por sorteio, inscrição ou organizador), entre Inscritos e Espera.</p>' +
        '<p><b>BYE limpo no chaveamento</b> — R1 mostra apenas jogos reais (BYE matches ocultos). Na R2+, times que avançaram de BYE exibem badge verde "BYE" ao lado do nome.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>3º lugar com número do jogo</b> — Card da disputa de 3º lugar agora exibe o número do jogo em azul (ex: "Jogo 15") como todas as outras partidas, em vez de repetir "3º LUGAR" no cabeçalho do card.</p>' +
        '<p><b>Headers sticky</b> — Botão Voltar + "Só meus jogos" fixos no topo do chaveamento. Voltar + filtros de check-in fixos nos Inscritos. Voltar/Confirmar fixos nos painéis de simulação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Repescagem com critérios de desempate</b> — Classificação na repescagem agora usa todos os critérios configurados do torneio (saldo de pontos, vitórias, confronto direto, saldo de sets/games para GSM, etc.) em vez de apenas placar simples. Passam tantos quantos houverem vagas na rodada seguinte.</p>' +
        '<p><b>Escopo do W.O.</b> — Novo toggle na criação: "Individual" (substitui só o ausente, parceiro fica) ou "Time Inteiro" (time eliminado por W.O.). Configurável por torneio.</p>' +
        '<p><b>Inscrições após encerramento</b> — 3 modos: Fechadas (bloqueado), Lista de Espera (suplentes), Novos Confrontos (suplentes podem gerar jogos extras). Aplica-se a inscrição própria, times e adição pelo organizador.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Check-in redesenhado</b> — Toggle Presente/Ausente substitui botao na lista de chamada. Botao W.O. substitui Ausente. Lista de espera na pagina de inscritos com toggle+W.O. Substituicao automatica: marca W.O. e o proximo presente da lista de espera entra no lugar.</p>' +
        '<p><b>Paineis de decisao com header sticky</b> — Botoes cancelar/voltar ficam fixos no topo ao rolar paineis de potencia de 2 e times incompletos.</p>' +
        '<p><b>Dashboard</b> — Box lista de espera com contagem separada. Inscritos descontam lista de espera. Barra de progresso conta partidas de grupos Rei/Rainha. Nome do organizador exibido corretamente.</p>' +
        '<p><b>Limpeza</b> — Botao Clonar removido (templates o substituem). Lista de participantes inline oculta apos sorteio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Convite sem login obrigatorio</b> — Visitantes podem ver o torneio sem fazer login. Se inscricoes abertas, scroll ate o botao "Inscrever-se" com destaque. Se inscricoes fechadas, mensagem explicativa e sugestao para criar proprio torneio. Login so e solicitado ao clicar em Inscrever-se.</p>' +
        '<p><b>Add Bot corrigido</b> — Bots agora persistem no Firestore (antes sync() ignorava participantes). Bots criados como objetos completos com nome, email e uid.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.78-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Countdown excludente da Liga</b> — Liga exibe apenas um countdown por vez, por prioridade: 🏁 Início da Liga (antes de começar) → 🎲 Próximo sorteio (quando há sorteio agendado dentro da temporada) → 🏁 Fim da temporada (quando não há mais sorteios). Tempo decorrido ("Em andamento") não aparece em Ligas. Aplicado tanto no dashboard quanto no detalhe do torneio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.75-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Voltar hierárquico</b> — Botão Voltar no detalhe do torneio agora navega por hierarquia (sempre volta ao dashboard) em vez de seguir o histórico do browser. Resolve loop infinito ao navegar nível 3 → nível 2 → nível 3.</p>' +
        '<p><b>Testes 116/116</b> — Corrigidos 4 testes que falhavam: TBD conta como partida pendente, templates async com retorno string, runner suporta funções async.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.74-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria v0.8.70-73</b> — Correções de segurança e lógica: atributo style duplicado no card de folga (bracket.js), ID de torneio escapado no toggle Liga ativo (XSS), progresso do torneio não conta mais folgas como partidas pendentes, guard contra 0 jogadores ativos na geração de rodadas (Liga), oninput→onchange nos campos de data/hora do sorteio Liga.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.73-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Card de folga</b> — Jogadores em folga (Liga) exibem card compacto com emoji, nome e pontuação média recebida, em vez de card de partida normal.</p>' +
        '<p><b>Notificação pós-sorteio</b> — Todos os participantes recebem notificação quando uma rodada é sorteada (Liga/Suíço), tanto na primeira rodada quanto nas seguintes.</p>' +
        '<p><b>Folgas não bloqueiam rodada</b> — Matches de folga (isSitOut) não contam como "partidas incompletas" ao encerrar rodada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.72-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Fase de Grupos com duplas</b> — Formato Grupos + Eliminatórias agora forma duplas/times antes de distribuir nos grupos. Jogadores individuais são sorteados em times automaticamente.</p>' +
        '<p><b>Botão Voltar corrigido</b> — Botão Voltar no detalhe do torneio agora usa history.back() para retornar à tela anterior (dashboard, explorar, etc.) em vez de sempre ir ao dashboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.71-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga: folga justa e toggle ativo</b> — Jogadores que não completam grupo recebem folga com pontuação média (sem BYE/extra). Sistema distribui folgas de forma igualitária. Toggle "Participando dos sorteios" permite que o inscrito se desative temporariamente — recebe pontuação média e não entra no próximo sorteio. Histórico de folgas (sitOutHistory) garante rotação justa entre todos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.70-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Liga simplificada</b> — Configurações da Liga enxutas: removidos duração da temporada e toggle duplicado de formato de rodada. Dropdowns substituídos por botões exclusivos. Data de início sincroniza com agendamento. Datas reposicionadas após agendamento de sorteios.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Repescagem com melhor perdedor</b> — Sem BYEs: melhores perdedores de R1 jogam repescagem, vencedores + melhor perdedor (por desempenho R1) classificam. Barra de progresso conta todas as partidas do torneio. Numeracao: final = ultimo jogo. Lista de espera visivel no card com contagem separada.</p>' +
        '<p><b>Templates no Firestore</b> — Templates de torneio migrados de localStorage para Firestore (sincroniza entre dispositivos). Botao "Carregar Template" no modal de criacao quando ha templates salvos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.62-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Navegacao por grupos</b> — Botoes coloridos (A, B, C...) no cabecalho fixo do chaveamento para scroll direto ao grupo desejado em Fase de Grupos.</p>' +
        '<p><b>Painel de configuracao de grupos</b> — Ao sortear formato Fase de Grupos, painel dedicado exibe todas as distribuicoes possiveis de grupos com detalhes de classificados e fase eliminatoria.</p>' +
        '<p><b>Badge BYE elegante</b> — Partidas com BYE exibem badge compacto e sinalizado no area do placar, visivel mesmo com campo de resultado presente.</p>' +
        '<p><b>Numeracao de final/3o lugar corrigida</b> — Partidas ocultas com BYE nao contam na numeracao; final e 3o lugar sempre sequenciais apos ultima semifinal real.</p>' +
        '<p><b>Menu hamburger com header fixo</b> — Menu mobile empurra o cabecalho de voltar para baixo dinamicamente em vez de ficar oculto.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.60-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel dedicado de resto</b> — Quando sobram participantes que nao formam time, painel roxo exclusivo com 3 opcoes: reabrir inscricoes, lista de espera ou exclusao. Visualmente distinto do painel de potencia de 2.</p>' +
        '<p><b>Sub-escolha de remocao</b> — Organizador decide entre sorteio aleatorio ou ultimos inscritos para lista de espera/exclusao.</p>' +
        '<p><b>Tipo de jogo e inscricao independentes</b> — Trocar entre Simples/Duplas nao altera mais o modo de inscricao.</p>' +
        '<p><b>Games fixos no Personalizado</b> — Toggle para disputar N games fixos (quem vence mais ganha) em vez de melhor de N.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.59-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sortear direto</b> — Tela intermediaria de pre-sorteio eliminada. Clicar em Sortear abre o painel de resolucao diretamente.</p>' +
        '<p><b>Inscricao individual por padrao</b> — Criacao rapida agora usa modo Individual como padrao.</p>' +
        '<p><b>Descricoes dinamicas nos presets de pontuacao</b> — As descricoes dos presets (1 Set, Melhor de 3, etc.) atualizam em tempo real conforme a configuracao.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.58-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Inscricoes encerram automaticamente</b> — Quando a data/hora limite e atingida, inscricoes sao encerradas sem input do organizador. Quando o maximo de participantes e atingido, tambem encerra automaticamente. Validacao server-side bloqueia inscricoes mesmo via convite.</p>' +
        '<p><b>CRITICO: Race condition de inscricoes corrigida</b> — Inscricoes nao somem mais. Sync do organizador nao sobrescreve participantes. Desinscricao e adicionar participante agora usam transacoes atomicas.</p>' +
        '<p><b>Formato da Partida redesenhado</b> — Presets visuais (1 Set, Melhor de 3, Melhor de 5, 4 Sets, Personalizado). Toggle de vantagem automatico por modalidade.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.50-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Tipo de Jogo com toggles</b> — "Simples" e "Duplas" agora sao toggles independentes com icones e descricoes. Ambos ligados = chaves paralelas. Sincroniza automaticamente com Modo de Inscricao.</p>' +
        '<p><b>Modo de Inscricao com toggles</b> — "Individual" e "Times Montados" como toggles independentes. Ambos ligados = aceita os dois tipos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.48-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Mesclar participantes com suporte mobile</b> — Touch drag-and-drop (long press) na lista de inscritos para mesclar duplicatas.</p>' +
        '<p><b>Lista de inscritos sempre visivel</b> — Inscritos Confirmados exibido mesmo apos o sorteio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.47-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Mesclar participantes por arrastar e soltar</b> — O organizador pode arrastar um participante sobre outro na lista de inscritos para mescla-los.</p>' +
        '<p><b>Deteccao automatica por iniciais</b> — Detecta que "C M" sao as iniciais de "Cica Mange" e corrige automaticamente.</p>' +
        '<p><b>Historico de nomes no perfil e propagacao em team strings.</b></p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.43-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Ordenação de inscritos</b> — Botões A-Z e 🕐 no cabeçalho "Inscritos Confirmados" permitem alternar entre ordem alfabética e ordem cronológica de inscrição. Funciona tanto no modo normal quanto no modo check-in.</p>' +
        '<p><b>Lista de inscritos sem duplicatas</b> — Participantes que aparecem tanto como individual quanto em time agora são exibidos uma única vez (com parceiro e adversário). Deduplicação também remove entradas de nomes antigos que já existem dentro de equipes.</p>' +
        '<p><b>Edição inline de nomes</b> — Organizadores podem clicar diretamente no nome de qualquer participante para editá-lo. Enter ou Tab confirma, Escape cancela. A alteração é propagada em todos os dados do torneio (partidas, classificação, check-in).</p>' +
        '<p><b>Propagação de nomes em times</b> — Troca de nome no perfil agora atualiza corretamente nomes dentro de equipes (ex: "C M / Ana" → "Ciça / Ana").</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.41-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Deduplicação de participantes</b> — Quando um participante troca o nome no perfil, o sistema agora detecta e remove automaticamente a entrada duplicada (nome antigo + nome novo), mantendo apenas o nome atual. Limpeza executada ao visualizar o torneio e antes do sorteio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.40-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Inscrições bloqueadas quando encerradas</b> — Quando o organizador encerra as inscrições, ninguém mais pode se inscrever por nenhum caminho: link de convite, dashboard, +Participante, +Time. Validação adicionada em 5 pontos do código incluindo a transação do banco de dados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.39-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Placar inline consistente</b> — Torneios com sistema Game-Set-Match agora usam campos de placar direto no card da partida (mesmo padrão dos jogos normais), em vez de botão separado que abria modal. Confirmar e Editar resultado funcionam de forma idêntica em todos os formatos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Chaveamento corrigido</b> — Renderização do bracket falhava silenciosamente por referência a variável inexistente, impedindo qualquer chaveamento de aparecer.</p>' +
        '<p><b>Repescagem: BYE automático</b> — Quando a repescagem tem número ímpar de sobreviventes, o jogador excedente agora recebe BYE e avança automaticamente, garantindo o número correto de classificados para a R2.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Repescagem real</b> — Sistema de repescagem reescrito: todos os times jogam a 1a rodada, perdedores disputam mini-chave de repescagem, e os classificados se juntam aos vencedores na 2a rodada formando potência de 2. Antes a repescagem se comportava como BYE.</p>' +
        '<p><b>W.O. na lista de presença</b> — Na lista de inscritos de torneios em andamento, o label &quot;Ausente&quot; foi substituído por &quot;W.O.&quot; nos cards individuais e no botão de filtro.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.30-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Manual corrigido</b> — Texto das notas de versão no manual não vaza mais como texto sem formatação. Aspas duplas no conteúdo quebravam o atributo de busca do HTML, fazendo o texto aparecer como bloco ilegível abaixo das seções do manual.</p>' +
        '<p><b>Coroa do organizador</b> — Ícone de coroa agora aparece corretamente ao lado direito do nome do organizador nos cards de participantes, em vez de abaixo do avatar.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Botão Clonar removido</b> — A função de clonar torneio foi substituída pelo sistema de Templates, que faz o mesmo com mais flexibilidade.</p>' +
        '<p><b>Inscritos ocultos após sorteio</b> — Na página de detalhes do torneio, a lista de inscritos e check-in não aparece mais quando o chaveamento já foi gerado. Acesse via botão 👥 Inscritos no bracket.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.24-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Nome do organizador</b> — Organizador agora exibe nome do perfil em vez do e-mail. Torneios antigos sem nome salvo resolvem automaticamente via lista de participantes.</p>' +
        '<p><b>Lista de chamada movida</b> — Banner de jogos prontos para chamar (check-in) foi removido do topo do chaveamento e movido para a página de Inscritos, acessível pelo botão 👥 Inscritos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painéis de decisão</b> — Botão Cancelar/Voltar agora fica em cabeçalho sticky no topo dos painéis de potência de 2, participantes restantes e sub-escolhas. Sempre visível ao rolar.</p>' +
        '<p><b>Lista de espera no dashboard</b> — Cards do dashboard agora mostram stat-box de lista de espera quando há jogadores em standby, e contagem de inscritos não inclui mais os jogadores na espera.</p>' +
        '<p><b>Barra de progresso corrigida</b> — Contagem de partidas agora inclui jogos dentro de grupos com rodadas (Rei/Rainha, Grupos + Eliminatórias).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login reordenado</b> — Opções de login ordenadas da mais fácil para a mais complexa: Link Mágico (recomendado), SMS, E-mail/Senha, e Google por último. Seção Google inclui passo a passo inline.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.27-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Login redesenhado</b> — Tela de login sem abas: todas as opções visíveis. Manual com guia detalhado em 4 passos coloridos e índice clicável.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Mapa no Local do Torneio</b> — Ao criar ou editar torneio, o campo de local agora exibe mapa interativo com pin ao selecionar o local. Botão de geolocalização (📍) permite usar sua posição atual. Mesmo padrão visual do mapa de preferências do perfil.</p>' +
        '<p><b>Comunicações do Perfil</b> — Novo toggle "Todas" (verde) nas preferências de comunicação. Ativar "Todas" liga automaticamente Importantes e Fundamentais. Desativar "Fundamentais" exibe aviso de que nenhuma comunicação será recebida, com confirmação obrigatória.</p>' +
        '<p><b>GSM Set Fixo</b> — O toggle de tie-break agora permanece visível quando Set Fixo está ativo, permitindo ao organizador escolher se empates vão para tie-break ou se partidas podem terminar empatadas (útil em Liga e Suíço).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auto-amizade com quem convidou</b> — Links de compartilhamento agora incluem referência de quem convidou (?ref=UID). A auto-amizade é criada apenas com quem compartilhou o link, não mais com o organizador.</p>' +
        '<p><b>Correção de nomes desatualizados</b> — Quando um usuário altera o nome no perfil, a mudança agora se propaga automaticamente para todos os torneios em que participa, sem necessidade de novo login. Funciona para todos os usuários e em todas as páginas do app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Atualizações em tempo real</b> — Debounce de atualização reduzido para respostas mais rápidas. Retry automático quando modal ou input está aberto. Service Worker agora usa network-first para assets do app (sempre busca versão mais recente).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>i18n PT-BR</b> — Acentuação corrigida em todo o app. Mapa de local do torneio nos detalhes. Toggle de acesso ao local (público/restrito). Correção de scroll ao salvar resultado. Correção de nomes desatualizados em inscrições.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Toggle Público/Privado</b> — Visibilidade do torneio agora usa toggle switch "Público" (padrão ativado). Desativar torna o torneio privado.</p>' +
        '<p><b>Coroa do organizador</b> — Ícone de coroa no card de detalhe reposicionado para canto inferior direito, igual ao card do dashboard.</p>' +
        '<p><b>Templates acessíveis</b> — Botão "Usar Template" agora aparece corretamente no modal de criação rápida. Corrigido MutationObserver que não detectava abertura do modal.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Lista de Espera visível</b> — Stat-box "Lista de Espera" agora aparece corretamente após mover participantes para espera no painel de resolução.</p>' +
        '<p><b>Set Fixo</b> — Novo modo de pontuação "Set Fixo" no GSM: disputa de N games fixos onde ganha quem vencer mais.</p>' +
        '<p><b>Login por email e telefone</b> — Abas de login: Email Link (passwordless) e Telefone (SMS). Removidos Apple e Facebook. Google Sign-In mantido.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Notificações em tempo real</b> — Listener Firestore onSnapshot para notificações instantâneas. Classificação de eliminatórias corrigida (posições únicas por jogador). Botão de organização movido para o card de detalhes. Busca de local no perfil corrigida.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Correções diversas</b> — Seção de organizador restaurada nos detalhes do torneio. Exclusão de torneio corrigida (conflito de variável com i18n). Organizadores não-competidores excluídos da contagem de participantes. Espaçamento entre cards de partida no bracket.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de segurança e correções</b> — uid do participante agora salvo na inscrição (corrige notificações de co-organização). Escaping de backslash em onclick handlers. Escape de nomes em atributos data-player-name. Toggle switch adotado em todo o app. Dead code removido.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.8.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Organização (compartilhar/transferir)</b> — Novo botão "👑 Organização" nas ferramentas do organizador. Picker de participantes para compartilhar (co-organizador) ou transferir organização. Transferência agora concede privilégios completos de criador. Notificações de convite com aceite/recusa.</p>' +
        '<p><b>i18n completo</b> — Todas as strings hardcoded restantes em explore, notifications, rules, enrollment e organizer conectadas ao sistema _t(). ~35 novas chaves para host-transfer.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.7.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Foto do Organizador</b> — Foto/ícone de perfil do organizador agora aparece à esquerda do nome em todos os cards de organizador: seção ORGANIZAÇÃO, cabeçalho de detalhes do torneio e cards do dashboard. Fotos de organizadores e co-organizadores são pré-carregadas junto com as dos participantes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.5.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Landing Page</b> — Página de apresentação para visitantes não logados com hero, features, "como funciona" e call-to-action. Funciona nos 4 temas.</p>' +
        '<p><b>Internacionalização (i18n)</b> — Sistema de tradução _t(key) com suporte a Português e Inglês. Seletor de idioma no perfil. Infraestrutura pronta para tradução gradual.</p>' +
        '<p><b>Templates de Torneio</b> — Salve configurações de torneio como template reutilizável. Botão "Salvar como Template" no detalhe do torneio. "Usar Template" na criação rápida. Limite de 10 no plano Free.</p>' +
        '<p><b>Analytics do Organizador</b> — Painel colapsável "Minhas Estatísticas" no dashboard: total de torneios, participantes únicos, média por torneio, gráficos por formato/esporte, mês mais ativo.</p>' +
        '<p><b>Templates de Email</b> — Templates HTML prontos para notificações por email (inscrição, resultado, lembrete, atualização). Preparação client-side para Cloud Function futura.</p>' +
        '<p><b>Cleanup</b> — Identificado e removido js/tournaments.js raiz (6.264 linhas de dead code). ~20 novos testes automatizados (total ~97).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.12-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painel Unificado de Resolução Numérica</b> — Os 3 painéis de decisão (times incompletos, número ímpar, potência de 2) foram consolidados em um único painel com diagnóstico completo. Gauge visual mostra potência inferior/atual/superior com contagem de participantes. Cores Nash contínuas verde→vermelho com maior distinção visual. Botão ✕ para excluir opções temporariamente e recalcular Nash. Novas opções: Repescagem e Exclusão em todos os cenários.</p>' +
        '<p><b>Simplificação de Esportes</b> — Apenas modalidades derivadas do tênis: Beach Tennis, Pickleball, Tênis, Tênis de Mesa, Padel. Ícones de esporte limpos em todo o app.</p>' +
        '<p><b>Formato como Botões</b> — Dropdown de formato substituído por botões excludentes com descrição dinâmica.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sorteio de times corrigido</b> — A verificação de potência de 2 agora conta times (não participantes individuais). Painéis de decisão exibem contagem correta de times quando teamSize > 1.</p>' +
        '<p><b>Botão Convidar sem restrições</b> — Botão "Convidar" visível e funcional para organizadores e participantes em qualquer estado do torneio.</p>' +
        '<p><b>Botões de ação após sorteio</b> — Grid de ações: Regras, Inscritos, QR Code, Imprimir, Exportar CSV, Modo TV. Layout 2 colunas no mobile.</p>' +
        '<p><b>Iniciar Torneio no bracket</b> — Re-renderiza o bracket corretamente após clique.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Painéis de Decisão com Nash</b> — Painéis de potência de 2 e times incompletos agora exibem indicador visual de equilíbrio de Nash em cada opção. Cores por temperatura: verde (melhor equilíbrio) → amarelo → laranja → azul (menor equilíbrio). Badge "Nash X%" em cada botão. Layout 3x2 com Enquete ao lado do Formato Suíço.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria Completa v2</b> — ~68 issues identificadas e corrigidas em 30 arquivos (~24.600 linhas auditadas). 14 CRITICAL, 22 HIGH, 18 MEDIUM, 14 LOW.</p>' +
        '<p><b>Segurança (XSS)</b> — showNotification/showConfirmDialog/showAlertDialog/showInputDialog agora sanitizam título automaticamente. ~30 onclick/oninput handlers com IDs não-escapados corrigidos em bracket.js, bracket-ui.js, tournaments-draw-prep.js, tournaments-draw.js, tournaments-organizer.js, tournaments-categories.js, main.js, auth.js, pre-draw.js, create-tournament.js. Dados da Google Places API e OpenWeather API sanitizados.</p>' +
        '<p><b>Bugs Corrigidos</b> — firebase-db.js: substring matching (.includes) na verificação de duplicata de inscrição → comparação exata. dashboard.js: operador lógico sem parênteses fazia torneios Liga encerrados mostrarem "Inscrições Abertas". auth.js: race condition — flag de login limpa antes do auto-enroll executar. tournaments-categories.js: função _groupEligibleCategories estava ausente (perdida na refatoração v0.4.2). participants.js: variável isVip usada antes da declaração. tournaments.js: substring matching na detecção de participante → comparação exata. dashboard.js: displayName null causava crash.</p>' +
        '<p><b>Memory Leaks</b> — hints.js: event listeners nos botões de balloon agora usam {once:true}. Auto-dismiss timeout limpo ao descartar manualmente. Resize/scroll listeners removidos ao desativar hints.</p>' +
        '<p><b>Melhorias</b> — notifications-view.js: seletor DOM frágil substituído por classe CSS. rules.js: datas inválidas tratadas graciosamente. create-tournament.js: booleanos GSM salvos consistentemente como string. dashboard.js: detecção de times suporta formato objeto. store.js: fallback redundante removido.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria de Segurança</b> — ~25 vulnerabilidades XSS corrigidas em 8 arquivos (bracket.js, bracket-ui.js, explore.js, pre-draw.js, notifications-view.js, auth.js, rules.js). Todos os dados de usuário sanitizados com _safeHtml().</p>' +
        '<p><b>Bug Fixes</b> — firebase-db.js usava fromUid ao invés de toUid ao verificar pedidos de amizade mútuos (corrigido). dashboard.js truthy check em sorteioRealizado — arrays vazios não são mais tratados como sorteio realizado. dashboard.js substring matching na detecção de participante — agora usa comparação exata (email/uid/displayName). bracket-ui.js tiebreak com pontos iguais — agora valida margem e pontuação mínima.</p>' +
        '<p><b>CSS</b> — Removidos temas mortos (high-contrast, alternative). Overrides de hint corrigidos para sunset/ocean.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Sistema Game-Set-Match (GSM)</b> — Sistema completo de pontuação por sets, games e tiebreaks para torneios de raquete e similares. Configuração na criação/edição do torneio com padrões por esporte (Beach Tennis, Tênis, Padel, Pickleball, Tênis de Mesa, Vôlei). Contagem numérica ou estilo tênis (15-30-40). Regra de vantagem, tiebreak e super tiebreak configuráveis. Preferências do usuário salvas por esporte. No bracket, botão "Lançar Sets" abre overlay dedicado com validação em tempo real. Resultados exibidos como "6-4 3-6 7-6(5)". Novos critérios de desempate automáticos: saldo de sets (±S), saldo de games (±G), tiebreaks vencidos. Colunas extras na tabela de classificação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.4.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Auditoria Completa + Novos Temas</b> — Revisão linha a linha de todo o codebase com ~34 bugs corrigidos. Correções críticas: "Reabrir Inscrição" restaurado, race condition de inscrições durante decisão de potência de 2, operador lógico sem parênteses, XSS em notificações e explorar, enroll-modal reescrito. 4 temas disponíveis: Noturno, Claro, Pôr do Sol e Oceano. CSS refatorado para variáveis por tema. Dashboard cards com gradientes adaptativos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.3.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Duração Estimada do Torneio</b> — Quando a data/hora de término não está preenchida, a página de detalhes exibe simulação de duração para 8, 16, 32 e 64 participantes. Se houver inscritos, mostra também a estimativa com o número real. Cálculo considera formato (Eliminatórias, Dupla Elim., Grupos + Elim., Suíço, Liga), duração da partida, número de quadras e tempos de chamada/aquecimento. Mostra número de partidas e horário estimado de término quando há hora de início definida.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.3.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Consistência Visual Total</b> — Todos os botões sólidos e coloridos (sem cinza). Botões topbar (Apoie, Pro, Organizador) padronizados. Ferramentas do Organizador reordenadas: Editar, Comunicar, +Participantes, +Times, Add Bot, CSV, Clonar, Categorias, Encerrar Inscrições, Sortear, Encerrar Torneio, Apagar. Botão Visão simplificado para "Organizador"/"Participante". Info-pill, info-box e stat-box com opacidade otimizada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.3.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Abril 2026)</span></div>' +
        '<p><b>Padronização Visual de Botões</b> — Sistema unificado de botões com classes CSS reutilizáveis (btn-primary, btn-success, btn-warning, btn-danger, btn-tool, etc). Todos os botões agora têm border-radius, font-size, padding e peso de fonte consistentes. Cores sólidas em todo o app (exceto Add Bot e Apagar que mantêm transparência). Efeito hover-lift implementado no CSS. Mais de 200 botões padronizados em 10+ arquivos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.42-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Excluir Conta</b> — Opção para o usuário excluir permanentemente sua conta e todos os dados associados (perfil, notificações, inscrições, torneios organizados). Disponível no modal de perfil com dupla confirmação (digitação de "EXCLUIR"). Conformidade LGPD.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.41-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Plano Pro + Apoio Voluntário</b> — Modelo freemium: plano gratuito (3 torneios, 32 participantes) e plano Pro (R$19,90/mês — ilimitado, upload de logo, Modo TV sem marca). Pagamento automatizado via Stripe (cartão de crédito). Botão "Apoie" para contribuição voluntária via PIX (qualquer valor). Botões Pro e Apoie na barra de navegação.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.40-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Push Notifications</b> — Notificações push via Firebase Cloud Messaging. Ao fazer login, o app solicita permissão e registra o token FCM no seu perfil. Notificações de novos rounds, inscrições e resultados chegam mesmo com o app fechado. Auto-draw para Liga/Ranking via Cloud Functions (rodadas geradas automaticamente a cada hora).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.39-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Previsão do Tempo</b> — Ao criar/editar torneio, se o local e data de início estiverem definidos (até 5 dias no futuro), a previsão do tempo é exibida automaticamente. Integração com OpenWeatherMap (temperatura, descrição, ícone).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.38-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Paginação no Dashboard</b> — Torneios carregam em lotes de 12. Botão "Carregar mais (N restantes)" aparece quando há mais de 12 torneios. Melhora performance para quem gerencia muitos torneios. Paginação reseta ao trocar de filtro.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.37-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Acessibilidade</b> — Link "Pular para o conteúdo" para navegação por teclado. ARIA roles e labels na topbar, navegação e conteúdo principal. aria-expanded no menu hamburger. Focus trap em modais (Tab circula dentro do modal). Live region para leitores de tela (notificações anunciadas). Melhoria geral de acessibilidade WCAG 2.1.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.36-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Modo Compacto do Dashboard</b> — Toggle "Cards/Lista" no dashboard para alternar entre visualização em cards (padrão) e lista compacta. Modo lista mostra nome, esporte, formato, data, participantes, progresso e status em uma linha por torneio. Ideal para quem gerencia muitos torneios. Preferência salva em localStorage.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.35-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Atalhos de Teclado Globais</b> — Navegação rápida: D (Dashboard), E (Explorar), N (Novo Torneio), / (Busca), ? (Ajuda), ESC (Fechar modal). Atalhos desativados automaticamente quando digitando em campos de texto. Nova seção "⌨️ Atalhos de Teclado" no modal de Ajuda.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.34-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Histórico de Atividades</b> — Seção "📜 Histórico de Atividades" na página de detalhes do torneio. Log visual cronológico com: criação, inscrições, sorteio, início, resultados de partidas e encerramento. Colapsável, mostra últimos 15 eventos com opção de expandir anteriores. Construído dinamicamente dos dados existentes sem necessidade de banco extra.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.33-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Estatísticas do Jogador</b> — Clique no nome de qualquer jogador para ver suas estatísticas consolidadas. Modal com: torneios disputados, vitórias, derrotas, empates, partidas totais, % de aproveitamento, títulos. Lista de torneios disputados com links. Disponível no chaveamento, classificação e lista de inscritos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.32-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>QR Code do Torneio</b> — Botão "📱 QR Code" na página do torneio e no chaveamento. Modal com QR code para compartilhar link. Ideal para projetar em eventos presenciais. Opções: copiar link, baixar PNG e imprimir. Suporta tema claro/escuro.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.31-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Busca Rápida (Ctrl+K)</b> — Pressione Ctrl+K (ou Cmd+K no Mac) para abrir busca global. Encontre torneios por nome, esporte, formato ou local. Busque jogadores inscritos. Ações rápidas: Novo Torneio, Dashboard, Explorar, Notificações, Ajuda. Busca com debounce de 150ms.</p>' +
        '<p><b>Torneios Encerrados</b> — Separados em seção colapsável no dashboard.</p>' +
        '<p><b>Compartilhar Resultado</b> — Botão "📤" em partidas concluídas para WhatsApp/clipboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.28-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Tema Claro/Escuro</b> — Botão 🌙/☀️ no header para alternar entre tema escuro e claro. Preferência salva em localStorage. Transição suave de 300ms. CSS com overrides específicos para o tema claro.</p>' +
        '<p><b>Countdown de Início</b> — Torneios com data futura mostram "Começa hoje/amanhã/em X dias" nos cards e detalhes.</p>' +
        '<p><b>Navegação Suave</b> — Scroll-to-top + fade-in ao navegar entre views.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.25-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Confrontos Diretos</b> — Matriz head-to-head abaixo da classificação (Liga/Suíço) mostrando o retrospecto entre cada par de jogadores. Formato V-E-D com cores indicativas: verde para vantagem, vermelho para desvantagem. Seção expansível, suporta categorias. Ideal para análise de desempate.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.24-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Ordenação de Colunas</b> — Clique nos cabeçalhos da tabela de classificação (Liga/Suíço) para ordenar por qualquer coluna: posição, nome, pontos, vitórias, empates, derrotas, saldo ou jogos. Setas indicam a direção da ordenação. Funciona em todas as categorias.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.23-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Modo TV</b> — Botão "📺 Modo TV" na página do chaveamento abre exibição fullscreen otimizada para projetores e TVs no local do torneio. Mostra classificação/bracket em tela cheia com fundo escuro, relógio em tempo real e auto-refresh a cada 30 segundos. Saia com ESC ou botão Sair.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.22-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Favoritar Torneios</b> — Clique na estrela (☆/★) ao lado do nome do torneio nos cards do dashboard ou na página de detalhes para marcar como favorito. Favoritos são salvos localmente por usuário e aparecem como filtro dedicado no dashboard quando há ao menos 1 favorito.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.21-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Imprimir Chaveamento</b> — Botão "Imprimir" na página do chaveamento/classificação. Layout otimizado para impressão com fundo branco, tabelas com bordas, avatares ocultados e orientação paisagem automática. Ideal para exibir brackets em locais de torneio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.20-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Testes Automatizados</b> — 21 testes unitários cobrindo funções core: classificação, progresso de partidas, formatos de torneio, sanitização HTML e mais. Página de testes acessível em /tests.html.</p>' +
        '<p><b>_isLigaFormat global</b> — Helper de formato Liga/Ranking agora disponível globalmente desde o carregamento do script (antes dependia da renderização da view).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.19-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Confrontos do Jogador</b> — Clique no nome de qualquer jogador na classificação para ver seu histórico completo de partidas: adversários, placares, resultados e resumo de V/E/D.</p>' +
        '<p><b>Notificação de Resultado</b> — Participantes agora recebem notificação automática quando o resultado de sua partida é registrado.</p>' +
        '<p><b>Última Atualização</b> — A página de detalhes do torneio mostra quando foi a última modificação (resultado, inscrição, etc).</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.18-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Rodadas Anteriores</b> — Na classificação de Liga e Suíço, agora é possível expandir e ver todas as rodadas anteriores com os resultados de cada partida. Seção colapsada por padrão para não poluir a tela.</p>' +
        '<p><b>Estatísticas do Torneio</b> — Resumo com destaques: jogador com mais vitórias, maior sequência de vitórias consecutivas e total de partidas disputadas. Aparece automaticamente a partir da 2ª rodada.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.17-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Exportar Resultados (CSV)</b> — Botão "Exportar CSV" na página do torneio e no chaveamento. Baixa um arquivo CSV com classificação (Liga/Suíço) ou resultados das partidas (Eliminatórias). Compatível com Excel.</p>' +
        '<p><b>Clonar Torneio</b> — Botão "Clonar" na página do torneio cria uma cópia com mesmas configurações (formato, local, categorias, regras) mas sem participantes nem resultados. Ideal para torneios recorrentes.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.16-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Compartilhar Torneio</b> — Botão "Compartilhar" na página de detalhes, visível para todos. Usa compartilhamento nativo no mobile ou copia o link.</p>' +
        '<p><b>Progresso do Torneio</b> — Barra visual mostrando % de partidas concluídas nos cards e na página de detalhes.</p>' +
        '<p><b>Countdown de Inscrições</b> — Aviso "Inscrições encerram em X dias" quando o prazo está próximo.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.15-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Suas Próximas Partidas</b> — O dashboard agora exibe um widget com suas partidas pendentes em torneios ativos. Mostra o oponente, torneio e esporte — clique para ir direto à partida.</p>' +
        '<p><b>Modo Offline</b> — Banner informativo quando a conexão cai e confirmação quando reconecta. Complemento do PWA para melhor experiência offline.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.14-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Filtro de Torneios</b> — A lista de torneios agora possui barra de busca por nome, esporte ou formato, além de filtro por status (Inscrições Abertas, Em Andamento, Encerrados). Filtragem instantânea sem recarregar a página.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.13-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>PWA — Instale no celular!</b> — scoreplace.app agora pode ser instalado como app no seu dispositivo. Acesse pelo navegador e toque em "Adicionar à tela inicial". Carregamento mais rápido com cache inteligente (stale-while-revalidate).</p>' +
        '<p><b>Histórico de Torneios</b> — O perfil agora mostra a lista dos últimos torneios com posição final (🥇🥈🥉), formato e status. Clique para ir direto ao torneio.</p>' +
        '<p><b>Temporada Liga — Auto-encerramento</b> — Torneios Liga com duração de temporada definida agora são encerrados automaticamente quando o prazo expira. Standings finais são calculados e salvos. Aviso visual "⚠️ X dias restantes" quando faltam 7 dias ou menos.</p>' +
        '<p><b>Segurança</b> — Nomes de participantes são agora sanitizados contra XSS na lista de participantes. Utilitário global <code>_safeHtml()</code> disponível para uso em todo o app.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.11-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Meu Desempenho</b> — O perfil do jogador agora exibe estatísticas pessoais: torneios participados, partidas jogadas, vitórias, derrotas, empates, aproveitamento (%) e títulos conquistados. Os dados são calculados em tempo real a partir de todos os torneios no sistema.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.10-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Validação de datas</b> — Ao criar ou editar torneio, o sistema agora valida que a data de fim é posterior à de início e que o prazo de inscrição é anterior ao início.</p>' +
        '<p><b>Inscrição automática</b> — Se a inscrição automática após login falhar (conexão lenta), o usuário agora recebe um aviso para tentar manualmente.</p>' +
        '<p><b>Migração Liga/Ranking</b> — Ao salvar torneio no formato Liga, campos legados do formato Ranking são removidos automaticamente do banco de dados.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.9-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Fase de Grupos — empates</b> — Empates agora são contabilizados corretamente na classificação dos grupos e no cross-seeding para a fase eliminatória (1 ponto por empate).</p>' +
        '<p><b>Dupla Eliminatória — limpeza</b> — Removido código morto na geração do lower bracket que poderia causar inconsistências.</p>' +
        '<p><b>Notificações seguras</b> — Mensagens de notificação agora são sanitizadas contra XSS. Convites de torneio exibem botão "Ver Torneio" direto na notificação.</p>' +
        '<p><b>Exclusão de torneio</b> — Feedback de erro melhorado quando a exclusão no servidor falha.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.8-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Botão Encerrar Torneio</b> — O organizador agora pode encerrar qualquer torneio manualmente a qualquer momento. Confirmação com aviso de partidas pendentes.</p>' +
        '<p><b>Pódio / Classificação Final</b> — Torneios encerrados exibem pódio visual com 🥇🥈🥉 na página de detalhes. Eliminatórias mostram campeão, vice e 3º lugar. Suíço/Liga mostram top 3 por pontos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.7-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Encerramento automático</b> — Torneios Eliminatórias e Dupla Eliminatória agora são marcados como "Encerrado" automaticamente quando todas as partidas (incluindo disputa de 3º lugar) são concluídas. Status "🏆 Encerrado" exibido no dashboard e nos detalhes do torneio.</p>' +
        '<p><b>Status padronizado</b> — Corrigida inconsistência onde torneios Suíço encerrados não eram reconhecidos como finalizados pelo dashboard e outras telas. Inscrições bloqueadas em torneios encerrados.</p>' +
        '<p><b>Dashboard corrigido</b> — Detecção de sorteio no dashboard agora usa verificação correta para arrays vazios, evitando falsos positivos.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Liga unificada</b> — Os formatos Liga e Ranking foram unificados em um único formato "Liga" com todas as opções combinadas: duração de temporada (indefinida, 3, 6, 12 meses ou personalizada), inscrições abertas configuráveis, agendamento de sorteios automáticos, regras de inatividade e pontuação de novos jogadores. Torneios existentes no formato Ranking continuam funcionando normalmente.</p>' +
        '<p><b>Empate em Liga/Suíço</b> — Empates agora são permitidos em partidas de Liga e Suíço (1 ponto para cada). Classificação atualizada com coluna "E" (empates). Cálculo de Buchholz e Sonneborn-Berger corrigido para considerar empates.</p>' +
        '<p><b>Proteção contra re-sorteio</b> — Refazer o sorteio agora pede confirmação. Se já houver resultados registrados, avisa que serão perdidos.</p>' +
        '<p><b>Eliminatórias com categorias</b> — Torneios Eliminatórias agora geram chaveamento separado por categoria (Masc, Fem, Misto, etc.).</p>' +
        '<p><b>Validação pré-sorteio</b> — Participantes sem categoria são identificados com aviso antes do sorteio.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.4-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Auto-inscrição com categorias</b> — Ao aceitar convite, o participante agora é inscrito automaticamente na categoria correta com base no gênero do perfil. Organizador recebe notificação da nova inscrição.</p>' +
        '<p><b>Cancelar inscrição seguro</b> — Corrigido bug onde nomes parciais podiam causar remoção do participante errado (ex: "Ana" removendo "Ana Paula"). Agora usa comparação exata.</p>' +
        '<p><b>Criar torneio — limpeza</b> — Removida referência a campo de periodicidade de Liga que não existia mais no HTML, eliminando erros silenciosos na criação e edição.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.3-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Detecção de mesclagem aprimorada</b> — O ícone de desmesclar agora aparece em qualquer categoria resultante de mesclagem: categorias com "/" no nome (ex: "Fem A/B"), prefixos puros quando há categorias de habilidade (ex: "Masc"), e categorias com histórico de mesclagem. Unmerge inferido para categorias sem mergeHistory.</p>' +
        '<p><b>Cache-buster por versão</b> — Todos os arquivos JS e CSS agora usam ?v=0.2.3-alpha em vez de numeração sequencial, facilitando o rastreamento de versão no cache do navegador.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.2-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Enquete entre participantes</b> — Nova opção nos painéis de resolução de times incompletos e ajuste de potência de 2. O organizador cria uma enquete com prazo (padrão 48h), e os inscritos votam na solução preferida. Contagem regressiva ao vivo, notificação automática, voto secreto antes de votar e resultados revelados após o voto. A opção recomendada é calculada pelo equilíbrio de Nash. Organizador aplica o resultado com um clique.</p>' +
        '<p><b>Desmesclar categorias</b> — Categorias mescladas agora mostram um ícone de desmesclagem. Ao clicar, desfaz a mesclagem e reatribui participantes às categorias originais usando histórico de mesclagem.</p>' +
        '<p><b>Remover participante da categoria</b> — No detalhe da categoria, cada card de jogador tem botão para removê-lo, deixando-o como "sem categoria" para reatribuição.</p>' +
        '<p><b>Simplificação Misto</b> — "Misto Aleatório" e "Misto Obrigatório" são exibidos como "Misto" em cards, badges e inscrição. Nome completo apenas nas regras, card do torneio e detalhes.</p>' +
        '<p><b>Multi-categoria</b> — Participantes podem se inscrever em categorias não-excludentes (ex: Masc A + Misto A). Categorias Fem/Masc são mutuamente exclusivas (radio); Misto é aditivo (checkbox).</p>' +
        '<p><b>Ordenação de mesclagem</b> — Ao mesclar categorias, os sufixos são ordenados por força (A antes de B). Se todas as habilidades forem mescladas, simplifica para apenas o prefixo (ex: "Masc" ao invés de "Masc A/B/C/D").</p>' +
        '<p><b>Handler de times incompletos</b> — Implementada a função _handleIncompleteOption que estava ausente, habilitando todas as opções do painel de resolução.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.1-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Gerenciador de Categorias redesenhado</b> — Categorias agora aparecem acima dos participantes sem categoria. Cards compactos (sem lista de nomes, apenas contagem). Categorias agrupadas por gênero na mesma linha (Fem, Masc, etc.). Clique em uma categoria para ver os inscritos em detalhe com avatar e e-mail.</p>' +
        '<p><b>Navegação no gerenciador</b> — Botão "Voltar" abaixo do título em ambas as telas (visão geral e detalhe da categoria). Botão × mantido no canto superior direito para fechar.</p>' +
        '<p><b>Drag-and-drop corrigido</b> — Arrastar e soltar participantes para categorias agora funciona de forma consistente para todos os participantes (não apenas o primeiro). Suporte a toque para dispositivos móveis.</p>' +
        '<p><b>Atribuição automática de categorias</b> — Quando o organizador cria ou edita categorias em um torneio com inscritos, participantes com gênero no perfil são atribuídos automaticamente à categoria correspondente. Badge "(perfil)" indica a origem da atribuição.</p>' +
        '<p><b>Indicadores de categoria nos participantes</b> — "(perfil)" para categorias atribuídas pelo perfil; "(sem cat.)" em vermelho para participantes sem categoria; "(sem cat.)" ao lado da categoria para quem foi alocado manualmente, preservando o histórico.</p>' +
        '<p><b>Notificação de atribuição de categoria</b> — Participantes recebem um alerta ao acessar o torneio informando sua categoria atribuída, com botão "Questionar Organizador" que abre e-mail pré-preenchido.</p>' +
        '<p><b>Gênero salvo na inscrição</b> — O gênero do perfil do participante é agora salvo no registro de inscrição, permitindo atribuição automática futura caso categorias sejam adicionadas depois.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.2.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Formato Ranking</b> — Novo formato de torneio com temporada contínua (3, 6, 12 meses ou personalizado). Inscrições permanecem abertas durante toda a temporada. O botão "Encerrar Inscrições" não aparece. Estimativa de tempo é ocultada dos detalhes.</p>' +
        '<p><b>Sorteios automáticos</b> — Formatos Ranking e Suíço agora suportam sorteios periódicos. O organizador define data/hora do primeiro sorteio e intervalo em dias para os próximos. Contagem regressiva (dias, horas, minutos) é exibida tanto na view de detalhes quanto na classificação. Modo manual disponível via toggle.</p>' +
        '<p><b>Sistema de Categorias</b> — Funcionalidade transversal a todos os formatos de torneio. O organizador define categorias de gênero (Feminino, Masculino, Misto Aleatório, Misto Obrigatório) via botões toggle e categorias de habilidade (A, B, C, D...) via campo de texto. O sistema gera automaticamente as categorias combinadas (ex: Fem A, Fem B, Masc A, Masc B) com preview em tempo real.</p>' +
        '<p><b>Inscrição com categoria</b> — Torneios com categorias pedem ao participante que escolha sua categoria no ato da inscrição. Se o perfil já tiver gênero, as opções são filtradas automaticamente. Modal de seleção com botões estilizados para múltiplas opções; confirmação automática quando há apenas uma opção elegível.</p>' +
        '<p><b>Sorteio e classificação por categoria</b> — O algoritmo de emparelhamento Suíço agora gera confrontos separados por categoria. Cada categoria tem sua própria tabela de classificação com pontuação, vitórias, derrotas, saldo e desempates independentes.</p>' +
        '<p><b>Gerenciador de Categorias</b> — Botão "Categorias" na view de detalhes permite ao organizador: visualizar cards de cada categoria com contagem de inscritos e lista de nomes; mesclar categorias por arrastar e soltar (Fem A + Fem B → Fem A/B) com confirmação e renomeação inteligente; atribuir categoria a participantes sem categoria arrastando-os para o card desejado. Categoria original preservada no registro.</p>' +
        '<p><b>Convite por QR Code</b> — Painel de convite agora inclui QR Code exclusivo do torneio, gerado automaticamente. Basta apontar a câmera do celular para escanear e acessar a inscrição. Ideal para compartilhar em eventos presenciais.</p>' +
        '<p><b>Dashboard</b> — Texto alterado de "Inscrições Abertas" para "Inscrições Disponíveis" na saudação do dashboard.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.1.6-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Logo de Torneio</b> — Geração automática de logo via Canvas com paletas temáticas por esporte. Botões para regerar, travar, baixar e upload de imagem própria.</p>' +
        '<p><b>Sistema de Notificações</b> — Notificações para inscrição/desinscrição, alterações, lembretes automáticos e torneios próximos ao CEP.</p>' +
        '<p><b>Central de Ajuda</b> — Manual completo com seções pesquisáveis.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.1.5-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Explorar e Comunidade</b> — Nova aba para encontrar jogadores, enviar pedidos de amizade, ver conhecidos de torneios anteriores e buscar por nome, cidade ou esporte.</p>' +
        '<p><b>Dashboard aprimorada</b> — Cards de resumo clicáveis, filtros por modalidade/local/formato com pills coloridas, cards de torneio com foto do local como fundo.</p>' +
        '<p><b>Notificações</b> — View dedicada para visualizar notificações recebidas.</p>' +
        '</div>' +
        '<div style="margin-bottom:1rem;">' +
        '<div style="font-weight:700; color:var(--text-bright); font-size:0.9rem; margin-bottom:6px;">v0.1.0-alpha <span style="color:var(--text-muted); font-weight:400; font-size:0.75rem;">(Março 2026)</span></div>' +
        '<p><b>Lançamento Alpha</b> — Primeira versão funcional com Firebase Auth (e-mail/senha, Google, Facebook, Apple), Cloud Firestore, 5 formatos de torneio, criação rápida, fluxo de convite por link/WhatsApp/e-mail, CSS responsivo completo, 5 temas visuais e busca de local via Google Places.</p>' +
        '</div>' +
        '<p style="font-size:0.7rem; color:var(--text-muted); opacity:0.7; text-align:center; margin-top:1rem;">scoreplace.app está em fase Alpha. Novas funcionalidades são adicionadas frequentemente!</p>'
    }
  ];

  // Build sections HTML
  var sectionsHtml = '';
  for (var i = 0; i < helpSections.length; i++) {
    var s = helpSections[i];
    sectionsHtml += '<div class="help-section" data-help-id="' + s.id + '" data-search="' + (s.title + ' ' + s.content).replace(/<[^>]+>/g, '').toLowerCase().replace(/"/g, '&quot;') + '" style="margin-bottom: 1rem;">' +
      '<div onclick="this.parentElement.classList.toggle(\'open\')" style="display:flex; align-items:center; gap:10px; padding:10px 12px; background:rgba(99,102,241,0.06); border:1px solid rgba(99,102,241,0.12); border-radius:10px; cursor:pointer; transition:all 0.2s; user-select:none;" onmouseover="this.style.background=\'rgba(99,102,241,0.12)\'" onmouseout="this.style.background=\'rgba(99,102,241,0.06)\'">' +
        '<span style="font-size:1.2rem; flex-shrink:0;">' + s.icon + '</span>' +
        '<span style="font-size:0.85rem; font-weight:600; color:var(--text-bright); flex:1;">' + s.title + '</span>' +
        '<span class="help-chevron" style="font-size:0.7rem; color:var(--text-muted); transition:transform 0.2s;">&#9660;</span>' +
      '</div>' +
      '<div class="help-body" style="display:none; padding:12px 14px 8px; font-size:0.8rem; color:var(--text-main); line-height:1.7;">' + s.content + '</div>' +
    '</div>';
  }

  var html = '<div class="modal-overlay" id="modal-help">' +
    '<div class="modal" style="max-width:560px; padding:0; max-height:85vh; display:flex; flex-direction:column;">' +
      // Sticky header — Voltar pill (tournament-details style) stays pinned while body scrolls.
      '<div style="position:sticky; top:0; z-index:2; background:var(--bg-card, var(--bg-darker)); padding:12px 1.5rem 10px; flex-shrink:0; border-bottom:1px solid rgba(255,255,255,0.06);">' +
        '<div style="display:flex; align-items:center; justify-content:space-between; gap:8px; margin-bottom:10px;">' +
          '<button class="btn btn-outline btn-sm hover-lift" onclick="if(typeof closeModal===\'function\')closeModal(\'modal-help\');" style="display:inline-flex; align-items:center; gap:6px; padding:6px 16px; border-radius:20px;">' +
            '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>' +
            ' Voltar' +
          '</button>' +
          '<h2 style="margin:0; font-size:1rem; font-weight:800; color:var(--text-bright);">Central de Ajuda</h2>' +
          '<button onclick="if(typeof closeModal===\'function\')closeModal(\'modal-help\');" aria-label="Fechar" style="background:none;border:none;color:var(--text-muted,#94a3b8);font-size:1.5rem;cursor:pointer;line-height:1;padding:0 4px;">&times;</button>' +
        '</div>' +
        '<input type="text" id="help-search-input" placeholder="Buscar no manual..." style="width:100%; box-sizing:border-box; padding:10px 14px; border-radius:10px; border:1px solid var(--border-color); background:var(--bg-darker); color:var(--text-color); font-size:0.85rem; outline:none;" oninput="window._filterHelpSections(this.value)">' +
      '</div>' +
      '<div id="help-sections-container" style="padding:1.25rem 1.5rem 1.5rem; overflow-y:auto; flex:1;">' +
        sectionsHtml +
        '<div id="help-no-results" style="display:none; text-align:center; padding:2rem 1rem; color:var(--text-muted); font-size:0.85rem;">Nenhum resultado encontrado. Tente outras palavras.</div>' +
      '</div>' +
    '</div>' +
  '</div>';

  document.body.insertAdjacentHTML('beforeend', html);

  // Add CSS for open/close animation
  var style = document.createElement('style');
  style.textContent = '.help-section.open .help-body { display: block !important; } .help-section.open .help-chevron { transform: rotate(180deg); } .help-section .help-body p { margin: 0 0 8px; } .help-section .help-body p:last-child { margin-bottom: 0; }';
  document.head.appendChild(style);

  // Auto-open "Sobre" section by default
  var aboutSection = document.querySelector('.help-section[data-help-id="about"]');
  if (aboutSection) aboutSection.classList.add('open');

  // Search/filter function
  window._filterHelpSections = function(query) {
    var q = (query || '').toLowerCase().trim();
    var sections = document.querySelectorAll('#help-sections-container .help-section');
    var noResults = document.getElementById('help-no-results');
    var visibleCount = 0;

    sections.forEach(function(sec) {
      var searchData = sec.getAttribute('data-search') || '';
      if (!q || searchData.indexOf(q) !== -1) {
        sec.style.display = '';
        visibleCount++;
        // Auto-open matching sections when searching, close non-matching
        if (q) {
          sec.classList.add('open');
        }
      } else {
        sec.style.display = 'none';
        sec.classList.remove('open');
      }
    });

    // If search is empty, close all except "about"
    if (!q) {
      sections.forEach(function(sec) {
        if (sec.getAttribute('data-help-id') !== 'about') sec.classList.remove('open');
        else sec.classList.add('open');
      });
    }

    if (noResults) noResults.style.display = visibleCount === 0 ? '' : 'none';
  };

  // Scroll to section and open it
  window._helpScrollTo = function(sectionId) {
    var sec = document.querySelector('.help-section[data-help-id="' + sectionId + '"]');
    if (!sec) return;
    sec.classList.add('open');
    sec.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };
})();

// === Modal Criação Rápida ===
(function setupQuickCreateModal() {
  if (document.getElementById('modal-quick-create')) return;
  const html = `
    <div class="modal-overlay" id="modal-quick-create">
      <div class="modal" style="max-width:420px;">
        ${typeof window._renderBackHeader === 'function' ? window._renderBackHeader({label:(window._t||function(k){return k;})('btn.back')||'Voltar',onClickOverride:"if(typeof closeModal==='function')closeModal('modal-quick-create');",middleHtml:'<span style="flex:1;text-align:center;font-size:0.9rem;font-weight:700;color:var(--text-bright);">'+ ((window._t||function(k){return k;})('quickCreate.title')||'Novo Torneio') +'</span>'}) : '<div></div>'}
        <div style="padding:1.25rem 2rem 2rem;">
        <div class="form-group" style="margin-bottom:1.25rem;">
          <label class="form-label">${(window._t || function(k){return k;})('quickCreate.sportLabel')}</label>
          <div id="qc-sport-buttons" style="display:flex;gap:6px;flex-wrap:wrap;">
            <button type="button" class="qc-sport-btn qc-sport-active" data-sport="🎾 Beach Tennis" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid #fbbf24;background:rgba(251,191,36,0.15);color:#fbbf24;font-weight:600;">🎾 Beach Tennis</button>
            <button type="button" class="qc-sport-btn" data-sport="🥒 Pickleball" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">🥒 Pickleball</button>
            <button type="button" class="qc-sport-btn" data-sport="🎾 Tênis" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">🎾 Tênis</button>
            <button type="button" class="qc-sport-btn" data-sport="🏓 Tênis de Mesa" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">🏓 Tênis de Mesa</button>
            <button type="button" class="qc-sport-btn" data-sport="🏸 Padel" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">🏸 Padel</button>
            <button type="button" class="qc-sport-btn" data-sport="🏐 Vôlei de Praia" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">🏐 Vôlei de Praia</button>
            <button type="button" class="qc-sport-btn" data-sport="⚽ Futevôlei" onclick="window._qcSelectSport(this)" style="padding:8px 14px;border-radius:10px;font-size:0.85rem;cursor:pointer;transition:all 0.15s;border:2px solid rgba(255,255,255,0.18);background:rgba(255,255,255,0.06);color:var(--text-main);font-weight:500;">⚽ Futevôlei</button>
          </div>
          <select class="form-control" id="quick-create-sport" style="display:none;">
            <option>🎾 Beach Tennis</option>
            <option>🥒 Pickleball</option>
            <option>🎾 Tênis</option>
            <option>🏓 Tênis de Mesa</option>
            <option>🏸 Padel</option>
            <option>🏐 Vôlei de Praia</option>
            <option>⚽ Futevôlei</option>
          </select>
        </div>
        <div id="qc-template-area" style="margin-bottom:10px;display:none;"></div>
        <div style="display:flex; flex-direction:column; gap:10px;">
          <button class="btn btn-primary btn-block" id="btn-quick-create">
            🏆 ${(window._t || function(k){return k;})('quickCreate.create')}
          </button>
          <button class="btn btn-tool-amber btn-block" id="btn-quick-template">
            💾 Usar Template
          </button>
          <button class="btn btn-secondary btn-block" id="btn-quick-advanced">
            ⚙️ ${(window._t || function(k){return k;})('quickCreate.advanced')}
          </button>
          <button class="btn btn-secondary btn-block" onclick="if(typeof closeModal==='function')closeModal('modal-quick-create');">
            ${(window._t || function(k){return k;})('btn.cancel')}
          </button>
        </div>
        </div>
      </div>
    </div>`;
  document.body.insertAdjacentHTML('beforeend', html);

  // Quick Create sport button selection
  window._qcSelectSport = function(btn) {
    var btns = document.querySelectorAll('#qc-sport-buttons .qc-sport-btn');
    btns.forEach(function(b) {
      b.classList.remove('qc-sport-active');
      b.style.border = '2px solid rgba(255,255,255,0.18)';
      b.style.background = 'rgba(255,255,255,0.06)';
      b.style.color = 'var(--text-main)';
    });
    btn.classList.add('qc-sport-active');
    btn.style.border = '2px solid #fbbf24';
    btn.style.background = 'rgba(251,191,36,0.15)';
    btn.style.color = '#fbbf24';
    // Sync hidden select
    var sel = document.getElementById('quick-create-sport');
    if (sel) {
      var val = btn.getAttribute('data-sport');
      for (var i = 0; i < sel.options.length; i++) {
        if (sel.options[i].text === val) { sel.selectedIndex = i; break; }
      }
    }
  };

  // Criar Torneio (rápido com auto-nome)
  document.getElementById('btn-quick-create').addEventListener('click', function () {
    const sportRaw = document.getElementById('quick-create-sport').value || '';
    const sportClean = sportRaw.replace(/^[^\w\u00C0-\u024F]+/u, '').trim() || 'Esportes';
    const userName = (window.AppStore.currentUser && window.AppStore.currentUser.displayName)
      ? window.AppStore.currentUser.displayName.split(' ')[0] : 'Organizador';
    let autoName = 'Torneio Eliminatórias de ' + sportClean + ' de ' + userName;

    // Impede nome duplicado — adiciona sufixo numérico se necessário
    let suffix = 1;
    while (window.AppStore.tournaments.some(function(t) { return t.name && t.name.trim().toLowerCase() === autoName.toLowerCase(); })) {
      suffix++;
      autoName = 'Torneio Eliminatórias de ' + sportClean + ' de ' + userName + ' (' + suffix + ')';
    }

    // Determine team size from sport defaults
    const _qcSportTeamDefaults = {
      'Beach Tennis': 2, 'Pickleball': 2, 'Tênis': 1, 'Tênis de Mesa': 1, 'Padel': 2
    };
    const qcTeamSize = _qcSportTeamDefaults[sportClean] || 1;

    // Honor _venuePrefill left by #venues "Criar torneio aqui" — turns the
    // discover → create loop into one click.
    var _venuePref = null;
    try {
      var _raw = sessionStorage.getItem('_venuePrefill');
      if (_raw) { _venuePref = JSON.parse(_raw); sessionStorage.removeItem('_venuePrefill'); }
    } catch (e) {}

    const tourData = {
      id: 'tour_' + Date.now(),
      name: autoName,
      sport: sportRaw,
      format: 'Eliminatórias Simples',
      venue: (_venuePref && _venuePref.venueName) || '',
      venuePlaceId: (_venuePref && _venuePref.placeId) || '',
      venueLat: (_venuePref && _venuePref.lat) || null,
      venueLon: (_venuePref && _venuePref.lon) || null,
      isPublic: true,
      enrollmentMode: 'individual',
      teamSize: qcTeamSize,
      gameTypes: qcTeamSize > 1 ? 'duplas' : 'simples',
      thirdPlace: true,
      elimThirdPlace: true,
      status: 'open',
      createdAt: new Date().toISOString(),
      organizerId: window.AppStore.currentUser ? window.AppStore.currentUser.uid : 'local',
      organizerName: window.AppStore.currentUser ? window.AppStore.currentUser.displayName : 'Organizador',
      organizerEmail: window.AppStore.currentUser ? window.AppStore.currentUser.email : 'visitante@local',
      creatorEmail: window.AppStore.currentUser ? window.AppStore.currentUser.email : 'visitante@local',
      creatorUid: window.AppStore.currentUser ? window.AppStore.currentUser.uid : '',
      coHosts: [],
      participants: [],
      matches: [],
      tiebreakers: ['confronto_direto', 'saldo_pontos', 'vitorias', 'buchholz', 'sonneborn_berger', 'sorteio'],
      scoring: (window._sportScoringDefaults && window._sportScoringDefaults[sportClean])
        ? Object.assign({}, window._sportScoringDefaults[sportClean])
        : { type: 'simple', setsToWin: 1, gamesPerSet: 1, tiebreakEnabled: false, tiebreakPoints: 7, tiebreakMargin: 2, superTiebreak: false, superTiebreakPoints: 10, countingType: 'numeric', advantageRule: false }
    };

    window.AppStore.addTournament(tourData);
    if (typeof closeModal === 'function') closeModal('modal-quick-create');
    // Flag to auto-scroll to Edit button and show hint on tournament detail page
    try { sessionStorage.setItem('scoreplace_scroll_to_edit', '1'); } catch (e) {}
    window.location.hash = '#tournaments/' + tourData.id;
    if (typeof showNotification === 'function') {
      showNotification(window._t ? window._t('quickCreate.created') : 'Torneio Criado!', autoName, 'success');
    }
  });

  // Detalhes Avançados — abre formulário completo com sport pré-selecionado
  document.getElementById('btn-quick-advanced').addEventListener('click', function () {
    const sportVal = document.getElementById('quick-create-sport').value;
    if (typeof closeModal === 'function') closeModal('modal-quick-create');

    // Reset formulário completo
    const form = document.getElementById('form-create-tournament');
    if (form) form.reset();
    const editId = document.getElementById('edit-tournament-id');
    if (editId) editId.value = '';
    const title = document.getElementById('create-modal-title');
    if (title) title.innerText = _t('create.newTournament');
    const pub = document.getElementById('tourn-public');
    if (pub) pub.checked = true;
    const liga = document.getElementById('liga-open-enrollment');
    if (liga) liga.checked = true;
    // elimThirdPlace is always true — no toggle needed

    // Pré-selecionar sport
    const sportSelect = document.getElementById('select-sport');
    if (sportSelect) {
      const opt = Array.from(sportSelect.options).find(o => o.value === sportVal || o.text === sportVal);
      if (opt) sportSelect.value = opt.value;
      if (typeof window._onSportChange === 'function') window._onSportChange();
    }

    // Reset format/draw-mode button visuals to match reset hidden selects
    // (form.reset() resets <select> values but not inline-styled button states)
    const fmtSel = document.getElementById('select-formato');
    if (fmtSel) fmtSel.value = 'elim_simples';
    const defaultFmtBtn = document.querySelector('#formato-buttons .formato-btn[data-value="elim_simples"]');
    if (defaultFmtBtn && typeof window._selectFormato === 'function') window._selectFormato(defaultFmtBtn);
    const drawModeHidden = document.getElementById('draw-mode');
    if (drawModeHidden) drawModeHidden.value = 'sorteio';
    const defaultDrawBtn = document.querySelector('#draw-mode-buttons .draw-mode-btn[data-value="sorteio"]');
    if (defaultDrawBtn && typeof window._selectDrawMode === 'function') window._selectDrawMode(defaultDrawBtn);

    if (typeof window._onFormatoChange === 'function') window._onFormatoChange();
    // If a venue prefill arrived from #venues "Criar torneio aqui", populate
    // the venue fields before we open the modal so the user sees it ready.
    var _advVenuePref = null;
    try {
      var _advRaw = sessionStorage.getItem('_venuePrefill');
      if (_advRaw) { _advVenuePref = JSON.parse(_advRaw); sessionStorage.removeItem('_venuePrefill'); }
    } catch (e) {}
    if (_advVenuePref) {
      var _venueInp = document.getElementById('tourn-venue');
      var _latInp = document.getElementById('tourn-venue-lat');
      var _lonInp = document.getElementById('tourn-venue-lon');
      var _pidInp = document.getElementById('tourn-venue-place-id');
      if (_venueInp) _venueInp.value = _advVenuePref.venueName || '';
      if (_latInp)   _latInp.value = _advVenuePref.lat != null ? _advVenuePref.lat : '';
      if (_lonInp)   _lonInp.value = _advVenuePref.lon != null ? _advVenuePref.lon : '';
      if (_pidInp)   _pidInp.value = _advVenuePref.placeId || '';
    }

    if (typeof openModal === 'function') openModal('modal-create-tournament');
    if (typeof window._refreshTemplateBtn === 'function') window._refreshTemplateBtn();
    // Ensure GSM summary renders after modal is visible
    setTimeout(function() {
      if (typeof window._updateGSMSummaryFromHidden === 'function') window._updateGSMSummaryFromHidden();
      if (typeof window._initPlacesAutocomplete === 'function') window._initPlacesAutocomplete();
      if (typeof window._autoShowVenueMap === 'function') window._autoShowVenueMap();
      // Render the venue map immediately if we have coords from the prefill.
      if (_advVenuePref && _advVenuePref.lat != null && _advVenuePref.lon != null &&
          typeof window._initVenueCreateMap === 'function') {
        window._initVenueCreateMap(parseFloat(_advVenuePref.lat), parseFloat(_advVenuePref.lon), _advVenuePref.venueName || '');
      }
    }, 100);
  });

  // ─── Template Integration ────────────────────────────────────────────────
  // "Usar Template" button handler (always visible, loads on demand)
  document.getElementById('btn-quick-template').addEventListener('click', function() {
    var area = document.getElementById('qc-template-area');
    if (!area) return;
    var templates = typeof window._getTemplates === 'function' ? window._getTemplates() : [];
    var _t = window._t || function(k) { return k; };
    if (templates.length === 0) {
      area.style.display = 'block';
      area.innerHTML = '<p style="color:var(--text-muted);font-size:0.85rem;text-align:center;">Nenhum template salvo. Salve um a partir de um torneio existente (Ferramentas do Organizador → Salvar como Template).</p>';
      return;
    }
    var html = '<div style="max-height:200px;overflow-y:auto;display:flex;flex-direction:column;gap:6px;">';
    templates.forEach(function(tpl, i) {
      var sportIcon = tpl.sport ? tpl.sport.split(' ')[0] : '🏆';
      html += '<div style="display:flex;align-items:center;gap:8px;padding:8px 12px;background:var(--bg-card);border:1px solid var(--border-color);border-radius:10px;cursor:pointer;" ' +
        'onclick="window._qcApplyTemplate(' + i + ')">' +
        '<span style="font-size:1.2rem;">' + sportIcon + '</span>' +
        '<div style="flex:1;min-width:0;">' +
          '<div style="font-weight:600;font-size:0.85rem;color:var(--text-bright);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">' + window._safeHtml(tpl.name) + '</div>' +
          '<div style="font-size:0.75rem;color:var(--text-muted);">' + window._safeHtml(tpl.format || '') + '</div>' +
        '</div>' +
        '<button class="btn btn-micro btn-danger-ghost" onclick="event.stopPropagation();window._qcDeleteTemplate(\'' + window._safeHtml(tpl._id || String(i)) + '\')" title="Apagar">✕</button>' +
      '</div>';
    });
    html += '</div>';
    area.style.display = 'block';
    area.innerHTML = html;
  });

  window._qcApplyTemplate = function(index) {
    var tpl = typeof window._applyTemplate === 'function' ? window._applyTemplate(index) : null;
    if (!tpl) return;
    if (typeof closeModal === 'function') closeModal('modal-quick-create');
    // Open advanced form and pre-fill
    var form = document.getElementById('form-create-tournament');
    if (form) form.reset();
    var editId = document.getElementById('edit-tournament-id');
    if (editId) editId.value = '';
    var title = document.getElementById('create-modal-title');
    if (title) title.innerText = _t('create.newFromTemplate');
    var pub = document.getElementById('tourn-public');
    if (pub) pub.checked = true;

    // Pre-fill from template
    if (typeof window._prefillFromTemplate === 'function') {
      window._prefillFromTemplate(tpl);
    }
    if (typeof openModal === 'function') openModal('modal-create-tournament');
    if (typeof window._refreshTemplateBtn === 'function') window._refreshTemplateBtn();
    setTimeout(function() {
      if (typeof window._updateGSMSummaryFromHidden === 'function') window._updateGSMSummaryFromHidden();
      if (typeof window._initPlacesAutocomplete === 'function') window._initPlacesAutocomplete();
      if (typeof window._autoShowVenueMap === 'function') window._autoShowVenueMap();
    }, 100);
  };

  window._qcDeleteTemplate = async function(templateId) {
    if (typeof window._deleteTemplate === 'function') await window._deleteTemplate(templateId);
    var _t = window._t || function(k) { return k; };
    if (typeof showNotification === 'function') showNotification(_t('template.deleted'), '', 'info');
    // Refresh the list
    document.getElementById('btn-quick-template').click();
  };
})();

// Inicializa estrutura base da UI (Modais, Menus)
setupUI();

setupCreateTournamentModal();
setupLoginModal();
setupProfileModal();
setupResultModal();
setupEnrollModal();

// Dois banners complementares para usuários iOS:
//   1. Chrome/Firefox/Edge iOS → sugere abrir no Safari (install só lá)
//   2. Safari iOS ainda não instalou como PWA → explica o passo a passo
// Ambos dismissable, persistidos em localStorage, não atrapalham quem já
// instalou (detectado via display-mode standalone + navigator.standalone).
(function iosInstallHints() {
  try {
    var ua = navigator.userAgent || '';
    var isIOS = /iPad|iPhone|iPod/.test(ua) && !window.MSStream;
    if (!isIOS) return;
    // Já instalado como PWA? Não mostra nada.
    if (window.matchMedia && window.matchMedia('(display-mode: standalone)').matches) return;
    if (window.navigator.standalone === true) return;

    var isNonSafariIOS = /CriOS|FxiOS|EdgiOS|OPiOS/.test(ua);

    var mkBanner = function(opts) {
      var key = opts.key;
      if (localStorage.getItem(key) === '1') return;
      var banner = document.createElement('div');
      banner.style.cssText = 'position:fixed;left:12px;right:12px;bottom:12px;z-index:10050;background:linear-gradient(135deg,#1e293b,#0f172a);border:1px solid rgba(251,191,36,0.35);border-radius:14px;padding:12px 14px;color:#fff;box-shadow:0 10px 30px rgba(0,0,0,0.5);font-size:0.82rem;';
      banner.innerHTML =
        '<div style="display:flex;align-items:flex-start;gap:10px;">' +
          '<div style="flex-shrink:0;font-size:1.2rem;line-height:1;margin-top:2px;">' + opts.icon + '</div>' +
          '<div style="flex:1;min-width:0;line-height:1.4;">' +
            '<div style="font-weight:700;color:#fbbf24;margin-bottom:2px;">' + opts.title + '</div>' +
            '<div style="color:var(--text-main);font-size:0.76rem;">' + opts.body + '</div>' +
          '</div>' +
          '<button data-dismiss-key="' + key + '" style="flex-shrink:0;background:transparent;border:none;color:var(--text-muted);font-size:1.1rem;cursor:pointer;padding:4px 8px;line-height:1;">✕</button>' +
        '</div>';
      var add = function() {
        document.body.appendChild(banner);
        banner.querySelector('button[data-dismiss-key]').addEventListener('click', function() {
          try { localStorage.setItem(key, '1'); } catch (e) {}
          banner.remove();
        });
      };
      if (document.body) add(); else document.addEventListener('DOMContentLoaded', add);
    };

    if (isNonSafariIOS) {
      // Browser não-Safari no iPhone — instalar PWA e shortcuts só funcionam
      // no Safari por limitação do iOS.
      mkBanner({
        key: 'scoreplace_safari_hint_dismissed',
        icon: '🧭',
        title: 'Para melhor experiência, use Safari',
        body: 'No iPhone, instalar como app e atalhos de partida só funcionam pelo Safari. Copie o link e abra por lá.'
      });
    } else {
      // Safari iOS sem install — ensina o fluxo (não há API programática
      // de install no iOS; só via Share sheet).
      mkBanner({
        // key bumpada v2 para mostrar o tutorial melhorado mesmo para
        // usuários que tinham dispensado a versão anterior sem conseguir instalar.
        key: 'scoreplace_ios_install_dismissed_v2',
        icon: '📲',
        title: 'Instalar no iPhone',
        body:
          'Abra em tela cheia e ative os atalhos:<br>' +
          '<b>1.</b> Abrir o menu de <b>Compartilhar</b>:<br>' +
          '&nbsp;&nbsp;&nbsp;• Se o Safari mostra o ícone <span style="display:inline-block;border:1px solid currentColor;border-radius:4px;padding:0 4px;margin:0 2px;vertical-align:middle;font-size:0.72rem;">⬆</span> direto na barra de baixo, toque nele.<br>' +
          '&nbsp;&nbsp;&nbsp;• Em <b>iOS 17+</b> (tab bar compacto) o ícone fica escondido: toque nos <b>três pontinhos <span style="display:inline-block;border:1px solid currentColor;border-radius:4px;padding:0 4px;margin:0 2px;vertical-align:middle;font-size:0.72rem;">•••</span></b> no canto direito da barra → depois em <b>"Compartilhar"</b>. Alternativa: toque em <b>"Aa"</b> ao lado da URL → "Compartilhar".<br>' +
          '<b>2.</b> Na lista que abre, <b>role pra baixo</b> até <b>"Adicionar à Tela de Início"</b>. Se não achar, toque em <b>"Editar Ações"</b> no final da lista e ative a opção.<br>' +
          '<b>3.</b> Toque nela, depois em <b>Adicionar</b> no canto superior direito.<br><br>' +
          '<span style="color:var(--text-muted);">⚠️ Não funciona em <b>Navegação Privativa</b> — precisa ser aba normal. Requer iOS 13+.</span>'
      });
    }
  } catch (e) {}
})();

// Tema aplicado automaticamente via IIFE em store.js (initThemeSystem removido — função não existia)

// Load cached tournaments for instant first-paint (before Firebase auth resolves)
if (window.AppStore && window.AppStore._loadFromCache) {
  window.AppStore._loadFromCache();
}

// Inicia o Roteador SPA
// Firebase onAuthStateChanged will handle auto-login and real-time data from Firestore
initRouter();

console.log("scoreplace.app v" + (window.SCOREPLACE_VERSION || '?') + " Inicializado com Sucesso");

// === Offline/Online indicator ===
(function() {
  var banner = document.createElement('div');
  banner.id = 'offline-banner';
  banner.style.cssText = 'display:none;position:fixed;bottom:0;left:0;right:0;z-index:99999;padding:10px 16px;text-align:center;font-size:0.85rem;font-weight:600;transition:transform 0.3s ease;';
  banner.innerHTML = '<span id="offline-banner-text"></span>';
  document.body.appendChild(banner);

  function showBanner(text, bg, color, autoHide) {
    var b = document.getElementById('offline-banner');
    var t = document.getElementById('offline-banner-text');
    if (!b || !t) return;
    t.textContent = text;
    b.style.background = bg;
    b.style.color = color;
    b.style.display = 'block';
    b.style.transform = 'translateY(0)';
    if (autoHide) {
      setTimeout(function() {
        b.style.transform = 'translateY(100%)';
        setTimeout(function() { b.style.display = 'none'; }, 350);
      }, 3000);
    }
  }

  window.addEventListener('offline', function() {
    showBanner('Sem conexão — modo offline', 'rgba(239,68,68,0.95)', '#fff', false);
  });
  window.addEventListener('online', function() {
    showBanner('Conexão restaurada', 'rgba(16,185,129,0.95)', '#fff', true);
  });

  // Show immediately if already offline
  if (!navigator.onLine) {
    showBanner('Sem conexão — modo offline', 'rgba(239,68,68,0.95)', '#fff', false);
  }
})();

// === Liga auto-draw poller ===
// Ticks every minute. Only fires draws on the organizer's browser for Liga
// tournaments with drawManual=off and drawFirstDate set.
(function() {
  function tick() {
    if (typeof window._checkLigaAutoDraws !== 'function') return;
    try {
      var p = window._checkLigaAutoDraws();
      if (p && typeof p.catch === 'function') {
        p.catch(function(e) { console.warn('[auto-draw poller]', e); });
      }
    } catch (e) {
      console.warn('[auto-draw poller]', e);
    }
  }
  // Run shortly after load (once the store and user are ready)
  setTimeout(tick, 8000);
  // Re-run every 60 seconds
  setInterval(tick, 60000);
})();

// === Quick Search (Ctrl+K / Cmd+K) ===
(function() {
  // Create search overlay
  var overlay = document.createElement('div');
  overlay.id = 'quick-search-overlay';
  overlay.style.cssText = 'display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(0,0,0,0.6);z-index:99998;backdrop-filter:blur(4px);-webkit-backdrop-filter:blur(4px);';
  overlay.onclick = function(e) { if (e.target === overlay) window._closeQuickSearch(); };

  var modal = document.createElement('div');
  modal.style.cssText = 'position:fixed;top:15%;left:50%;transform:translateX(-50%);width:90%;max-width:540px;background:var(--bg-card);border:1px solid var(--border-color);border-radius:16px;box-shadow:0 25px 60px rgba(0,0,0,0.5);z-index:99999;overflow:hidden;';

  modal.innerHTML =
    '<div style="padding:16px 20px;border-bottom:1px solid var(--border-color);display:flex;align-items:center;gap:10px;">' +
    '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--text-muted)" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>' +
    '<input type="text" id="quick-search-input" placeholder="Buscar torneios, jogadores..." style="flex:1;background:transparent;border:none;color:var(--text-bright);font-size:1.05rem;outline:none;" autocomplete="off">' +
    '<kbd style="font-size:0.65rem;color:var(--text-muted);background:rgba(255,255,255,0.06);padding:3px 8px;border-radius:4px;border:1px solid rgba(255,255,255,0.1);">ESC</kbd>' +
    '</div>' +
    '<div id="quick-search-results" style="max-height:360px;overflow-y:auto;padding:8px;"></div>';

  overlay.appendChild(modal);
  document.body.appendChild(overlay);

  window._openQuickSearch = function() {
    overlay.style.display = 'block';
    var input = document.getElementById('quick-search-input');
    if (input) { input.value = ''; input.focus(); }
    _showQuickSearchDefaults();
  };

  window._closeQuickSearch = function() {
    overlay.style.display = 'none';
  };

  function _showQuickSearchDefaults() {
    var resultsDiv = document.getElementById('quick-search-results');
    if (!resultsDiv) return;
    var html = '<div style="padding:8px 12px;font-size:0.7rem;font-weight:600;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;">Ações rápidas</div>';
    var actions = [
      { icon: '➕', label: 'Novo Torneio', action: "if(typeof openModal==='function')openModal('modal-quick-create');window._closeQuickSearch();" },
      { icon: '🏠', label: 'Dashboard', action: "window.location.hash='#dashboard';window._closeQuickSearch();" },
      { icon: '🔍', label: 'Explorar Comunidade', action: "window.location.hash='#explore';window._closeQuickSearch();" },
      { icon: '🔔', label: 'Notificações', action: "window.location.hash='#notifications';window._closeQuickSearch();" },
      { icon: '❓', label: 'Ajuda / Manual', action: "if(typeof openModal==='function')openModal('modal-help');window._closeQuickSearch();" }
    ];
    actions.forEach(function(a) {
      html += '<div onclick="' + a.action + '" style="display:flex;align-items:center;gap:12px;padding:10px 14px;border-radius:10px;cursor:pointer;transition:background 0.15s;" onmouseover="this.style.background=\'rgba(255,255,255,0.06)\'" onmouseout="this.style.background=\'transparent\'">' +
        '<span style="font-size:1.1rem;">' + a.icon + '</span>' +
        '<span style="font-size:0.9rem;color:var(--text-bright);">' + a.label + '</span>' +
        '</div>';
    });
    resultsDiv.innerHTML = html;
  }

  function _performQuickSearch(query) {
    var resultsDiv = document.getElementById('quick-search-results');
    if (!resultsDiv) return;
    if (!query || query.length < 2) { _showQuickSearchDefaults(); return; }

    var q = query.toLowerCase();
    var _sh = window._safeHtml || function(s){return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');};
    var html = '';

    // Torneios: scoped (tournaments) UNIÃO discovery (publicDiscovery).
    // Antes só pesquisava o scoped list — missava qualquer torneio público
    // que o usuário ainda não tivesse entrado, deixando a busca rápida com
    // cobertura incompleta.
    var scoped = (window.AppStore && window.AppStore.tournaments) ? window.AppStore.tournaments : [];
    var discovery = (window.AppStore && Array.isArray(window.AppStore.publicDiscovery)) ? window.AppStore.publicDiscovery : [];
    var allTournaments = scoped.slice();
    var seenIds = {};
    scoped.forEach(function(t) { if (t && t.id) seenIds[String(t.id)] = true; });
    discovery.forEach(function(t) {
      if (t && t.id && !seenIds[String(t.id)]) { allTournaments.push(t); seenIds[String(t.id)] = true; }
    });

    var matchedTournaments = allTournaments.filter(function(t) {
      return (t.name && t.name.toLowerCase().indexOf(q) !== -1) ||
             (t.sport && t.sport.toLowerCase().indexOf(q) !== -1) ||
             (t.venueName && t.venueName.toLowerCase().indexOf(q) !== -1) ||
             (t.venue && t.venue.toLowerCase && t.venue.toLowerCase().indexOf(q) !== -1) ||
             (t.format && t.format.toLowerCase().indexOf(q) !== -1);
    }).slice(0, 6);

    if (matchedTournaments.length > 0) {
      html += '<div style="padding:8px 12px;font-size:0.7rem;font-weight:600;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;">Torneios</div>';
      matchedTournaments.forEach(function(t) {
        var status = t.status === 'finished' ? '🏆 Encerrado' : (t.status === 'active' ? '▶ Ativo' : '📋 Aberto');
        var _safeId = String(t.id || '').replace(/'/g, "\\'").replace(/\\/g, "\\\\");
        html += '<div onclick="window.location.hash=\'#tournaments/' + _safeId + '\';window._closeQuickSearch();" style="display:flex;align-items:center;gap:12px;padding:10px 14px;border-radius:10px;cursor:pointer;transition:background 0.15s;" onmouseover="this.style.background=\'rgba(255,255,255,0.06)\'" onmouseout="this.style.background=\'transparent\'">' +
          '<span style="font-size:1.1rem;">🏅</span>' +
          '<div style="flex:1;overflow:hidden;">' +
          '<div style="font-size:0.88rem;font-weight:600;color:var(--text-bright);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">' + _sh(t.name) + '</div>' +
          '<div style="font-size:0.7rem;color:var(--text-muted);">' + _sh(t.format || '') + ' — ' + _sh(t.sport || '') + '</div>' +
          '</div>' +
          '<span style="font-size:0.65rem;color:var(--text-muted);">' + status + '</span>' +
          '</div>';
      });
    }

    // Locais: busca no cache AppStore.tournaments pelos venues únicos dos
    // torneios (não temos cache global de venues client-side; fazer call
    // a VenueDB.listVenues debounced aqui custaria quota pra cada
    // keystroke). O cache atual já cobre os venues relevantes pro usuário
    // — dashboard/torneios recentes. Deep-link pra #venues/<placeId>.
    var venueMap = {};
    allTournaments.forEach(function(t) {
      if (!t || !t.venuePlaceId) return;
      var key = String(t.venuePlaceId);
      if (venueMap[key]) return;
      var vname = t.venue || t.venueName || '';
      var vaddr = t.venueAddress || '';
      if ((vname && vname.toLowerCase().indexOf(q) !== -1) ||
          (vaddr && vaddr.toLowerCase().indexOf(q) !== -1)) {
        venueMap[key] = { placeId: key, name: vname, address: vaddr };
      }
    });
    var venueMatches = Object.keys(venueMap).map(function(k) { return venueMap[k]; }).slice(0, 5);
    if (venueMatches.length > 0) {
      html += '<div style="padding:8px 12px;font-size:0.7rem;font-weight:600;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;margin-top:4px;">Locais</div>';
      venueMatches.forEach(function(v) {
        var _safePid = String(v.placeId || '').replace(/'/g, "\\'").replace(/\\/g, "\\\\");
        html += '<div onclick="window.location.hash=\'#venues/' + _safePid + '\';window._closeQuickSearch();" style="display:flex;align-items:center;gap:12px;padding:10px 14px;border-radius:10px;cursor:pointer;transition:background 0.15s;" onmouseover="this.style.background=\'rgba(255,255,255,0.06)\'" onmouseout="this.style.background=\'transparent\'">' +
          '<span style="font-size:1.1rem;">🏢</span>' +
          '<div style="flex:1;overflow:hidden;">' +
          '<div style="font-size:0.88rem;font-weight:600;color:var(--text-bright);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">' + _sh(v.name) + '</div>' +
          (v.address ? '<div style="font-size:0.7rem;color:var(--text-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">' + _sh(v.address) + '</div>' : '') +
          '</div>' +
          '</div>';
      });
    }

    // Use allTournaments (scoped + discovery) pra busca de jogadores também,
    // assim nomes em torneios públicos aparecem mesmo sem o usuário ter entrado.
    var tournaments = allTournaments;

    // Search players across tournaments
    var playerSet = {};
    tournaments.forEach(function(t) {
      if (!t.participants) return;
      var arr = Array.isArray(t.participants) ? t.participants : Object.values(t.participants);
      arr.forEach(function(p) {
        var name = typeof p === 'string' ? p : (p.displayName || p.name || '');
        if (name && name.toLowerCase().indexOf(q) !== -1 && !name.includes('BYE')) {
          if (!playerSet[name]) playerSet[name] = [];
          playerSet[name].push(t.name);
        }
      });
    });

    var playerNames = Object.keys(playerSet).slice(0, 5);
    if (playerNames.length > 0) {
      html += '<div style="padding:8px 12px;font-size:0.7rem;font-weight:600;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px;margin-top:4px;">Jogadores</div>';
      playerNames.forEach(function(name) {
        var tourneys = playerSet[name].slice(0, 2).join(', ');
        html += '<div style="display:flex;align-items:center;gap:12px;padding:10px 14px;border-radius:10px;cursor:default;">' +
          '<span style="font-size:1.1rem;">👤</span>' +
          '<div style="flex:1;overflow:hidden;">' +
          '<div style="font-size:0.88rem;font-weight:600;color:var(--text-bright);">' + (window._safeHtml ? window._safeHtml(name) : name) + '</div>' +
          '<div style="font-size:0.7rem;color:var(--text-muted);">' + tourneys + '</div>' +
          '</div></div>';
      });
    }

    if (!html) {
      html = '<div style="padding:2rem;text-align:center;color:var(--text-muted);font-size:0.85rem;">Nenhum resultado para "' + (window._safeHtml ? window._safeHtml(query) : query) + '"</div>';
    }
    resultsDiv.innerHTML = html;
  }

  // Input handler with debounce
  var _searchTimeout = null;
  document.getElementById('quick-search-input').addEventListener('input', function() {
    var val = this.value.trim();
    clearTimeout(_searchTimeout);
    _searchTimeout = setTimeout(function() { _performQuickSearch(val); }, 150);
  });

  // Keyboard shortcuts
  document.addEventListener('keydown', function(e) {
    // Ctrl+K or Cmd+K to open
    if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
      e.preventDefault();
      if (overlay.style.display === 'none') window._openQuickSearch();
      else window._closeQuickSearch();
    }
    // ESC to close
    if (e.key === 'Escape' && overlay.style.display !== 'none') {
      window._closeQuickSearch();
    }
  });
})();

// === Global Keyboard Shortcuts ===
(function setupGlobalShortcuts() {
  document.addEventListener('keydown', function(e) {
    // Skip if typing in an input, textarea, select, or contentEditable
    var tag = (e.target.tagName || '').toLowerCase();
    if (tag === 'input' || tag === 'textarea' || tag === 'select' || e.target.isContentEditable) return;
    // Skip if any modifier key is held (except for specific combos)
    if (e.ctrlKey || e.metaKey || e.altKey) return;
    // Skip if a modal is open
    if (document.getElementById('qr-modal-overlay') || document.getElementById('player-stats-overlay')) return;
    var quickSearchOverlay = document.getElementById('quick-search-overlay');
    if (quickSearchOverlay && quickSearchOverlay.style.display !== 'none') return;

    switch (e.key.toLowerCase()) {
      case 'd':
        e.preventDefault();
        window.location.hash = '#dashboard';
        break;
      case 'e':
        e.preventDefault();
        window.location.hash = '#tournaments';
        break;
      case 'n':
        e.preventDefault();
        if (typeof window.quickCreate === 'function') window.quickCreate();
        else window.location.hash = '#create';
        break;
      case '?':
        e.preventDefault();
        var helpModal = document.getElementById('modal-help');
        if (helpModal) {
          helpModal.style.display = helpModal.style.display === 'none' ? 'flex' : 'none';
        }
        break;
      case '/':
        // Focus quick search
        e.preventDefault();
        if (typeof window._openQuickSearch === 'function') window._openQuickSearch();
        break;
    }
  });

  // Show keyboard shortcuts hint in help modal
  window._keyboardShortcutsHtml = '<div style="margin-top:1rem;padding:12px;background:rgba(255,255,255,0.05);border-radius:10px;">' +
    '<div style="font-weight:700;font-size:0.85rem;color:var(--text-bright);margin-bottom:8px;">⌨️ Atalhos de Teclado</div>' +
    '<div style="display:grid;grid-template-columns:60px 1fr;gap:4px 12px;font-size:0.78rem;">' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">Ctrl+K</kbd><span style="color:var(--text-muted);">Busca rápida</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">/</kbd><span style="color:var(--text-muted);">Busca rápida</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">D</kbd><span style="color:var(--text-muted);">Dashboard</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">E</kbd><span style="color:var(--text-muted);">Explorar torneios</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">N</kbd><span style="color:var(--text-muted);">Novo torneio</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">?</kbd><span style="color:var(--text-muted);">Ajuda</span>' +
    '<kbd style="background:rgba(255,255,255,0.1);padding:2px 6px;border-radius:4px;text-align:center;font-family:monospace;color:var(--text-bright);">ESC</kbd><span style="color:var(--text-muted);">Fechar modal</span>' +
    '</div></div>';
})();

// === Accessibility Helpers ===
(function setupA11y() {
  // Focus trap for modals — keeps Tab focus within the modal
  window._trapFocus = function(modalEl) {
    if (!modalEl) return;
    var focusable = modalEl.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');
    if (focusable.length === 0) return;
    var first = focusable[0];
    var last = focusable[focusable.length - 1];
    first.focus();
    modalEl._focusTrapHandler = function(e) {
      if (e.key !== 'Tab') return;
      if (e.shiftKey) {
        if (document.activeElement === first) { e.preventDefault(); last.focus(); }
      } else {
        if (document.activeElement === last) { e.preventDefault(); first.focus(); }
      }
    };
    modalEl.addEventListener('keydown', modalEl._focusTrapHandler);
  };
  window._releaseFocusTrap = function(modalEl) {
    if (modalEl && modalEl._focusTrapHandler) {
      modalEl.removeEventListener('keydown', modalEl._focusTrapHandler);
      delete modalEl._focusTrapHandler;
    }
  };

  // Announce to screen readers via live region
  var liveRegion = document.createElement('div');
  liveRegion.setAttribute('role', 'status');
  liveRegion.setAttribute('aria-live', 'polite');
  liveRegion.setAttribute('aria-atomic', 'true');
  liveRegion.style.cssText = 'position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;';
  liveRegion.id = 'a11y-announcer';
  document.body.appendChild(liveRegion);

  window._announce = function(message) {
    var el = document.getElementById('a11y-announcer');
    if (el) { el.textContent = ''; setTimeout(function() { el.textContent = message; }, 50); }
  };

  // Enhance existing openModal/closeModal with ARIA + focus trap
  var _origOpenModal = window.openModal;
  if (typeof _origOpenModal === 'function') {
    window.openModal = function(id) {
      _origOpenModal(id);
      var modal = document.getElementById(id);
      if (modal) {
        modal.setAttribute('role', 'dialog');
        modal.setAttribute('aria-modal', 'true');
        window._trapFocus(modal);
        window._announce('Modal aberto');
      }
    };
  }
  var _origCloseModal = window.closeModal;
  if (typeof _origCloseModal === 'function') {
    window.closeModal = function(id) {
      var modal = document.getElementById(id);
      if (modal) window._releaseFocusTrap(modal);
      _origCloseModal(id);
      window._announce('Modal fechado');
    };
  }

  // Add ARIA labels to notification toasts
  var _origNotif = window.showNotification;
  if (typeof _origNotif === 'function') {
    window.showNotification = function(title, msg, type) {
      _origNotif(title, msg, type);
      window._announce(title + ': ' + msg);
    };
  }
})();
